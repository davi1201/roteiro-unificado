---
phase: 12-polimento-ux-performance-deploy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - roteiro-unificado/vercel.json
  - roteiro-unificado/.gitignore
  - roteiro-unificado/.env.production.example
autonomous: true
requirements: [UX-04]
user_setup:
  - service: supabase
    why: "Projeto Supabase de produção separado (D-01); credenciais para o Vercel"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard (projeto PROD) -> Settings -> API -> Project URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard (projeto PROD) -> Settings -> API -> anon public key"
must_haves:
  truths:
    - "Existe workflow CI que roda npm ci + npm run build + npm test em cada PR para main, com working-directory roteiro-unificado"
    - "Existe vercel.json com rewrite SPA de /(.*) para /index.html"
    - ".env.production esta no .gitignore — credenciais de prod nunca commitadas"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI obrigatorio (D-06)"
      contains: "working-directory: roteiro-unificado"
    - path: "roteiro-unificado/vercel.json"
      provides: "SPA rewrites para deep linking"
      contains: "/index.html"
    - path: "roteiro-unificado/.gitignore"
      provides: "Bloqueio de .env.production"
      contains: ".env.production"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "roteiro-unificado/package-lock.json"
      via: "cache-dependency-path"
      pattern: "cache-dependency-path: roteiro-unificado/package-lock.json"
---

<objective>
Configurar a infraestrutura de CI/CD e variaveis de ambiente de producao como arquivos estaticos versionados: (1) workflow GitHub Actions ci.yml rodando build + test em cada PR para main com working-directory roteiro-unificado (D-06), (2) vercel.json com SPA rewrites (consumido no deploy do Plan 05), (3) garantir que .env.production esta no .gitignore antes que qualquer credencial seja criada (mitiga vazamento de credenciais), e (4) criar .env.production.example documentando as vars necessarias sem valores reais.

Purpose: Previne deploy de codigo quebrado (CI obrigatorio, D-06), evita 404 em rotas SPA no Vercel CDN, e impede que credenciais de producao vazem no repositorio. Estes arquivos precisam existir antes do deploy operacional do Plan 05.
Output: ci.yml, vercel.json, .gitignore atualizado, .env.production.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md
@.planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md

<interfaces>
<!-- Padroes verificados (RESEARCH Patterns 2 e 3; PATTERNS.md). -->
Estrutura do repo: git root na raiz; app em subdiretorio roteiro-unificado/ (package.json e package-lock.json la). Por isso TODOS os steps npm do CI precisam de working-directory: roteiro-unificado e cache-dependency-path: roteiro-unificado/package-lock.json (Pitfall 1).

Scripts do package.json: build = "tsc -b && vite build"; test = "vitest". Comando CI sem watch: npm test -- --run (167 testes, ~7s).

.gitignore atual (roteiro-unificado/.gitignore) ignora: node_modules, dist, .env.local, .env.*.local, .env. NAO cobre .env.production (nao casa .env.*.local nem .env exato) -> adicionar explicitamente.

.env.local.example existente documenta VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY.

vercel.json (RESEARCH Pattern 2, verbatim Vercel docs): objeto com $schema "https://openapi.vercel.sh/vercel.json" e rewrites array contendo { source: "/(.*)", destination: "/index.html" }. Local: roteiro-unificado/vercel.json (raiz do subdir do app).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloquear .env.production no gitignore e criar exemplo documentado</name>
  <files>roteiro-unificado/.gitignore, roteiro-unificado/.env.production.example</files>
  <read_first>
    - roteiro-unificado/.gitignore (verificar padroes atuais — .env.local, .env.*.local, .env)
    - roteiro-unificado/.env.local.example (estrutura de referencia das vars)
    - .planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md (Pitfall 3 + secao Variaveis de Ambiente)
  </read_first>
  <action>
    No roteiro-unificado/.gitignore, na secao de environment variables (que ja contem .env.local, .env.*.local, .env), adicionar uma linha .env.production para garantir que o arquivo de credenciais de producao nunca seja commitado (o padrao .env.*.local NAO casa .env.production, e .env casa apenas o nome exato). Criar roteiro-unificado/.env.production.example seguindo o mesmo formato de .env.local.example: um comentario indicando origem (Supabase Dashboard projeto PROD -> Settings -> API) e as duas chaves VITE_SUPABASE_URL= e VITE_SUPABASE_ANON_KEY= com valores-placeholder (NAO valores reais), mais um comentario deixando claro que as vars reais de producao sao configuradas no Vercel Dashboard (Settings -> Environment Variables) e que .env.production (sem .example) nunca deve ser commitado. NAO criar .env.production com credenciais reais.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -qx ".env.production" .gitignore && test -f .env.production.example && ! test -f .env.production</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contem uma linha exata .env.production
    - .env.production.example existe e contem VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY com placeholders (sem chaves JWT reais)
    - Nenhum arquivo .env.production (sem .example) existe no repositorio
    - git check-ignore roteiro-unificado/.env.production retorna o caminho (confirma que esta ignorado)
  </acceptance_criteria>
  <done>.env.production esta protegido pelo gitignore; exemplo documentado criado; nenhuma credencial real no repo.</done>
</task>

<task type="auto">
  <name>Task 2: Criar vercel.json com SPA rewrites</name>
  <files>roteiro-unificado/vercel.json</files>
  <read_first>
    - .planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md (secao vercel.json — padrao verbatim)
    - .planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md (Pattern 2 + Pitfall 4)
  </read_first>
  <action>
    Criar roteiro-unificado/vercel.json (na raiz do subdiretorio do app, onde esta o package.json — NAO na raiz do repositorio git) com um objeto JSON contendo a chave $schema apontando para "https://openapi.vercel.sh/vercel.json" e a chave rewrites como um array com um unico objeto contendo source igual a "/(.*)" e destination igual a "/index.html". Esse rewrite faz o Vercel CDN servir index.html para qualquer rota, permitindo deep-linking SPA (ex: acesso direto a /admin/dashboard e /form/:orgId sem retornar 404). Registrar no SUMMARY que o Vercel Dashboard precisara ter "Root Directory" = roteiro-unificado (configuracao manual de UI feita no Plan 05).
  </action>
  <verify>
    <automated>cd roteiro-unificado && node -e "const v=require('./vercel.json'); if(v.rewrites[0].source!=='/(.*)'||v.rewrites[0].destination!=='/index.html')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - roteiro-unificado/vercel.json existe e e JSON valido
    - Contem "source": "/(.*)" e "destination": "/index.html" dentro de rewrites
    - Contem a chave $schema com o valor do schema oficial Vercel
  </acceptance_criteria>
  <done>vercel.json criado no subdir do app com rewrite SPA correto, JSON valido.</done>
</task>

<task type="auto">
  <name>Task 3: Criar workflow GitHub Actions CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - roteiro-unificado/package.json (scripts build e test que o CI executa)
    - .planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md (secao ci.yml — padrao verbatim)
    - .planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md (Pattern 3 + Pitfall 1)
  </read_first>
  <action>
    Criar .github/workflows/ci.yml (na raiz do repositorio git, NAO dentro de roteiro-unificado) com: name CI; gatilho on pull_request branches [main]; um job ci em runs-on ubuntu-latest. Steps na ordem: (1) uses actions/checkout@v4; (2) uses actions/setup-node@v4 com node-version '20', cache 'npm' e cache-dependency-path roteiro-unificado/package-lock.json; (3) step "Install dependencies" com working-directory roteiro-unificado e run npm ci; (4) step "Type check + build" com working-directory roteiro-unificado e run npm run build; (5) step "Test" com working-directory roteiro-unificado e run npm test -- --run. TODOS os steps de npm DEVEM ter working-directory roteiro-unificado (o app esta em subdiretorio — sem isso o CI falha com "No package.json found", Pitfall 1). NAO hardcodar nenhum segredo no arquivo; o CI nao precisa de credenciais Supabase para build+test (os testes usam jsdom/mocks). Node 20 corresponde ao ambiente local (v20.19.3). Apos criar, validar a sintaxe YAML.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/ci.yml','utf8');['working-directory: roteiro-unificado','cache-dependency-path: roteiro-unificado/package-lock.json','npm test -- --run','npm run build','actions/setup-node@v4'].forEach(s=>{if(!y.includes(s)){console.error('MISSING: '+s);process.exit(1)}});console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml existe e e YAML valido
    - Contem working-directory: roteiro-unificado em cada step npm (install, build, test)
    - Contem cache-dependency-path: roteiro-unificado/package-lock.json
    - Contem npm ci, npm run build e npm test -- --run nos steps corretos
    - Gatilho e pull_request com branches [main]; sem segredos hardcoded
  </acceptance_criteria>
  <done>Workflow CI criado na raiz do repo; roda build+test em PRs para main no subdiretorio correto; YAML valido; sem segredos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo->CI | Codigo de PR executado em runner GitHub Actions |
| repo->bundle | Variaveis de ambiente injetadas no build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | .env.production / bundle | mitigate | .env.production adicionado ao .gitignore antes de criar credenciais; vars reais ficam no Vercel Dashboard; .env.production.example so contem placeholders |
| T-12-CI | Tampering | .github/workflows/ci.yml | mitigate | Workflow nao contem segredos hardcoded; usa actions oficiais @v4; build+test bloqueiam merge de codigo quebrado (D-06) |
| T-12-SC | Tampering | npm installs | mitigate | Nenhum pacote novo nesta fase; CI roda npm ci contra package-lock.json versionado (instala exatamente o lock) |
</threat_model>

<verification>
- git check-ignore confirma .env.production ignorado
- vercel.json e JSON valido com rewrite SPA
- ci.yml contem working-directory roteiro-unificado em todos os steps npm
- Nenhum segredo hardcoded em nenhum arquivo
</verification>

<success_criteria>
- D-06: CI obrigatorio criado (build + test em PR para main)
- SPA rewrites configurados (vercel.json)
- T-12-01 mitigado: credenciais de prod nunca commitadas
</success_criteria>

<output>
Create `.planning/phases/12-polimento-ux-performance-deploy/12-04-SUMMARY.md` when done
</output>
