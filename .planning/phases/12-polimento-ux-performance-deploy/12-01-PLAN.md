---
phase: 12-polimento-ux-performance-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/components/layouts/AdminSidebar.tsx
  - roteiro-unificado/src/features/form/FormLayout.tsx
  - roteiro-unificado/src/components/layouts/AdminLayout.tsx
  - roteiro-unificado/src/features/form/CompanyDashboard.tsx
  - roteiro-unificado/src/features/form/HistoryPage.tsx
autonomous: false
requirements: [UX-02, UX-03, UX-05]
must_haves:
  truths:
    - "Login, formulário, dashboard, histórico e exportação renderizam sem overflow horizontal em 1280px, 1024px e 768px"
    - "Em 768px a sidebar desktop aparece e a tab bar mobile some (transição md:)"
    - "Indicador de aba ativa e barra de progresso permanecem visíveis e corretos em todos os breakpoints"
    - "Todos os caminhos de carregamento de dados (dashboard, histórico, shell do formulário) exibem <Skeleton> com aria-busy=\"true\" durante isLoading/isFetching"
  artifacts:
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "Shell do formulário responsivo verificado em 768px"
      contains: "md:"
    - path: "roteiro-unificado/src/features/form/CompanyDashboard.tsx"
      provides: "Estado de carregamento do dashboard com <Skeleton> aria-busy"
      contains: "Skeleton"
    - path: "roteiro-unificado/src/features/form/HistoryPage.tsx"
      provides: "Estado de carregamento do histórico com <Skeleton> aria-busy"
      contains: "Skeleton"
  key_links:
    - from: "src/features/form/FormLayout.tsx"
      to: "viewport breakpoints md:"
      via: "Tailwind responsive classes"
      pattern: "md:(flex|hidden|ml-\\[240px\\])"
    - from: "src/features/form/CompanyDashboard.tsx"
      to: "<Skeleton> em isLoading"
      via: "branch de carregamento"
      pattern: "isLoading.*Skeleton"
---

<objective>
Auditoria de responsividade e estados de carregamento (UX-02, UX-03, UX-05): percorrer os cinco fluxos principais (login, formulário, dashboard, histórico, exportação) em três breakpoints — 1280px, 1024px e 768px — e corrigir overflow horizontal, elementos sobrepostos e texto ilegível; verificar que o indicador de aba ativa e a barra de progresso continuam corretos em todos os tamanhos; e auditar que todos os caminhos de carregamento de dados exibem `<Skeleton>` com `aria-busy="true"`.

Purpose: Garantir que a aplicação funciona em desktop e tablet (formulário preenchido em campo pela construtora) sem regressões visuais e com feedback de carregamento consistente (UX-05) antes do deploy de produção.
Output: Correções pontuais em layouts responsivos + `<Skeleton>` adicionado onde faltar + relatório de checkpoint visual aprovado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md
@.planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md

<interfaces>
<!-- Breakpoint pattern em uso no projeto (Tailwind v4 md: = 768px). Extraído de FormLayout.tsx via 12-PATTERNS.md. -->
Padrão de responsividade existente (FormLayout.tsx):
- `<aside className="... hidden w-[240px] md:flex">` — sidebar desktop aparece em md (768px+)
- `<div className="... md:ml-[240px]">` — conteúdo recebe offset em md
- `<main className="... px-6 pt-11 pb-6 md:px-8 md:pb-8">` — padding cresce em md
- `<div className="... md:hidden">` — tab bar mobile some em md

AdminSidebar.tsx colapsa entre 240px (expandida) e 60px (recolhida) via hook useSidebarCollapsed (Phase 9.5).

Componente Skeleton disponível em `roteiro-unificado/src/components/ui/skeleton.tsx` (exportado por `src/components/ui/index.ts`). Caminhos de carregamento de dados que usam isLoading/isFetching: `src/features/form/FormLayout.tsx`, `src/features/form/CompanyDashboard.tsx`, `src/features/form/HistoryPage.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar e corrigir overflow/sobreposição em layouts e páginas</name>
  <files>roteiro-unificado/src/components/layouts/AdminSidebar.tsx, roteiro-unificado/src/features/form/FormLayout.tsx, roteiro-unificado/src/components/layouts/AdminLayout.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.tsx (shell do formulário — padrão md: de sidebar/offset/padding)
    - roteiro-unificado/src/components/layouts/AdminSidebar.tsx (sidebar colapsável 240px/60px — padrão de transição)
    - roteiro-unificado/src/components/layouts/AdminLayout.tsx (margin dinâmico do conteúdo admin)
    - .planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md (seção "Padrão de Responsividade")
  </read_first>
  <action>
    Rodar `npm run dev` e percorrer cada fluxo (login, /form/:orgId, /admin/dashboard, /form/:orgId/history, e os botões de exportação se existirem) em DevTools Device Mode nas larguras exatas 1280px, 1024px e 768px. Para cada problema encontrado de overflow horizontal, elemento sobreposto ou texto cortado, aplicar a correção mínima usando o padrão Tailwind existente: usar `overflow-x-auto` em containers de tabela/grid largos, `min-w-0` em flex children que estouram, `flex-wrap` em rows que não cabem, e ajustar classes `md:` para que a transição sidebar-desktop ↔ tab-bar-mobile ocorra exatamente em 768px. NÃO introduzir novos breakpoints fora de `md:` (768px) — o projeto usa somente `md:`. NÃO alterar a lógica de colapso 240px/60px do AdminSidebar, apenas garantir que não causa overflow do conteúdo adjacente. Documentar no SUMMARY cada arquivo tocado e o problema corrigido; se um fluxo não tiver problemas, registrar "sem ajuste".
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run` sai com código 0 (167 testes passando, sem regressão)
    - FormLayout.tsx mantém a classe `md:flex` na sidebar e `md:hidden` na tab bar mobile (transição em 768px preservada)
    - Nenhum container de conteúdo principal tem largura fixa em px que cause overflow < 768px; grids/tabelas largas usam `overflow-x-auto`
    - SUMMARY lista os 5 fluxos × 3 breakpoints com status (corrigido / sem ajuste) para cada
  </acceptance_criteria>
  <done>Cada um dos 5 fluxos renderiza sem overflow horizontal nem sobreposição em 1280px, 1024px e 768px; correções aplicadas com classes Tailwind `md:` existentes; suite de testes verde.</done>
</task>

<task type="auto">
  <name>Task 2: Auditar e garantir estados de carregamento com Skeleton (UX-05)</name>
  <files>roteiro-unificado/src/features/form/FormLayout.tsx, roteiro-unificado/src/features/form/CompanyDashboard.tsx, roteiro-unificado/src/features/form/HistoryPage.tsx</files>
  <read_first>
    - roteiro-unificado/src/components/ui/skeleton.tsx (componente Skeleton existente — props e padrão de uso)
    - roteiro-unificado/src/features/form/FormLayout.tsx (branch isLoading/isFetching do shell do formulário)
    - roteiro-unificado/src/features/form/CompanyDashboard.tsx (branch isLoading das queries do dashboard)
    - roteiro-unificado/src/features/form/HistoryPage.tsx (branch isLoading das queries de histórico)
  </read_first>
  <action>
    Auditar todos os caminhos principais de carregamento de dados e garantir que cada branch `isLoading`/`isFetching` renderiza `<Skeleton>` em vez de tela em branco ou spinner ad-hoc. Para cada um dos três arquivos (FormLayout, CompanyDashboard, HistoryPage): localizar o branch de carregamento e confirmar que ele renderiza `<Skeleton>` (importado de `@/components/ui` ou `src/components/ui/skeleton.tsx`) e que o container do estado de carregamento expõe `aria-busy="true"` para acessibilidade. Onde o `<Skeleton>` estiver ausente, adicioná-lo usando o componente existente — NÃO criar um novo componente de skeleton nem um spinner novo. Manter o formato de skeleton aproximado ao conteúdo real (ex: linhas para listas, blocos para cards). NÃO alterar a lógica de fetch nem as queries; apenas o que é renderizado durante o carregamento. Documentar no SUMMARY, por arquivo, se o Skeleton já existia ("sem ajuste") ou foi adicionado, e onde o `aria-busy` foi aplicado.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Todos os branches `isLoading`/`isFetching` em FormLayout.tsx, CompanyDashboard.tsx e HistoryPage.tsx renderizam `<Skeleton>` com `aria-busy="true"` no container
    - `npm test -- --run` sai com código 0 (testes existentes passam, sem regressão)
    - SUMMARY registra por arquivo: Skeleton já presente ou adicionado, e onde aria-busy foi aplicado
  </acceptance_criteria>
  <done>Todos os caminhos principais de carregamento de dados exibem `<Skeleton>` com `aria-busy="true"`; nenhum componente novo de skeleton criado; suite de testes verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Correções de responsividade aplicadas nos layouts (FormLayout, AdminSidebar, AdminLayout) cobrindo os 5 fluxos em 1280px/1024px/768px, e estados de carregamento (<Skeleton> com aria-busy) verificados em dashboard, histórico e shell do formulário. Indicador de aba ativa e barra de progresso verificados.</what-built>
  <how-to-verify>
    1. Rodar `cd roteiro-unificado && npm run dev` e abrir o app no Chrome.
    2. Abrir DevTools (F12) → Device Toolbar (Cmd+Shift+M) → definir largura manual.
    3. Para cada largura 1280px, 1024px e 768px, percorrer: /login → fazer login → /form/:orgId (navegar entre 2-3 abas) → /form/:orgId/history → /admin/dashboard.
    4. Confirmar em cada tela: (a) nenhuma barra de rolagem horizontal aparece, (b) nenhum texto ou botão fica cortado/sobreposto, (c) o indicador de aba ativa (ring/cor) está visível e a barra de progresso no topo do formulário reflete o preenchimento.
    5. Em 768px especificamente: confirmar que a sidebar desktop do formulário aparece e a tab bar mobile NÃO aparece.
    6. Estados de carregamento (UX-05): no DevTools → Network, aplicar throttling "Slow 4G" e recarregar dashboard e histórico; confirmar que aparece um Skeleton (placeholder cinza animado) durante o carregamento, não tela em branco nem layout quebrado.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas visuais/de carregamento restantes por breakpoint.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→render | Nenhuma entrada não-confiável nova; trabalho puramente visual/CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-RESP | Information Disclosure | layouts responsivos | accept | Sem dados sensíveis expostos por mudanças de CSS; ajustes são visuais e não alteram fetch nem RLS |
</threat_model>

<verification>
- `npm test -- --run` verde (167 testes)
- Checkpoint humano de responsividade aprovado para os 5 fluxos × 3 breakpoints
- Estados de carregamento com Skeleton/aria-busy confirmados em dashboard, histórico e shell do formulário
</verification>

<success_criteria>
- UX-02: app funciona sem overflow/sobreposição em 1280px, 1024px e 768px
- UX-03: indicador de aba ativa e barra de progresso corretos em todos os breakpoints
- UX-05: todos os caminhos de carregamento de dados exibem `<Skeleton>` com `aria-busy="true"`
- Nenhuma regressão na suite de testes
</success_criteria>

<output>
Create `.planning/phases/12-polimento-ux-performance-deploy/12-01-SUMMARY.md` when done
</output>
