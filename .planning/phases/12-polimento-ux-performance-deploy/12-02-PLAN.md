---
phase: 12-polimento-ux-performance-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/components/ui/button.tsx
  - roteiro-unificado/src/components/ui/badge.tsx
autonomous: false
requirements: [UX-01]
must_haves:
  truths:
    - "Nenhum texto branco aparece sobre fundo laranja #F28C28 (bg-accent) — contraste WCAG AA ≥ 4.5:1"
    - "Botões e ícones interativos têm foco visível e aria-label quando não têm texto"
    - "Combinações de cor texto/fundo de button e badge atendem WCAG AA"
  artifacts:
    - path: "roteiro-unificado/src/components/ui/button.tsx"
      provides: "Variantes de botão com contraste WCAG AA"
      contains: "text-gray-900"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "WCAG AA contrast"
      via: "text-gray-900 sobre bg-accent"
      pattern: "bg-accent text-gray-900"
---

<objective>
Auditoria de acessibilidade básica (UX-01): corrigir a falha de contraste WCAG AA de texto branco sobre laranja `#F28C28` (ratio 2.45:1 — FALHA) substituindo por texto escuro `text-gray-900` (`#111827`, ratio 7.23:1 — PASSA); garantir `aria-label` em botões icônicos sem texto e foco visível em elementos interativos.

Purpose: A paleta azul/laranja (UX-01) precisa atender WCAG AA antes do deploy. Branco sobre laranja é ilegível para usuários com baixa visão e reprova auditorias de acessibilidade. Branco sobre azul (`#123B66`, 11.39:1) já PASSA e não deve ser alterado.
Output: button.tsx e badge.tsx auditados/corrigidos para contraste; checkpoint visual de acessibilidade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md
@.planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md

<interfaces>
<!-- Padrões de contraste e acessibilidade extraídos de 12-PATTERNS.md / 12-RESEARCH.md -->
Tabela de contraste WCAG AA (verificada computacionalmente):
- Branco (#FFF) sobre Azul (#123B66) = 11.39:1 → PASSA (não alterar)
- Branco (#FFF) sobre Laranja (#F28C28 = bg-accent) = 2.45:1 → FALHA (corrigir)
- Escuro (#111827 = text-gray-900) sobre Laranja = 7.23:1 → PASSA (correção)

button.tsx atual (variantes via cva): primary='bg-primary text-white', secondary, ghost, danger='bg-g1 text-white'. NÃO há variante accent hoje — auditar se alguma variante ou JSX inline usa bg-accent text-white.

badge.tsx atual: G1/G2/G4/G5='text-white', G3='text-primary-900' (já correto sobre amarelo).

Padrão aria-label (AdminSidebar.tsx): `<button type="button" aria-label="..."><svg aria-hidden="true" focusable="false">`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir contraste WCAG AA em button, badge e JSX inline</name>
  <files>roteiro-unificado/src/components/ui/button.tsx, roteiro-unificado/src/components/ui/badge.tsx</files>
  <read_first>
    - roteiro-unificado/src/components/ui/button.tsx (variantes cva atuais — primary/secondary/ghost/danger)
    - roteiro-unificado/src/components/ui/badge.tsx (gradeConfig G1-G5 — G3 já usa text-primary-900)
    - .planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md (seção button.tsx — correção de contraste)
    - .planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md (seção "Achado Crítico: Contraste de Cores WCAG AA")
  </read_first>
  <action>
    Rodar `grep -rn "bg-accent" roteiro-unificado/src/` para localizar todos os usos de `bg-accent` (laranja #F28C28). Para cada ocorrência que combine `bg-accent` com `text-white` (em button.tsx, badge.tsx ou JSX inline de pages/features), substituir `text-white` por `text-gray-900`. Se durante o build atual NÃO existir variante `accent` em button.tsx (cenário esperado pelo PATTERNS.md), adicionar a variante `accent: 'bg-accent text-gray-900 hover:bg-accent/90'` ao objeto de variantes do cva — para que qualquer CTA laranja futuro já nasça com contraste correto — e registrar no SUMMARY que nenhuma ocorrência pré-existente de `bg-accent text-white` foi encontrada. Em badge.tsx, verificar que G1 (`bg-g1`, vermelho) com `text-white` atende contraste; se o vermelho for claro demais (ratio < 4.5:1), trocar para `text-white` apenas se passar — caso contrário documentar. NÃO alterar `text-white` sobre `bg-primary` (azul, 11.39:1 — PASSA). NÃO inventar novas cores fora dos tokens existentes (`bg-accent`, `bg-primary`, `text-gray-900`, `text-primary-900`).
  </action>
  <verify>
    <automated>cd roteiro-unificado && if grep -rln "bg-accent" src/ 2>/dev/null | xargs -r grep -l "text-white" | grep -q .; then echo "FALHA: bg-accent com text-white encontrado"; exit 1; fi; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "bg-accent" roteiro-unificado/src/` não retorna nenhuma linha que combine `bg-accent` com `text-white` (todas usam `text-gray-900` ou outra cor escura)
    - Se uma variante `accent` foi adicionada, button.tsx contém literalmente `bg-accent text-gray-900`
    - `text-white` sobre `bg-primary` permanece inalterado em button.tsx (linha primary)
    - `npm test -- --run` sai com código 0
  </acceptance_criteria>
  <done>Nenhuma ocorrência de texto branco sobre fundo laranja resta no código; correções usam `text-gray-900`; tokens azuis intocados; testes verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Contraste WCAG AA corrigido (texto escuro sobre laranja), e auditoria de aria-label/foco visível em botões icônicos.</what-built>
  <how-to-verify>
    1. Rodar `cd roteiro-unificado && npm run dev`.
    2. Abrir DevTools → painel Accessibility (ou aba Lighthouse → categoria Accessibility) na página de login e no formulário.
    3. Confirmar que nenhum elemento laranja (`bg-accent`) exibe texto branco — o texto deve ser escuro e legível.
    4. Navegar com a tecla Tab por inputs, selects, checkboxes e botões — confirmar que cada elemento mostra anel de foco visível (`focus-visible:ring`).
    5. Inspecionar botões só com ícone (ex: toggle da sidebar, fechar dialog) — confirmar atributo `aria-label` descritivo e `aria-hidden="true"` no SVG interno.
    6. Opcional: rodar Lighthouse Accessibility e confirmar que não há violação de contraste reportada.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou liste os elementos com contraste/aria/foco ainda pendentes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→render | Mudanças de classe CSS; sem entrada não-confiável nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-A11Y | Information Disclosure | componentes UI (button/badge) | accept | Ajuste de contraste é cosmético; não altera autorização, fetch nem RLS |
</threat_model>

<verification>
- `grep` confirma ausência de `bg-accent text-white` no código
- `npm test -- --run` verde
- Checkpoint humano de acessibilidade aprovado (contraste + foco + aria-label)
</verification>

<success_criteria>
- UX-01: paleta azul/laranja atende WCAG AA (≥ 4.5:1 para texto normal)
- Botões icônicos têm aria-label; todos os interativos têm foco visível
- Nenhuma regressão de testes
</success_criteria>

<output>
Create `.planning/phases/12-polimento-ux-performance-deploy/12-02-SUMMARY.md` when done
</output>
