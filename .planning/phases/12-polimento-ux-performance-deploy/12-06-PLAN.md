---
phase: 12-polimento-ux-performance-deploy
plan: 06
type: execute
wave: 3
depends_on: [12-05]
files_modified:
  - .planning/phases/12-polimento-ux-performance-deploy/12-06-SUMMARY.md
autonomous: false
requirements: [UX-04, UX-06]
must_haves:
  truths:
    - "O fluxo end-to-end completo funciona em producao: login admin -> criar org -> login construtora -> preencher -> autosave -> submeter -> historico -> exportar"
    - "Feedback de autosave (toast) aparece em producao (UX-04)"
    - "Nenhuma credencial Supabase de dev aparece no bundle de producao"
  artifacts:
    - path: ".planning/phases/12-polimento-ux-performance-deploy/12-06-SUMMARY.md"
      provides: "Registro de cada passo do smoke test com resultado pass/fail"
  key_links:
    - from: "producao (Vercel)"
      to: "Supabase prod"
      via: "fluxo end-to-end real"
      pattern: "smoke test"
---

<objective>
Smoke tests pos-deploy (UX-04, UX-06): validar em producao todo o fluxo end-to-end — login admin, criar org, login construtora, preencher formulario, autosave, submeter, ver historico, exportar PDF e Excel — registrando o resultado de cada passo. Tambem verificar que nenhuma credencial Supabase de dev vazou no bundle de producao. O seed de dados do piloto e feito via o proprio painel admin em producao (D-03), validando o fluxo de onboarding real.

Purpose: Confirmacao final de que o sistema completo (UX-01 a UX-06) funciona em producao antes de declarar o piloto pronto. Verifica integracao real Vercel <-> Supabase prod com RLS.
Output: 12-06-SUMMARY.md com checklist de smoke test preenchido + verificacao de seguranca do bundle.

PRE-CONDICAO: Phases 10 (PDF) e 11 (Excel) precisam estar implementadas para testar exportacao. Se nao estiverem, os passos de exportacao sao marcados N/A com nota — os demais passos do fluxo permanecem obrigatorios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-polimento-ux-performance-deploy/12-CONTEXT.md
@.planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md

<interfaces>
<!-- Checklist de smoke test do ROADMAP Phase 12 + verificacao de credenciais (RESEARCH). -->
Fluxo end-to-end (ROADMAP UAT Phase 12): login admin -> criar org -> login construtora -> preencher formulario -> autosave (toast) -> submeter -> ver historico -> exportar PDF -> exportar Excel.

Seed via app (D-03): admin loga em prod -> /admin -> cria as 5 orgs do piloto -> convida usuarios das construtoras. Valida onboarding real.

Verificacao de credenciais (RESEARCH Security Domain): apos build de prod, grep no dist/ por host do Supabase de dev. A chave anon e publica por design (RLS protege os dados) — o que NAO pode vazar e o host/credenciais do projeto de DEV. Open Question 3: confirmar se Phases 10/11 estao implementadas antes de testar exportacao.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar ausencia de credenciais de dev no bundle de producao</name>
  <files>.planning/phases/12-polimento-ux-performance-deploy/12-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md (Security Domain + Variaveis de Ambiente)
    - roteiro-unificado/.env.local (host do projeto de dev para grep — NAO copiar valores ao SUMMARY)
  </read_first>
  <action>
    Rodar um build local apontando para as credenciais de dev NAO deve ser usado para esta verificacao; em vez disso, gerar o build de producao e confirmar que ele nao contem o host do projeto Supabase de DEV. Extrair o host do projeto de dev de .env.local (o subdominio antes de .supabase.co em VITE_SUPABASE_URL). Rodar `cd roteiro-unificado && npm run build` e depois rodar o comando de verificacao que extrai o host de dev de .env.local e faz grep recursivo em dist/ — deve retornar "OK: no dev credentials found in dist/" quando o build usa env vars de prod. Como o build de producao real ocorre no Vercel com as env vars do dashboard, complementar abrindo o bundle JS servido na URL de producao (DevTools -> Sources) e confirmar que o host presente e o de PROD, nao o de dev. Registrar no 12-06-SUMMARY.md: o comando de verificacao executado, o resultado (OK/host de prod), e a confirmacao de que nenhum host de dev aparece no bundle servido em producao. NAO escrever as chaves anon (dev ou prod) no SUMMARY — apenas o host e o veredito.
  </action>
  <verify>
    <automated>DEV_HOST=$(grep VITE_SUPABASE_URL roteiro-unificado/.env.local 2>/dev/null | sed 's|.*https://||' | cut -d. -f1 | tr -d '"' | tr -d "'") && if [ -n "$DEV_HOST" ] && grep -r "$DEV_HOST" roteiro-unificado/dist/ 2>/dev/null; then echo "CREDENTIAL LEAK DETECTED: $DEV_HOST found in dist/"; exit 1; else echo "OK: no dev credentials found in dist/"; fi</automated>
  </verify>
  <acceptance_criteria>
    - O comando de verificacao retorna "OK: no dev credentials found in dist/" (exit 0) sobre o bundle de prod; se encontrar o host de dev, sai com codigo 1 ("CREDENTIAL LEAK DETECTED")
    - O host presente no bundle servido em producao e o do projeto Supabase de PROD
    - 12-06-SUMMARY.md registra comando, resultado e veredito — sem expor chaves anon
  </acceptance_criteria>
  <done>Bundle de producao nao contem credenciais/host do Supabase de dev; verificacao registrada (T-12-01 confirmado).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deploy de producao pronto e bundle verificado sem credenciais de dev. Falta validar o fluxo end-to-end real em producao (seed via app + smoke test completo).</what-built>
  <how-to-verify>
    Na URL de producao (https://<app>.vercel.app), executar e registrar pass/fail de cada passo:
    1. Login admin: logar com a conta admin -> redireciona para /admin/dashboard.
    2. Criar org (D-03/seed): criar uma org "Construtora Piloto 1" com CNPJ valido -> aparece na lista.
    3. Adicionar usuario construtora a org e definir senha/convite.
    4. Login construtora: logar com a conta da construtora -> redireciona para /form/:orgId.
    5. Preencher: digitar em campos de 2-3 abas (ex: Identificacao, Torre Decisao).
    6. Autosave (UX-04): aguardar ~1.5s -> toast "Salvo automaticamente" aparece.
    7. Persistencia: F5 / reabrir -> dados preservados.
    8. Submeter: "Enviar Avaliacao" -> confirmar dialog -> status muda para enviado.
    9. Historico: acessar /form/:orgId/history -> versao listada com data e G1-G5.
    10. Exportar PDF (se Phase 10 implementada): clicar Exportar PDF -> download ocorre. Se nao implementada, marcar N/A.
    11. Exportar Excel (se Phase 11 implementada): clicar Exportar Excel -> download .xlsx ocorre. Se nao implementada, marcar N/A.
    12. UX-06: confirmar visualmente que o formulario usa o layout FormCard redesenhado (nao copia do HTML).
    13. Isolamento RLS: logar com construtora de OUTRA org e confirmar que nao ve dados da primeira.
  </how-to-verify>
  <resume-signal>Digite "aprovado" com o resultado pass/fail de cada um dos 13 passos, ou liste as falhas encontradas em producao.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet->prod | Usuarios reais acessam o app em producao |
| construtora A -> dados construtora B | Isolamento multi-tenant via RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | bundle de producao | mitigate | grep no bundle servido confirma ausencia do host Supabase de dev; chave anon e publica por design |
| T-12-RLS | Information Disclosure | Supabase prod (multi-tenant) | mitigate | Smoke test passo 13 verifica que construtora A nao ve dados de construtora B (RLS ativo em prod) |
| T-12-SUBMIT | Tampering | transicao draft->submitted | accept | Fix SAVE-03 (Phase 08.1) ja aplicado; smoke test passo 8 confirma submissao funciona em prod |
</threat_model>

<verification>
- grep do host de dev no bundle de prod retorna vazio
- Checklist de smoke test (13 passos) com resultado registrado no SUMMARY
- Isolamento RLS confirmado entre orgs em producao
</verification>

<success_criteria>
- UX-04: toast de autosave funciona em producao
- UX-06: formulario redesenhado (FormCard) confirmado em producao
- Fluxo end-to-end completo passa em producao (ou exportacao marcada N/A se Phases 10/11 pendentes)
- T-12-01 e T-12-RLS confirmados
</success_criteria>

<output>
Create `.planning/phases/12-polimento-ux-performance-deploy/12-06-SUMMARY.md` when done (checklist de smoke test preenchido + veredito de seguranca do bundle)
</output>
