---
phase: 12-polimento-ux-performance-deploy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/vite.config.ts
autonomous: false
requirements: [UX-05]
must_haves:
  truths:
    - "O bundle de produção é dividido em chunks separados (react-vendor, supabase, router) em vez de um único bundle de 812KB"
    - "npm run build completa sem erro e sem warning de chunk > 500KB para o bundle principal"
    - "Lighthouse Performance ≥ 85 e FCP < 1.5s no build de produção"
  artifacts:
    - path: "roteiro-unificado/vite.config.ts"
      provides: "Code splitting via Rolldown (Vite 8)"
      contains: "rolldownOptions"
  key_links:
    - from: "vite.config.ts"
      to: "Rolldown code splitting"
      via: "build.rolldownOptions.output.codeSplitting.groups"
      pattern: "codeSplitting"
---

<objective>
Otimização de bundle e performance (UX-05): configurar `build.rolldownOptions.output.codeSplitting.groups` no `vite.config.ts` (API do Vite 8 / Rolldown — NÃO usar `manualChunks` removido) para separar o bundle único atual de 812KB em chunks distintos: `react-vendor` (react + react-dom), `supabase` (@supabase/supabase-js) e `router` (react-router-dom). Medir FCP/LCP com Lighthouse; alvo: FCP < 1.5s, Lighthouse Performance ≥ 85.

Purpose: O build atual emite um único chunk de 812KB (229KB gzip), acima do warning de 500KB do Vite — degradando o First Contentful Paint. Separar vendor de app code permite cache duradouro do browser e melhora o load inicial, suportando UX-05 (loading rápido).
Output: vite.config.ts com codeSplitting + relatório de Lighthouse no checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md
@.planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md

<interfaces>
<!-- vite.config.ts atual (extraído do arquivo). Modificação adiciona build.rolldownOptions sem alterar plugins/resolve. -->
vite.config.ts atual:
- imports: defineConfig (vite), react (@vitejs/plugin-react), tailwindcss (@tailwindcss/vite), path, fileURLToPath (url)
- `const __dirname = path.dirname(fileURLToPath(import.meta.url))`
- config: `{ plugins: [react(), tailwindcss()], resolve: { alias: { '@': path.resolve(__dirname, './src') } } }`

Vite pinado em 8.0.14 (Rolldown bundler). API correta: `build.rolldownOptions.output.codeSplitting.groups` com itens `{ name, test (RegExp), priority (number) }`.

Grupos prescritos (RESEARCH Pattern 1):
- react-vendor: test /node_modules[\\/]react(-dom)?[\\/]/, priority 30
- supabase: test /node_modules[\\/]@supabase[\\/]/, priority 20
- router: test /node_modules[\\/]react-router(-dom)?[\\/]/, priority 10

Anti-pattern: build.rollupOptions.output.manualChunks (removido/depreciado no Vite 8).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar code splitting Rolldown no vite.config.ts</name>
  <files>roteiro-unificado/vite.config.ts</files>
  <read_first>
    - roteiro-unificado/vite.config.ts (estrutura atual — plugins + resolve.alias)
    - roteiro-unificado/package.json (confirmar versões: react 19.2.6, @supabase/supabase-js ^2.106.1, react-router-dom ^7.15.1, vite 8.0.14)
    - .planning/phases/12-polimento-ux-performance-deploy/12-PATTERNS.md (seção vite.config.ts — padrão a adicionar)
    - .planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md (Pattern 1 + Pitfall 2)
  </read_first>
  <action>
    Adicionar a chave `build` ao objeto de configuração do `defineConfig` em vite.config.ts, preservando `plugins` e `resolve` existentes. Dentro de `build`, criar `rolldownOptions.output.codeSplitting.groups` como um array com três entradas, cada uma um objeto com `name`, `test` (expressão regular) e `priority` (número): primeira entrada name `react-vendor` com test casando `node_modules` + `react` ou `react-dom` e priority 30; segunda entrada name `supabase` com test casando `node_modules` + `@supabase` e priority 20; terceira entrada name `router` com test casando `node_modules` + `react-router` ou `react-router-dom` e priority 10. Usar exatamente o padrão de regex `/node_modules[\\/]...[\\/]/` documentado no PATTERNS.md. NÃO usar `build.rollupOptions` nem `manualChunks` (removido no Vite 8). Após editar, rodar `npm run build` e confirmar no output que múltiplos chunks nomeados aparecem (react-vendor, supabase, router) e que o warning de chunk > 500KB para o bundle principal desapareceu.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -q "rolldownOptions" vite.config.ts && ! grep -q "manualChunks" vite.config.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contém `rolldownOptions` e a string `codeSplitting`
    - vite.config.ts NÃO contém `manualChunks` nem `rollupOptions`
    - `npm run build` sai com código 0
    - Output do build lista chunks nomeados `react-vendor`, `supabase` e `router` separados do chunk principal (verificável no SUMMARY com o output dos tamanhos)
    - Bundle principal não dispara mais o warning "Some chunks are larger than 500 kB"
  </acceptance_criteria>
  <done>Build de produção gera chunks separados react-vendor/supabase/router via Rolldown codeSplitting; sem warning de chunk grande; tsc -b passa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code splitting configurado no vite.config.ts via Rolldown; bundle dividido em react-vendor, supabase e router.</what-built>
  <how-to-verify>
    1. Rodar `cd roteiro-unificado && npm run build` e observar o output: confirmar chunks `react-vendor`, `supabase`, `router` listados separadamente, cada um menor que o antigo bundle único de 812KB.
    2. Rodar `npm run preview` para servir o build de produção localmente (porta padrão 4173).
    3. Abrir o URL do preview no Chrome → DevTools → aba Lighthouse → selecionar categoria Performance → Generate report (modo Desktop).
    4. Confirmar Lighthouse Performance ≥ 85 e First Contentful Paint < 1.5s.
    5. Na aba Network, confirmar que os chunks de vendor são carregados como arquivos JS separados.
  </how-to-verify>
  <resume-signal>Digite "aprovado" com o score de Performance e o FCP medidos, ou descreva o que ficou abaixo do alvo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build→artifact | Configuração de build; nenhum input runtime não-confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-BUNDLE | Tampering | vite.config.ts | accept | Mudança apenas de configuração de splitting; não altera código de runtime nem credenciais |
| T-12-SC | Tampering | npm installs | mitigate | Nenhum pacote novo instalado nesta fase (RESEARCH: audit não aplicável); CI roda `npm ci` com lock file versionado |
</threat_model>

<verification>
- `grep -q rolldownOptions` e `! grep manualChunks` em vite.config.ts
- `npm run build` verde com chunks separados
- Lighthouse Performance ≥ 85, FCP < 1.5s (checkpoint humano)
</verification>

<success_criteria>
- UX-05: bundle dividido reduz First Load; loading mais rápido
- Targets atingidos: Lighthouse Performance ≥ 85, FCP < 1.5s
- API Vite 8 correta (rolldownOptions, não manualChunks)
</success_criteria>

<output>
Create `.planning/phases/12-polimento-ux-performance-deploy/12-03-SUMMARY.md` when done
</output>
