---
phase: 12-polimento-ux-performance-deploy
plan: 05
type: execute
wave: 2
depends_on: [12-03, 12-04]
files_modified:
  - .planning/phases/12-polimento-ux-performance-deploy/12-05-SUMMARY.md
autonomous: false
requirements: [UX-04]
user_setup:
  - service: supabase
    why: "Criar projeto Supabase de producao separado e aplicar migrations (D-01, D-02)"
    dashboard_config:
      - task: "Criar novo projeto de producao no Supabase (plano Free permite 2 projetos)"
        location: "https://supabase.com/dashboard -> New project"
  - service: vercel
    why: "Hospedar o app SPA com auto-deploy a partir do GitHub (D-05)"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard (PROD) -> Settings -> API -> Project URL (configurar em Vercel -> Settings -> Environment Variables)"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard (PROD) -> Settings -> API -> anon key (configurar em Vercel)"
    dashboard_config:
      - task: "Conectar repositorio GitHub e definir Root Directory = roteiro-unificado"
        location: "Vercel Dashboard -> Add New Project -> Import Git Repository -> Settings -> General -> Root Directory"
      - task: "Habilitar branch protection em main exigindo o status check ci (apos primeiro CI verde)"
        location: "GitHub -> Settings -> Branches -> Branch protection rules"
must_haves:
  truths:
    - "Banco de producao tem as 11 migrations aplicadas via supabase db push"
    - "App esta deployado no Vercel e acessivel via URL de producao"
    - "Acesso direto a rotas SPA (/admin/dashboard, /form/:orgId) em producao nao retorna 404"
    - "Branch protection em main exige CI verde antes de merge"
  artifacts:
    - path: ".planning/phases/12-polimento-ux-performance-deploy/12-05-SUMMARY.md"
      provides: "Registro do deploy: URL de producao, project-ref Supabase, status das migrations"
  key_links:
    - from: "Vercel build"
      to: "Supabase producao"
      via: "VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY no Vercel Dashboard"
      pattern: "VITE_SUPABASE"
---

<objective>
Provisionar a producao: (1) criar o projeto Supabase de producao separado (D-01) e aplicar as 11 migrations via `supabase db push` (D-02), (2) conectar o repositorio ao Vercel com Root Directory = roteiro-unificado e env vars de producao, (3) realizar o primeiro deploy e validar SPA deep-linking, e (4) habilitar branch protection em main exigindo o CI (D-06). Este plano e majoritariamente operacional — Claude executa via CLI o que for possivel (supabase db push) e usa checkpoints para os passos que so existem no dashboard (criar projeto Supabase, configurar Vercel, ativar branch protection).

Purpose: Colocar a aplicacao no ar com banco de producao isolado de dev (credenciais de dev nunca chegam a prod, T-12-01) e deep-linking funcionando (T-12-02).
Output: App em producao no Vercel + banco prod migrado + 12-05-SUMMARY.md com URL e refs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-polimento-ux-performance-deploy/12-CONTEXT.md
@.planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md

<interfaces>
<!-- Fluxos de deploy verificados (RESEARCH Pattern 4 + Pitfalls 4,5,6). -->
Supabase: CLI 2.101.0 instalado. 11 migrations em supabase/migrations/ (20260522000001..08, 20260523000001, 20260523000002, 20260525000001). Fluxo prod: supabase link --project-ref <prod-ref> && supabase db push && supabase migration list. Edge Function create-user em supabase/functions/ (deploy via supabase functions deploy create-user) [ASSUMED A3 — pode falhar se APIs mudaram].

Vercel: Root Directory DEVE ser roteiro-unificado (Pitfall 4 — senao "No package.json found"). vercel.json (criado no Plan 04) ja tem o rewrite SPA. Auto-deploy: push para main -> producao (D-05).

Branch protection: so ativar APOS o primeiro CI rodar verde uma vez, selecionando o check ci pelo nome exato (Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Pre-requisito de provisionamento: o projeto Supabase de producao precisa existir antes de aplicar migrations, e a conta Vercel precisa estar disponivel.</what-built>
  <how-to-verify>
    1. Acessar https://supabase.com/dashboard -> New project. Criar um projeto NOVO de producao (NAO reutilizar o de dev — D-01). O plano Free permite 2 projetos ativos.
    2. Anotar: o project-ref (ex: abcdefghijklm), a database password, o Project URL e a anon key (Settings -> API).
    3. Confirmar que voce tem uma conta no Vercel (https://vercel.com) — criar se necessario.
    4. Fornecer o project-ref do Supabase de producao para o proximo passo.
  </how-to-verify>
  <resume-signal>Cole o project-ref de producao do Supabase e confirme que a conta Vercel existe.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Aplicar migrations ao banco de producao via Supabase CLI</name>
  <files>.planning/phases/12-polimento-ux-performance-deploy/12-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/12-polimento-ux-performance-deploy/12-RESEARCH.md (Pattern 4 + Pitfall 6)
    - supabase/migrations/ (confirmar as 11 migrations existentes)
  </read_first>
  <action>
    Usando o project-ref de producao fornecido no checkpoint anterior, rodar `supabase link --project-ref <prod-ref>` (Claude executa via Bash; se pedir a database password ou autenticacao, surge um auth gate dinamico para o usuario fornecer). Em seguida rodar `supabase db push` para aplicar as 11 migrations ao banco de producao, e `supabase migration list` para confirmar que todas as 11 estao aplicadas no remoto. SEMPRE linkar antes de push (Pitfall 6 — sem link, push pode falhar ou agir no projeto errado). Confirmar com o usuario via output que o projeto linkado e o de PRODUCAO (nao o de dev) antes de rodar push. Se a Edge Function create-user existir em supabase/functions/, rodar `supabase functions deploy create-user` (se falhar, registrar como pendencia no SUMMARY — A3). Registrar no 12-05-SUMMARY.md: project-ref de prod, output de migration list, e status do deploy da Edge Function.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && supabase migration list 2>/dev/null | tail -20 || echo "MANUAL — confirmar via output do supabase migration list que as 11 migrations estao aplicadas no remoto de producao"</automated>
  </verify>
  <acceptance_criteria>
    - supabase migration list mostra as 11 migrations (20260522000001..08, 20260523000001, 20260523000002, 20260525000001) com status aplicado no remoto de producao
    - O projeto linkado e o de PRODUCAO (project-ref de prod), confirmado no output
    - 12-05-SUMMARY.md registra o project-ref de prod e o resultado do migration list
  </acceptance_criteria>
  <done>Banco de producao tem as 11 migrations aplicadas; projeto correto linkado; resultado registrado no SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Migrations aplicadas ao banco de producao. Agora o deploy no Vercel exige configuracao no dashboard (sem CLI equivalente confiavel para a conexao inicial do repo).</what-built>
  <how-to-verify>
    1. No Vercel Dashboard -> Add New Project -> Import Git Repository -> selecionar o repositorio (privado, D-04).
    2. Em Settings -> General -> Root Directory, definir `roteiro-unificado` (Pitfall 4 — sem isso o build falha com "No package.json found").
    3. Em Settings -> Environment Variables (escopo Production), adicionar VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY com os valores do projeto Supabase de PRODUCAO (Settings -> API). NAO usar os valores de dev (D-01, T-12-01).
    4. Disparar o primeiro deploy (Deploy). Aguardar build concluir.
    5. Abrir a URL de producao gerada (https://<app>.vercel.app) e confirmar que a tela de login carrega.
    6. Testar deep-linking SPA: acessar diretamente https://<app>.vercel.app/admin/dashboard e https://<app>.vercel.app/login no browser e confirmar que NAO retorna 404 (o vercel.json rewrite deve servir index.html). Sem login, /admin/dashboard deve redirecionar para /login (guard de auth), nao dar 404.
    7. Fornecer a URL de producao final.
  </how-to-verify>
  <resume-signal>Cole a URL de producao do Vercel e confirme que login carrega e deep-linking nao retorna 404.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>App deployado e acessivel. Falta ativar branch protection exigindo o CI (D-06).</what-built>
  <how-to-verify>
    1. Garantir que o CI (Plan 04) ja rodou verde ao menos uma vez: abrir um PR de teste para main e aguardar o check ci ficar verde (Pitfall 5 — branch protection so reconhece o check apos a primeira execucao).
    2. Em GitHub -> Settings -> Branches -> Add branch protection rule para `main`.
    3. Habilitar "Require status checks to pass before merging" e selecionar o check `ci` pelo nome exato.
    4. Salvar a regra. Confirmar que merges para main agora exigem CI verde.
  </how-to-verify>
  <resume-signal>Digite "aprovado" confirmando que branch protection em main exige o check ci, ou descreva o bloqueio.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev->prod | Credenciais e dados de dev nao devem cruzar para producao |
| internet->Vercel CDN | Acesso publico ao app SPA |
| Vercel->Supabase prod | App em prod consulta o banco de producao com RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | env vars de prod | mitigate | Projeto Supabase de prod separado (D-01); vars configuradas no Vercel Dashboard, nunca commitadas; credenciais de dev nunca usadas em prod |
| T-12-02 | Spoofing / Elevation | SPA routing em prod | mitigate | vercel.json rewrite serve index.html; guards de auth (ProtectedRoute/AdminRoute) verificam sessao Supabase em toda rota protegida — acesso direto sem sessao redireciona para /login, nao expoe dados |
| T-12-RLS | Information Disclosure | Supabase prod | mitigate | As 11 migrations incluem RLS; db push aplica policies; isolamento multi-tenant garantido pelo banco, nao pela chave anon (publica por design) |
</threat_model>

<verification>
- supabase migration list mostra 11 migrations aplicadas em prod
- URL de producao carrega login; deep-linking nao retorna 404
- Branch protection em main exige check ci
- grep no bundle de prod nao revela credenciais de dev (validado no Plan 06)
</verification>

<success_criteria>
- D-01/D-02: projeto prod separado com migrations aplicadas
- D-05: auto-deploy GitHub -> Vercel ativo
- D-06: branch protection exigindo CI
- T-12-02: deep-linking SPA funciona em producao
</success_criteria>

<output>
Create `.planning/phases/12-polimento-ux-performance-deploy/12-05-SUMMARY.md` when done (registrar URL de producao, project-ref Supabase, status migrations e Edge Function)
</output>
