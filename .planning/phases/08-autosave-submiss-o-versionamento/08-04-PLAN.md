---
phase: 08-autosave-submiss-o-versionamento
plan: 04
type: execute
wave: 3
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - roteiro-unificado/src/features/form/FormLayout.tsx
  - roteiro-unificado/src/features/form/HistoryPage.tsx
autonomous: true
requirements:
  - SAVE-01
  - SAVE-02
  - SAVE-03
  - SAVE-05
  - SAVE-06
  - UX-04
  - UX-05

must_haves:
  truths:
    - "FormLayout busca draft do Supabase ao montar via useQuery e hidrata o Zustand store via hydrateFromAssessment"
    - "Skeleton é exibido no lugar do conteúdo de seção enquanto isLoading é true"
    - "useAutosave é montado no FormLayout e persiste rascunhos automaticamente"
    - "Sticky footer com botão 'Enviar Avaliação' aparece apenas quando activeTab === TabKey.Nda"
    - "Dialog de confirmação abre ao clicar em 'Enviar Avaliação' com copy exato do UI-SPEC"
    - "HistoryPage lista todas as avaliações da org com skeleton durante loading e card por versão"
    - "Card de versão mais recente mostra botão 'Iniciar Nova Revisão'; demais mostram 'Ver detalhes'"
  artifacts:
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "FormLayout atualizado com useQuery draft, useAutosave, hydration e submit dialog"
      contains: "useAutosave"
    - path: "roteiro-unificado/src/features/form/HistoryPage.tsx"
      provides: "Página /form/:orgId/history com lista de versões e botão nova revisão"
      exports: ["HistoryPage"]
  key_links:
    - from: "FormLayout.tsx"
      to: "useAutosave.ts"
      via: "useAutosave(tenantId)"
      pattern: "useAutosave\\(tenantId\\)"
    - from: "FormLayout.tsx"
      to: "formStore.hydrateFromAssessment"
      via: "useEffect([draftQuery.data])"
      pattern: "hydrateFromAssessment"
    - from: "HistoryPage.tsx"
      to: "useNewRevision.ts"
      via: "newRevisionMutation.mutate()"
      pattern: "useNewRevision"
---

<objective>
Integrar todos os artefatos dos planos 08-01, 08-02 e 08-03 nos dois componentes principais:
(1) FormLayout.tsx recebe useQuery para carregar draft, useEffect para hidratação, useAutosave
para persistência, skeleton durante loading, estado local para dialog de submissão, e sticky footer;
(2) HistoryPage.tsx é criado do zero como página de histórico de versões com useQuery, skeleton,
cards de versão com badges e botões de ação.

Purpose: Conectar toda a camada de persistência à UI. Após este plano, o fluxo completo
autosave → submissão → histórico → nova revisão está funcional no browser.
Output: FormLayout.tsx modificado + HistoryPage.tsx criado.
</objective>

<execution_context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/ROADMAP.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/08-autosave-submiss-o-versionamento/08-CONTEXT.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/08-autosave-submiss-o-versionamento/08-RESEARCH.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/08-autosave-submiss-o-versionamento/08-PATTERNS.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/08-autosave-submiss-o-versionamento/08-UI-SPEC.md

<interfaces>
<!-- FormLayout.tsx — estado atual (ler antes de modificar) -->
<!--
// Imports atuais (linhas 1-20):
import { useEffect } from 'react'
import { useParams, useLocation, useNavigate, Navigate } from 'react-router-dom'
import { useAuth } from '@/features/auth/useAuth'
import { useFormStore, TabKey } from '@/stores/formStore'
// ... imports de Section components e ui

// Corpo do componente (linhas 62+):
export function FormLayout() {
  const { orgId: routeOrgId } = useParams<{ orgId: string }>()
  const { orgId: authOrgId, signOut, isLoading } = useAuth()
  const location = useLocation()
  const navigate = useNavigate()
  const toast = useToast()
  const tenantId = routeOrgId ?? ''
  const store = useFormStore(tenantId)
  // ... guards e hash sync useEffect
  // ... return JSX (linhas 111-141)
}

// JSX atual — ponto de integração:
<main className="flex-1 p-4 md:p-6">
  <h1 className="text-xl font-semibold text-gray-900">{activeTabConfig.label}</h1>
  <ReadinessClassification tenantId={tenantId} />
  {renderSection(store.activeTab, tenantId)}   // ← substituir por conditional com skeleton
</main>
-->

<!-- Padrão de useQuery no FormLayout (per PATTERNS.md) -->
<!--
import { useQuery } from '@tanstack/react-query'
import { supabase } from '@/lib/supabase'

const draftQuery = useQuery({
  queryKey: ['assessment', 'draft', tenantId],
  queryFn: async () => {
    const { data, error } = await supabase.from('assessments').select('*')
      .eq('org_id', tenantId).eq('status', 'draft')
      .order('version', { ascending: false }).limit(1).maybeSingle()
    if (error) throw error
    return data  // null se sem draft (D-03)
  },
  staleTime: 30_000,
  enabled: !!tenantId,
})
-->

<!-- Padrão de hidratação (TanStack v5 — sem onSuccess em useQuery) -->
<!--
import { createFormStore } from '@/stores/formStore'

useEffect(() => {
  if (draftQuery.data?.form_data) {
    createFormStore(tenantId).getState().hydrateFromAssessment(draftQuery.data.form_data)
  }
}, [draftQuery.data, tenantId])  // eslint-disable-line react-hooks/exhaustive-deps
-->

<!-- Dialog de confirmação (per UI-SPEC + PATTERNS.md) -->
<!--
// Copiar estrutura de ArchiveOrgDialog.tsx; usar Dialog existente em src/components/ui/dialog.tsx
// Copy exato (per UI-SPEC §Copywriting Contract):
// - DialogTitle: "Enviar Avaliação?"
// - DialogDescription: "Após o envio, esta versão ficará imutável. Você poderá iniciar uma nova revisão a partir dela."
// - Botão cancelar: "Manter Rascunho" variant="secondary"
// - Botão confirmar: "Confirmar Envio" variant="primary" isLoading={submitMutation.isPending}
-->

<!-- HistoryPage — analog OrgDetail.tsx (ler antes de criar) -->
<!--
// Padrão de estrutura (OrgDetail.tsx linhas 1-98):
// - useParams<{ orgId: string }>()
// - isLoading guard no topo com Skeleton
// - Card com CardHeader + CardContent + lista de items
// - botões de ação em cada item

// Badge de status (per UI-SPEC §Card de Versão):
// "Enviado": className="bg-accent text-white text-xs font-semibold px-3 py-1 rounded-full"
// "Rascunho": className="bg-gray-100 text-gray-700 text-xs font-semibold px-3 py-1 rounded-full"

// Botão "Iniciar Nova Revisão" — apenas na versão com index === 0 (mais recente) E status !== 'draft'
// (se já existe um draft ativo, o botão não deve aparecer — há um draft em andamento)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Atualizar FormLayout com useQuery, hydratação, useAutosave, skeleton e dialog de submissão</name>
  <files>roteiro-unificado/src/features/form/FormLayout.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.tsx — ler o arquivo inteiro; mapear:
      (a) linhas de imports atuais para saber onde adicionar novos imports;
      (b) linha onde store = useFormStore(tenantId) para adicionar draftQuery logo após;
      (c) linha 136 `{renderSection(store.activeTab, tenantId)}` — substituir por condicional skeleton;
      (d) linha 138 `</main>` — adicionar sticky footer com dialog após
    - roteiro-unificado/src/components/ui/dialog.tsx — verificar os subcomponentes disponíveis
      (Dialog, DialogHeader, DialogTitle, DialogContent, DialogDescription, DialogFooter)
    - roteiro-unificado/src/components/ui/skeleton.tsx — verificar a interface do componente Skeleton
    - roteiro-unificado/src/components/admin/ArchiveOrgDialog.tsx — ler para copiar padrão de
      Dialog + isLoading + botões; é o template de confirmação
    - .planning/phases/08-autosave-submiss-o-versionamento/08-UI-SPEC.md §Botão "Enviar Avaliação"
      e §Dialog de Confirmação — copy exato e classes CSS
    - .planning/phases/08-autosave-submiss-o-versionamento/08-RESEARCH.md §Armadilha 2 (loop infinito
      na hidratação) e §Armadilha 5 (sticky footer invade conteúdo NDA)
  </read_first>
  <action>
    Modificar roteiro-unificado/src/features/form/FormLayout.tsx em quatro partes:

    PARTE 1 — Novos imports (adicionar após imports existentes):
    - `import { useState } from 'react'` (se não existir; verificar se useEffect já está importado)
    - `import { useQuery } from '@tanstack/react-query'`
    - `import { supabase } from '@/lib/supabase'`
    - `import { Skeleton } from '@/components/ui/skeleton'`
    - `import { Dialog, DialogHeader, DialogTitle, DialogContent, DialogDescription, DialogFooter } from '@/components/ui/dialog'`
    - `import { useAutosave } from '@/hooks/useAutosave'`
    - `import { useSubmitAssessment } from './useSubmitAssessment'`
    - `import { createFormStore } from '@/stores/formStore'`
    (Adicionar também useState se ainda não está nos imports do React)

    PARTE 2 — No corpo de FormLayout, após `const store = useFormStore(tenantId)`:

    a) State local para dialog:
       `const [isSubmitOpen, setIsSubmitOpen] = useState(false)`

    b) useQuery para draft (per D-01 e D-03):
       `const draftQuery = useQuery({ queryKey: ['assessment', 'draft', tenantId],
         queryFn: async () => { ... .maybeSingle() ... }, staleTime: 30_000, enabled: !!tenantId })`

    c) useEffect de hidratação (após draftQuery) — TanStack v5 sem onSuccess:
       `useEffect(() => { if (draftQuery.data?.form_data) {
         createFormStore(tenantId).getState().hydrateFromAssessment(draftQuery.data.form_data)
       } }, [draftQuery.data, tenantId])  // eslint-disable-line react-hooks/exhaustive-deps`

    d) Montar hooks:
       `useAutosave(tenantId)`
       `const submitMutation = useSubmitAssessment(tenantId)`

    PARTE 3 — No JSX, substituir `{renderSection(store.activeTab, tenantId)}` por:
    ```
    {draftQuery.isLoading ? (
      <div className="mt-4 space-y-4" aria-busy="true">
        <Skeleton className="h-10 w-full" />
        <Skeleton className="h-10 w-3/4" />
        <Skeleton className="h-10 w-full" />
        <Skeleton className="h-24 w-full" />
      </div>
    ) : (
      renderSection(store.activeTab, tenantId)
    )}
    ```

    Modificar o `<main>` para adicionar pb-20 quando na aba NDA (per RESEARCH.md Armadilha 5):
    `<main className={\`flex-1 p-4 md:p-6\${store.activeTab === TabKey.Nda ? ' pb-20' : ''}\`}>`

    PARTE 4 — Após `</main>`, antes do `</div>` que fecha o flex wrapper, adicionar:

    a) Sticky footer (per UI-SPEC §Botão "Enviar Avaliação"):
    ```
    {store.activeTab === TabKey.Nda && !draftQuery.isLoading && (
      <div className="sticky bottom-0 border-t border-gray-200 bg-white px-4 py-4 md:px-6
                      flex justify-end">
        <Button variant="primary" size="lg" className="w-full sm:w-auto"
          onClick={() => setIsSubmitOpen(true)}>
          Enviar Avaliação
        </Button>
      </div>
    )}
    ```

    b) Dialog de confirmação (per UI-SPEC §Dialog de Confirmação — copy exato):
    ```
    <Dialog open={isSubmitOpen} onOpenChange={setIsSubmitOpen}>
      <DialogHeader>
        <DialogTitle>Enviar Avaliação?</DialogTitle>
      </DialogHeader>
      <DialogContent>
        <DialogDescription>
          Após o envio, esta versão ficará imutável. Você poderá iniciar uma nova revisão
          a partir dela.
        </DialogDescription>
      </DialogContent>
      <DialogFooter>
        <Button variant="secondary" size="md"
          disabled={submitMutation.isPending}
          onClick={() => setIsSubmitOpen(false)}>
          Manter Rascunho
        </Button>
        <Button variant="primary" size="md"
          isLoading={submitMutation.isPending}
          onClick={() => submitMutation.mutate()}>
          {submitMutation.isPending ? 'Enviando...' : 'Confirmar Envio'}
        </Button>
      </DialogFooter>
    </Dialog>
    ```

    ATENÇÃO: Verificar a API exata do Dialog em dialog.tsx antes de usar onOpenChange vs onClose.
    Usar o prop correto conforme a implementação existente.

    INDICADOR INLINE DE AUTOSAVE (per UI-SPEC §Indicador de Autosave no Header):
    Adicionar após `<h1>` e antes de `<ReadinessClassification>`:
    ```
    {/* Indicador inline opcional — estado de autosave */}
    ```
    Nota: A implementação completa do indicador inline (lastSavedAt state no useAutosave) é
    considerada discretion do implementador. O mínimo obrigatório é o toast — o indicador inline
    pode ser omitido se complicar o scope desta tarefa.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v "^$" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - FormLayout.tsx importa e chama `useAutosave(tenantId)`
    - FormLayout.tsx importa e chama `useQuery` com queryKey ['assessment', 'draft', tenantId]
    - FormLayout.tsx tem `useEffect([draftQuery.data, tenantId])` chamando `hydrateFromAssessment`
      via `createFormStore(tenantId).getState()` (sem criar subscription React — evita Armadilha 2)
    - JSX substitui `renderSection(...)` por condicional com `draftQuery.isLoading ? <Skeleton> : renderSection`
    - `<main>` tem `pb-20` quando `store.activeTab === TabKey.Nda`
    - Sticky footer com "Enviar Avaliação" visível apenas quando `store.activeTab === TabKey.Nda`
    - Dialog de confirmação presente com títulos, descrição e botões conforme UI-SPEC copywriting contract
    - Dialog botão confirmar tem `isLoading={submitMutation.isPending}`
    - Dialog botão cancelar tem texto "Manter Rascunho"
    - `npx tsc --noEmit` passa sem erros em FormLayout.tsx
    - `npm run dev` inicia sem erros de compilação
  </acceptance_criteria>
  <done>
    FormLayout.tsx compilando com todos os hooks integrados. Autosave ativo, skeleton funcional,
    dialog de submissão configurado com copy correto.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar HistoryPage — página /form/:orgId/history com lista de versões</name>
  <files>roteiro-unificado/src/features/form/HistoryPage.tsx</files>
  <read_first>
    - roteiro-unificado/src/pages/admin/OrgDetail.tsx — ler para copiar: padrão de useParams,
      isLoading guard com Skeleton, estrutura de Card/CardHeader/CardContent, botões de ação;
      é o analog mais próximo (role-match no 08-PATTERNS.md)
    - roteiro-unificado/src/components/ui/index.ts — verificar o barrel para saber quais
      componentes estão disponíveis para import via '@/components/ui'
    - roteiro-unificado/src/components/ui/badge.tsx — verificar props do Badge (grade: 'G1'..'G5')
    - roteiro-unificado/src/features/form/useNewRevision.ts — verificar a assinatura (plano 08-03)
    - .planning/phases/08-autosave-submiss-o-versionamento/08-UI-SPEC.md §HistoryPage — layout
      max-w-3xl, card specs, badge colors, copy exato de botões e estados
    - .planning/phases/08-autosave-submiss-o-versionamento/08-PATTERNS.md §HistoryPage — template
      completo com useAssessmentHistory, isLoading guard, e estrutura JSX
  </read_first>
  <action>
    Criar roteiro-unificado/src/features/form/HistoryPage.tsx seguindo o template do PATTERNS.md
    §HistoryPage e as especificações do UI-SPEC §HistoryPage:

    Imports:
    - { useParams, useNavigate } from 'react-router-dom'
    - { useQuery } from '@tanstack/react-query'
    - { supabase } from '@/lib/supabase'
    - { Button, Card, CardHeader, CardContent } from '@/components/ui'
    - { Skeleton } from '@/components/ui/skeleton'
    - { Badge } from '@/components/ui/badge'
    - { useNewRevision } from './useNewRevision'

    Função local (não exportar) `useAssessmentHistory(orgId: string)`:
    - useQuery com queryKey ['assessments', orgId]
    - queryFn: supabase.from('assessments').select('id, version, status, submitted_at,
      readiness_level_mgmt, readiness_level_tech, created_at').eq('org_id', orgId)
      .order('version', { ascending: false })
    - if error: throw error; return data ?? []
    - staleTime: 60_000; enabled: !!orgId

    Componente exportado `export function HistoryPage()`:
    - `const { orgId } = useParams<{ orgId: string }>()`
    - `const navigate = useNavigate()`
    - `const { data: history, isLoading } = useAssessmentHistory(orgId ?? '')`
    - `const newRevisionMutation = useNewRevision(orgId ?? '')`

    Guard de isLoading (per OrgDetail.tsx pattern):
    Retornar skeleton de 3 cards fake enquanto isLoading, conforme UI-SPEC §Skeleton da HistoryPage:
    ```
    <div className="space-y-4">
      {[1, 2, 3].map(i => (
        <div key={i} className="rounded-lg border border-gray-200 bg-white p-4 space-y-3">
          <Skeleton className="h-5 w-24" />
          <Skeleton className="h-4 w-48" />
          <Skeleton className="h-6 w-16" />
        </div>
      ))}
    </div>
    ```

    Layout principal (per UI-SPEC §HistoryPage):
    - Container: `max-w-3xl mx-auto px-4 py-8`
    - Botão voltar acima do h1: `<Button variant="ghost" size="sm" onClick={() => navigate(\`/form/${orgId}\`)}>← Voltar ao Formulário</Button>`
    - `<h1 className="text-xl font-semibold text-gray-900">Histórico de Avaliações</h1>`
    - `<p className="text-sm text-gray-500 mt-1">Todas as versões enviadas pela sua empresa</p>`

    Empty state (nenhum assessment):
    - Heading: "Nenhuma avaliação enviada ainda"
    - Body: "Preencha o formulário e clique em 'Enviar Avaliação' para criar sua primeira versão."

    Lista de versões (per UI-SPEC §Card de Versão):
    Cada item da lista renderiza um `<article>`:
    - aria-label: `Versão ${row.version} — ${row.status === 'submitted' ? 'Enviada' : 'Rascunho'}`
    - Classes do card: `bg-white rounded-lg border border-gray-200 shadow-sm p-4 md:p-6`
    - Layout flex-col sm:flex-row para separar metadados e botões
    - "Versão N": `text-sm font-semibold text-gray-900`
    - Data de envio (se submitted_at): `Enviada em ${formatted}` — formatar com
      `new Date(row.submitted_at).toLocaleDateString('pt-BR')` + horário
    - Status badge:
      - 'submitted': `<span className="bg-accent text-white text-xs font-semibold px-3 py-1 rounded-full">Enviado</span>`
      - 'draft': `<span className="bg-gray-100 text-gray-700 text-xs font-semibold px-3 py-1 rounded-full">Rascunho</span>`
    - Nível gerencial: `<Badge grade={row.readiness_level_mgmt as 'G1'|'G2'|'G3'|'G4'|'G5'} />` — verificar API do Badge
    - Nível técnico: `<span className="text-sm text-gray-600">{row.readiness_level_tech ?? '—'}</span>`
    - Botão "Ver detalhes": `variant="secondary" size="sm"` — em todos os registros submitted
    - Botão "Iniciar Nova Revisão": `variant="primary" size="sm"` — APENAS no item com index === 0
      E status === 'submitted' (se o mais recente já é draft, não exibir — há revisão em andamento)
      - `isLoading={newRevisionMutation.isPending} onClick={() => newRevisionMutation.mutate()}`

    ATENÇÃO: "Iniciar Nova Revisão" só aparece na versão mais recente (index 0 no array ordenado
    por version DESC) E quando status === 'submitted'. Se history[0].status === 'draft', significa
    que já existe um rascunho ativo — neste caso, exibir apenas "Ver detalhes" em todos os registros.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v "^$" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo roteiro-unificado/src/features/form/HistoryPage.tsx existe
    - Exporta `HistoryPage` como named export
    - Contém `useAssessmentHistory` com queryKey ['assessments', orgId] e staleTime: 60_000
    - Contém skeleton de 3 cards durante isLoading com aria-busy implication
    - Cada versão renderiza como `<article>` com aria-label adequado
    - Badge de status 'submitted' usa className com `bg-accent text-white`
    - Badge de status 'draft' usa className com `bg-gray-100 text-gray-700`
    - Botão "Iniciar Nova Revisão" aparece apenas em index===0 && status==='submitted'
    - Empty state com headings conforme UI-SPEC §Copywriting Contract
    - Botão "← Voltar ao Formulário" com navigate para `/form/${orgId}`
    - `npx tsc --noEmit` passa sem erros em HistoryPage.tsx
  </acceptance_criteria>
  <done>
    HistoryPage.tsx criado e compilando. Exibe lista de versões com skeleton loading,
    badges de status corretos e fluxo de nova revisão configurado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → FormLayout | Dados do Supabase chegam ao componente via useQuery e hidratam o Zustand store |
| FormLayout → Dialog | Usuário confirma ação de submissão que é irreversível |
| HistoryPage → useNewRevision | Botão de nova revisão aciona INSERT no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Information Disclosure | draftQuery.data — form_data JSONB | accept | Dados da própria org acessíveis ao usuário autenticado; RLS impede cross-tenant; sem dado de outra org no payload |
| T-08-13 | Tampering | hydrateFromAssessment via useEffect | mitigate | `createFormStore(tenantId).getState()` acessa store sem subscription React; tenantId fixo do URL guard; dados vêm do banco via RLS |
| T-08-14 | Tampering | HistoryPage orgId de useParams | mitigate | HistoryPage está dentro de ProtectedRoute; orgId é validado via RLS no queryFn; dados de outra org retornam array vazio |
| T-08-15 | Denial of Service | draftQuery.isLoading — skeleton infinito | accept | TanStack Query tem retry automático; falha de rede mostra erro via isError; skeleton é temporário |
| T-08-16 | Tampering | "Iniciar Nova Revisão" sem draft ativo | mitigate | Lógica de index===0 && status==='submitted' impede botão quando draft já existe; RLS bloqueia INSERT duplicado via constraint 08-01 |
| T-08-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- npx tsc --noEmit passa sem erros em FormLayout.tsx e HistoryPage.tsx
- npm run dev inicia sem erros de compilação
- FormLayout.tsx contém useAutosave(tenantId), draftQuery, useEffect de hidratação, skeleton, sticky footer e dialog
- HistoryPage.tsx contém useAssessmentHistory, skeleton loading, cards com badges e botão nova revisão condicional
</verification>

<success_criteria>
FormLayout.tsx e HistoryPage.tsx compilando e integrados. O fluxo completo draft → submit →
histórico → nova revisão pode ser executado no browser após wiring do router no plano 08-05.
</success_criteria>

<output>
Criar `.planning/phases/08-autosave-submiss-o-versionamento/08-04-SUMMARY.md` ao concluir.
</output>
