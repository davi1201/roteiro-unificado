---
phase: 08-autosave-submiss-o-versionamento
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/package.json
  - roteiro-unificado/vitest.config.ts
  - roteiro-unificado/src/hooks/useAutosave.test.ts
  - roteiro-unificado/src/lib/readiness.test.ts
  - roteiro-unificado/src/features/form/useSubmitAssessment.test.ts
  - roteiro-unificado/src/features/form/useNewRevision.test.ts
autonomous: true
requirements:
  - SAVE-01
  - SAVE-02
  - SAVE-03
  - SAVE-04
  - SAVE-05
  - UX-04

must_haves:
  truths:
    - "Vitest está instalado e `npx vitest run` no diretório roteiro-unificado sai com código 0"
    - "vitest.config.ts existe com environment jsdom e include pattern src/**/*.test.ts"
    - "package.json contém script `test: vitest`"
    - "Arquivos stub de teste existem — apenas estrutura describe/it.todo, sem lógica de teste ainda"
  artifacts:
    - path: "roteiro-unificado/vitest.config.ts"
      provides: "Configuração Vitest com jsdom"
      contains: "jsdom"
    - path: "roteiro-unificado/src/hooks/useAutosave.test.ts"
      provides: "Stub de testes para useAutosave (SAVE-01, SAVE-02, UX-04)"
      contains: "describe"
    - path: "roteiro-unificado/src/lib/readiness.test.ts"
      provides: "Stub de testes para calculateReadiness"
      contains: "describe"
    - path: "roteiro-unificado/src/features/form/useSubmitAssessment.test.ts"
      provides: "Stub de testes para useSubmitAssessment (SAVE-03)"
      contains: "describe"
    - path: "roteiro-unificado/src/features/form/useNewRevision.test.ts"
      provides: "Stub de testes para useNewRevision (SAVE-04, SAVE-05)"
      contains: "describe"
  key_links:
    - from: "vitest.config.ts"
      to: "src/**/*.test.ts"
      via: "include pattern"
      pattern: "include.*test\\.ts"
    - from: "package.json scripts"
      to: "vitest"
      via: "\"test\": \"vitest\""
      pattern: "\"test\".*vitest"
---

<objective>
Instalar a infraestrutura de testes Vitest no projeto roteiro-unificado e criar os arquivos stub
de teste exigidos pelo VALIDATION.md da Phase 8. Sem este Wave 0, os planos 08-02 e 08-03
(que usam `tdd="true"`) referenciam arquivos de teste que não existem, tornando seus `<verify>`
inválidos.

Este plano NÃO implementa lógica de teste — apenas instala o framework, cria vitest.config.ts,
adiciona o script "test" ao package.json e cria arquivos stub com `describe` + `it.todo` para
cada hook a ser testado nos planos subsequentes.

Purpose: Cumprir a regra Nyquist — toda tarefa TDD precisa de um arquivo de teste existente
para o ciclo RED→GREEN→REFACTOR funcionar. Sem este Wave 0, `npx vitest run` falha porque
o binário não está instalado.
Output: Vitest instalado, vitest.config.ts criado, package.json atualizado, 4 arquivos stub criados.
</objective>

<execution_context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/ROADMAP.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/08-autosave-submiss-o-versionamento/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Instalar Vitest e criar vitest.config.ts</name>
  <files>roteiro-unificado/package.json, roteiro-unificado/vitest.config.ts</files>
  <read_first>
    - roteiro-unificado/package.json — verificar scripts existentes para não sobrescrever
      e confirmar que vitest não está já instalado em devDependencies
    - roteiro-unificado/tsconfig.json — verificar compilerOptions.types para saber se é
      necessário adicionar "vitest/globals" na configuração
  </read_first>
  <action>
    Executar a instalação no diretório roteiro-unificado:

    1. Instalar dependências de desenvolvimento:
       `cd roteiro-unificado && npm install -D vitest @vitest/coverage-v8 @testing-library/react @testing-library/user-event jsdom`

       Estes pacotes são verificados como legítimos (vitest: vitejs.dev, @testing-library: testing-library.com,
       jsdom: github.com/jsdom/jsdom). Nenhum é [SLOP] ou [ASSUMED].

    2. Adicionar script ao package.json em `scripts`:
       `"test": "vitest"`
       Adicionar após o script "build" existente. Não remover nem alterar scripts existentes.

    3. Criar roteiro-unificado/vitest.config.ts com o seguinte conteúdo (adaptado ao Vitest 3.x):
       - `import { defineConfig } from 'vitest/config'`
       - `import react from '@vitejs/plugin-react'`
       - `import path from 'path'`
       - `export default defineConfig({ ... })`
       - `plugins: [react()]`
       - `test.environment: 'jsdom'`
       - `test.include: ['src/**/*.test.ts', 'src/**/*.test.tsx']`
       - `test.globals: true`
       - `resolve.alias: { '@': path.resolve(__dirname, './src') }` — mesmo alias do vite.config.ts
         (verificar o alias exato em roteiro-unificado/vite.config.ts antes de copiar)

    IMPORTANTE: O `@vitejs/plugin-react` já está instalado como devDependency (é o bundler do Vite).
    Verificar em package.json que existe antes de adicionar ao config; se não existir, adicionar ao
    comando de install acima.

    NÃO usar `globals: true` no tsconfig sem verificar se causa conflito com tipos existentes.
    Usar `import { describe, it, expect } from 'vitest'` explicitamente nos arquivos de teste
    como fallback seguro se globals causar conflito.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" &amp;&amp; npx vitest run --reporter=verbose 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    package.json contém `"test": "vitest"` em scripts. vitest.config.ts existe com environment
    jsdom, include pattern e alias @. `npx vitest run` pode ser invocado (pode sair 0 mesmo
    sem arquivos de teste, ou com aviso de "no test files found" — ambos aceitáveis aqui;
    a Tarefa 2 cria os stubs que fazem o comando sair com código 0).
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar arquivos stub de teste para os hooks da Phase 8</name>
  <files>
    roteiro-unificado/src/hooks/useAutosave.test.ts,
    roteiro-unificado/src/lib/readiness.test.ts,
    roteiro-unificado/src/features/form/useSubmitAssessment.test.ts,
    roteiro-unificado/src/features/form/useNewRevision.test.ts
  </files>
  <action>
    Criar quatro arquivos stub de teste. Cada arquivo deve conter apenas a estrutura
    `describe` + `it.todo` — SEM lógica de teste, sem imports de módulos que ainda não existem,
    sem chamadas a funções não implementadas. O objetivo é que `npx vitest run` saia com código 0
    (it.todo passa vacuamente no Vitest).

    Arquivo 1 — src/hooks/useAutosave.test.ts:
    ```
    import { describe, it } from 'vitest'

    describe('useAutosave', () => {
      it.todo('SAVE-01: faz upsert com status draft após 1500ms de inatividade')
      it.todo('SAVE-02: cancela timer anterior ao receber nova mudança (debounce)')
      it.todo('UX-04: exibe toast "Salvo às HH:MM" após upsert com sucesso')
      it.todo('UX-04: exibe toast de warning quando upsert falha')
      it.todo('cleanup: cancela timer e faz unsubscribe ao desmontar')
    })
    ```

    Arquivo 2 — src/lib/readiness.test.ts:
    ```
    import { describe, it } from 'vitest'

    describe('calculateReadiness', () => {
      it.todo('retorna null para gerencial quando sectionData está vazio')
      it.todo('retorna G1 quando critérios mínimos de gestão estão presentes')
      it.todo('retorna HAB-A quando critérios de habilitação estão presentes')
      it.todo('ndaAceito é false quando campo nda não está preenchido')
    })
    ```

    Arquivo 3 — src/features/form/useSubmitAssessment.test.ts:
    ```
    import { describe, it } from 'vitest'

    describe('useSubmitAssessment', () => {
      it.todo('SAVE-03: muda status do draft para submitted via UPDATE')
      it.todo('SAVE-03: inclui submitted_at com timestamp atual')
      it.todo('SAVE-03: incrementa version no UPDATE')
      it.todo('navega para /form/:orgId/history após sucesso')
      it.todo('exibe toast de erro e não navega em caso de falha')
    })
    ```

    Arquivo 4 — src/features/form/useNewRevision.test.ts:
    ```
    import { describe, it } from 'vitest'

    describe('useNewRevision', () => {
      it.todo('SAVE-04: não sobrescreve registros submitted — usa INSERT')
      it.todo('SAVE-05: copia form_data da versão submitted mais recente')
      it.todo('SAVE-05: incrementa version no INSERT (latest.version + 1)')
      it.todo('navega para /form/:orgId após sucesso')
      it.todo('exibe toast de erro em caso de falha no INSERT')
    })
    ```

    Cada arquivo usa `import { describe, it } from 'vitest'` explícito em vez de globals
    para evitar dependência de configuração de globals no tsconfig.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" &amp;&amp; npx vitest run --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Quatro arquivos stub existem. `npx vitest run` sai com código 0 — todos os `it.todo`
    passam vacuamente no Vitest. A infraestrutura está pronta para os planos 08-02 e 08-03
    substituírem os stubs por testes reais durante a implementação TDD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| npm install → devDependencies | Pacotes instalados como devDependencies; não entram no bundle de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | npm install — pacotes de teste | mitigate | Todos os 5 pacotes são verificados: vitest (vitejs.dev), @vitest/coverage-v8 (vitejs.dev), @testing-library/react (testing-library.com), @testing-library/user-event (testing-library.com), jsdom (github.com/jsdom/jsdom) — nenhum é [ASSUMED] ou [SLOP] |
| T-08-SC | Tampering | npm installs | mitigate | slopcheck aplicado — todos os pacotes acima são legítimos e verificados; checkpoint não necessário pois todos são [VERIFIED] |
</threat_model>

<verification>
- `cd roteiro-unificado && npx vitest run` sai com código 0
- roteiro-unificado/vitest.config.ts existe e contém `jsdom`
- roteiro-unificado/package.json contém `"test": "vitest"` em scripts
- Os quatro arquivos stub existem em seus respectivos paths
- Nenhum arquivo stub contém lógica de teste real (apenas it.todo)
</verification>

<success_criteria>
Infraestrutura Vitest instalada e operacional. `npx vitest run` passa com 0 falhas.
Os planos 08-02 e 08-03 podem usar `vitest run` em seus `<verify>` com segurança.
</success_criteria>

<output>
Criar `.planning/phases/08-autosave-submiss-o-versionamento/08-00-SUMMARY.md` ao concluir.
</output>
