---
phase: 08-autosave-submiss-o-versionamento
plan: 05
type: execute
wave: 5
depends_on:
  - "08-04"
files_modified:
  - roteiro-unificado/src/router.tsx
autonomous: false
requirements:
  - SAVE-06
  - UX-05

must_haves:
  truths:
    - "Rota /form/:orgId/history está registrada no router e protegida por ProtectedRoute"
    - "Navegar para /form/:orgId/history renderiza HistoryPage sem erro 404"
    - "Preencher campo e aguardar 1.5s exibe toast 'Salvo às HH:MM'"
    - "Clicar 'Enviar Avaliação' na aba NDA abre o dialog de confirmação"
    - "Confirmar envio redireciona para /form/:orgId/history e o histórico lista a versão enviada"
    - "Skeleton aparece ao abrir /form/:orgId pela primeira vez com rede lenta"
  artifacts:
    - path: "roteiro-unificado/src/router.tsx"
      provides: "Rota /form/:orgId/history com HistoryPage dentro do bloco ProtectedRoute"
      contains: "HistoryPage"
  key_links:
    - from: "router.tsx"
      to: "HistoryPage.tsx"
      via: "path: '/form/:orgId/history'"
      pattern: "form.*orgId.*history"
---

<objective>
Registrar a rota `/form/:orgId/history` no router do app e executar checkpoint de verificação
visual do fluxo completo da Phase 8 no browser. Este é o último plano da fase: conecta
HistoryPage ao roteamento e valida que autosave, submissão, histórico e skeleton estão
funcionando end-to-end.

Purpose: Tornar HistoryPage acessível via URL e confirmar que toda a camada de persistência
está operacional no browser.
Output: router.tsx modificado + verificação humana do fluxo completo.
</objective>

<execution_context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/ROADMAP.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/08-autosave-submiss-o-versionamento/08-CONTEXT.md

<interfaces>
<!-- router.tsx — estrutura atual (ler antes de modificar) -->
<!--
import { createBrowserRouter, Navigate } from 'react-router-dom'
// ... imports de páginas
import { ProtectedRoute } from '@/components/routing/ProtectedRoute'
import { AdminRoute } from '@/components/routing/AdminRoute'

export const router = createBrowserRouter([
  { path: '/', element: <Navigate to="/login" replace /> },
  { path: '/login', element: <LoginPage /> },
  { path: '/forgot-password', element: <ForgotPasswordPage /> },
  { path: '/reset-password', element: <ResetPasswordPage /> },
  {
    element: <ProtectedRoute />,
    children: [
      { path: '/form/:orgId', element: <FormLayout /> },
      // ADICIONAR: { path: '/form/:orgId/history', element: <HistoryPage /> }
    ],
  },
  {
    element: <AdminRoute />,
    children: [
      { path: '/admin/dashboard', element: <AdminDashboard /> },
      { path: '/admin/orgs/:orgId', element: <OrgDetail /> },
    ],
  },
])
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar rota /form/:orgId/history no router.tsx</name>
  <files>roteiro-unificado/src/router.tsx</files>
  <read_first>
    - roteiro-unificado/src/router.tsx — ler o arquivo inteiro para confirmar a estrutura atual
      do bloco ProtectedRoute.children e onde adicionar a nova rota
    - roteiro-unificado/src/features/form/HistoryPage.tsx — confirmar que HistoryPage é um
      named export (não default) para usar import correto
  </read_first>
  <action>
    Modificar roteiro-unificado/src/router.tsx em duas etapas:

    1. Adicionar import de HistoryPage no bloco de imports de features/form:
       `import { HistoryPage } from '@/features/form/HistoryPage'`
       Adicionar junto ao import existente de FormLayout (para manter organização por feature).

    2. Dentro do bloco `element: <ProtectedRoute />`, no array `children`, adicionar após a
       rota `/form/:orgId`:
       `{ path: '/form/:orgId/history', element: <HistoryPage /> }`

    A ordem das rotas importa: React Router v6 faz matching mais específico primeiro, então
    `/form/:orgId/history` deve aparecer antes ou depois de `/form/:orgId` — ambos funcionam,
    mas coloque após para manter ordem de especificidade crescente.

    Não modificar nada além desses dois pontos — o resto do router permanece intocado.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v "^$" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - router.tsx contém `import { HistoryPage } from '@/features/form/HistoryPage'`
    - router.tsx contém `{ path: '/form/:orgId/history', element: <HistoryPage /> }` dentro do bloco ProtectedRoute.children
    - `npx tsc --noEmit` passa sem erros em router.tsx
    - `npm run dev` inicia sem erros de compilação
  </acceptance_criteria>
  <done>
    router.tsx atualizado. Rota /form/:orgId/history acessível e protegida por ProtectedRoute.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plano 08-00: Vitest instalado, vitest.config.ts criado, arquivos stub de teste criados.
    Plano 08-01: Migration UNIQUE parcial aplicada ao banco Supabase com colunas (org_id, status).
    Plano 08-02: formStore.hydrateFromAssessment + useAutosave com debounce 1500ms.
    Plano 08-03: useSubmitAssessment (UPDATE draft→submitted) + useNewRevision (INSERT append-only).
    Plano 08-04: FormLayout com useQuery draft + skeleton + dialog + sticky footer. HistoryPage criada.
    Plano 08-05 Tarefa 1: Rota /form/:orgId/history registrada.
  </what-built>
  <how-to-verify>
    Iniciar o servidor: `cd roteiro-unificado && npm run dev`

    Verificação 1 — Autosave funcional:
    1. Fazer login como construtora em /login
    2. Navegar para /form/:orgId
    3. Verificar que o Skeleton (4 barras cinzas pulsantes) aparece brevemente antes do conteúdo
    4. Digitar qualquer texto em qualquer campo da aba ativa
    5. Aguardar 1.5s sem digitar
    6. Confirmar que um toast "Salvo às HH:MM" aparece no canto inferior direito com horário atual
    7. Abrir o DevTools → Network → confirmar requisição POST/PATCH para Supabase em assessments

    Verificação 2 — Submissão formal:
    1. Navegar até a aba NDA (última aba)
    2. Verificar que existe um sticky footer fixo na parte inferior com botão "Enviar Avaliação"
    3. Verificar que nas outras abas o sticky footer NÃO aparece
    4. Clicar em "Enviar Avaliação"
    5. Confirmar que o dialog "Enviar Avaliação?" abre com:
       - Título "Enviar Avaliação?"
       - Descrição "Após o envio, esta versão ficará imutável. Você poderá iniciar uma nova revisão a partir dela."
       - Botão "Manter Rascunho" (à esquerda)
       - Botão "Confirmar Envio" (à direita)
    6. Clicar "Confirmar Envio"
    7. Confirmar redirect automático para /form/:orgId/history após o envio

    Verificação 3 — Histórico de versões:
    1. Confirmar que a página /form/:orgId/history carrega com lista de avaliações
    2. Verificar que existe pelo menos 1 card com badge "Enviado" (bg-accent laranja)
    3. Verificar que o card mais recente tem botão "Iniciar Nova Revisão"
    4. Verificar que existe botão "← Voltar ao Formulário" no topo

    Verificação 4 — Nova revisão:
    1. Clicar "Iniciar Nova Revisão" no histórico
    2. Confirmar redirect para /form/:orgId
    3. Verificar que os campos do formulário estão pré-preenchidos com dados da avaliação anterior

    Verificação 5 — Persistência após recarregar:
    1. Na aba de Identificação, alterar qualquer campo
    2. Aguardar 1.5s até toast de salvamento aparecer
    3. Fechar e reabrir o browser (ou pressionar F5)
    4. Navegar para /form/:orgId
    5. Confirmar que o dado alterado está preservado (vindo do Supabase, não de sessionStorage)

    Verificação 6 — Autosave com falha:
    1. Abrir DevTools → Network → Clicar em "Block request URL" para a URL do Supabase
    2. Digitar algo no formulário e aguardar 1.5s
    3. Confirmar que aparece toast warning "Falha ao salvar — tentando novamente"
    4. Remover o block
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se todos os 6 pontos de verificação passaram.
    Ou descreva o(s) problema(s) encontrado(s) para que o executor possa corrigir antes de continuar.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| ProtectedRoute → HistoryPage | Rota nova dentro de ProtectedRoute — requer autenticação |
| URL params → HistoryPage | orgId de useParams deve ser validado via RLS no queryFn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Spoofing | /form/:orgId/history — acesso sem autenticação | mitigate | Rota dentro de ProtectedRoute (React Router); sem sessão válida → redirect para /login |
| T-08-18 | Information Disclosure | /form/:orgId/history — acesso cross-tenant via URL manipulation | mitigate | useAssessmentHistory faz query com .eq('org_id', orgId); RLS is_org_member(org_id) bloqueia qualquer org_id que não seja do usuário autenticado; retorna array vazio para org alheia |
| T-08-19 | Tampering | "Iniciar Nova Revisão" acionado sem versão submitted | accept | Lógica React (index===0 && status==='submitted') previne botão; RLS bloqueia INSERT duplicado via constraint 08-01 |
| T-08-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- npx tsc --noEmit passa sem erros em router.tsx
- npm run dev inicia sem erros de compilação
- Checkpoint humano: todos os 6 pontos de verificação aprovados pelo usuário
</verification>

<success_criteria>
Rota registrada e verificação humana aprovada para:
- Autosave funcionando com toast "Salvo às HH:MM"
- Submissão criando versão imutável com redirect para /history
- HistoryPage listando versões com badges e botão de nova revisão
- Skeleton exibido durante carregamento inicial
- Persistência após refresh confirmada
- Toast de falha confirmado com bloqueio de rede
</success_criteria>

<output>
Criar `.planning/phases/08-autosave-submiss-o-versionamento/08-05-SUMMARY.md` ao concluir.
</output>
