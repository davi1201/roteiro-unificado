---
phase: 07-campos-habilita-es-nda-classifica-o-g1-g5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/schemas/hab-venda.ts
  - roteiro-unificado/src/schemas/hab-repositorios.ts
  - roteiro-unificado/src/schemas/hab-responsaveis.ts
  - roteiro-unificado/src/schemas/hab-classificacao.ts
  - roteiro-unificado/src/schemas/nda.ts
autonomous: true
requirements:
  - FORM-02
  - FORM-03
must_haves:
  truths:
    - "Cada um dos 5 schemas Zod corresponde 1-para-1 a uma das 5 abas restantes (HabVenda, HabRepositorios, HabResponsaveis, HabClassificacao, Nda) (D-08)"
    - "habVendaSchema, habRepositoriosSchema e habResponsaveisSchema usam estrutura aninhada `{ items: z.object({slug1: itemSchema, ...}).optional() }` (padrão torreSiengeSchema) — colunas seguem tipos D-09: existeControle=SelectField enum, repositorioPrincipal=SelectField enum, responsavelInterno=string, observacoes=string"
    - "ndaSchema é o único schema com campo obrigatório — `aceitaTermos: z.literal(true, {errorMap: () => ({message: 'Você deve aceitar os termos do NDA para continuar'})})` (D-07)"
    - "Cada schema exporta uma constante REQUIRED_COUNT (4 com valor 0, NDA com valor 1)"
    - "Cada schema de matriz exporta um array `as const` de slugs (HAB_SCENARIOS, HAB_DOCUMENT_DOMAINS, HAB_RESPONSIBILITIES) e o tipo derivado correspondente"
  artifacts:
    - path: "roteiro-unificado/src/schemas/hab-venda.ts"
      provides: "habVendaSchema + HabVendaData + HAB_SCENARIOS + HabScenarioSlug + HAB_VENDA_REQUIRED_COUNT"
      contains: "scenarios: z.object"
    - path: "roteiro-unificado/src/schemas/hab-repositorios.ts"
      provides: "habRepositoriosSchema + HabRepositoriosData + HAB_DOCUMENT_DOMAINS + HabDocumentSlug + HAB_REPOSITORIOS_REQUIRED_COUNT"
      contains: "documents: z.object"
    - path: "roteiro-unificado/src/schemas/hab-responsaveis.ts"
      provides: "habResponsaveisSchema + HabResponsaveisData + HAB_RESPONSIBILITIES + HabResponsabilidadeSlug + HAB_RESPONSAVEIS_REQUIRED_COUNT"
      contains: "responsibilities: z.object"
    - path: "roteiro-unificado/src/schemas/hab-classificacao.ts"
      provides: "habClassificacaoSchema + HabClassificacaoData + HAB_CLASSIFICACAO_REQUIRED_COUNT"
      contains: "classificacaoFinal: z.enum(['hab-a', 'hab-b', 'hab-c', 'hab-d', 'hab-e'])"
    - path: "roteiro-unificado/src/schemas/nda.ts"
      provides: "ndaSchema + NdaData + NDA_REQUIRED_COUNT (=1)"
      contains: "z.literal(true"
  key_links:
    - from: "roteiro-unificado/src/schemas/hab-classificacao.ts"
      to: "roteiro-unificado/src/lib/readiness.ts"
      via: "enum slug match"
      pattern: "hab-a.*hab-b.*hab-c.*hab-d.*hab-e"
    - from: "roteiro-unificado/src/schemas/nda.ts"
      to: "calculateReadiness"
      via: "aceitaTermos field name match"
      pattern: "aceitaTermos.*z\\.literal\\(true"
---

<objective>
Criar os 5 schemas Zod das abas restantes (Hab. Venda, Hab. Repositórios, Hab. Responsáveis, Hab. Classificação, NDA) com todos os campos do HTML de referência preservados (FORM-02) e validação de campo obrigatório do NDA via `z.literal(true)` (FORM-03). Schemas seguem 1-para-1 o inventário em RESEARCH.md §"Inventário Completo de Campos do HTML".

Purpose: Estabelecer a fonte de verdade tipada para todas as Section components da Wave 2. Schemas vazios bloqueariam todos os planos subsequentes — entregá-los na Wave 1 paralelamente à fundação (07-01) maximiza paralelismo.
Output: 5 arquivos Zod em `src/schemas/` seguindo o padrão `torre-classificacao.ts` (flat opcional) e `torre-sienge.ts` (matriz aninhada com `.optional()` no objeto pai).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-PATTERNS.md
@roteiro-unificado/src/schemas/torre-classificacao.ts
@roteiro-unificado/src/schemas/torre-sienge.ts
@roteiro-unificado/src/schemas/torre-decisao.ts
@roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
<!-- Padrão torre-sienge.ts (matriz aninhada com .optional()) -->
```typescript
const moduleSchema = z.object({
  contratado: z.enum([...]).optional(),
  /* demais colunas */
})

export const torreSiengeSchema = z.object({
  modules: z.object({
    cadastros: moduleSchema,
    /* demais slugs */
  }).optional(),  // ← .optional() OBRIGATÓRIO (RESEARCH Pitfall 2)
})

export type TorreSiengeData = z.infer<typeof torreSiengeSchema>
export const TORRE_SIENGE_REQUIRED_COUNT = 0
export const SIENGE_MODULES = [{ slug: 'cadastros', label: '...' }, /* ... */] as const
export type SiengeModuleSlug = (typeof SIENGE_MODULES)[number]['slug']
```

<!-- Padrão torre-classificacao.ts (flat opcional) -->
```typescript
export const torreClassificacaoSchema = z.object({
  classificacaoFinal: z.enum(['t360-a', /* ... */]).optional(),
  /* outros selects + textareas */
})

export type TorreClassificacaoData = z.infer<typeof torreClassificacaoSchema>
export const TORRE_CLASSIFICACAO_REQUIRED_COUNT = 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar schemas das três matrizes (Hab. Venda, Hab. Repositórios, Hab. Responsáveis)</name>
  <files>roteiro-unificado/src/schemas/hab-venda.ts, roteiro-unificado/src/schemas/hab-repositorios.ts, roteiro-unificado/src/schemas/hab-responsaveis.ts</files>
  <read_first>
    - roteiro-unificado/src/schemas/torre-sienge.ts (template exato: itemSchema reutilizado + objeto pai `.optional()` + array `as const` de slugs + tipo derivado)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md (seções "Aba Hab. Venda", "Aba Hab. Repositórios", "Aba Hab. Responsáveis" + arrays HAB_SCENARIOS, HAB_DOCUMENT_DOMAINS, HAB_RESPONSIBILITIES com slugs)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (linhas 626-665: arrays JavaScript scenarioData, documentData, responsibilities — fonte de verdade dos slugs)
  </read_first>
  <action>
    Criar três arquivos seguindo EXATAMENTE o padrão de `torre-sienge.ts` (importar zod, definir itemSchema interno reutilizado, schema raiz com objeto aninhado `.optional()`, exportar tipo data via `z.infer`, exportar `REQUIRED_COUNT = 0`, exportar array `as const` de slugs+labels, exportar tipo de slug derivado).

    **hab-venda.ts:**
    - `scenarioSchema` interno com 5 campos: `acontece` (enum `['sim','nao','parcial','nao-sabe']` opcional), `importancia` (enum `['alta','media','baixa','eventual']` opcional), `quemConduz` (z.string().optional()), `principalDificuldade` (z.string().optional()), `observacoes` (z.string().optional())
    - Campos flat (acima da matriz): `principalFormaVenda` (enum `['venda-propria','contratos-privados','licitacoes','obras-terceiros','financiamento-habitacional','misto']` opcional), `quemPedeDocumentos` (enum `['bancos','clientes-privados','orgaos-publicos','seguradoras','cartorios','investidores','misto']` opcional), `prazoTipico` (enum `['mesmo-dia','1-3-dias','ate-1-semana','mais-1-semana','sem-padrao']` opcional), `perdeuOportunidade` (enum `['sim','nao','quase','nao-sabe']` opcional), `principaisExigencias` (z.string().optional()), `ondeCostumaTravar` (z.string().optional())
    - `scenarios: z.object({ 'venda-propria': scenarioSchema, 'contratos-privados': scenarioSchema, 'financiamento-habitacional': scenarioSchema, 'financiamento-investimento': scenarioSchema, 'licitacoes': scenarioSchema, 'homologacao': scenarioSchema, 'programas-convenios': scenarioSchema, 'seguros-garantias': scenarioSchema, 'fiscalizacoes': scenarioSchema, 'abertura-obra': scenarioSchema }).optional()`
    - `HAB_SCENARIOS` array conforme RESEARCH.md §"Slugs recomendados para scenarioData" (10 entradas com slug+label)
    - `export const HAB_VENDA_REQUIRED_COUNT = 0`
    - `export type HabScenarioSlug = (typeof HAB_SCENARIOS)[number]['slug']`

    **hab-repositorios.ts:**
    - `documentSchema` interno com 5 campos: `existeControle` (enum `['sim','parcial','nao','nao-sabe']` opcional), `repositorioPrincipal` (enum `['ged','pasta-local','google-drive','sharepoint','nao-possui']` opcional, **per D-09 — não usar string livre**), `responsavelInterno` (z.string().optional()), `terceirosEnvolvidos` (z.string().optional() — mantém FORM-02 incluindo coluna do HTML), `observacoes` (z.string().optional())
    - Campos flat: `ondeDocumentosVivem` (z.array(z.enum) com 12 opções: 'google-drive', 'onedrive-sharepoint', 'dropbox', 'servidor-local', 'sienge-erp', 'sistema-juridico', 'contador', 'escritorio-juridico', 'email-whatsapp', 'pastas-fisicas', 'terceiros-fornecedores', 'nao-ha-padrao' — todas `.optional()`); `existePadraoPastas`, `existePadraoNomes`, `controlamValidade`, `existeTrilhaVersao` (todos enum `['sim','parcial','nao']` opcional); `observacoesRepositorios` (z.string().optional())
    - `documents: z.object({...14 slugs...}).optional()` com chaves: 'empresa-cnpj', 'fiscal-tributario', 'trabalhista-previdenciario', 'contabil-financeiro', 'obras-alvaras', 'responsaveis-tecnicos', 'contratos-procuracoes', 'financiamentos-bancos', 'seguros-caucoes', 'fornecedores-terceiros', 'sst', 'ambiental-cartorial', 'certificacoes', 'kits-clientes'
    - `HAB_DOCUMENT_DOMAINS` array conforme RESEARCH.md §"Slugs recomendados para documentData" (14 entradas)
    - `export const HAB_REPOSITORIOS_REQUIRED_COUNT = 0`
    - `export type HabDocumentSlug = (typeof HAB_DOCUMENT_DOMAINS)[number]['slug']`

    **hab-responsaveis.ts:**
    - `responsabilidadeSchema` interno com 5 campos: `quemFaz` (z.string().optional()), `existeSubstituto` (enum `['sim','parcial','nao','nao-sabe']` opcional), `terceiroDependente` (enum `['sim','parcial','nao','nao-sabe']` opcional), `maiorDificuldade` (z.string().optional()), `observacoes` (z.string().optional())
    - Campos flat: `dificuldadesRecorrentes` (z.array(z.enum) com 12 opções: 'documento-vencido', 'documento-nao-localizado', 'dependencia-contador', 'dependencia-juridico', 'dependencia-engenharia', 'dependencia-fornecedores', 'duvida-aplicabilidade', 'falta-padrao-envio', 'retrabalho-kit', 'documentos-whatsapp', 'falta-historico', 'sem-dono-processo'); `tempoMedioKit` (enum `['mesmo-dia','1-3-dias','ate-1-semana','mais-1-semana','nao-medem']` opcional); `existeChecklist`, `existeRenovacao`, `existeValidacao` (todos enum `['sim','parcial','nao']` opcional); `observacoesRotina` (z.string().optional())
    - `responsibilities: z.object({...10 slugs...}).optional()` com chaves: 'solicitar-oportunidade', 'localizar-internos', 'solicitar-terceiros', 'validar-completude', 'renovar-certidoes', 'montar-kit', 'aprovar-envio', 'registrar-historico', 'acompanhar-pendencias', 'atualizar-repositorio'
    - `HAB_RESPONSIBILITIES` array conforme RESEARCH.md §"Slugs recomendados para responsibilities" (10 entradas)
    - `export const HAB_RESPONSAVEIS_REQUIRED_COUNT = 0`
    - `export type HabResponsabilidadeSlug = (typeof HAB_RESPONSIBILITIES)[number]['slug']`

    Adicionar JSDoc no topo de cada arquivo explicando que é schema de matriz aninhada (padrão torre-sienge) e mencionando o array de slugs/labels exportado.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "(hab-venda|hab-repositorios|hab-responsaveis)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist: `src/schemas/hab-venda.ts`, `src/schemas/hab-repositorios.ts`, `src/schemas/hab-responsaveis.ts`
    - `grep -c "\.optional()" roteiro-unificado/src/schemas/hab-venda.ts` returns at least 7 (6 flat fields + scenarios object)
    - `grep -c "as const" roteiro-unificado/src/schemas/hab-venda.ts` returns 1 (HAB_SCENARIOS array)
    - `grep -c "as const" roteiro-unificado/src/schemas/hab-repositorios.ts` returns 1
    - `grep -c "as const" roteiro-unificado/src/schemas/hab-responsaveis.ts` returns 1
    - `grep -E "'venda-propria'|venda-propria:" roteiro-unificado/src/schemas/hab-venda.ts` finds entries (slug present in both array and schema)
    - `grep -E "'empresa-cnpj'|empresa-cnpj:" roteiro-unificado/src/schemas/hab-repositorios.ts` finds entries
    - `grep -E "'solicitar-oportunidade'|solicitar-oportunidade:" roteiro-unificado/src/schemas/hab-responsaveis.ts` finds entries
    - `grep -c "export const HAB_VENDA_REQUIRED_COUNT" roteiro-unificado/src/schemas/hab-venda.ts` returns 1
    - `grep -c "export const HAB_REPOSITORIOS_REQUIRED_COUNT" roteiro-unificado/src/schemas/hab-repositorios.ts` returns 1
    - `grep -c "export const HAB_RESPONSAVEIS_REQUIRED_COUNT" roteiro-unificado/src/schemas/hab-responsaveis.ts` returns 1
    - `grep -c "ged.*pasta-local.*google-drive\|repositorioPrincipal.*z.enum" roteiro-unificado/src/schemas/hab-repositorios.ts` returns at least 1 (repositorioPrincipal is enum, NOT string)
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>Três schemas de matriz criados com escopo aninhado .optional(), arrays as const com slugs+labels, tipos derivados exportados; nenhum erro TS</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar schemas flat de Hab. Classificação e NDA</name>
  <files>roteiro-unificado/src/schemas/hab-classificacao.ts, roteiro-unificado/src/schemas/nda.ts</files>
  <read_first>
    - roteiro-unificado/src/schemas/torre-classificacao.ts (template exato: selects opcionais + textareas opcionais + REQUIRED_COUNT)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md (seção "Aba Hab. Classificação" e "Aba NDA" com lista completa de campos)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md (D-07: aceitaTermos é único obrigatório)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (seções id="h_fechamento" e id="nda")
  </read_first>
  <action>
    **hab-classificacao.ts:** Schema flat seguindo padrão `torre-classificacao.ts`. Campos:
    - `classificacaoFinal: z.enum(['hab-a','hab-b','hab-c','hab-d','hab-e']).optional()` (consumido por `calculateReadiness` — slugs lowercase devem bater com `habMap` no readiness.ts)
    - `abordagemRecomendada: z.enum(['implantar-direto','implantar-carga-assistida','ativar-repositorio-responsaveis','comecar-dossie-especifico','fase-preparatoria']).optional()`
    - `escopoInicialSugerido: z.enum(['empresa-cnpj','obra-empreendimento','financiamento','licitacao','contrato-privado-homologacao','fiscalizacao-auditoria','misto-reduzido']).optional()`
    - `complexidadePreco: z.enum(['baixa','media','alta','critica']).optional()`
    - `fase1: z.string().optional()`, `fase2: z.string().optional()`, `riscosPrincipais: z.string().optional()`, `evidenciasEssenciais: z.string().optional()`, `observacoesFinais: z.string().optional()`
    - `export type HabClassificacaoData = z.infer<typeof habClassificacaoSchema>`
    - `export const HAB_CLASSIFICACAO_REQUIRED_COUNT = 0`
    Adicionar JSDoc no topo mencionando que `classificacaoFinal` é o campo que alimenta `calculateReadiness` (linkando lib/readiness.ts).

    **nda.ts:** Schema com campo obrigatório único:
    - `nomeRepresentante: z.string().optional()`
    - `cargo: z.string().optional()`
    - `cpf: z.string().optional()`
    - `dataAceite: z.string().default(() => new Date().toLocaleDateString('pt-BR'))` — auto-preenchida ao montar
    - `aceitaTermos: z.literal(true, { errorMap: () => ({ message: 'Você deve aceitar os termos do NDA para continuar' }) })` — único campo obrigatório (D-07)
    - `observacoes: z.string().optional()`
    - `export type NdaData = z.infer<typeof ndaSchema>`
    - `export const NDA_REQUIRED_COUNT = 1`
    Adicionar JSDoc no topo explicando que esta é a única aba com campo obrigatório (per D-07) e que o erro deve ser visível ao tentar submeter.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "(hab-classificacao|nda\.ts)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Two files exist: `src/schemas/hab-classificacao.ts`, `src/schemas/nda.ts`
    - `grep -c "z.enum(\['hab-a', 'hab-b', 'hab-c', 'hab-d', 'hab-e'\])" roteiro-unificado/src/schemas/hab-classificacao.ts` returns 1 OR `grep -E "'hab-a'.*'hab-b'.*'hab-c'.*'hab-d'.*'hab-e'" roteiro-unificado/src/schemas/hab-classificacao.ts` returns at least 1 match
    - `grep -c "export const HAB_CLASSIFICACAO_REQUIRED_COUNT = 0" roteiro-unificado/src/schemas/hab-classificacao.ts` returns 1
    - `grep -c "z.literal(true" roteiro-unificado/src/schemas/nda.ts` returns 1
    - `grep -c "Você deve aceitar os termos do NDA" roteiro-unificado/src/schemas/nda.ts` returns 1
    - `grep -c "export const NDA_REQUIRED_COUNT = 1" roteiro-unificado/src/schemas/nda.ts` returns 1
    - `grep -c "toLocaleDateString('pt-BR')" roteiro-unificado/src/schemas/nda.ts` returns 1
    - `grep -c "aceitaTermos" roteiro-unificado/src/schemas/nda.ts` returns at least 1
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>hab-classificacao.ts e nda.ts existem; classificacaoFinal usa enum hab-a..hab-e compatível com readiness.ts; aceitaTermos é z.literal(true) com errorMap; NDA_REQUIRED_COUNT=1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Não aplicável a esta fase | Schemas são definições estáticas — sem persistência ou crossing de trust boundary nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | ndaSchema.aceitaTermos | mitigate | `z.literal(true)` aceita apenas o valor exato `true`; outros valores (false, undefined, "true" string, 1) falham a validação com mensagem definida via errorMap |
| T-07-02-02 | Tampering | matriz schemas (scenarios/documents/responsibilities) | mitigate | Schemas usam `z.enum([...])` para selects — apenas slugs definidos são aceitos; valores fora do enum são rejeitados pelo zodResolver no RHF |
| T-07-02-03 | Tampering | repositorioPrincipal (Hab. Repositórios) | mitigate | D-09 mudou de string livre (HTML) para `z.enum(['ged','pasta-local','google-drive','sharepoint','nao-possui'])` — reduz superfície de input e bloqueia entrada arbitrária |
| T-07-02-SC | Tampering | Package installs | accept | Sem instalação nova — apenas `zod` já instalada |

## Applicable ASVS Categories (L1)

- V5 Input Validation: schemas Zod cobrem todos os campos das 5 abas; campos opcionais aceitam undefined/string; enums rejeitam valores fora do conjunto; checkbox obrigatório rejeita ausência
</threat_model>

<verification>
1. `npx tsc --noEmit` em `roteiro-unificado/` exits 0
2. `grep -c "export const HAB_VENDA_REQUIRED_COUNT\|export const HAB_REPOSITORIOS_REQUIRED_COUNT\|export const HAB_RESPONSAVEIS_REQUIRED_COUNT\|export const HAB_CLASSIFICACAO_REQUIRED_COUNT\|export const NDA_REQUIRED_COUNT" roteiro-unificado/src/schemas/{hab-venda,hab-repositorios,hab-responsaveis,hab-classificacao,nda}.ts` retorna 5
3. Manual: abrir cada schema e validar 1-para-1 contra inventário em 07-RESEARCH.md §"Inventário Completo de Campos do HTML"
4. Manual: confirmar que `nda.ts` tem `errorMap` no `z.literal(true)` e `hab-classificacao.ts` usa enum lowercase `hab-a..hab-e` (consumido pelo `habMap` em readiness.ts)
</verification>

<success_criteria>
- 5 schemas Zod criados em `src/schemas/`
- Todos compilam sem erros TypeScript
- 4 schemas com REQUIRED_COUNT=0, NDA com REQUIRED_COUNT=1
- Arrays de slugs (`HAB_SCENARIOS`, `HAB_DOCUMENT_DOMAINS`, `HAB_RESPONSIBILITIES`) exportados `as const` com 10/14/10 entradas
- Tipos derivados (`HabScenarioSlug`, `HabDocumentSlug`, `HabResponsabilidadeSlug`) exportados
- `classificacaoFinal` em `hab-classificacao.ts` usa slugs lowercase batendo com `habMap` em `readiness.ts`
- `aceitaTermos` em `nda.ts` é `z.literal(true)` com `errorMap` português
</success_criteria>

<output>
Criar `.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-02-SUMMARY.md` ao concluir, listando os 5 schemas e o número de entradas de cada array de slugs.
</output>
