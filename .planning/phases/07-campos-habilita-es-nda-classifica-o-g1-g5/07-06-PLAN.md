---
phase: 07-campos-habilita-es-nda-classifica-o-g1-g5
plan: 06
type: execute
wave: 3
depends_on:
  - 07-03
  - 07-04
  - 07-05
files_modified:
  - roteiro-unificado/src/features/form/FormLayout.tsx
autonomous: false
requirements:
  - FORM-02
  - FORM-03
  - FORM-07
  - UX-02
must_haves:
  truths:
    - "Construtora navega para qualquer uma das 5 novas abas (Hab. Venda, Hab. Repositórios, Hab. Responsáveis, Hab. Classificação, NDA) e vê o formulário com campos — sem placeholder de fase futura"
    - "ReadinessClassification aparece em todas as abas, entre o <h1> e o renderSection() do <main>"
    - "Todas as 10 abas do formulário exibem conteúdo ao clicar na navegação lateral"
    - "Construtora preenche o select 'Nível gerencial' na Torre Decisão e o badge G1-G5 aparece imediatamente em ReadinessClassification"
    - "Construtora preenche o select 'Classificação final' na aba Hab. Classificação e o badge HAB-X aparece imediatamente"
    - "Construtora marca o checkbox 'Li e aceito' na aba NDA e o indicador 'NDA aceito' aparece imediatamente"
    - "Formulário é operável em viewport 768px sem overflow horizontal nas 5 abas novas (UX-02)"
  artifacts:
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "Shell com switch completo para as 10 abas + ReadinessClassification em todas as abas"
      contains: "case TabKey.HabVenda"
  key_links:
    - from: "FormLayout.tsx renderSection"
      to: "5 Section components da Phase 7"
      via: "switch case + import"
      pattern: "case TabKey\\.(HabVenda|HabRepositorios|HabResponsaveis|HabClassificacao|Nda):"
    - from: "FormLayout.tsx <main>"
      to: "ReadinessClassification"
      via: "JSX render entre h1 e renderSection"
      pattern: "<ReadinessClassification tenantId="
---

<objective>
Religar o FormLayout para renderizar os 5 Section components novos no switch (substituindo o placeholder default) e adicionar o componente `ReadinessClassification` no `<main>` para exibir o resultado em tempo real em todas as abas. Concluir a Phase 7 com checkpoint humano de verificação visual no browser cobrindo todas as 10 abas + comportamento da engine de prontidão + erro do checkbox NDA + responsividade 768px.

Purpose: Finalizar a Phase 7 com uma integração compacta (uma modificação de arquivo) + verificação humana que confirma os critérios UAT da fase. O checkpoint cobre os 6 critérios UAT em `ROADMAP.md §Phase 7` em uma única sessão.
Output: 1 arquivo modificado (FormLayout.tsx) + verificação visual aprovada pelo usuário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-PATTERNS.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-UI-SPEC.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-03-SUMMARY.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-04-SUMMARY.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-05-SUMMARY.md
@roteiro-unificado/src/features/form/FormLayout.tsx
@roteiro-unificado/roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
<!-- FormLayout.tsx (estado atual — modificar) -->
- Linha 10-14: imports dos 5 sections existentes (Identificacao, TorreDecisao, TorreSienge, TorreAcesso, TorreClassificacao)
- Linha 21-40: função `renderSection(activeTab, tenantId)` com switch sobre activeTab — 5 cases + default placeholder
- Linha 122-124: `<main>` com `<h1>{activeTabConfig.label}</h1>` seguido de `{renderSection(store.activeTab, tenantId)}`

<!-- 5 Section components a importar -->
- `HabVendaSection`, `HabRepositoriosSection`, `HabResponsaveisSection`, `HabClassificacaoSection` em `./sections/`
- `NdaSection` em `./sections/NdaSection`

<!-- ReadinessClassification -->
- Exportado de `./ReadinessClassification`
- Props: `{ tenantId: string }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar imports + 5 cases novos no switch + ReadinessClassification no main</name>
  <files>roteiro-unificado/src/features/form/FormLayout.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.tsx (estado atual completo — linhas 1-129)
    - roteiro-unificado/src/features/form/sections/HabVendaSection.tsx (recém-criado em 07-03)
    - roteiro-unificado/src/features/form/sections/HabRepositoriosSection.tsx (recém-criado em 07-03)
    - roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx (recém-criado em 07-04)
    - roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx (recém-criado em 07-04)
    - roteiro-unificado/src/features/form/sections/NdaSection.tsx (recém-criado em 07-05)
    - roteiro-unificado/src/features/form/ReadinessClassification.tsx (recém-criado em 07-05)
  </read_first>
  <action>
    Modificar `roteiro-unificado/src/features/form/FormLayout.tsx`:

    **(a) Adicionar 6 imports** após a linha 14 (após `import { TorreClassificacaoSection } from './sections/TorreClassificacaoSection'`):
    - `import { HabVendaSection } from './sections/HabVendaSection'`
    - `import { HabRepositoriosSection } from './sections/HabRepositoriosSection'`
    - `import { HabResponsaveisSection } from './sections/HabResponsaveisSection'`
    - `import { HabClassificacaoSection } from './sections/HabClassificacaoSection'`
    - `import { NdaSection } from './sections/NdaSection'`
    - `import { ReadinessClassification } from './ReadinessClassification'`

    **(b) Adicionar 5 cases no switch** dentro da função `renderSection`, ANTES do `default` (linha 33):
    - `case TabKey.HabVenda: return <HabVendaSection tenantId={tenantId} />`
    - `case TabKey.HabRepositorios: return <HabRepositoriosSection tenantId={tenantId} />`
    - `case TabKey.HabResponsaveis: return <HabResponsaveisSection tenantId={tenantId} />`
    - `case TabKey.HabClassificacao: return <HabClassificacaoSection tenantId={tenantId} />`
    - `case TabKey.Nda: return <NdaSection tenantId={tenantId} />`

    **(c) Atualizar o comentário JSDoc da função `renderSection`** (linhas 16-20): substituir "O default cobre as 5 abas Habilitações/NDA — serão implementadas na Phase 7." por "Todas as 10 abas têm cases explícitos após Phase 7. O `default` é fallback defensivo para o tipo enum exhaustivo (não deve ser acionado em runtime)."

    **(d) Atualizar a mensagem do `default`** (linha 33-39) para refletir que agora é um estado impossível: substituir o texto atual por `<p className="mt-2 text-sm text-gray-500">Aba desconhecida.</p>` (estado defensivo, não deve aparecer).

    **(e) Inserir `<ReadinessClassification tenantId={tenantId} />`** dentro do `<main>`, ENTRE o `<h1>` (linha 123) e a chamada `{renderSection(store.activeTab, tenantId)}` (linha 124). Resultado: `<h1>...</h1>` → `<ReadinessClassification tenantId={tenantId} />` → `{renderSection(...)}`.

    Não alterar nada mais no FormLayout.tsx — preservar ProgressBar, cross-tenant guard, hash sync, sidebar, signOut, etc.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit && npm run lint 2>&1 | grep -E "FormLayout" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { HabVendaSection } from './sections/HabVendaSection'" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "import { HabRepositoriosSection } from './sections/HabRepositoriosSection'" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "import { HabResponsaveisSection } from './sections/HabResponsaveisSection'" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "import { HabClassificacaoSection } from './sections/HabClassificacaoSection'" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "import { NdaSection } from './sections/NdaSection'" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "import { ReadinessClassification } from './ReadinessClassification'" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "case TabKey.HabVenda:" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "case TabKey.HabRepositorios:" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "case TabKey.HabResponsaveis:" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "case TabKey.HabClassificacao:" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "case TabKey.Nda:" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "<ReadinessClassification tenantId" roteiro-unificado/src/features/form/FormLayout.tsx` returns 1
    - `grep -c "Esta seção ainda não possui campos" roteiro-unificado/src/features/form/FormLayout.tsx` returns 0 (placeholder removido/substituído)
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
    - `npm run lint` exits 0 ou apenas com warnings pré-existentes não relacionados à FormLayout
  </acceptance_criteria>
  <done>FormLayout.tsx tem os 6 imports, 5 cases novos no switch, ReadinessClassification entre h1 e renderSection; tsc + lint passam</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual — verificar 10 abas + engine G1-G5 + erro NDA + responsividade 768px</name>
  <what-built>
    Phase 7 completa: 5 Section components novos (HabVenda, HabRepositorios, HabResponsaveis, HabClassificacao, Nda), engine `calculateReadiness`, componente `ReadinessClassification` em tempo real, e wiring final no FormLayout. Cobertura: FORM-02 (todos campos do HTML), FORM-03 (validação Zod incluindo NDA), FORM-07 (classificação G1-G5/HAB-X automática), UX-02 (responsivo desktop+tablet).
  </what-built>
  <how-to-verify>
    1. **Rodar dev server**: `cd roteiro-unificado && npm run dev` — abrir `http://localhost:5173`
    2. **Login**: entrar como construtora (usuário de teste do piloto)
    3. **Navegar para `/form/:orgId`** — confirmar que a sidebar continua mostrando as 10 abas
    4. **Verificar ReadinessClassification (badge bar)**:
       - Em qualquer aba (ex: Identificação): badge bar deve estar visível entre o título da aba e o conteúdo
       - Estado inicial (sem preencher nada): texto "Preencha as abas de classificação para ver o resultado"
    5. **Engine G1-G5 (Torre Decisão)**:
       - Ir para aba Torre Decisão
       - Selecionar "G3 — Médio" no select "Nível gerencial"
       - Badge "G3 — Médio" amarelo aparece imediatamente no badge bar SEM precisar salvar/recarregar
       - Mudar para "G5 — Excelente" — badge muda para verde imediatamente
    6. **Engine HAB-X (Hab. Classificação)**:
       - Ir para aba Hab. Classificação
       - Selecionar "HAB-A — Pronta para operação"
       - Badge "HAB-A — Pronta" verde aparece no badge bar (ao lado do G5)
       - Mudar para "HAB-D — Risco alto" — badge muda para laranja
    7. **NDA — checkbox obrigatório**:
       - Ir para aba NDA
       - Verificar que texto legal aparece em div scrollable (max altura 400px, scroll interno funciona)
       - Verificar que campo "Data de aceite" está disabled e preenchido com data de hoje (formato DD/MM/AAAA)
       - SEM marcar o checkbox: tentar mudar para outra aba (válido — RHF mode onBlur só valida ao perder foco no campo); voltar para NDA e clicar em qualquer outro campo de input/textarea para forçar blur — observar se erro "Você deve aceitar os termos do NDA para continuar" aparece abaixo do checkbox (pode ser que apareça apenas em submit explícito; verificar comportamento)
       - Marcar o checkbox "Li e aceito os termos do NDA mútuo"
       - Indicador "NDA aceito" verde aparece no badge bar
    8. **Cobertura de campos (FORM-02)**:
       - Visitar cada aba nova (Hab. Venda, Hab. Repositórios, Hab. Responsáveis, Hab. Classificação, NDA)
       - Confirmar que cada uma renderiza os campos descritos em `07-RESEARCH.md §Inventário Completo de Campos do HTML` (10 cenários em Hab Venda, 14 domínios em Hab Repositórios, 10 atividades em Hab Responsáveis, 4 selects + 5 textareas em Hab Classificação, NDA com texto + 4 campos + checkbox + textarea)
       - Comparar campo a campo contra `roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html`
    9. **Persistência cross-tab**:
       - Preencher alguns campos em Hab. Venda (ex: select "Principal forma de venda" + textarea "Onde costuma travar?")
       - Navegar para outra aba e voltar para Hab. Venda
       - Confirmar que os valores foram preservados
    10. **Responsividade 768px (UX-02)**:
       - Redimensionar browser para 768px (ou usar DevTools responsive mode iPad)
       - Confirmar que: sidebar vira pills horizontais no topo, formulário não tem overflow horizontal, grid das matrizes (2 colunas em desktop) vira 1 coluna, badge bar quebra linhas com flex-wrap
       - Testar especificamente nas 5 abas novas: Hab. Venda, Hab. Repositórios, Hab. Responsáveis, Hab. Classificação, NDA
    11. **Sem erros de console**: abrir DevTools Console e confirmar zero erros vermelhos durante toda a navegação acima
  </how-to-verify>
  <resume-signal>Digite "approved" se todos os 11 passos passaram, ou descreva o problema encontrado (ex: "G3 não aparece amarelo, fica como G1 vermelho" — indicaria slug mismatch entre schema e engine; ex: "campo X faltando na aba Y" — indicaria FORM-02 incompleto).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route + URL → renderSection | URL hash define a aba ativa; switch usa enum TabKey fechado (sem fall-through arbitrário) |
| store.sectionData → ReadinessClassification | Engine pura tolera valores inesperados (mapas explícitos com `?? null`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | FormLayout switch | mitigate | Enum TabKey é fechado; switch tem case explícito para todas as 10 TabKeys + default defensivo "Aba desconhecida" — sem code path acionando renderização de componente fora do conjunto |
| T-07-06-02 | Information Disclosure | ReadinessClassification em todas as abas | accept | Resultado de prontidão é visível para o usuário da org logada; o cross-tenant guard no FormLayout (linha 86) já bloqueia acesso a tenants externos |
| T-07-06-03 | Tampering | ReadinessClassification useMemo | mitigate | calculateReadiness é pura e usa mapas com `?? null` — slugs inválidos retornam null em vez de crash; UI exibe empty state |
| T-07-06-04 | Denial of Service | Render loop entre Sections e ReadinessClassification | mitigate | useMemo observa `store.sectionData` (referência única do Zustand); subscription pattern nas Sections evita loop; padrão validado em Phase 6 |
| T-07-06-SC | Tampering | Package installs | accept | Sem nova instalação |

## Applicable ASVS Categories (L1)

- V5 Input Validation: switch fechado sobre enum, sem deserialização de string arbitrária para componente
- V4 Access Control: cross-tenant guard preservado (FormLayout linhas 77-87)
</threat_model>

<verification>
1. `npx tsc --noEmit` em `roteiro-unificado/` exits 0
2. `npm run lint` em `roteiro-unificado/` exits 0 ou só com warnings pré-existentes
3. `npm run build` em `roteiro-unificado/` exits 0 — bundle de produção sem erros
4. Checkpoint humano aprovado (todos os 11 passos do `how-to-verify` confirmados)
5. UAT cobertos pelo checkpoint visual (todos os 6 itens do `ROADMAP.md §Phase 7 UAT`)
</verification>

<success_criteria>
- FormLayout.tsx modificado com 6 novos imports, 5 novos cases no switch, ReadinessClassification entre h1 e renderSection
- Build de produção (`npm run build`) completa sem erros
- Checkpoint humano aprovado com nota "approved" — 11 passos verificados
- Engine G1-G5 atualiza badge em tempo real ao mudar select na Torre Decisão (FORM-07)
- Engine HAB-X atualiza badge em tempo real ao mudar select na Hab. Classificação (FORM-07)
- Checkbox NDA é obrigatório (`z.literal(true)`); marcar o checkbox faz aparecer indicador "NDA aceito" (FORM-03)
- Todas as 5 abas novas têm os campos descritos no HTML de referência (FORM-02)
- Formulário operável em 768px sem overflow horizontal nas 5 abas novas (UX-02)
- Zero erros no console durante toda a navegação
</success_criteria>

<output>
Criar `.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-06-SUMMARY.md` ao concluir, com:
- Confirmação do checkpoint humano aprovado
- Resumo dos 6 critérios UAT da Phase 7 (todos verificados)
- Lista das 5 Section components ligadas no FormLayout
- Nota explícita de que a Phase 7 está finalizada e o próximo passo é a Phase 8 (autosave + versionamento)
</output>
