---
phase: 07-campos-habilita-es-nda-classifica-o-g1-g5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/components/ui/input-field.tsx
  - roteiro-unificado/src/components/ui/index.ts
  - roteiro-unificado/src/constants/nda-text.ts
  - roteiro-unificado/src/lib/readiness.ts
autonomous: true
requirements:
  - FORM-07
must_haves:
  truths:
    - "InputField é exportado em src/components/ui/index.ts e pode ser importado de @/components/ui"
    - "NDA_TEXT é uma constante readonly de string[] com pelo menos 10 parágrafos extraídos do HTML de referência (cláusulas 1 a 11) (D-06)"
    - "calculateReadiness é função pura que aceita Partial<Record<TabKey, Record<string, unknown>>> e retorna ReadinessResult com campos gerencial, habilitacoes, ndaAceito — lê nivelGerencial (D-01) e classificacaoFinal (D-02) do sectionData (D-03)"
    - "calculateReadiness retorna null para gerencial/habilitacoes quando o campo correspondente não está preenchido (string vazia, undefined ou slug não mapeado) (D-03)"
  artifacts:
    - path: "roteiro-unificado/src/components/ui/input-field.tsx"
      provides: "InputField wrapper RHF para <input> simples com label + Controller + error inline"
      exports: ["InputField"]
    - path: "roteiro-unificado/src/components/ui/index.ts"
      provides: "Barrel export incluindo InputField"
      contains: "export { InputField } from './input-field'"
    - path: "roteiro-unificado/src/constants/nda-text.ts"
      provides: "Constante NDA_TEXT com texto integral do NDA"
      exports: ["NDA_TEXT"]
    - path: "roteiro-unificado/src/lib/readiness.ts"
      provides: "Engine pura calculateReadiness + tipo ReadinessResult"
      exports: ["calculateReadiness", "ReadinessResult"]
  key_links:
    - from: "roteiro-unificado/src/lib/readiness.ts"
      to: "@/stores/formStore (TabKey)"
      via: "import"
      pattern: "from '@/stores/formStore'"
    - from: "roteiro-unificado/src/components/ui/input-field.tsx"
      to: "./input (Input component)"
      via: "import"
      pattern: "from './input'"
---

<objective>
Criar a infraestrutura base da Phase 7: o wrapper `InputField` para campos de texto livres, a constante `NDA_TEXT` com o texto legal do NDA, e a engine pura `calculateReadiness` que lê selects de classificação do `sectionData` e retorna o status de prontidão.

Purpose: Estabelecer as três peças de fundação que todos os Section components da Phase 7 vão consumir, sem nenhum acoplamento entre si. Isolar essa fundação na Wave 1 permite que os planos subsequentes (schemas + sections) trabalhem em paralelo sem disputa de arquivos.
Output: 4 arquivos novos/modificados em camadas isoladas (ui wrapper, constants, lib).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-PATTERNS.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-UI-SPEC.md
@roteiro-unificado/src/components/ui/select-field.tsx
@roteiro-unificado/src/components/ui/input.tsx
@roteiro-unificado/src/components/ui/index.ts
@roteiro-unificado/src/stores/formStore.ts
@roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
<!-- Contratos extraídos do codebase para o executor não precisar explorar -->

From src/stores/formStore.ts:
- `export enum TabKey { Identificacao='identificacao', TorreDecisao='torre-decisao', TorreSienge='torre-sienge', TorreAcesso='torre-acesso', TorreClassificacao='torre-classificacao', HabVenda='hab-venda', HabRepositorios='hab-repositorios', HabResponsaveis='hab-responsaveis', HabClassificacao='hab-classificacao', Nda='nda' }`
- store.sectionData type: `Partial<Record<TabKey, Record<string, unknown>>>`

From src/schemas/torre-decisao.ts (confirma slugs lowercase):
- `nivelGerencial: z.enum(['g1', 'g2', 'g3', 'g4', 'g5']).optional()`

From src/components/ui/input.tsx:
- `interface InputProps extends InputHTMLAttributes<HTMLInputElement> { error?: boolean; errorMessage?: string }`
- Default export `Input` forwardRef.

From src/components/ui/select-field.tsx (template a replicar):
- Padrão: `<div className="flex flex-col gap-1"><label className="text-sm font-semibold text-gray-700">...<Controller name control render={...} />{error && <p className="text-g1 text-xs">{error}</p>}</div>`
- Props: `name`, `control`, `label`, `placeholder?`, `required?`, `error?`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar InputField e atualizar barrel export</name>
  <files>roteiro-unificado/src/components/ui/input-field.tsx, roteiro-unificado/src/components/ui/index.ts</files>
  <read_first>
    - roteiro-unificado/src/components/ui/select-field.tsx (template exato a replicar — substituir Select por Input)
    - roteiro-unificado/src/components/ui/input.tsx (API de InputProps — forwardRef, error/errorMessage)
    - roteiro-unificado/src/components/ui/index.ts (estado atual do barrel — verificar ausência de InputField)
  </read_first>
  <action>
    Criar arquivo `roteiro-unificado/src/components/ui/input-field.tsx` com componente genérico `InputField<T extends FieldValues>` que aceita props `name: FieldPath<T>`, `control: Control<T>`, `label: string`, `placeholder?: string`, `required?: boolean`, `disabled?: boolean`, `error?: string`. Importar `Controller`, `Control`, `FieldPath`, `FieldValues` de `react-hook-form` e `Input` de `./input`. Estrutura idêntica a `select-field.tsx`: wrapper `<div className="flex flex-col gap-1">`, label `<label className="text-sm font-semibold text-gray-700">` com asterisco `<span className="text-g1 ml-0.5">*</span>` quando `required`, `Controller` que renderiza `<Input {...field} placeholder={placeholder} disabled={disabled} error={!!error} errorMessage={error} />`, e parágrafo `<p className="text-g1 text-xs">{error}</p>` quando há erro. Em seguida, adicionar a linha `export { InputField } from './input-field'` no `roteiro-unificado/src/components/ui/index.ts` na seção "Field components (form Controller wrappers) — Phase 6" (após linha `export { ConditionalField } from './conditional-field'`).
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "(input-field|index\.ts)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/components/ui/input-field.tsx` exists and exports `InputField` (named export)
    - `grep -c "export { InputField }" roteiro-unificado/src/components/ui/index.ts` returns 1
    - `grep -c "import.*Controller.*from 'react-hook-form'" roteiro-unificado/src/components/ui/input-field.tsx` returns 1
    - `grep -c "import { Input } from './input'" roteiro-unificado/src/components/ui/input-field.tsx` returns 1
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
    - The component signature is `function InputField<T extends FieldValues>(props: { name: FieldPath<T>; control: Control<T>; label: string; placeholder?: string; required?: boolean; disabled?: boolean; error?: string })`
  </acceptance_criteria>
  <done>InputField criado seguindo padrão SelectField, exportado de @/components/ui sem erro de TypeScript</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar constante NDA_TEXT com cláusulas extraídas do HTML</name>
  <files>roteiro-unificado/src/constants/nda-text.ts</files>
  <read_first>
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (extrair texto entre as linhas 542-586 do HTML — cláusulas 1 a 11 do NDA mútuo)
  </read_first>
  <action>
    Criar pasta `roteiro-unificado/src/constants/` se não existir, e arquivo `nda-text.ts` exportando `NDA_TEXT: readonly string[]` como `as const`. Cada elemento do array é um parágrafo do NDA (formato: "Cláusula N — texto completo"). Extrair todas as 11 cláusulas do HTML de referência (`roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html`) a partir da seção `id="nda"`. Incluir cabeçalho do NDA (título do acordo + identificação Parte A/Parte B em texto livre, sem campos) seguido das cláusulas numeradas. O array deve ter pelo menos 10 entradas. Não usar markdown, HTML ou JSX dentro das strings — apenas texto puro com pontuação. Adicionar comentário JSDoc no topo: `/** Texto integral do NDA mútuo — fonte: HTML de referência seção id="nda". Cada item é um parágrafo, renderizado como <p> no NdaSection. */`
  </action>
  <verify>
    <automated>cd roteiro-unificado && node -e "const m = require('./src/constants/nda-text.ts'.replace('.ts','')); console.log('check');" 2>/dev/null; grep -c "Cláusula" roteiro-unificado/src/constants/nda-text.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/constants/nda-text.ts` exists
    - `grep -c "export const NDA_TEXT" roteiro-unificado/src/constants/nda-text.ts` returns 1
    - `grep -c "as const" roteiro-unificado/src/constants/nda-text.ts` returns at least 1
    - `grep -c "Cláusula" roteiro-unificado/src/constants/nda-text.ts` returns at least 10
    - Array elements are plain strings (no HTML tags like `<p>`, `<br>` inside strings)
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>Arquivo nda-text.ts existe com NDA_TEXT exportado, contendo pelo menos 10 cláusulas como strings puras</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar engine calculateReadiness em src/lib/readiness.ts</name>
  <files>roteiro-unificado/src/lib/readiness.ts</files>
  <read_first>
    - roteiro-unificado/src/stores/formStore.ts (linhas 10-21: TabKey enum + sectionData type)
    - roteiro-unificado/src/schemas/torre-decisao.ts (linha 30: confirmar que nivelGerencial usa slugs lowercase g1..g5)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md (D-03 — assinatura exata da função)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md (Padrão 3 — implementação exemplo)
  </read_first>
  <action>
    Criar arquivo `roteiro-unificado/src/lib/readiness.ts` exportando: (1) interface `ReadinessResult` com campos `gerencial: 'G1'|'G2'|'G3'|'G4'|'G5' | null`, `habilitacoes: 'HAB-A'|'HAB-B'|'HAB-C'|'HAB-D'|'HAB-E' | null`, `ndaAceito: boolean`; (2) função `calculateReadiness(sectionData: Partial<Record<TabKey, Record<string, unknown>>>): ReadinessResult` (per D-03). Implementação: ler `sectionData[TabKey.TorreDecisao]`, `sectionData[TabKey.HabClassificacao]`, `sectionData[TabKey.Nda]` (default `{}`). Extrair `rawGerencial = torreDecisao['nivelGerencial'] as string | undefined` e `rawHab = habClassificacao['classificacaoFinal'] as string | undefined`. Mapear via `gerencialMap: Record<string, ReadinessResult['gerencial']>` com chaves lowercase `'g1','g2','g3','g4','g5'` para `'G1'..'G5'`, e `habMap` com chaves `'hab-a'..'hab-e'` para `'HAB-A'..'HAB-E'`. Retornar `{ gerencial: rawGerencial ? (gerencialMap[rawGerencial] ?? null) : null, habilitacoes: rawHab ? (habMap[rawHab] ?? null) : null, ndaAceito: nda['aceitaTermos'] === true }`. Importar `TabKey` de `@/stores/formStore`. Função é pura — sem efeitos, sem useState/useMemo (D-03). Adicionar JSDoc explicando que é função pura testável e que `null` indica campo não preenchido.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "readiness" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/lib/readiness.ts` exists
    - `grep -c "export interface ReadinessResult" roteiro-unificado/src/lib/readiness.ts` returns 1
    - `grep -c "export function calculateReadiness" roteiro-unificado/src/lib/readiness.ts` returns 1
    - `grep -c "import.*TabKey.*from '@/stores/formStore'" roteiro-unificado/src/lib/readiness.ts` returns 1
    - `grep -E "g1:.*'G1'|'g1':.*'G1'" roteiro-unificado/src/lib/readiness.ts` returns at least 1 match (gerencialMap exists)
    - `grep -E "hab-a:.*HAB-A|'hab-a':.*'HAB-A'" roteiro-unificado/src/lib/readiness.ts` returns at least 1 match (habMap exists)
    - `grep -c "ndaAceito" roteiro-unificado/src/lib/readiness.ts` returns at least 2
    - Function does not import React/useMemo/useState — purely a transform function
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>readiness.ts existe com calculateReadiness pura, mapeando slugs lowercase para labels G1-G5/HAB-A..E, retornando null para campos vazios</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Não aplicável a esta fase | Nenhum input do usuário cruza fronteira de trust nesta fase — todos os dados ficam no client (Zustand + sessionStorage namespaceado por tenantId). Persistência em Supabase será Phase 8. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | calculateReadiness | mitigate | Função é pura (sem mutação) e usa cast `as string | undefined` antes de mapear; retorna `null` para slugs desconhecidos via `gerencialMap[raw] ?? null` — slugs inválidos não causam crash nem retornam resultado fraudulento |
| T-07-01-02 | Information Disclosure | NDA_TEXT | accept | Texto legal é público e visível para qualquer usuário autenticado da org; sem dados sensíveis embutidos |
| T-07-01-03 | Tampering | InputField | mitigate | Wrapper apenas — não introduz sanitização nova; React escapa JSX automaticamente; campo de input usa Controller + value/onChange controlados (sem dangerouslySetInnerHTML) |
| T-07-01-SC | Tampering | Package installs (npm) | accept | Nenhuma instalação nova nesta fase — RESEARCH.md Package Legitimacy Audit confirmou: apenas pacotes já instalados (react-hook-form, zod, zustand) são consumidos. Sem `npm install` na Phase 7. |

## Applicable ASVS Categories (L1)

- V5 Input Validation: parcial — `calculateReadiness` valida slugs via mapa explícito (rejeita valores não esperados retornando null). Validação Zod completa de campos vem nos planos 07-02..07-05.
</threat_model>

<verification>
Para confirmar que o plano foi concluído com sucesso:

1. `npx tsc --noEmit` em `roteiro-unificado/` exits 0 — sem erros de TypeScript
2. `grep -c "export { InputField }" roteiro-unificado/src/components/ui/index.ts` retorna 1
3. `grep -c "export const NDA_TEXT" roteiro-unificado/src/constants/nda-text.ts` retorna 1
4. `grep -c "export function calculateReadiness" roteiro-unificado/src/lib/readiness.ts` retorna 1
5. Manual: abrir `src/lib/readiness.ts` e confirmar que o gerencialMap usa slugs lowercase (`g1..g5`) batendo com o schema `torre-decisao.ts` linha 30
</verification>

<success_criteria>
- 4 arquivos criados/modificados (input-field.tsx, index.ts, nda-text.ts, readiness.ts)
- TypeScript compila sem erros
- `InputField`, `NDA_TEXT`, `calculateReadiness`, `ReadinessResult` todos exportáveis dos caminhos esperados
- NDA_TEXT contém pelo menos 10 parágrafos extraídos do HTML
- calculateReadiness é função pura (sem React, sem efeitos)
</success_criteria>

<output>
Criar `.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-01-SUMMARY.md` ao concluir, com nota explícita de que esta é fundação Wave 1 consumida por todos os demais planos da fase.
</output>
