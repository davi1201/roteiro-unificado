---
phase: 07-campos-habilita-es-nda-classifica-o-g1-g5
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx
  - roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx
autonomous: true
requirements:
  - FORM-02
  - FORM-03
  - UX-02
must_haves:
  truths:
    - "HabResponsaveisSection renderiza CheckboxGroupField de dificuldades (12 opções) + 4 selects + 1 textarea + matriz de 10 atividades × 5 colunas — grupos de campos, não tabela HTML (D-08)"
    - "HabClassificacaoSection renderiza 4 selects de síntese + 5 textareas de planejamento (todos opcionais) — consultor seleciona classificação HAB-A/E via select direto, sem algoritmo de inferência (D-02)"
    - "classificacaoFinal em HabClassificacaoSection usa enum slugs hab-a..hab-e que alimenta calculateReadiness — nível gerencial G1-G5 já existe em Torre Decisão (D-01)"
    - "Ambos os componentes seguem o subscription pattern anti-loop e mode: 'onBlur'"
  artifacts:
    - path: "roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx"
      provides: "Section component da aba Hab. Responsáveis"
      exports: ["HabResponsaveisSection"]
    - path: "roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx"
      provides: "Section component da aba Hab. Classificação"
      exports: ["HabClassificacaoSection"]
  key_links:
    - from: "HabClassificacaoSection.classificacaoFinal"
      to: "calculateReadiness.habMap"
      via: "slug enum match"
      pattern: "hab-a.*hab-b.*hab-c.*hab-d.*hab-e"
    - from: "HabResponsaveisSection"
      to: "habResponsaveisSchema + HAB_RESPONSIBILITIES"
      via: "import"
      pattern: "from '@/schemas/hab-responsaveis'"
---

<objective>
Implementar os dois Section components restantes da matriz de habilitação — Hab. Responsáveis (10 atividades × 5 colunas + checkboxes) e Hab. Classificação (síntese com selects que alimentam `calculateReadiness`). A aba Classificação contém o campo `classificacaoFinal` (HAB-A..E) crítico para a engine.

Purpose: Completar 4 dos 5 Section components em paralelo com 07-03 e 07-05, mantendo cada plano abaixo de 50% de contexto. HabResponsaveis segue padrão TorreSiengeSection; HabClassificacao segue padrão TorreClassificacaoSection (mais leve).
Output: 2 arquivos `.tsx` em `src/features/form/sections/` consumindo schemas da 07-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-PATTERNS.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-UI-SPEC.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-01-SUMMARY.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-02-SUMMARY.md
@roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx
@roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx
@roteiro-unificado/src/components/ui/index.ts
@roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
<!-- TabKey enum (já existente em formStore.ts) -->
- `TabKey.HabResponsaveis = 'hab-responsaveis'`
- `TabKey.HabClassificacao = 'hab-classificacao'`

<!-- Schemas criados em 07-02 -->
- `habResponsaveisSchema`, `HabResponsaveisData`, `HAB_RESPONSIBILITIES`, `HabResponsabilidadeSlug` em `@/schemas/hab-responsaveis`
- `habClassificacaoSchema`, `HabClassificacaoData`, `HAB_CLASSIFICACAO_REQUIRED_COUNT` em `@/schemas/hab-classificacao`

<!-- Padrão estrutural já documentado em 07-03 — replicar -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implementar HabResponsaveisSection com checkboxes + 4 selects + matriz 10 atividades</name>
  <files>roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx (template de matriz aninhada)
    - roteiro-unificado/src/schemas/hab-responsaveis.ts (criado em 07-02 — tipos + enums + HAB_RESPONSIBILITIES)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md (seção "Aba Hab. Responsáveis" — labels exatas)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-UI-SPEC.md (layout: max-w-5xl, separadores)
  </read_first>
  <action>
    Criar `HabResponsaveisSection.tsx` no padrão de TorreSiengeSection + HabVendaSection. Recebe `tenantId: string`. `useForm<HabResponsaveisData>` com schema, defaultValues, `mode: 'onBlur'`. Subscription anti-loop apontando para `TabKey.HabResponsaveis`.

    Cast de erros: `type ResponsibilityErrors = Record<HabResponsabilidadeSlug, Record<string, { message?: string }>>; const respErrors = (errors.responsibilities as unknown as ResponsibilityErrors) ?? {}`.

    Estrutura JSX (`<form className="max-w-5xl" noValidate>`):
    1. **Grupo "Dificuldades operacionais recorrentes"** (`<div className="mb-6">`):
       - `<h2 className="mb-2 text-base font-semibold text-gray-900">Dificuldades operacionais recorrentes</h2>`
       - `CheckboxGroupField name="dificuldadesRecorrentes"` com label "Selecione as dificuldades observadas" e 12 options: `[{value:'documento-vencido',label:'Documento vencido sem alerta'},{value:'documento-nao-localizado',label:'Documento não localizado'},{value:'dependencia-contador',label:'Dependência do contador'},{value:'dependencia-juridico',label:'Dependência do jurídico'},{value:'dependencia-engenharia',label:'Dependência da engenharia'},{value:'dependencia-fornecedores',label:'Dependência de fornecedores'},{value:'duvida-aplicabilidade',label:'Dúvida sobre aplicabilidade'},{value:'falta-padrao-envio',label:'Falta de padrão de envio'},{value:'retrabalho-kit',label:'Retrabalho para montar kit'},{value:'documentos-whatsapp',label:'Documentos em WhatsApp/e-mail'},{value:'falta-historico',label:'Falta de histórico'},{value:'sem-dono-processo',label:'Ninguém é dono do processo'}]`
    2. `<hr className="my-6 border-gray-100" />`
    3. **Grupo "Controles gerais"** (`<div className="space-y-4">`):
       - `<h2 className="mb-2 text-base font-semibold text-gray-900">Controles gerais</h2>`
       - `SelectField name="tempoMedioKit"` label "Tempo médio para montar um kit" options `[{value:'mesmo-dia',label:'Mesmo dia'},{value:'1-3-dias',label:'1 a 3 dias'},{value:'ate-1-semana',label:'Até 1 semana'},{value:'mais-1-semana',label:'Mais de 1 semana'},{value:'nao-medem',label:'Não medem'}]`
       - 3 `SelectField` com nomes `existeChecklist`, `existeRenovacao`, `existeValidacao` e labels "Existe checklist padrão?", "Existe rotina de renovação?", "Existe validação antes do envio?". Cada um com options `[{value:'sim',label:'Sim'},{value:'parcial',label:'Parcial'},{value:'nao',label:'Não'}]`
       - `TextareaField name="observacoesRotina"` label "Observações sobre rotina e dificuldades"
    4. `<hr className="my-6 border-gray-100" />`
    5. **Grupo matriz "Mapa rápido de responsabilidades"**:
       - `<h2 className="mb-4 text-base font-semibold text-gray-900">Mapa rápido de responsabilidades</h2>`
       - `{HAB_RESPONSIBILITIES.map((activity) => { const respErr = respErrors[activity.slug] ?? {}; return <article key={activity.slug} className="mb-4 rounded-md border border-gray-200 bg-white p-4"> ... </article> })}`
       - Cada article: `<h3 className="mb-3 text-sm font-semibold text-gray-900">{activity.label}</h3>`
       - `<div className="grid grid-cols-1 gap-3 md:grid-cols-2">` com:
         * `TextareaField name={\`responsibilities.${activity.slug}.quemFaz\` as FieldPath<HabResponsaveisData>}` label "Quem faz hoje?" rows={2}
         * `SelectField name={\`responsibilities.${activity.slug}.existeSubstituto\` as FieldPath<HabResponsaveisData>}` label "Existe substituto?" options (Sim, Parcial, Não, Não sabe)
         * `SelectField name={\`responsibilities.${activity.slug}.terceiroDependente\` as FieldPath<HabResponsaveisData>}` label "Terceiro depende?" options (Sim, Parcial, Não, Não sabe)
         * `TextareaField name={\`responsibilities.${activity.slug}.maiorDificuldade\` as FieldPath<HabResponsaveisData>}` label "Maior dificuldade" rows={2}
       - `<div className="mt-3"><TextareaField name={\`responsibilities.${activity.slug}.observacoes\` as FieldPath<HabResponsaveisData>} label="Observações" rows={2} /></div>`

    Importar `CheckboxGroupField`, `SelectField`, `TextareaField` de `@/components/ui`. Tipos + `HAB_RESPONSIBILITIES` de `@/schemas/hab-responsaveis`. Resto idêntico aos demais sections.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "HabResponsaveisSection" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` exists
    - `grep -c "export function HabResponsaveisSection" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` returns 1
    - `grep -c "TabKey.HabResponsaveis" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` returns at least 2
    - `grep -c "HAB_RESPONSIBILITIES.map" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` returns 1
    - `grep -c "dificuldadesRecorrentes\|tempoMedioKit\|existeChecklist\|existeRenovacao\|existeValidacao\|observacoesRotina" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` returns at least 6
    - `grep -c "CheckboxGroupField" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` returns at least 2 (import + usage)
    - `grep -c "subscription.unsubscribe" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` returns 1
    - `grep -c "as FieldPath<HabResponsaveisData>" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` returns at least 5
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>HabResponsaveisSection compila, renderiza checkboxes + 4 selects + textarea + 10 atividades × 5 colunas, usa subscription pattern</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementar HabClassificacaoSection com 4 selects + 5 textareas</name>
  <files>roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx (template exato — clonar e adaptar para hab-classificacao)
    - roteiro-unificado/src/schemas/hab-classificacao.ts (criado em 07-02 — tipos + enums)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md (seção "Aba Hab. Classificação" — labels)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md (D-02: classificacaoFinal alimenta calculateReadiness)
  </read_first>
  <action>
    Criar `HabClassificacaoSection.tsx` clonando `TorreClassificacaoSection.tsx` e substituindo: tipo+schema (`HabClassificacaoData`, `habClassificacaoSchema`), `TabKey.HabClassificacao`, e os campos. Componente recebe `tenantId: string`. Mesmo padrão de useForm + subscription anti-loop.

    Definir option constants no topo do arquivo (acima do componente):
    - `classificacaoFinalOptions = [{value:'hab-a',label:'HAB-A — Pronta para operação'},{value:'hab-b',label:'HAB-B — Organizada com lacunas'},{value:'hab-c',label:'HAB-C — Controle parcial'},{value:'hab-d',label:'HAB-D — Risco alto'},{value:'hab-e',label:'HAB-E — Não recomendada sem ativação'}]`
    - `abordagemOptions = [{value:'implantar-direto',label:'Implantar direto'},{value:'implantar-carga-assistida',label:'Implantar com carga inicial assistida'},{value:'ativar-repositorio-responsaveis',label:'Ativar repositório e responsáveis primeiro'},{value:'comecar-dossie-especifico',label:'Começar por dossiê específico'},{value:'fase-preparatoria',label:'Fazer fase preparatória antes do produto'}]`
    - `escopoOptions = [{value:'empresa-cnpj',label:'Empresa/CNPJ'},{value:'obra-empreendimento',label:'Obra/empreendimento'},{value:'financiamento',label:'Financiamento'},{value:'licitacao',label:'Licitação'},{value:'contrato-privado-homologacao',label:'Contrato privado/homologação'},{value:'fiscalizacao-auditoria',label:'Fiscalização/auditoria'},{value:'misto-reduzido',label:'Misto reduzido'}]`
    - `complexidadeOptions = [{value:'baixa',label:'Baixa'},{value:'media',label:'Média'},{value:'alta',label:'Alta'},{value:'critica',label:'Crítica'}]`

    Estrutura JSX (`<form className="max-w-2xl" noValidate>`):
    1. **Grupo "Síntese para planejamento"** (`<div className="space-y-4">`):
       - `SelectField name="classificacaoFinal"` label "Classificação final" options={classificacaoFinalOptions} (esse é o campo que alimenta `calculateReadiness`)
       - `SelectField name="abordagemRecomendada"` label "Abordagem recomendada" options={abordagemOptions}
       - `SelectField name="escopoInicialSugerido"` label "Escopo inicial sugerido" options={escopoOptions}
       - `SelectField name="complexidadePreco"` label "Complexidade / preço" options={complexidadeOptions}
       - Cada campo recebe `error={errors.{name}?.message as string | undefined}`
    2. `<hr className="my-6 border-gray-100" />`
    3. **Grupo "Plano e riscos"** (`<div className="space-y-4">`):
       - `TextareaField name="fase1"` label "Fase 1 sugerida"
       - `TextareaField name="fase2"` label "Fase 2 sugerida"
       - `TextareaField name="riscosPrincipais"` label "Riscos principais para contrato"
       - `TextareaField name="evidenciasEssenciais"` label "Evidências essenciais a solicitar"
       - `TextareaField name="observacoesFinais"` label "Observações finais para proposta, escopo, preço e cronograma"

    Importar `SelectField`, `TextareaField` de `@/components/ui`. `habClassificacaoSchema`, `HabClassificacaoData` de `@/schemas/hab-classificacao`. `TabKey`, `useFormStore` de `@/stores/formStore`. Resto idêntico ao TorreClassificacaoSection.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "HabClassificacaoSection" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` exists
    - `grep -c "export function HabClassificacaoSection" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` returns 1
    - `grep -c "TabKey.HabClassificacao" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` returns at least 2
    - `grep -c "classificacaoFinal\|abordagemRecomendada\|escopoInicialSugerido\|complexidadePreco" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` returns at least 4
    - `grep -c "fase1\|fase2\|riscosPrincipais\|evidenciasEssenciais\|observacoesFinais" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` returns at least 5
    - `grep -c "'hab-a'\|hab-a'" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` returns at least 1 (option value matches schema enum)
    - `grep -c "subscription.unsubscribe" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` returns 1
    - `grep -c "max-w-2xl" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` returns 1
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>HabClassificacaoSection compila, renderiza 4 selects + 5 textareas com labels do HTML, usa subscription pattern; classificacaoFinal usa slugs hab-a..hab-e compatíveis com calculateReadiness</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → Zustand store | Inputs validados via zodResolver antes de atingir o store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | HabClassificacaoSection.classificacaoFinal | mitigate | `z.enum(['hab-a','hab-b','hab-c','hab-d','hab-e'])` rejeita slugs fora do conjunto; option values devem bater 1:1 com enum (acceptance criteria do task verifica) |
| T-07-04-02 | Tampering | HabResponsaveisSection matriz | mitigate | zodResolver(habResponsaveisSchema) — enums + strings opcionais; React escapa JSX |
| T-07-04-03 | Denial of Service | Render loop via watch() em deps | mitigate | Subscription pattern com deps `[]` + eslint-disable — padrão estabelecido |
| T-07-04-SC | Tampering | Package installs | accept | Sem nova instalação |

## Applicable ASVS Categories (L1)

- V5 Input Validation: zodResolver + enums fechados
- V3 Session Management: sectionData isolado por tenantId
</threat_model>

<verification>
1. `npx tsc --noEmit` em `roteiro-unificado/` exits 0
2. Manual: confirmar visualmente que `classificacaoFinalOptions` em HabClassificacaoSection.tsx usa values lowercase `hab-a..hab-e` (compatibilidade com `habMap` em `src/lib/readiness.ts`)
3. `grep -E "value:.*'hab-a'" roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx` retorna match
4. Manual: comparar campos com inventário em 07-RESEARCH.md §"Aba Hab. Responsáveis" e §"Aba Hab. Classificação"
</verification>

<success_criteria>
- 2 Section components criados
- Ambos compilam sem erros TypeScript
- HabResponsaveisSection: CheckboxGroupField + 4 selects + textarea + 10 atividades × 5 colunas
- HabClassificacaoSection: 4 selects (incluindo classificacaoFinal hab-a..hab-e) + 5 textareas
- Ambos usam subscription pattern anti-loop e mode: 'onBlur'
- Slugs do classificacaoFinal em HabClassificacaoSection batem com habMap em readiness.ts
</success_criteria>

<output>
Criar `.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-04-SUMMARY.md` ao concluir, confirmando a compatibilidade de slugs entre HabClassificacaoSection.classificacaoFinal e calculateReadiness.habMap.
</output>
