---
phase: 07-campos-habilita-es-nda-classifica-o-g1-g5
plan: 05
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - roteiro-unificado/src/features/form/sections/NdaSection.tsx
  - roteiro-unificado/src/features/form/ReadinessClassification.tsx
autonomous: true
requirements:
  - FORM-02
  - FORM-03
  - FORM-07
  - UX-02
must_haves:
  truths:
    - "NdaSection renderiza texto legal NDA_TEXT integral em div scrollable seguido dos campos de aceite — texto hardcoded em constants/nda-text.ts (D-05, D-06)"
    - "Checkbox aceitaTermos no NdaSection é implementado via Controller direto (não CheckboxGroupField — gerencia boolean, não array) — único campo obrigatório da fase (D-07)"
    - "Tentativa de submit sem marcar o checkbox exibe a mensagem 'Você deve aceitar os termos do NDA para continuar' (FORM-03, D-07)"
    - "Data de aceite é auto-preenchida com new Date().toLocaleDateString('pt-BR') e o campo fica disabled"
    - "ReadinessClassification calcula resultado via useMemo sobre store.sectionData, exibe Badge G1-G5 + HAB-X + indicador NDA aceito (D-04)"
    - "ReadinessClassification mostra empty state quando gerencial e habilitacoes são null"
  artifacts:
    - path: "roteiro-unificado/src/features/form/sections/NdaSection.tsx"
      provides: "Section component da aba NDA com texto legal + campos de aceite"
      exports: ["NdaSection"]
    - path: "roteiro-unificado/src/features/form/ReadinessClassification.tsx"
      provides: "Componente display de prontidão em tempo real consumindo calculateReadiness"
      exports: ["ReadinessClassification"]
  key_links:
    - from: "NdaSection"
      to: "NDA_TEXT (constants/nda-text.ts)"
      via: "import + map render"
      pattern: "NDA_TEXT\\.map"
    - from: "ReadinessClassification"
      to: "calculateReadiness (lib/readiness.ts)"
      via: "useMemo"
      pattern: "useMemo\\(\\s*\\(\\)\\s*=>\\s*calculateReadiness"
    - from: "NdaSection.aceitaTermos"
      to: "ndaSchema.aceitaTermos via zodResolver"
      via: "z.literal(true)"
      pattern: "Controller[\\s\\S]*name=\"aceitaTermos\""
---

<objective>
Fechar o último Section (NDA) e entregar o componente de display `ReadinessClassification`. O NdaSection é o único com checkbox obrigatório (`z.literal(true)`) — exige implementação inline de Controller para boolean. O ReadinessClassification consome a engine pura da 07-01 e o store, exibindo Badge G1-G5 + HAB-X + indicador NDA em tempo real.

Purpose: Completar a interface de input (NDA) e a interface de output (ReadinessClassification) em paralelo. Ambos consomem artefatos da 07-01 e 07-02; estão na mesma Wave para finalizar a Wave 2 com paralelismo máximo.
Output: 2 arquivos `.tsx` — um Section component e um display component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-RESEARCH.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-PATTERNS.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-UI-SPEC.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-01-SUMMARY.md
@.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-02-SUMMARY.md
@roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx
@roteiro-unificado/src/components/ui/badge.tsx
@roteiro-unificado/src/components/ui/checkbox-group-field.tsx
@roteiro-unificado/src/components/ui/index.ts

<interfaces>
<!-- Badge component (já existente) -->
- `Badge({ grade: Grade, className?: string })` — Grade = 'G1'|'G2'|'G3'|'G4'|'G5'

<!-- calculateReadiness (criado em 07-01) -->
- `calculateReadiness(sectionData): ReadinessResult`
- `ReadinessResult = { gerencial: 'G1'|...'G5'|null, habilitacoes: 'HAB-A'|...'HAB-E'|null, ndaAceito: boolean }`

<!-- NDA_TEXT (criado em 07-01) -->
- `NDA_TEXT: readonly string[]` exportado de `@/constants/nda-text`

<!-- ndaSchema (criado em 07-02) -->
- Campos: nomeRepresentante?, cargo?, cpf?, dataAceite (default new Date().toLocaleDateString('pt-BR')), aceitaTermos: z.literal(true) — único obrigatório, observacoes?

<!-- CheckboxGroupField gerencia arrays (confirmado: src/components/ui/checkbox-group-field.tsx linha 33: defaultValue={[] as ...}); para boolean usar Controller direto -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implementar NdaSection com texto scrollable + campos de aceite + checkbox literal(true)</name>
  <files>roteiro-unificado/src/features/form/sections/NdaSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx (estrutura base do componente)
    - roteiro-unificado/src/components/ui/checkbox-group-field.tsx (confirmar que gerencia array — não usar para boolean)
    - roteiro-unificado/src/schemas/nda.ts (criado em 07-02 — ndaSchema com z.literal(true) e dataAceite default)
    - roteiro-unificado/src/constants/nda-text.ts (criado em 07-01 — NDA_TEXT array)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-UI-SPEC.md (layout: container scrollable max-h-[400px], campos abaixo)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-PATTERNS.md (seção NdaSection: padrão Controller direto para checkbox booleano)
  </read_first>
  <action>
    Criar `NdaSection.tsx`. Recebe `tenantId: string`. Imports: `useEffect` (react), `useForm, Controller` (react-hook-form), `zodResolver` (@hookform/resolvers/zod), `useFormStore, TabKey` (@/stores/formStore), `ndaSchema, NdaData` (@/schemas/nda), `InputField, TextareaField` (@/components/ui), `NDA_TEXT` (@/constants/nda-text).

    Setup do form:
    - `const { control, watch, formState: { errors } } = useForm<NdaData>({ resolver: zodResolver(ndaSchema), defaultValues: { ...(store.sectionData[TabKey.Nda] ?? {}), dataAceite: new Date().toLocaleDateString('pt-BR'), aceitaTermos: (store.sectionData[TabKey.Nda]?.aceitaTermos as boolean | undefined) ?? false } as Partial<NdaData>, mode: 'onBlur' })`
    - Nota: `aceitaTermos` deve ser inicializado como `false` (não `undefined`) para que `z.literal(true)` produza erro visível ao tentar submeter (RESEARCH Pitfall 3)
    - Subscription anti-loop: `useEffect(() => { const sub = watch((v) => store.updateSection(TabKey.Nda, v as Record<string, unknown>)); return () => sub.unsubscribe() }, [])` com `eslint-disable-line react-hooks/exhaustive-deps`

    Estrutura JSX (`<form className="max-w-2xl" noValidate>`):
    1. **Container do texto legal:**
       - `<div className="max-h-[400px] overflow-y-auto border border-gray-200 rounded-lg p-4 bg-gray-50 text-sm leading-relaxed mb-6">`
       - `{NDA_TEXT.map((paragraph, i) => (<p key={i} className="mb-3 last:mb-0 text-gray-700">{paragraph}</p>))}`
       - `</div>`
    2. **Grupo "Campos de aceite"** (`<div className="space-y-4">`):
       - `<h2 className="mb-2 text-base font-semibold text-gray-900">Aceite</h2>`
       - `InputField name="nomeRepresentante"` label "Nome do representante legal" placeholder "Nome completo"
       - `InputField name="cargo"` label "Cargo" placeholder "Ex: Diretor, Sócio-administrador"
       - `InputField name="cpf"` label "CPF do representante" placeholder "000.000.000-00"
       - `InputField name="dataAceite"` label "Data de aceite" disabled (campo auto-preenchido — não permitir edição)
       - **Checkbox aceitaTermos via Controller direto** (NÃO usar CheckboxGroupField — esse gerencia arrays):
         ```
         <div className="flex flex-col gap-1">
           <Controller
             name="aceitaTermos"
             control={control}
             render={({ field }) => (
               <label className="flex items-center gap-2 cursor-pointer">
                 <input
                   type="checkbox"
                   className="accent-primary h-4 w-4"
                   checked={field.value === true}
                   onChange={(e) => field.onChange(e.target.checked)}
                 />
                 <span className="text-sm font-semibold text-gray-700">
                   Li e aceito os termos do NDA mútuo
                   <span className="text-g1 ml-0.5">*</span>
                 </span>
               </label>
             )}
           />
           {errors.aceitaTermos && (
             <p className="text-g1 text-xs">{errors.aceitaTermos.message as string}</p>
           )}
         </div>
         ```
       - `TextareaField name="observacoes"` label "Observações adicionais" rows={3}

    NÃO usar `CheckboxGroupField` para `aceitaTermos` — esse componente espera arrays (linha 33 de checkbox-group-field.tsx). Implementação inline via Controller é necessária.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "NdaSection" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/sections/NdaSection.tsx` exists
    - `grep -c "export function NdaSection" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns 1
    - `grep -c "TabKey.Nda" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns at least 2
    - `grep -c "NDA_TEXT.map" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns 1
    - `grep -c "max-h-\\[400px\\] overflow-y-auto" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns 1
    - `grep -c "Controller" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns at least 2 (import + JSX usage)
    - `grep -c "name=\"aceitaTermos\"" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns 1
    - `grep -c "type=\"checkbox\"" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns at least 1
    - `grep -c "errors.aceitaTermos" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns at least 1
    - `grep -c "toLocaleDateString('pt-BR')" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns 1
    - `grep -c "CheckboxGroupField" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns 0 (NÃO usar — incompatível com boolean literal)
    - `grep -c "subscription.unsubscribe" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns 1
    - `grep -c "disabled" roteiro-unificado/src/features/form/sections/NdaSection.tsx` returns at least 1 (campo dataAceite disabled)
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>NdaSection compila, renderiza NDA_TEXT em div scrollable + 4 InputFields + checkbox via Controller direto + textarea; aceitaTermos inicializado como false; campo dataAceite disabled e auto-preenchido</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementar ReadinessClassification (badge de prontidão em tempo real)</name>
  <files>roteiro-unificado/src/features/form/ReadinessClassification.tsx</files>
  <read_first>
    - roteiro-unificado/src/components/ui/badge.tsx (componente Badge existente — Grade type, gradeConfig)
    - roteiro-unificado/src/lib/readiness.ts (criado em 07-01 — calculateReadiness + ReadinessResult)
    - roteiro-unificado/src/stores/formStore.ts (useFormStore para sectionData)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-UI-SPEC.md (layout do badge bar)
    - .planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-CONTEXT.md (D-04: useMemo + observa sectionData)
  </read_first>
  <action>
    Criar `ReadinessClassification.tsx` em `roteiro-unificado/src/features/form/`. Recebe `tenantId: string`.

    Imports: `useMemo` (react), `useFormStore` (@/stores/formStore), `calculateReadiness` (@/lib/readiness), `Badge, Grade` (@/components/ui).

    Implementação:
    - `const store = useFormStore(tenantId)`
    - `const result = useMemo(() => calculateReadiness(store.sectionData), [store.sectionData])` (D-04: useMemo observa sectionData inteiro, recalcula apenas quando o objeto muda)
    - Definir mapa `habConfig` interno (constant fora do componente) para os 5 badges HAB-X (não é Grade, então não usa o Badge):
      ```
      const habConfig: Record<'HAB-A'|'HAB-B'|'HAB-C'|'HAB-D'|'HAB-E', { bg: string; text: string; label: string }> = {
        'HAB-A': { bg: 'bg-green-100', text: 'text-green-700', label: 'HAB-A — Pronta' },
        'HAB-B': { bg: 'bg-blue-100', text: 'text-blue-700', label: 'HAB-B — Organizada' },
        'HAB-C': { bg: 'bg-yellow-100', text: 'text-yellow-700', label: 'HAB-C — Parcial' },
        'HAB-D': { bg: 'bg-orange-100', text: 'text-orange-700', label: 'HAB-D — Risco alto' },
        'HAB-E': { bg: 'bg-red-100', text: 'text-red-700', label: 'HAB-E — Não recomendada' },
      }
      ```

    Estrutura JSX (single wrapper div):
    - `<div className="flex flex-wrap items-center gap-3 rounded-lg border border-gray-200 bg-white p-3 mb-4">`
    - `<span className="text-sm font-semibold text-gray-700">Prontidão atual:</span>`
    - **Badge gerencial:** se `result.gerencial` é truthy: `<Badge grade={result.gerencial as Grade} />`. Senão (gerencial null E habilitacoes null E NDA não aceito): exibir empty state `<span className="text-sm text-gray-400">Preencha as abas de classificação para ver o resultado</span>`
    - **Badge HAB:** se `result.habilitacoes` é truthy: usar habConfig: `<span className={\`inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold ${habConfig[result.habilitacoes].bg} ${habConfig[result.habilitacoes].text}\`}>{habConfig[result.habilitacoes].label}</span>`
    - **Indicador NDA:** se `result.ndaAceito === true`: `<span className="text-xs text-green-700 font-medium">NDA aceito</span>`
    - `</div>`

    A renderização condicional deve garantir que o empty state aparece apenas quando TODOS os três estão vazios (gerencial null E habilitacoes null E ndaAceito false). Se qualquer um estiver presente, mostrar os badges correspondentes (e não o empty state).
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "ReadinessClassification" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/ReadinessClassification.tsx` exists
    - `grep -c "export function ReadinessClassification" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns 1
    - `grep -c "useMemo" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns at least 2 (import + usage)
    - `grep -c "calculateReadiness" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns at least 2 (import + call)
    - `grep -c "store.sectionData" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns at least 1
    - `grep -c "Badge\\|grade=" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns at least 2 (Badge import + usage)
    - `grep -c "'HAB-A'\\|'HAB-B'\\|'HAB-C'\\|'HAB-D'\\|'HAB-E'" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns at least 5 (habConfig keys)
    - `grep -c "NDA aceito\\|ndaAceito" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns at least 2
    - `grep -c "Prontidão atual:" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns 1
    - `grep -c "Preencha as abas de classificação para ver o resultado" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns 1
    - `grep -c "flex flex-wrap items-center gap-3" roteiro-unificado/src/features/form/ReadinessClassification.tsx` returns 1
    - `npx tsc --noEmit` exits 0 (rodado em roteiro-unificado/)
  </acceptance_criteria>
  <done>ReadinessClassification compila, usa useMemo sobre sectionData, exibe Badge G1-G5 + HAB-X colorido + indicador NDA aceito, mostra empty state quando vazio</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkbox boolean → ndaSchema | Estado do checkbox passa por z.literal(true) antes de ser considerado aceito |
| store.sectionData → ReadinessClassification | Dados não validados do store são lidos pelo calculateReadiness; engine usa cast `as string | undefined` + mapas explícitos para tolerar valores inesperados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | NdaSection.aceitaTermos checkbox | mitigate | `z.literal(true)` rejeita qualquer valor diferente de `true`; Controller força inicialização como `false` (RESEARCH Pitfall 3) — submissão com checkbox unchecked produz erro com mensagem definida pelo errorMap |
| T-07-05-02 | Information Disclosure | NDA_TEXT exibido em scrollable div | accept | Texto legal é público — exibido para todos os usuários autenticados da org; sem dados sensíveis |
| T-07-05-03 | Tampering | ReadinessClassification.useMemo | mitigate | Função pura `calculateReadiness` (07-01) usa mapas explícitos — slugs fora do conjunto retornam null; sem code injection possível via sectionData |
| T-07-05-04 | Tampering | NdaSection.dataAceite | mitigate | Campo `disabled` impede input do usuário; valor preenchido via `new Date().toLocaleDateString('pt-BR')` no defaultValues — sem caminho de injection |
| T-07-05-SC | Tampering | Package installs | accept | Sem nova instalação |

## Applicable ASVS Categories (L1)

- V5 Input Validation: z.literal(true) garante aceite formal; campos de texto livre validados como string
- V3 Session Management: sectionData isolado por tenantId
</threat_model>

<verification>
1. `npx tsc --noEmit` em `roteiro-unificado/` exits 0
2. `grep -c "CheckboxGroupField" roteiro-unificado/src/features/form/sections/NdaSection.tsx` retorna 0 (NdaSection NÃO usa CheckboxGroupField)
3. `grep -c "useMemo" roteiro-unificado/src/features/form/ReadinessClassification.tsx` retorna pelo menos 2
4. Manual: confirmar que o checkbox em NdaSection é inicializado como `false` no defaultValues — submit sem marcar deve gerar erro visível
5. Verificação visual será feita no plano 07-06 (checkpoint após wiring)
</verification>

<success_criteria>
- 2 componentes criados
- NdaSection: texto NDA scrollable + 4 InputFields + Checkbox via Controller direto + Textarea; aceitaTermos inicializado como false; dataAceite auto-preenchido e disabled
- ReadinessClassification: useMemo sobre sectionData + Badge G1-G5 via componente Badge + spans coloridos para HAB-X + indicador NDA + empty state
- TypeScript compila sem erros
- Slugs HAB-A..HAB-E em habConfig batem com retorno de calculateReadiness
- CheckboxGroupField NÃO é usado para aceitaTermos (incompatível com boolean literal)
</success_criteria>

<output>
Criar `.planning/phases/07-campos-habilita-es-nda-classifica-o-g1-g5/07-05-SUMMARY.md` ao concluir, confirmando que aceitaTermos foi inicializado como `false` (não `undefined`) para garantir mensagem de erro visível.
</output>
