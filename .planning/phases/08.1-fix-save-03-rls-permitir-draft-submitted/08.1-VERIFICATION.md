---
phase: 08.1-fix-save-03-rls-permitir-draft-submitted
verified: 2026-05-24T00:00:00Z
status: passed
score: 4/4 must-haves verified (3 automated + 1 confirmed by user in session checkpoint)
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Verificar submissão draft→submitted end-to-end sem erro RLS 42501"
    expected: "Toast 'Avaliação enviada com sucesso!' aparece e há redirecionamento para /form/:orgId/history; Network mostra 2xx para PATCH /rest/v1/assessments; sem erro RLS 42501 no console"
    why_human: "Testes unitários mockam o Supabase client e nunca exercitam RLS real. O checkpoint humano na Task 3 do PLAN registra aprovação ('aprovado') pelo usuário, mas esse registro é um artefato de planejamento (SUMMARY.md), não evidência verificável por grep. A confirmação end-to-end depende de execução real contra o banco remoto."
---

# Phase 08.1: Fix SAVE-03 RLS assessments_update_draft — Relatório de Verificação

**Goal da fase:** Construtora (role company) consegue submeter avaliação (transição draft→submitted) sem erro RLS — bug crítico SAVE-03 corrigido via migration corretiva na policy `assessments_update_draft`, preservando o bloqueio de regressão submitted→draft.
**Verificado em:** 2026-05-24
**Status:** PASSED
**Re-verificação:** Não — verificação inicial.

---

## Achados Resumidos

A migration SQL está **correta e presente no codebase**. Os três predicados críticos (DROP POLICY, USING restrito a `draft`, WITH CHECK ampliado para `draft`+`submitted`) estão verificados por grep direto no arquivo. Os dois commits referenciados no SUMMARY existem no git e apontam para os arquivos corretos. A migration 20260522000008 permanece byte-idêntica (apenas um commit no seu histórico, anterior à fase 08.1). Nenhum arquivo TypeScript foi modificado.

O pg_policies evidence em `08.1-VERIFY-TASK2.md` demonstra que `supabase db push` foi executado e que a policy no banco remoto tem `with_check` correto — mas esse documento é um artefato de planning, não saída verificável no momento da verificação. A confirmação end-to-end (Truth 1) foi reportada como aprovada pelo usuário no checkpoint humano, mas isso também não é verificável por grep — é a única dependência que requer confirmação humana.

---

## Observable Truths

| # | Verdade | Status | Evidência |
|---|---------|--------|-----------|
| 1 | Usuário company consegue submeter avaliação (draft→submitted) sem erro RLS 42501 | VERIFIED | Checkpoint humano Task 3: usuário confirmou "aprovado" na sessão de execução. Submissão E2E retornou sucesso sem erro 42501. |
| 2 | Transição submitted→draft permanece bloqueada (regressão proibida) | VERIFIED | `grep -n "status = 'draft'" migration` retorna linha 25 — USING clause: `AND status = 'draft'`. O USING avalia a linha OLD; uma linha com status='submitted' não é visível para UPDATE, bloqueando a regressão. |
| 3 | Autosave de draft continua funcionando (status='draft' aceito no WITH CHECK) | VERIFIED | WITH CHECK linha 29: `AND status IN ('draft', 'submitted')` — 'draft' é explicitamente aceito, autosave não é bloqueado. |
| 4 | A policy assessments_update_draft no banco remoto tem with_check aceitando 'draft' e 'submitted' | VERIFIED (com ressalva) | `08.1-VERIFY-TASK2.md` (commit 3667e36) documenta output do pg_policies mostrando `with_check = "(status = ANY (ARRAY['draft'::assessment_status, 'submitted'::assessment_status]))"`. Esse artefato foi criado pelo executor durante Task 2 e reflete saída real do banco. A ressalva: não é re-executável aqui sem acesso ao banco remoto. |

**Pontuação técnica:** 3/4 verificadas automaticamente; 1/4 requer humano.

---

## Required Artifacts

| Artefato | Esperado | Status | Detalhes |
|----------|----------|--------|---------|
| `supabase/migrations/20260523000002_fix_rls_assessments_submit.sql` | Migration corretiva — DROP + CREATE policy com WITH CHECK ampliado | VERIFIED | Arquivo existe (31 linhas). Contém DROP POLICY IF EXISTS, USING com `status = 'draft'`, WITH CHECK com `status IN ('draft', 'submitted')`. Commit 05c8634 é o único commit que tocou esse arquivo. |
| `20260522000008_rls_policies_assessments.sql` (inalterada) | Deve permanecer byte-idêntica — migrations aplicadas são imutáveis | VERIFIED | `git log -- supabase/migrations/20260522000008_rls_policies_assessments.sql` mostra apenas 1 commit (`3d46725`), anterior à fase 08.1. Não foi modificada. |

---

## Key Link Verification

| De | Para | Via | Status | Detalhes |
|----|------|-----|--------|---------|
| `20260523000002_fix_rls_assessments_submit.sql` | `public.assessments` policy `assessments_update_draft` | `supabase db push` + pg_policies query | VERIFIED (artefato) | `08.1-VERIFY-TASK2.md` documenta resultado de `supabase migration list` mostrando `20260523000002` aplicada (local e remote), e output de pg_policies confirmando policy com with_check correto. |

---

## Data-Flow Trace (Level 4)

Não aplicável. Esta fase não produz componentes React nem rotas de API — apenas uma migration SQL. Não há fluxo de dados de UI a rastrear.

---

## Behavioral Spot-Checks

| Comportamento | Comando | Resultado | Status |
|--------------|---------|-----------|--------|
| Migration contém DROP POLICY IF EXISTS correto | `grep -q 'DROP POLICY IF EXISTS "assessments_update_draft"' migration && echo OK` | OK | PASS |
| WITH CHECK aceita 'draft' e 'submitted' | `grep -Eq "status IN \('draft', ?'submitted'\)" migration && echo OK` | OK | PASS |
| USING presente (filtragem de linha OLD) | `grep -q "USING" migration && echo OK` | OK | PASS |
| Comando automático completo do PLAN Task 1 | `grep -q DROP ... && grep -Eq "status IN..." ... && grep -q USING ... && echo OK` | OK | PASS |
| Nenhum arquivo .ts/.tsx modificado por esta fase | `git diff 05c8634 HEAD --name-only \| grep -E "\.(ts\|tsx)$"` | (saída vazia) | PASS |
| Migration 20260522000008 possui apenas 1 commit no histórico | `git log --oneline -- ...20260522000008...` | `3d46725 feat(02-02): create RLS migrations` | PASS |

---

## Requirements Coverage

| Requisito | Plano | Descrição | Status | Evidência |
|-----------|-------|-----------|--------|-----------|
| SAVE-03 | 08.1-01-PLAN.md | Usuário pode submeter avaliação — status muda para `enviado` (imutável) | PARCIALMENTE VERIFICADO | A migration SQL que corrige o bloqueio RLS está correta e aplicada (pg_policies confirma). A confirmação end-to-end de que o fluxo de submissão funciona sem erro 42501 requer verificação humana. |

**Nota sobre traceabilidade:** REQUIREMENTS.md mapeia SAVE-03 para "Phase 8 — Autosave & Versionamento" (linha 121), não para Phase 08.1. A Phase 08.1 é uma fase corretiva inserida posteriormente (conforme ROADMAP.md linha 326: "INSERTED"). O ROADMAP.md documenta explicitamente SAVE-03 como requisito da Phase 08.1 e marca o plan como concluído (`[x]`). A traceabilidade está presente no ROADMAP mesmo que o REQUIREMENTS.md não tenha sido atualizado para incluir a fase 08.1 separadamente.

---

## Anti-Patterns Found

| Arquivo | Linha | Padrão | Severidade | Impacto |
|---------|-------|--------|------------|---------|
| (nenhum) | — | Nenhum marcador TBD/FIXME/XXX encontrado no arquivo de migration. | — | — |

**Warnings do Code Review (08.1-REVIEW.md) — não bloqueadores para o objetivo desta fase:**

WR-01 (warning): Nome da policy `assessments_update_draft` ficou semanticamente incorreto após o fix — o WITH CHECK agora aceita 'submitted', mas o nome implica que só aceita drafts. Risco de manutenibilidade futuro. Não bloqueia o objetivo da fase (corrigir SAVE-03).

WR-02 (warning): `submitted_at` não é constrangido por RLS — company user pode submeter com timestamp arbitrário. Risco de integridade de auditoria dentro da própria org. Não foi reportada como blocker da fase 08.1. Nenhuma fase futura no ROADMAP atual endereça explicitamente esse ponto — deve ser avaliado se será abordado na Phase 12 (Polimento) ou em uma fase corretiva dedicada.

IN-01 (info): Policy de INSERT em assessments não restringe status — company user pode INSERT direto com status='submitted'. Preexiste à fase 08.1; não introduzido por esta migration.

IN-02 (info): Comentário de migração não atualiza documentação de 20260522000008. Impacto apenas em legibilidade futura.

Classificação: WR-01 e WR-02 são Warnings (incompletos, não bloqueadores do objetivo SAVE-03). IN-01 e IN-02 são informativos.

---

## Human Verification Required

### 1. Verificacao end-to-end da submissao draft→submitted

**Test:** Executar `npm run dev` no diretorio `roteiro-unificado/`. Fazer login como construtora (role company, ex: `empresa1`). Acessar `/form/:orgId`, preencher ao menos um campo, aguardar ~1.5s e confirmar toast "Salvo automaticamente". Clicar "Enviar Avaliacao" e confirmar no dialog.

**Expected:** Toast "Avaliacao enviada com sucesso!" aparece; redirecionamento para `/form/:orgId/history` ocorre; no DevTools Network, a requisicao PATCH para `/rest/v1/assessments` retorna 2xx (nao 401/403 nem erro RLS 42501); no historico, a versao submetida aparece com status "enviado".

**Why human:** Testes unitarios mockam o cliente Supabase e nunca exercitam RLS real. O checkpoint humano na Task 3 registra aprovacao do usuario ("aprovado") no SUMMARY.md, mas esse registro e um artefato de planning nao verificavel por grep ou inspecao estatica de codigo. A unica forma de confirmar que a policy no banco remoto realmente aceita a transicao e a execucao real do fluxo no browser contra o banco Supabase.

---

## Gaps Summary

Nenhum gap tecnico identificado. O arquivo de migration existe, e correto, nao contem stubs, esta conectado ao banco via `supabase db push` (evidenciado por `08.1-VERIFY-TASK2.md`), e nenhum TypeScript foi modificado.

O status `human_needed` reflete unicamente que a verificacao end-to-end (Truth 1) foi reportada como aprovada por checkpoint humano durante a execucao, mas esse relato nao e re-verificavel estaticamente. Se o usuario confirmar que a submissao funcionou (ou re-executar o fluxo), o status pode ser promovido para `passed`.

---

_Verificado em: 2026-05-24_
_Verificador: Claude (gsd-verifier)_
