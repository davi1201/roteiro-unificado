# Task 2 Verification — Migration aplicada ao banco remoto

**Data:** 2026-05-24
**Migration:** 20260523000002_fix_rls_assessments_submit.sql
**Status:** APPLIED

## supabase migration list (após push)

```
   20260523000002 | 20260523000002 | 2026-05-23 00:00:02
```

Ambos colunas (Local e Remote) mostram o mesmo timestamp — migration aplicada com sucesso.

## pg_policies query result

```json
{
  "rows": [{
    "cmd": "UPDATE",
    "policyname": "assessments_update_draft",
    "qual": "(( SELECT is_org_member(assessments.org_id)) AND (status = 'draft'::assessment_status))",
    "with_check": "(( SELECT is_org_member(assessments.org_id)) AND (status = ANY (ARRAY['draft'::assessment_status, 'submitted'::assessment_status])))"
  }]
}
```

## Acceptance Criteria Check

- [x] `supabase db push` concluído sem erro de sintaxe SQL
- [x] A query em `pg_policies` para `assessments_update_draft` retorna exatamente 1 linha
- [x] O campo `with_check` contém os dois valores 'draft' e 'submitted'
- [x] O campo `qual` (USING) contém `status = 'draft'` (regressão submitted→draft permanece bloqueada)
- [x] A migration aparece como aplicada em `supabase migration list`
