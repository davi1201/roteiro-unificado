---
phase: 06-campos-do-formul-rio-torre-360
plan: 07
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-05
  - 06-06
files_modified:
  - roteiro-unificado/src/features/form/useFormSection.ts
  - roteiro-unificado/src/features/form/FormLayout.tsx
autonomous: false
requirements:
  - FORM-05
  - FORM-06
tags:
  - integration
  - useFormSection
  - FormLayout
  - completeness
  - wiring

must_haves:
  truths:
    - "useFormSection aceita control? opcional e mantém comportamento Phase 5 quando control ausente"
    - "Com control fornecido, useFormSection retorna errors achatado de formState.errors e completeness no range [0,1]"
    - "completeness === 1 quando todos os campos required do schema estão preenchidos sem erro"
    - "FormLayout renderiza o Section component correto via switch(activeTab) — 5 abas Torre 360 wireadas; demais abas mantêm fallback"
    - "Construtora logada acessa /form/:orgId e vê os campos reais das 5 abas Torre 360 (não mais placeholder)"
    - "Navegação entre abas preserva dados via Zustand sectionData"
    - "ProgressBadge da sidebar atualiza com completeness real para as 5 abas Torre 360 (não mais sentinel 0.01)"
  artifacts:
    - path: "roteiro-unificado/src/features/form/useFormSection.ts"
      provides: "Hook com sobrecarga: control? opcional + totalRequired? opcional"
      contains: "control?"
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "FormLayout renderizando Section components por activeTab"
      contains: "case TabKey.Identificacao"
  key_links:
    - from: "src/features/form/FormLayout.tsx"
      to: "5 Section components"
      via: "switch(activeTab) → <IdentificacaoSection /> | <TorreDecisaoSection /> | ..."
      pattern: "case TabKey\\.(Identificacao|TorreDecisao|TorreSienge|TorreAcesso|TorreClassificacao)"
    - from: "src/features/form/useFormSection.ts"
      to: "react-hook-form useFormState"
      via: "useFormState({ control })"
      pattern: "useFormState"
---

<objective>
Wiring final da Phase 6: (1) ampliar `useFormSection` para aceitar `control?` opcional (D-03) preservando o comportamento atual quando `control` ausente, calculando errors achatado + completeness real a partir do RHF; (2) ligar os 5 Section components Torre 360 ao `FormLayout` via switch por `activeTab`; (3) checkpoint humano de verificação visual no browser confirmando que construtora logada vê os campos reais navegáveis com preservação de estado entre abas.

Purpose: este plan transforma os componentes isolados (Plans 02-06) em um formulário real visitável pela construtora. Encerra a entrega da Phase 6, fechando FORM-05 (todos os campos com tipo correto) e FORM-06 (condicional qualBI funcional end-to-end).

Output: useFormSection com nova assinatura backward-compatible, FormLayout com switch wired, checkpoint humano aprovado para finalizar a fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-CONTEXT.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-01-PLAN.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-02-PLAN.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-03-PLAN.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-04-PLAN.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-05-PLAN.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-06-PLAN.md
@roteiro-unificado/src/features/form/useFormSection.ts
@roteiro-unificado/src/features/form/FormLayout.tsx
@roteiro-unificado/src/features/form/tabConfig.ts
@roteiro-unificado/src/stores/formStore.ts

<interfaces>
<!-- Estado pré-existente do useFormSection (Phase 5) que será ampliado: -->
From src/features/form/useFormSection.ts (atual):
- useFormSection(tenantId: string, tab: TabKey): {
    data: Record<string, unknown>;
    updateField: (field: string, value: unknown) => void;
    errors: Record<string, string>;   // sempre {} em Phase 5
    completeness: number;             // sentinel 0.01 ou 0
  }

<!-- Após esta ampliação (Phase 6): -->
Assinatura nova:
- useFormSection<T extends FieldValues = FieldValues>(tenantId: string, tab: TabKey, control?: Control<T>, totalRequired?: number): { ... mesma shape de retorno }

From react-hook-form:
- useFormState({ control }): { errors, isDirty, isValid, ... } — hook React; pode ser chamado sempre (mesmo se control undefined, retorna estado vazio)
- function flattenErrors(errors: FieldErrors): Record<string, string> — helper local (não exportado pelo RHF)

From FormLayout atual:
- linhas 90-95 do JSX: `<main className="flex-1 p-4 md:p-6"><h1>{activeTabConfig.label}</h1><p>Esta seção ainda não possui campos...</p></main>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ampliar useFormSection com control? opcional e completeness real</name>
  <files>roteiro-unificado/src/features/form/useFormSection.ts</files>
  <read_first>
    - roteiro-unificado/src/features/form/useFormSection.ts (assinatura e implementação atuais)
    - roteiro-unificado/src/stores/formStore.ts (TabKey, useFormStore — usados pelo hook)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-CONTEXT.md (D-03 — assinatura nova, compatibilidade retroativa)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Padrão 6 useFormSection atualizado + §Cálculo de completeness — useFormState({control}) é a API correta)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§useFormSection — bloco de compatibilidade retroativa, nota sobre useFormState não condicional)
  </read_first>
  <action>
    Modificar `src/features/form/useFormSection.ts` para a nova assinatura. Preservar exports atuais e comentário JSDoc atualizado.

    Mudanças:
    1. Adicionar imports: `import { useFormState, type Control, type FieldValues, type FieldErrors } from 'react-hook-form'`
    2. Tornar a função genérica: `export function useFormSection<T extends FieldValues = FieldValues>(tenantId: string, tab: TabKey, control?: Control<T>, totalRequired?: number)`
    3. Manter `const store = useFormStore(tenantId)`, `const data = store.sectionData[tab] ?? {}` e `const updateField = (...)` exatamente como hoje.
    4. CRÍTICO (regra de hooks): `useFormState` precisa ser chamado sempre, nunca condicionalmente. Implementação: `const formState = useFormState({ control: control as Control<FieldValues> | undefined })` — quando `control` é undefined, useFormState retorna estado vazio `{ errors: {}, isDirty: false, ... }` e não causa erro (verificar comportamento RHF — se causar, usar `useFormState({ control: control ?? ({} as Control<FieldValues>) })` ou estruturar com sub-hook interno). A solução preferida é criar um sub-hook `function useControlState<T extends FieldValues>(control?: Control<T>) { const noopControl = useMemo(() => ({}) as Control<FieldValues>, []); return useFormState({ control: (control ?? noopControl) as Control<FieldValues> }) }` e chamar incondicionalmente. Se RHF reclamar de control vazio, alternativa: usar `useForm()` interno dummy e returnar formState quando control real ausente.
    5. Função local `function flattenErrors(errs: FieldErrors): Record<string, string> { const out: Record<string, string> = {}; for (const [key, val] of Object.entries(errs ?? {})) { if (val && typeof val === 'object') { if ('message' in val && typeof (val as { message?: unknown }).message === 'string') { out[key] = (val as { message: string }).message } else { /* nested — recursar com prefixo key. */ Object.assign(out, prefixKeys(flattenErrors(val as FieldErrors), key + '.')) } } } return out } function prefixKeys(obj: Record<string, string>, prefix: string): Record<string, string> { const out: Record<string, string> = {}; for (const [k, v] of Object.entries(obj)) out[prefix + k] = v; return out }`
    6. Cálculo de errors: `const errors: Record<string, string> = control ? flattenErrors(formState.errors as FieldErrors) : {}`
    7. Cálculo de completeness:
       - Sem control: manter sentinel `const completeness = store.visitedTabs.has(tab) ? 0.01 : 0`
       - Com control: `if (totalRequired && totalRequired > 0) { const errorCount = Object.keys(errors).length; const filled = Math.max(0, totalRequired - errorCount); completeness = filled / totalRequired } else { /* totalRequired ausente ou 0 → tabela informativa: aba é 'completa' apenas se foi visitada E não tem erros */ completeness = formState.isDirty && errorCount === 0 ? 1 : (store.visitedTabs.has(tab) ? 0.5 : 0) }`
    8. Retornar `{ data, updateField, errors, completeness }` com a mesma shape de hoje.
    9. Atualizar o JSDoc no topo do arquivo: ampliar a explicação para mencionar a nova assinatura, comportamento de compatibilidade retroativa, e a referência a CONTEXT.md D-03. Manter pt-br.

    NÃO alterar a shape do retorno (consumidores existentes da Phase 5 — ex: ProgressBadge, TabNavigation — continuam funcionando). NÃO chamar `useFormState` condicionalmente. NÃO usar React state interno para errors/completeness (deve ser derivado direto do formState).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/features/form/useFormSection.ts` exists with `grep -E "export function useFormSection"`
    - Source: assinatura inclui generic e control? — `grep -E "useFormSection<T extends FieldValues" useFormSection.ts` retorna match
    - Source: assinatura inclui `control\\?: Control<T>` e `totalRequired\\?: number`
    - Source: contém `import { useFormState` de `'react-hook-form'`
    - Source: contém função `flattenErrors` (local) — `grep -E "function flattenErrors" useFormSection.ts` retorna match
    - Source: chama `useFormState` exatamente uma vez fora de blocos condicionais — `grep -c "useFormState(" useFormSection.ts` retorna 1
    - Source: contém branch de compatibilidade Phase 5 — `grep -E "store\\.visitedTabs\\.has\\(tab\\)" useFormSection.ts` retorna match (sentinel 0.01 preservado quando control ausente)
    - Source: retorna shape original — `grep -E "return \\{ data, updateField, errors, completeness" useFormSection.ts` retorna match
    - Source: arquivo NÃO contém `useState` ou `useEffect` para errors/completeness (devem ser derivações puras do formState e da store)
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
    - Behavior: `cd roteiro-unificado && npm run lint` não introduz novos erros
    - Behavior: arquivos da Phase 5 que usam useFormSection (TabNavigation, ProgressBadge se houver) continuam type-checking sem mudanças — confirmado pelo tsc limpo
  </acceptance_criteria>
  <done>useFormSection aceita control? opcional preservando compatibilidade Phase 5; quando control fornecido, errors são achatados via flattenErrors e completeness é derivado de totalRequired vs errorCount; tsc + lint limpos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ligar Section components ao FormLayout via switch por activeTab</name>
  <files>roteiro-unificado/src/features/form/FormLayout.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.tsx (estrutura atual, linhas 90-95 a substituir)
    - roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx (Plan 02 — assinatura props)
    - roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx (Plan 03)
    - roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx (Plan 04)
    - roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx (Plan 05)
    - roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx (Plan 06)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§FormLayout — switch wiring)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Padrão 5 FormLayout switch por activeTab)
  </read_first>
  <action>
    Modificar `src/features/form/FormLayout.tsx`. Preservar tudo do arquivo atual (cross-tenant guard, hash sync, sidebar, ProgressBar, TabNavigation, botão Sair) — APENAS substituir o bloco placeholder dentro de `<main>` (linhas ~90-95 atuais) pela renderização dinâmica da Section.

    Mudanças:
    1. Adicionar imports no topo do arquivo (após os imports existentes):
       - `import { IdentificacaoSection } from './sections/IdentificacaoSection'`
       - `import { TorreDecisaoSection } from './sections/TorreDecisaoSection'`
       - `import { TorreSiengeSection } from './sections/TorreSiengeSection'`
       - `import { TorreAcessoSection } from './sections/TorreAcessoSection'`
       - `import { TorreClassificacaoSection } from './sections/TorreClassificacaoSection'`

    2. Adicionar função pura (fora do componente, mas no mesmo arquivo, antes do `export function FormLayout`):
       ```
       function renderSection(activeTab: TabKey, tenantId: string) {
         switch (activeTab) {
           case TabKey.Identificacao:
             return <IdentificacaoSection tenantId={tenantId} />
           case TabKey.TorreDecisao:
             return <TorreDecisaoSection tenantId={tenantId} />
           case TabKey.TorreSienge:
             return <TorreSiengeSection tenantId={tenantId} />
           case TabKey.TorreAcesso:
             return <TorreAcessoSection tenantId={tenantId} />
           case TabKey.TorreClassificacao:
             return <TorreClassificacaoSection tenantId={tenantId} />
           default:
             return (
               <p className="mt-2 text-sm text-gray-500">
                 Esta seção ainda não possui campos. Em breve os campos estarão disponíveis aqui.
               </p>
             )
         }
       }
       ```
       O `default` cobre as 5 abas Habilitações/NDA (Phase 7) preservando o texto atual de placeholder.

    3. Substituir o bloco JSX dentro de `<main className="flex-1 p-4 md:p-6">`:
       - REMOVER as linhas atuais `<p className="mt-2 text-sm text-gray-500">Esta seção ainda não possui campos. Em breve os campos estarão disponíveis aqui.</p>` (linha ~92-94)
       - MANTER `<h1 className="text-xl font-semibold text-gray-900">{activeTabConfig.label}</h1>` (UI-SPEC §Layout requer)
       - ADICIONAR após o h1: `{renderSection(store.activeTab, tenantId)}`

    Resultado dentro de `<main>`:
    ```
    <main className="flex-1 p-4 md:p-6">
      <h1 className="text-xl font-semibold text-gray-900">{activeTabConfig.label}</h1>
      {renderSection(store.activeTab, tenantId)}
    </main>
    ```

    NÃO modificar a sidebar, ProgressBar, TabNavigation, cross-tenant guard, hash sync useEffect, ou handleSignOut. NÃO adicionar margem entre o h1 e a Section — cada Section define seu próprio espaçamento via `<form className="max-w-2xl">`.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5 &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: FormLayout.tsx importa os 5 Section components — `grep -E "from './sections/(IdentificacaoSection|TorreDecisaoSection|TorreSiengeSection|TorreAcessoSection|TorreClassificacaoSection)'" FormLayout.tsx | wc -l` retorna 5
    - Source: contém função `renderSection(activeTab: TabKey, tenantId: string)` — `grep -E "function renderSection" FormLayout.tsx` retorna match
    - Source: switch tem os 5 cases — `grep -E "case TabKey\\.(Identificacao|TorreDecisao|TorreSienge|TorreAcesso|TorreClassificacao):" FormLayout.tsx | wc -l` retorna 5
    - Source: cada case retorna `<{Section} tenantId={tenantId} />` — `grep -E "<(Identificacao|TorreDecisao|TorreSienge|TorreAcesso|TorreClassificacao)Section tenantId=\\{tenantId\\} />" FormLayout.tsx | wc -l` retorna 5
    - Source: bloco `<main>` invoca `renderSection(store.activeTab, tenantId)` — `grep -E "renderSection\\(store\\.activeTab, tenantId\\)" FormLayout.tsx` retorna match
    - Source: o h1 com `{activeTabConfig.label}` está preservado — `grep -E "<h1[^>]*>\\{activeTabConfig\\.label\\}</h1>" FormLayout.tsx` retorna match
    - Source: `default:` no switch retorna `<p>` com texto "Esta seção ainda não possui campos" (fallback para Phase 7 ainda não implementada)
    - Source: cross-tenant guard preservado — `grep -E "routeOrgId !== authOrgId" FormLayout.tsx` retorna match
    - Source: hash sync preservado — `grep -E "location\\.hash" FormLayout.tsx` retorna match
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
    - Behavior: `cd roteiro-unificado && npm run lint` não introduz novos erros
    - Behavior: `cd roteiro-unificado && npm run build` completa exit code 0
  </acceptance_criteria>
  <done>FormLayout renderiza dinamicamente o Section component correto via switch(activeTab); imports dos 5 components adicionados; o placeholder original vira default case (cobre as 5 abas Phase 7 ainda não implementadas); cross-tenant guard, sidebar e hash sync preservados; build limpo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — verificação visual das 5 abas Torre 360</name>
  <what-built>
    A construtora logada agora vê os campos reais das 5 abas Torre 360 ao acessar `/form/:orgId`:
    - Aba Identificação: 12 campos (Empresa + CNPJ com máscara obrigatórios + 10 opcionais)
    - Aba Torre Decisão: 4 selects + checkbox group (9 opções) + campo condicional "Qual BI?" que aparece/desaparece
    - Aba Torre Sienge: 12 módulos × 5 campos cada (60 campos em cards responsivos)
    - Aba Torre Acesso: 8 selects + 3 textareas + checkbox group (6 opções)
    - Aba Torre Classificação: 2 selects (T360-A..E + abordagem) + 6 textareas + checkbox group (9 evidências)

    Mudanças de aba preservam dados via Zustand sectionData. ProgressBadge da sidebar reflete completeness derivado do RHF para as 5 abas Torre 360. As 5 abas Habilitações/NDA continuam com o placeholder (entrega da Phase 7).
  </what-built>
  <how-to-verify>
    1. Iniciar dev server: `cd roteiro-unificado && npm run dev` (porta padrão Vite 5173)
    2. Abrir `http://localhost:5173/login` e fazer login com um usuário construtora (role `company`) cadastrado no Supabase. Se necessário, criar via painel admin (Phase 4) ou usar credenciais de seed da Phase 2.
    3. Após login, navegar para `/form/{seu-orgId}` (ou esperar redirect automático).
    4. **Aba Identificação:**
       - Verificar que aparecem 12 campos com labels do HTML de referência (Empresa, CNPJ, Data da reunião, etc.)
       - Tentar digitar `12345678901234` no CNPJ → deve aparecer formatado como `12.345.678/9012-34` (máscara client-side)
       - Sair do campo CNPJ com formato inválido (ex: `123`) → mensagem de erro "Formato inválido" deve aparecer abaixo
       - Sair do campo Empresa em branco → erro "Mínimo 2 caracteres"
    5. **Aba Torre Decisão** (clicar na aba ou usar `#torre-decisao` no URL):
       - Selecionar "Sim, Power BI" no "Existe BI hoje?" → campo "Qual ferramenta de BI?" deve APARECER abaixo
       - Selecionar "Não" no mesmo select → campo "Qual ferramenta de BI?" deve DESAPARECER (e o valor que você tinha digitado lá deve ser descartado se selecionar Sim novamente)
       - Marcar 3 ou 4 checkboxes do grupo "Decisões prioritárias"
       - Voltar para aba Identificação e retornar → os dados de Torre Decisão devem estar preservados
    6. **Aba Torre Sienge:**
       - Verificar que aparecem 12 cards (um por módulo Sienge) com nomes do HTML ("Cadastros: empresas...", "Financeiro: contas...", etc.)
       - Cada card tem 4 selects + 1 textarea
       - Preencher 2 módulos com selects diferentes; navegar para outra aba e voltar → preservado
       - Layout em desktop (≥ 768px) usa grid 2 colunas; em mobile (< 768px) coluna única
    7. **Aba Torre Acesso:**
       - 8 selects (Ambiente Sienge, API REST, Bulk Data, etc.) + 3 textareas + 1 checkbox group de 6 opções
       - Marcar e desmarcar checkboxes — estado preservado entre abas
    8. **Aba Torre Classificação:**
       - 2 selects (Classificação final T360-A..E, Abordagem recomendada) + 6 textareas + 1 checkbox group de 9 evidências
       - Selecionar T360-A; navegar; voltar — preservado
    9. **Sidebar (ProgressBadge):** após preencher Empresa + CNPJ válido na Identificação, o ícone da aba Identificação na sidebar deve mudar de círculo vazio (não visitado) para círculo meio-preenchido (em progresso) ou check (se completeness chegou a 1).
    10. **Refresh do browser (F5):** dados devem persistir (sessionStorage); fechar e reabrir a aba do browser limpa sectionData (sessionStorage por tab) mas mantém activeTab/visitedTabs (localStorage) — comportamento esperado pelo D-03 da Phase 5.
    11. **Abas Habilitações/NDA (Hab. Venda, Hab. Repositórios, Hab. Responsáveis, Hab. Classificação, NDA):** clicar deve mostrar o texto "Esta seção ainda não possui campos. Em breve os campos estarão disponíveis aqui." — confirma que o fallback default do switch está funcionando (Phase 7 implementará).
    12. **Console do browser:** sem erros vermelhos do React (sem warnings sobre keys faltando, sobre hooks chamados condicionalmente, sem loops infinitos de render).
  </how-to-verify>
  <resume-signal>Digite "approved" se a verificação visual confirma que as 5 abas Torre 360 funcionam conforme descrito, OU descreva os bugs/discrepâncias específicas encontradas (qual aba, qual campo, qual comportamento esperado vs observado).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash → activeTab → renderSection | Hash do URL escolhe qual Section monta. Hash inválido cai em TabKey.Identificacao via fallback no useEffect existente. Switch tem default para abas não implementadas. |
| useFormSection consumers (TabNavigation, ProgressBadge) | Continuam consumindo a mesma shape; backward-compat preservada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | DoS | Cada troca de aba monta novo Section com useForm | accept | Comportamento esperado de D-01 (useForm por aba). Custo: ~5-10ms por aba normalmente. Aceitável. |
| T-06-07-02 | Tampering | Hooks chamados condicionalmente em useFormSection | mitigate | useFormState chamado sempre (não dentro de if). Sub-hook pattern documentado na Task 1 action. Verificado via acceptance_criteria + lint react-hooks/rules-of-hooks. |
| T-06-07-03 | Information Disclosure | Cross-tenant na renderSection | mitigate | renderSection recebe `tenantId` derivado do cross-tenant guard EXISTENTE em FormLayout (linhas 53-55). Sem novo boundary criado. |
| T-06-07-04 | Tampering | Default case do switch expõe abas Phase 7 não implementadas | accept | Comportamento intencional: usuário vê placeholder informativo nas 5 abas Habilitações/NDA. Phase 7 substituirá. Sem dados sensíveis vazados. |
| T-06-07-SC | Tampering | Pacotes npm | accept | Nenhum pacote novo nesta fase ou plano. |

</threat_model>

<verification>
- `cd roteiro-unificado && npx tsc --noEmit` retorna exit code 0
- `cd roteiro-unificado && npm run lint` limpo nos arquivos modificados
- `cd roteiro-unificado && npm run build` completa sem erros
- `grep -E "useFormState\\(" roteiro-unificado/src/features/form/useFormSection.ts` retorna exatamente 1 (única invocação não condicional)
- `grep -c "Section tenantId" roteiro-unificado/src/features/form/FormLayout.tsx` retorna ≥ 5
- Checkpoint humano da Task 3 retorna "approved" (ou bugs específicos documentados e corrigidos antes de retomar)
</verification>

<success_criteria>
- useFormSection aceita control? + totalRequired? opcionais
- Quando control fornecido, useFormSection retorna errors achatado + completeness no range [0,1]
- Quando control ausente, mantém comportamento Phase 5 (errors {}, completeness sentinel 0.01)
- useFormState chamado incondicionalmente (regra de hooks respeitada)
- FormLayout importa e renderiza 5 Section components via switch(activeTab)
- Default case do switch preserva texto placeholder para abas Phase 7
- Cross-tenant guard, hash sync, sidebar, ProgressBar permanecem intocados
- tsc + lint + build limpos
- Checkpoint humano aprovado confirmando comportamento visual das 5 abas Torre 360 (incluindo condicional qualBI funcional, preservação cross-tab via Zustand, formato de CNPJ, e fallback das abas Phase 7)
</success_criteria>

<output>
Criar `.planning/phases/06-campos-do-formul-rio-torre-360/06-07-SUMMARY.md` ao concluir, com nota explícita sobre o resultado do checkpoint humano (approved | issues fixed in revision).
</output>
