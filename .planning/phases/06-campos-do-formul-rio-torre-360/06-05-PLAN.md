---
phase: 06-campos-do-formul-rio-torre-360
plan: 05
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - roteiro-unificado/src/schemas/torre-acesso.ts
  - roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx
autonomous: true
requirements:
  - FORM-05
tags:
  - zod
  - react-hook-form
  - form-section
  - torre-acesso

must_haves:
  truths:
    - "Schema Zod torreAcessoSchema cobre 10 selects/textareas + 6 checkboxes + 1 textarea final do HTML id='t_acesso'"
    - "TorreAcessoSection renderiza 8 SelectField (ambiente, subdominio, usuario leitura, homologacao, api, bulk, pacote, webhooks)"
    - "Renderiza 2 TextareaField (outras fontes, restrições segurança)"
    - "Renderiza 1 CheckboxGroupField 'Se todos os dados estiverem no Sienge' com 6 opções"
    - "Renderiza 1 TextareaField final 'Observação técnica essencial'"
    - "Edits sincronizam para sectionData[TabKey.TorreAcesso] sem loop"
  artifacts:
    - path: "roteiro-unificado/src/schemas/torre-acesso.ts"
      provides: "Schema Zod da aba Torre Acesso + tipo TorreAcessoData"
      contains: "export const torreAcessoSchema"
    - path: "roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx"
      provides: "Section component da aba Torre Acesso"
      contains: "export function TorreAcessoSection"
  key_links:
    - from: "src/features/form/sections/TorreAcessoSection.tsx"
      to: "src/schemas/torre-acesso.ts"
      via: "import { torreAcessoSchema, type TorreAcessoData }"
      pattern: "from '@/schemas/torre-acesso'"
    - from: "src/features/form/sections/TorreAcessoSection.tsx"
      to: "src/stores/formStore.ts"
      via: "updateSection(TabKey.TorreAcesso, ...)"
      pattern: "updateSection\\(TabKey\\.TorreAcesso"
---

<objective>
Implementar a aba Torre Acesso: schema Zod + Section component com 10 selects/textareas sobre ambiente Sienge e acesso técnico + 1 CheckboxGroup de "se todos os dados estiverem no Sienge" (6 opções) + 1 textarea final.

Purpose: cobrir FORM-05 para a 4ª aba Torre 360. Sem campos condicionais (RESEARCH §Inventário confirma FORM-06 não se aplica aqui). Estrutura segue exatamente o template Section consolidado nas Plans 02 e 03.

Output: schema Zod plano (sem aninhamento) + Section component sem ConditionalField.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-CONTEXT.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-01-PLAN.md
@roteiro-unificado/src/stores/formStore.ts
@roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
From @/components/ui (Wave 1):
- SelectField<T>, TextareaField<T>, CheckboxGroupField<T> — todos disponíveis após Plan 01

From src/stores/formStore.ts:
- TabKey.TorreAcesso = 'torre-acesso'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar schema Zod da aba Torre Acesso</name>
  <files>roteiro-unificado/src/schemas/torre-acesso.ts</files>
  <read_first>
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Inventário — Aba Torre Acesso: 10 selects/textareas + 6 checkboxes + 1 textarea)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§src/schemas/torre-acesso.ts segue padrão de createOrg/torre-decisao)
    - roteiro-unificado/src/schemas/createOrg.ts (padrão Zod import + export)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (id="t_acesso")
  </read_first>
  <action>
    Criar `src/schemas/torre-acesso.ts`. Importar `z` de `'zod'`. Exportar `torreAcessoSchema = z.object({...})` com EXATAMENTE estes 11 campos topo (todos `.optional()` pois HTML não marca required):

    Selects (8):
    1. `ambienteSienge: z.enum(['nuvem-data-center', 'local', 'hibrido', 'confirmar']).optional()`
    2. `subdominioTenant: z.enum(['sim', 'nao', 'confirmar']).optional()`
    3. `usuarioLeitura: z.enum(['possivel', 'nao-possivel', 'confirmar']).optional()`
    4. `ambienteHomologacao: z.enum(['sim', 'nao', 'confirmar']).optional()`
    5. `apiRest: z.enum(['disponivel', 'nao-disponivel', 'confirmar', 'parcial']).optional()`
    6. `bulkData: z.enum(['disponivel', 'nao-disponivel', 'confirmar', 'parcial']).optional()`
    7. `pacoteLimiteApi: z.enum(['sim', 'nao', 'confirmar-sienge-ti']).optional()`
    8. `webhooksRelevantes: z.enum(['sim', 'nao', 'confirmar', 'nao-aplicavel-fase-1']).optional()`

    Textareas (2):
    9. `outrasFontes: z.string().optional()`
    10. `restricoesSeguranca: z.string().optional()`

    Checkbox group (1 campo, 6 opções):
    11. `seTudoNoSienge: z.array(z.enum(['modulos-alimentados', 'empresas-obras-centros', 'historico-minimo', 'api-bulk-autorizado', 'limites-compativeis', 'regras-validadas'])).optional()`

    Textarea final (1):
    12. `observacaoTecnica: z.string().optional()`

    Exportar `export type TorreAcessoData = z.infer<typeof torreAcessoSchema>` e `export const TORRE_ACESSO_REQUIRED_COUNT = 0`.

    NÃO usar labels acentuados/com espaço em enum values. NÃO adicionar validação cruzada.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/schemas/torre-acesso.ts` exists and matches `grep -E "export const torreAcessoSchema"`
    - Source: contém as 12 chaves — `grep -E "\\b(ambienteSienge|subdominioTenant|usuarioLeitura|ambienteHomologacao|apiRest|bulkData|pacoteLimiteApi|webhooksRelevantes|outrasFontes|restricoesSeguranca|seTudoNoSienge|observacaoTecnica)\\b" torre-acesso.ts | sort -u | wc -l` retorna 12
    - Source: ambienteSienge enum = `['nuvem-data-center', 'local', 'hibrido', 'confirmar']`
    - Source: seTudoNoSienge é `z.array(z.enum(` com 6 valores
    - Source: contém `export type TorreAcessoData = z.infer<typeof torreAcessoSchema>` e `export const TORRE_ACESSO_REQUIRED_COUNT = 0`
    - Source: grep `Nuvem.*Data Center|"Local"|Power BI` (labels acentuados/HTML) retorna 0 em enum values (só slugs)
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
  </acceptance_criteria>
  <done>Schema Zod da Torre Acesso com 12 chaves cobrindo todos os campos do HTML id="t_acesso"; tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar TorreAcessoSection</name>
  <files>roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/schemas/torre-acesso.ts (Task 1)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§TorreAcessoSection — mesmo padrão de IdentificacaoSection sem condicionais)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md (§Layout — max-w-2xl)
    - roteiro-unificado/src/features/form/FormLayout.tsx (T-05-04-04)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (id="t_acesso" — labels e options)
  </read_first>
  <action>
    Criar `src/features/form/sections/TorreAcessoSection.tsx`.

    Imports: `useForm` de `react-hook-form`; `zodResolver` de `@hookform/resolvers/zod`; `useEffect` de `react`; `useFormStore`, `TabKey` de `@/stores/formStore`; `torreAcessoSchema`, tipo `TorreAcessoData` de `@/schemas/torre-acesso`; `SelectField`, `TextareaField`, `CheckboxGroupField` de `@/components/ui`.

    Props: `interface TorreAcessoSectionProps { tenantId: string }`. Export `TorreAcessoSection`.

    Dentro:
    1. `const store = useFormStore(tenantId)`
    2. `const { control, watch, formState: { errors } } = useForm<TorreAcessoData>({ resolver: zodResolver(torreAcessoSchema), defaultValues: (store.sectionData[TabKey.TorreAcesso] ?? {}) as Partial<TorreAcessoData>, mode: 'onBlur' })`
    3. Sync padrão `watch + useEffect → updateSection(TabKey.TorreAcesso, values)` com store fora das deps + eslint-disable.

    Constantes de options no topo (slug + label conforme HTML):
    - `ambienteOptions`: 4 — "Nuvem/Data Center" (nuvem-data-center), "Local" (local), "Híbrido" (hibrido), "Confirmar" (confirmar)
    - `simNaoConfirmarOptions`: 3 — "Sim" (sim), "Não" (nao), "Confirmar" (confirmar) — reutilizar para subdominioTenant e ambienteHomologacao
    - `usuarioLeituraOptions`: 3 — "Possível" (possivel), "Não possível" (nao-possivel), "Confirmar" (confirmar)
    - `apiBulkOptions`: 4 — "Disponível" (disponivel), "Não disponível" (nao-disponivel), "Confirmar" (confirmar), "Parcial" (parcial) — reutilizar para apiRest e bulkData
    - `pacoteOptions`: 3 — "Sim" (sim), "Não" (nao), "Confirmar com Sienge/TI" (confirmar-sienge-ti)
    - `webhooksOptions`: 4 — "Sim" (sim), "Não" (nao), "Confirmar" (confirmar), "Não aplicável na fase 1" (nao-aplicavel-fase-1)
    - `seTudoOptions`: 6 — "Módulos realmente alimentados" (modulos-alimentados), "Empresas, obras e centros corretos" (empresas-obras-centros), "Dados com histórico mínimo" (historico-minimo), "API/Bulk com acesso autorizado" (api-bulk-autorizado), "Limites compatíveis com o volume" (limites-compativeis), "Regras de indicadores validadas" (regras-validadas)

    JSX root: `<form className="max-w-2xl" noValidate>`. Organização em 3 grupos (separados por `<hr className="border-gray-100 my-6">`):

    Grupo "Ambiente e acessos":
    - `<SelectField name="ambienteSienge" control={control} label="Ambiente Sienge" options={ambienteOptions} ... />`
    - `<SelectField name="subdominioTenant" label="Subdomínio/tenant conhecido?" options={simNaoConfirmarOptions} ... />`
    - `<SelectField name="usuarioLeitura" label="Usuário técnico somente leitura?" options={usuarioLeituraOptions} ... />`
    - `<SelectField name="ambienteHomologacao" label="Ambiente de homologação?" options={simNaoConfirmarOptions} ... />`
    - `<SelectField name="apiRest" label="API REST" options={apiBulkOptions} ... />`
    - `<SelectField name="bulkData" label="Bulk Data" options={apiBulkOptions} ... />`
    - `<SelectField name="pacoteLimiteApi" label="Pacote/limite de API conhecido?" options={pacoteOptions} ... />`
    - `<SelectField name="webhooksRelevantes" label="Webhooks relevantes agora?" options={webhooksOptions} ... />`
    - `<TextareaField name="outrasFontes" label="Outras fontes oficiais ou relevantes" />`
    - `<TextareaField name="restricoesSeguranca" label="Restrições de segurança/confidencialidade" />`

    Grupo "Se todos os dados estiverem no Sienge":
    - `<CheckboxGroupField name="seTudoNoSienge" control={control} label="Checklist de viabilidade" options={seTudoOptions} error={errors.seTudoNoSienge?.message as string | undefined} />`

    Grupo final:
    - `<TextareaField name="observacaoTecnica" control={control} label="Observação técnica essencial" />`

    Para responsividade: agrupar Selects de 1 linha em `<div className="grid grid-cols-1 md:grid-cols-2 gap-4">` onde os labels forem curtos (subdominioTenant + usuarioLeitura, ambienteHomologacao + pacoteLimiteApi, apiRest + bulkData).

    NÃO usar handleSubmit. NÃO usar ConditionalField (sem condicionais nesta aba). NÃO incluir store nas deps.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx` exists and matches `grep -E "export function TorreAcessoSection"`
    - Source: contém `useForm<TorreAcessoData>(` com `resolver: zodResolver(torreAcessoSchema)`
    - Source: contém ≥ 8 ocorrências de `<SelectField` (para os 8 selects)
    - Source: contém ≥ 3 ocorrências de `<TextareaField` (outrasFontes, restricoesSeguranca, observacaoTecnica)
    - Source: contém exatamente 1 ocorrência de `<CheckboxGroupField` para seTudoNoSienge
    - Source: contém os labels checkbox do HTML — `grep -E "Módulos realmente alimentados|Empresas, obras e centros corretos|Dados com histórico mínimo|API/Bulk com acesso autorizado|Limites compatíveis com o volume|Regras de indicadores validadas" TorreAcessoSection.tsx | wc -l` retorna ≥ 6
    - Source: contém labels dos selects do HTML — `grep -E "Ambiente Sienge|Subdomínio/tenant conhecido|Usuário técnico somente leitura|Ambiente de homologação|API REST|Bulk Data|Pacote/limite de API conhecido|Webhooks relevantes agora|Outras fontes oficiais|Restrições de segurança|Observação técnica essencial" TorreAcessoSection.tsx | wc -l` retorna ≥ 11
    - Source: o sync useEffect NÃO inclui `store` nas deps
    - Source: grep `ConditionalField` retorna 0 ocorrências
    - Source: grep `useFormContext|FormProvider|handleSubmit` retorna 0 ocorrências
    - Source: grep `updateSection\\(TabKey\\.TorreAcesso` retorna ≥ 1 ocorrência
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
    - Behavior: `cd roteiro-unificado && npm run lint` não introduz novos erros
  </acceptance_criteria>
  <done>TorreAcessoSection renderiza os 12 campos do HTML id="t_acesso" sem ConditionalField; sync RHF→Zustand limpo; tsc + lint passam. Componente não está ligado no FormLayout ainda (Plan 07).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → Selects/CheckboxGroup → Zustand | Idem outras abas. Sem chamadas server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | Loop infinito watch+useEffect | mitigate | Padrão T-05-04-04 reaplicado. |
| T-06-05-02 | Information Disclosure | Campo restricoesSeguranca recebe dados sensíveis | mitigate | sectionData salvo em sessionStorage tenant-namespaced. Persistência server (Phase 8) deve avaliar criptografia em repouso conforme requisitos do piloto Sinduscon (fora do escopo desta fase). |
| T-06-05-03 | Tampering | Slugs vs labels acentuadas | mitigate | Enum values usam slugs (ex: `nuvem-data-center`); labels com acento ficam só em runtime do componente. |
| T-06-05-SC | Tampering | Pacotes npm | accept | Nenhum novo. |

</threat_model>

<verification>
- `cd roteiro-unificado && npx tsc --noEmit` retorna exit code 0
- `cd roteiro-unificado && npm run lint` limpo
- `grep -c "<SelectField" roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx` retorna ≥ 8
- `grep -c "<TextareaField" roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx` retorna ≥ 3
- `grep -c "ConditionalField" roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx` retorna 0
</verification>

<success_criteria>
- Schema cobre 12 chaves da aba Torre Acesso conforme HTML
- 8 SelectField + 3 TextareaField + 1 CheckboxGroupField na renderização
- Labels do HTML preservados; values usam slugs
- Layout responsivo grid-cols-2 em md+ para pares curtos
- Sync watch+useEffect sem `store` nas deps
- Sem ConditionalField (não há condicionais nesta aba)
- tsc + lint limpos
</success_criteria>

<output>
Criar `.planning/phases/06-campos-do-formul-rio-torre-360/06-05-SUMMARY.md` ao concluir.
</output>
