---
phase: 06-campos-do-formul-rio-torre-360
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - roteiro-unificado/src/schemas/torre-decisao.ts
  - roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx
autonomous: true
requirements:
  - FORM-05
  - FORM-06
tags:
  - zod
  - react-hook-form
  - form-section
  - torre-decisao
  - conditional-field

must_haves:
  truths:
    - "Schema Zod torreDecisaoSchema cobre todos os campos da aba Torre Decisão do HTML (6 inputs + checkboxes 9 opções + 2 finais)"
    - "Aba Torre Decisão renderiza SelectField para reuniaoGestao, comoInformacaoChega, existeBI, nivelGerencial"
    - "Selecionar 'sim-power-bi' ou 'sim-outra' em existeBI faz aparecer campo Input qualBI; selecionar 'em-implantacao' ou 'nao' faz desaparecer"
    - "Ao desaparecer, qualBI é desregistrado via unregister (não há valor stale na store)"
    - "CheckboxGroupField decisoesMelhorar gerencia array com 9 opções do HTML"
    - "Edits são sincronizados para sectionData[TabKey.TorreDecisao] via watch+useEffect"
  artifacts:
    - path: "roteiro-unificado/src/schemas/torre-decisao.ts"
      provides: "Schema Zod da aba Torre Decisão + tipo TorreDecisaoData + constante de obrigatórios"
      contains: "export const torreDecisaoSchema"
    - path: "roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx"
      provides: "Section component da aba Torre Decisão com campos condicionais (FORM-06)"
      contains: "ConditionalField"
  key_links:
    - from: "src/features/form/sections/TorreDecisaoSection.tsx"
      to: "ConditionalField + unregister RHF"
      via: "ConditionalField condition={mostraQualBI} unregisterFn={unregister}"
      pattern: "ConditionalField[\\s\\S]*unregisterFn=\\{unregister\\}"
    - from: "src/features/form/sections/TorreDecisaoSection.tsx"
      to: "src/schemas/torre-decisao.ts"
      via: "import { torreDecisaoSchema, type TorreDecisaoData }"
      pattern: "from '@/schemas/torre-decisao'"
    - from: "src/features/form/sections/TorreDecisaoSection.tsx"
      to: "watch para condição BI"
      via: "const watchedExisteBI = watch('existeBI')"
      pattern: "watch\\('existeBI'\\)"
---

<objective>
Implementar a aba Torre Decisão: schema Zod + Section component com selects de ritual e fontes de decisão, CheckboxGroup de prioridades, nível gerencial G1-G5, e o primeiro campo condicional do formulário (Qual BI? aparece quando "Existe BI hoje?" = "Sim, Power BI" ou "Sim, outra ferramenta").

Purpose: validar o padrão D-02 + ConditionalField em campo real. Esta aba prova que FORM-06 (campos condicionais com unregister + Zod optional) funciona end-to-end no design escolhido.

Output: arquivo Zod schema com selects + checkboxes + Section component com ConditionalField wiring correto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-CONTEXT.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-01-PLAN.md
@roteiro-unificado/src/stores/formStore.ts
@roteiro-unificado/src/schemas/createOrg.ts
@roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
<!-- Contratos disponíveis para o executor. -->

From @/components/ui (após Wave 1 / Plan 01):
- SelectField<T> — para os 4 selects desta aba
- TextareaField<T> — para campos "Relatórios/dashboards usados", "Números questionados ou divergentes", "Observações gerenciais essenciais"
- CheckboxGroupField<T> — para "Decisões que a Torre deve melhorar primeiro" (9 opções)
- ConditionalField — para campo qualBI (FORM-06)
- Input — para "Quem prepara a informação?"

From react-hook-form:
- useForm retorna { control, watch, unregister, formState: { errors }, register, setValue }
- unregister: (name: string, options?: { keepValue?: boolean }) => void

From src/stores/formStore.ts:
- TabKey.TorreDecisao = 'torre-decisao'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar schema Zod da aba Torre Decisão</name>
  <files>roteiro-unificado/src/schemas/torre-decisao.ts</files>
  <read_first>
    - roteiro-unificado/src/schemas/identificacao.ts (padrão Phase 6 estabelecido na Plan 02)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Inventário — Aba Torre Decisão + §Padrão 4 Zod v4 + §Armadilha 5 valores enum como slugs)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§src/schemas/torre-decisao.ts)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (fonte de verdade — seção id="t_decisao" com 6 campos + 9 checkboxes + 2 campos finais)
  </read_first>
  <action>
    Criar `src/schemas/torre-decisao.ts`. Importar `z` de `'zod'`. Exportar `torreDecisaoSchema = z.object({...})` com EXATAMENTE estes campos (todos `.optional()` porque HTML original não marca required; RESEARCH §Inventário confirma; CONTEXT.md A3 [ASSUMED]):

    Selects (com slugs como values, labels só ficam em options[] do componente):
    1. `reuniaoGestao: z.enum(['semanal', 'quinzenal', 'mensal', 'eventual', 'nao-estruturada']).optional()`
    2. `comoInformacaoChega: z.enum(['bi-dashboard', 'excel', 'relatorios-sienge', 'pdf', 'email-whatsapp', 'misto']).optional()`
    3. `existeBI: z.enum(['sim-power-bi', 'sim-outra', 'em-implantacao', 'nao']).optional()`
    4. `nivelGerencial: z.enum(['g1', 'g2', 'g3', 'g4', 'g5']).optional()`

    Campo condicional (sempre `.optional()` — RESEARCH §Armadilha 3):
    5. `qualBI: z.string().optional()`

    Inputs/textareas livres:
    6. `quemPreparaInfo: z.string().optional()`
    7. `relatoriosDiretoria: z.string().optional()`
    8. `numerosQuestionados: z.string().optional()`
    9. `observacoesGerenciais: z.string().optional()`

    Checkboxes (array):
    10. `decisoesMelhorar: z.array(z.enum(['caixa', 'contas', 'inadimplencia', 'margem', 'orcado-realizado', 'avanco-fisico', 'compras-criticas', 'vendas-estoque', 'pos-obra'])).optional()`

    Exportar: `export type TorreDecisaoData = z.infer<typeof torreDecisaoSchema>` e `export const TORRE_DECISAO_REQUIRED_COUNT = 0`.

    NÃO usar labels com acento/espaço dentro de `z.enum` (RESEARCH §Armadilha 5). NÃO marcar qualBI como required (ConditionalField requer optional).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/schemas/torre-decisao.ts` exists and matches `grep -E "export const torreDecisaoSchema"`
    - Source: contém todas as 10 chaves listadas — `grep -E "\\b(reuniaoGestao|comoInformacaoChega|existeBI|nivelGerencial|qualBI|quemPreparaInfo|relatoriosDiretoria|numerosQuestionados|observacoesGerenciais|decisoesMelhorar)\\b" torre-decisao.ts | sort -u | wc -l` retorna 10
    - Source: existeBI tem enum exato `['sim-power-bi', 'sim-outra', 'em-implantacao', 'nao']`
    - Source: nivelGerencial tem enum `['g1', 'g2', 'g3', 'g4', 'g5']`
    - Source: decisoesMelhorar é `z.array(z.enum(` com 9 valores listados
    - Source: arquivo contém `export type TorreDecisaoData = z.infer<typeof torreDecisaoSchema>`
    - Source: arquivo contém `export const TORRE_DECISAO_REQUIRED_COUNT = 0`
    - Source: grep `'Sim, Power BI'|'Power BI'|"Sim, Power BI"` retorna 0 (labels não aparecem em enums — só slugs)
    - Source: qualBI é `z.string().optional()` (verificar literal `qualBI: z.string().optional()`)
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
  </acceptance_criteria>
  <done>Schema Zod da Torre Decisão com slugs em todos os enums, qualBI optional (preparado para ConditionalField), 9 checkbox slugs em decisoesMelhorar; tipo e constante exportados; tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar TorreDecisaoSection com campo condicional qualBI</name>
  <files>roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/schemas/torre-decisao.ts (Task 1)
    - roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx (será criado em Plan 02 paralelo — assuma o template; se não existir, basear em CreateOrgModal.tsx + 06-PATTERNS.md)
    - roteiro-unificado/src/components/admin/CreateOrgModal.tsx (padrão useForm + zodResolver)
    - roteiro-unificado/src/features/form/FormLayout.tsx (loop infinito T-05-04-04)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§TorreDecisaoSection — ConditionalField wiring)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Padrão 3 Section + §Armadilha 2 watch loop + §Inventário Torre Decisão)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md (§Layout da Seção)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (labels e opções dos selects da aba Torre Decisão)
  </read_first>
  <action>
    Criar `src/features/form/sections/TorreDecisaoSection.tsx`.

    Imports: `useForm` de `react-hook-form`; `zodResolver` de `@hookform/resolvers/zod`; `useEffect` de `react`; `useFormStore`, `TabKey` de `@/stores/formStore`; `torreDecisaoSchema`, tipo `TorreDecisaoData` de `@/schemas/torre-decisao`; `Input`, `SelectField`, `TextareaField`, `CheckboxGroupField`, `ConditionalField` de `@/components/ui`.

    Props: `interface TorreDecisaoSectionProps { tenantId: string }`. Export nomeado `TorreDecisaoSection`.

    Dentro do componente:
    1. `const store = useFormStore(tenantId)`
    2. `const { control, watch, unregister, register, formState: { errors } } = useForm<TorreDecisaoData>({ resolver: zodResolver(torreDecisaoSchema), defaultValues: (store.sectionData[TabKey.TorreDecisao] ?? {}) as Partial<TorreDecisaoData>, mode: 'onBlur' })`
    3. Sync RHF→Zustand: `const values = watch(); useEffect(() => { store.updateSection(TabKey.TorreDecisao, values as Record<string, unknown>) }, [values])` com `// eslint-disable-next-line react-hooks/exhaustive-deps`. NÃO incluir `store` nas deps.
    4. Condição BI: `const watchedExisteBI = watch('existeBI'); const mostraQualBI = watchedExisteBI === 'sim-power-bi' || watchedExisteBI === 'sim-outra'`

    Definir constantes de options no topo do arquivo (antes do componente), cada uma com value (slug do schema) + label (texto do HTML):
    - `reuniaoOptions`: 5 opções — Semanal, Quinzenal, Mensal, Eventual, "Não estruturada"
    - `infoOptions`: 6 opções — "BI/dashboard", "Excel/planilhas", "Relatórios Sienge", "PDF/apresentação", "E-mail/WhatsApp", "Misto"
    - `biOptions`: 4 opções — "Sim, Power BI" (value `sim-power-bi`), "Sim, outra ferramenta" (`sim-outra`), "Em implantação" (`em-implantacao`), "Não" (`nao`)
    - `nivelGerencialOptions`: 5 opções G1..G5 com descrições do HTML ("G1 — Decisão informal", "G2 — Relatórios manuais", "G3 — BI parcial", "G4 — Gestão orientada por dados", "G5 — Gestão avançada")
    - `decisoesMelhorarOptions`: 9 opções (slug → label) — caixa: "Caixa realizado/projetado", contas: "Contas a pagar/receber", inadimplencia: "Inadimplência", margem: "Margem por obra", orcado-realizado: "Orçado x realizado", avanco-fisico: "Avanço físico/prazo", compras-criticas: "Compras críticas", vendas-estoque: "Vendas/estoque", pos-obra: "Pós-obra/qualidade"

    JSX root: `<form className="max-w-2xl" noValidate>`. Organização sugerida em 3 grupos (separados por `<hr className="border-gray-100 my-6">`):

    Grupo "Ritual e fontes de decisão":
    - `<SelectField name="reuniaoGestao" control={control} label="Existe reunião de gestão?" options={reuniaoOptions} placeholder="Selecione..." error={errors.reuniaoGestao?.message} />`
    - `<SelectField name="comoInformacaoChega" control={control} label="Como a informação chega?" options={infoOptions} ... />`
    - `<SelectField name="existeBI" control={control} label="Existe BI hoje?" options={biOptions} ... />`
    - `<ConditionalField condition={mostraQualBI} fieldName="qualBI" unregisterFn={unregister}>` envolvendo um wrapper `<div className="flex flex-col gap-1"><label className="text-sm font-semibold text-gray-700">Qual ferramenta de BI?</label><Input type="text" placeholder="Ex: Power BI Pro, Looker, Tableau..." {...register('qualBI')} error={!!errors.qualBI} errorMessage={errors.qualBI?.message} /></div>` `</ConditionalField>`
    - Campo Input para `quemPreparaInfo`: label "Quem prepara a informação?", placeholder "Controladoria, financeiro, engenharia, TI..."
    - `<TextareaField name="relatoriosDiretoria" control={control} label="Relatórios/dashboards usados pela diretoria" />`
    - `<TextareaField name="numerosQuestionados" control={control} label="Números questionados ou divergentes" />`

    Grupo "Decisões que a Torre deve melhorar primeiro":
    - `<CheckboxGroupField name="decisoesMelhorar" control={control} label="Decisões prioritárias" options={decisoesMelhorarOptions} error={errors.decisoesMelhorar?.message as string | undefined} />`

    Grupo final:
    - `<SelectField name="nivelGerencial" control={control} label="Nível gerencial atual" options={nivelGerencialOptions} ... />`
    - `<TextareaField name="observacoesGerenciais" control={control} label="Observações gerenciais essenciais" />`

    NÃO usar handleSubmit/onSubmit. NÃO incluir store nas deps. NÃO usar useFormContext.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx` exists and matches `grep -E "export function TorreDecisaoSection"`
    - Source: contém `useForm<TorreDecisaoData>(` com `resolver: zodResolver(torreDecisaoSchema)`
    - Source: destructure inclui `unregister` — `grep -E "const \\{[^}]*unregister[^}]*\\} = useForm" TorreDecisaoSection.tsx` retorna ≥ 1
    - Source: contém `watch('existeBI')` e a expressão `'sim-power-bi'` e `'sim-outra'` na linha que computa mostraQualBI
    - Source: contém `<ConditionalField` com `condition={mostraQualBI}` e `fieldName="qualBI"` e `unregisterFn={unregister}`
    - Source: contém pelo menos 4 ocorrências de `<SelectField` (reuniaoGestao, comoInformacaoChega, existeBI, nivelGerencial) — `grep -c "<SelectField" TorreDecisaoSection.tsx` retorna ≥ 4
    - Source: contém ≥ 1 ocorrência de `<CheckboxGroupField` para decisoesMelhorar
    - Source: contém ≥ 3 ocorrências de `<TextareaField` (relatoriosDiretoria, numerosQuestionados, observacoesGerenciais)
    - Source: contém ≥ 1 ocorrência de `<Input` para quemPreparaInfo
    - Source: o bloco de sync NÃO inclui `store` nas deps — linha contendo `}, [values]` está presente (sem `store` no array)
    - Source: contém constante de options com label "Sim, Power BI" mapeada para value "sim-power-bi" — `grep -B1 -A1 "Sim, Power BI" TorreDecisaoSection.tsx` mostra value `sim-power-bi` próximo
    - Source: opções de decisoesMelhorar incluem labels "Caixa realizado/projetado", "Contas a pagar/receber", "Inadimplência", "Margem por obra", "Orçado x realizado", "Avanço físico/prazo", "Compras críticas", "Vendas/estoque", "Pós-obra/qualidade" — `grep -E "Caixa realizado/projetado|Contas a pagar/receber|Inadimplência|Margem por obra|Orçado x realizado|Avanço físico/prazo|Compras críticas|Vendas/estoque|Pós-obra/qualidade" TorreDecisaoSection.tsx | wc -l` retorna ≥ 9
    - Source: grep `useFormContext|FormProvider|handleSubmit` retorna 0 ocorrências
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
    - Behavior: `cd roteiro-unificado && npm run lint` não introduz erros novos
  </acceptance_criteria>
  <done>TorreDecisaoSection renderiza todos os campos da aba com options completas do HTML, ConditionalField envolve qualBI com unregister wiring correto, sync RHF→Zustand sem loop; tsc e lint limpos. Componente não está ligado no FormLayout ainda (Plan 07).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → ConditionalField → Zustand | Quando o usuário desfaz a seleção que mostrava o ConditionalField, o valor de qualBI é removido (unregister keepValue:false). Sem chamadas server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | qualBI sobrevive a mudança em existeBI | mitigate | unregisterFn={unregister} no ConditionalField + Zod `qualBI: z.string().optional()` garantem que: (1) RHF remove o valor ao desmontar, (2) Zod aceita ausência como válido, (3) próxima sync watch→updateSection persiste estado sem qualBI. Verificado em acceptance_criteria. |
| T-06-03-02 | Tampering | Loop infinito watch+useEffect | mitigate | Padrão T-05-04-04 reaplicado: `store` fora das deps, `eslint-disable-next-line react-hooks/exhaustive-deps`. |
| T-06-03-03 | Information Disclosure | Slugs vs labels em enum | mitigate | Slugs (ex: 'sim-power-bi') ficam em storage/URL; labels com acento ficam apenas em runtime do componente. Reduz superfície de ataque XSS-via-storage-replay. |
| T-06-03-SC | Tampering | Pacotes npm | accept | Nenhum pacote novo nesta fase. |

</threat_model>

<verification>
- `cd roteiro-unificado && npx tsc --noEmit` retorna exit code 0
- `cd roteiro-unificado && npm run lint` limpo nos arquivos novos
- `grep -E "ConditionalField[\\s\\S]{0,200}unregisterFn=\\{unregister\\}" roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx` retorna match (multi-line)
- `grep -E "z.string\\(\\)\\.email\\(\\)" roteiro-unificado/src/schemas/torre-decisao.ts` retorna 0
</verification>

<success_criteria>
- Schema com 10 chaves cobrindo todos os campos da aba Torre Decisão do HTML
- Todos os enums usam slugs (não labels acentuadas)
- qualBI é `z.string().optional()` (compatível com ConditionalField + unregister)
- TorreDecisaoSection wireia ConditionalField com condition baseada em watch('existeBI') e unregisterFn=unregister
- Todos os campos do HTML §id="t_decisao" renderizados (4 selects + 1 input + 3 textareas + 1 checkbox group de 9 + 1 conditional Input)
- Labels de options refletem texto do HTML; values usam slugs do schema
- Sync watch+useEffect funcional sem `store` nas deps
- tsc + lint limpos
</success_criteria>

<output>
Criar `.planning/phases/06-campos-do-formul-rio-torre-360/06-03-SUMMARY.md` ao concluir.
</output>
