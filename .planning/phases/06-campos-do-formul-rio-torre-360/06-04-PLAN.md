---
phase: 06-campos-do-formul-rio-torre-360
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - roteiro-unificado/src/schemas/torre-sienge.ts
  - roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx
autonomous: true
requirements:
  - FORM-05
tags:
  - zod
  - react-hook-form
  - form-section
  - torre-sienge
  - nested-schema

must_haves:
  truths:
    - "Schema Zod torreSiengeSchema usa estrutura aninhada modules.{moduleSlug}.{column} (NÃO 60 campos flat)"
    - "12 módulos do HTML mapeados como chaves do objeto modules"
    - "Cada módulo tem 5 colunas: contratado, usoReal, confiancaDado, controleParalelo, observacoes"
    - "TorreSiengeSection renderiza tabela com 12 linhas (uma por módulo) e 5 colunas de campo por linha"
    - "Campo name usa notação de ponto: 'modules.cadastros.contratado' (RHF FieldPath aninhado)"
    - "Edits sincronizam para sectionData[TabKey.TorreSienge] sem loop"
  artifacts:
    - path: "roteiro-unificado/src/schemas/torre-sienge.ts"
      provides: "Schema Zod aninhado para 12 módulos × 5 colunas + tipo TorreSiengeData"
      contains: "modules: z.object"
    - path: "roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx"
      provides: "Section component com tabela de 12 módulos × 5 colunas Sienge"
      contains: "MODULES"
  key_links:
    - from: "src/schemas/torre-sienge.ts"
      to: "moduleSchema reutilizado 12x"
      via: "z.object({ modules: z.object({ cadastros: moduleSchema, ... }) })"
      pattern: "moduleSchema"
    - from: "src/features/form/sections/TorreSiengeSection.tsx"
      to: "nested names para RHF"
      via: "name={`modules.${moduleSlug}.contratado` as FieldPath<TorreSiengeData>}"
      pattern: "modules\\.\\$\\{|modules\\.cadastros|modules\\.financeiro"
---

<objective>
Implementar a aba Torre Sienge — a aba mais complexa da fase: tabela com 12 módulos Sienge × 5 colunas de avaliação por módulo (= 60 campos). Schema Zod usa estrutura aninhada para manter o código manutenível (RESEARCH §Armadilha 1).

Purpose: cobrir FORM-05 para a aba mais densa do formulário sem reduzir escopo. A decisão de implementação confirmada pelo CONTEXT.md / RESEARCH.md / PATTERNS.md é tabela completa com todos os 12 módulos sempre visíveis — fidelidade ao HTML original. Esta aba define como o RHF lida com paths de campo aninhados, padrão que será reutilizável em outras fases.

Output: schema aninhado + Section component renderizando a tabela de 12 módulos com 5 campos cada (4 SelectField + 1 TextareaField por linha).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-CONTEXT.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-01-PLAN.md
@roteiro-unificado/src/stores/formStore.ts
@roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
From @/components/ui (Wave 1):
- SelectField<T> — para 4 colunas select por módulo
- TextareaField<T> — para coluna observações por módulo

From react-hook-form:
- useForm<T>({...}): { control, watch, formState: { errors } }
- FieldPath<T> aceita notação de ponto para schemas aninhados ('modules.cadastros.contratado')

From src/stores/formStore.ts:
- TabKey.TorreSienge = 'torre-sienge'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar schema Zod aninhado torre-sienge.ts (12 módulos × 5 colunas)</name>
  <files>roteiro-unificado/src/schemas/torre-sienge.ts</files>
  <read_first>
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Inventário — Aba Torre Sienge: 12 módulos + 5 colunas + recomendação de tabela completa, §Armadilha 1 schema aninhado)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§src/schemas/torre-sienge.ts — schema aninhado completo com 12 chaves)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-CONTEXT.md (§decisions D-07)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (id="t_sienge" — verificar os 12 módulos e 5 colunas exatos)
  </read_first>
  <action>
    Criar `src/schemas/torre-sienge.ts`. Importar `z` de `'zod'`.

    Definir `moduleSchema` como `z.object({ contratado: z.enum(['sim', 'nao', 'nao-sabe', 'nao-aplicavel']).optional(), usoReal: z.enum(['total', 'parcial', 'baixo', 'nao-usa']).optional(), confiancaDado: z.enum(['alta', 'media', 'baixa', 'nao-confiavel']).optional(), controleParalelo: z.enum(['nao', 'excel', 'bi', 'outro', 'informal']).optional(), observacoes: z.string().optional() })`.

    Exportar `torreSiengeSchema = z.object({ modules: z.object({ cadastros: moduleSchema, financeiro: moduleSchema, inadimplencia: moduleSchema, orcamento: moduleSchema, planejamento: moduleSchema, medicoes: moduleSchema, compras: moduleSchema, comercial: moduleSchema, unidades: moduleSchema, contabilidade: moduleSchema, posObra: moduleSchema, bi: moduleSchema }).optional() })`.

    O objeto `modules` é `.optional()` para permitir defaultValues vazio na primeira montagem (RESEARCH §Armadilha 4).

    Exportar `export type TorreSiengeData = z.infer<typeof torreSiengeSchema>` e `export const TORRE_SIENGE_REQUIRED_COUNT = 0`.

    Também exportar uma constante `export const SIENGE_MODULES = [...]` com os 12 módulos em ordem do HTML para reutilizar no componente, cada item `{ slug: string, label: string }`:
    1. `{ slug: 'cadastros', label: 'Cadastros: empresas, obras, empreendimentos e centros de custo' }`
    2. `{ slug: 'financeiro', label: 'Financeiro: contas a pagar, receber, caixa e bancos' }`
    3. `{ slug: 'inadimplencia', label: 'Inadimplência e carteira de recebíveis' }`
    4. `{ slug: 'orcamento', label: 'Orçamento de obra e orçado x realizado' }`
    5. `{ slug: 'planejamento', label: 'Planejamento, cronograma e avanço físico' }`
    6. `{ slug: 'medicoes', label: 'Medições e contratos de empreiteiros' }`
    7. `{ slug: 'compras', label: 'Compras, suprimentos e custo comprometido' }`
    8. `{ slug: 'comercial', label: 'Comercial: vendas, reservas e contratos' }`
    9. `{ slug: 'unidades', label: 'Unidades, estoque imobiliário e tabela de preços' }`
    10. `{ slug: 'contabilidade', label: 'Contabilidade/fiscal e visão societária' }`
    11. `{ slug: 'posObra', label: 'Pós-obra, assistência técnica e qualidade' }`
    12. `{ slug: 'bi', label: 'BI, planilhas de diretoria e controles paralelos' }`

    Adicionar `as const` no array `SIENGE_MODULES` para preservar a tipagem literal dos slugs. Exportar também `export type SiengeModuleSlug = typeof SIENGE_MODULES[number]['slug']` para reutilizar no componente.

    NÃO criar 60 chaves planas no schema (anti-pattern RESEARCH §Armadilha 1). NÃO marcar módulos individuais como required.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/schemas/torre-sienge.ts` exists and matches `grep -E "export const torreSiengeSchema"`
    - Source: contém `const moduleSchema = z.object` (módulo reutilizável)
    - Source: moduleSchema tem 5 chaves — `grep -A 8 "const moduleSchema" torre-sienge.ts | grep -E "\\b(contratado|usoReal|confiancaDado|controleParalelo|observacoes):" | wc -l` retorna 5
    - Source: torreSiengeSchema contém `modules: z.object({` e referencia `moduleSchema` 12 vezes — `grep -c ": moduleSchema" torre-sienge.ts` retorna ≥ 12
    - Source: as 12 chaves de módulo aparecem: `grep -E "\\b(cadastros|financeiro|inadimplencia|orcamento|planejamento|medicoes|compras|comercial|unidades|contabilidade|posObra|bi):" torre-sienge.ts | sort -u | wc -l` retorna ≥ 12
    - Source: contém `export const SIENGE_MODULES` com `as const`
    - Source: SIENGE_MODULES tem os 12 labels do HTML — `grep -E "Cadastros: empresas|Financeiro: contas a pagar|Inadimplência e carteira|Orçamento de obra|Planejamento, cronograma|Medições e contratos|Compras, suprimentos|Comercial: vendas|Unidades, estoque|Contabilidade/fiscal|Pós-obra, assistência|BI, planilhas de diretoria" torre-sienge.ts | wc -l` retorna 12
    - Source: contém `export type TorreSiengeData = z.infer<typeof torreSiengeSchema>` e `export type SiengeModuleSlug`
    - Source: contém `export const TORRE_SIENGE_REQUIRED_COUNT = 0`
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
  </acceptance_criteria>
  <done>Schema aninhado com moduleSchema reutilizado para 12 módulos, SIENGE_MODULES array com slugs e labels do HTML, tipos e contagem exportados; tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar TorreSiengeSection com tabela de 12 módulos × 5 colunas</name>
  <files>roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/schemas/torre-sienge.ts (Task 1)
    - roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx ou TorreDecisaoSection.tsx (template Section quando existirem; senão CreateOrgModal.tsx)
    - roteiro-unificado/src/features/form/FormLayout.tsx (T-05-04-04)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§TorreSiengeSection — campos aninhados, name="modules.cadastros.contratado")
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Inventário Torre Sienge — 5 colunas com options exatas + §Armadilha 1)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md (§Layout da Seção — max-w-2xl não se aplica aqui; tabela precisa de largura maior)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (labels e opções das 5 colunas da aba t_sienge)
  </read_first>
  <action>
    Criar `src/features/form/sections/TorreSiengeSection.tsx`.

    Imports: `useForm` de `react-hook-form`; `zodResolver` de `@hookform/resolvers/zod`; `useEffect` de `react`; `useFormStore`, `TabKey` de `@/stores/formStore`; `torreSiengeSchema`, tipo `TorreSiengeData`, `SIENGE_MODULES`, `SiengeModuleSlug` de `@/schemas/torre-sienge`; `SelectField`, `TextareaField` de `@/components/ui`; tipo `FieldPath` de `react-hook-form`.

    Props: `interface TorreSiengeSectionProps { tenantId: string }`. Export `TorreSiengeSection`.

    Dentro do componente:
    1. `const store = useFormStore(tenantId)`
    2. `const { control, watch, formState: { errors } } = useForm<TorreSiengeData>({ resolver: zodResolver(torreSiengeSchema), defaultValues: (store.sectionData[TabKey.TorreSienge] ?? {}) as Partial<TorreSiengeData>, mode: 'onBlur' })`
    3. Sync RHF→Zustand padrão: `const values = watch(); useEffect(() => { store.updateSection(TabKey.TorreSienge, values as Record<string, unknown>) }, [values])` com `// eslint-disable-next-line react-hooks/exhaustive-deps`. Sem `store` nas deps.

    Definir constantes no topo do arquivo (antes do componente):
    - `contratadoOptions = [{value:'sim',label:'Sim'},{value:'nao',label:'Não'},{value:'nao-sabe',label:'Não sabe'},{value:'nao-aplicavel',label:'Não aplicável'}]`
    - `usoRealOptions = [{value:'total',label:'Total'},{value:'parcial',label:'Parcial'},{value:'baixo',label:'Baixo'},{value:'nao-usa',label:'Não usa'}]`
    - `confiancaOptions = [{value:'alta',label:'Alta'},{value:'media',label:'Média'},{value:'baixa',label:'Baixa'},{value:'nao-confiavel',label:'Não confiável'}]`
    - `paraleloOptions = [{value:'nao',label:'Não'},{value:'excel',label:'Sim, Excel'},{value:'bi',label:'Sim, BI'},{value:'outro',label:'Sim, outro sistema'},{value:'informal',label:'Sim, informal'}]`

    JSX root: `<form className="max-w-5xl" noValidate>` (mais largo que o padrão max-w-2xl porque é tabela).

    Renderização: a melhor abordagem para 12 módulos × 5 colunas em viewport responsivo é renderizar cada módulo como um Card individual com layout grid responsivo dentro, ao invés de uma `<table>` única (que quebra em mobile). Estrutura:

    ```
    {SIENGE_MODULES.map((module) => (
      <article key={module.slug} className="rounded-md border border-gray-200 bg-white p-4 mb-4">
        <h3 className="text-sm font-semibold text-gray-900 mb-3">{module.label}</h3>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
          <SelectField name={`modules.${module.slug}.contratado` as FieldPath<TorreSiengeData>} control={control} label="Contratado no Sienge?" options={contratadoOptions} error={errors.modules?.[module.slug]?.contratado?.message} />
          <SelectField name={`modules.${module.slug}.usoReal` as FieldPath<TorreSiengeData>} control={control} label="Uso real" options={usoRealOptions} ... />
          <SelectField name={`modules.${module.slug}.confiancaDado` as FieldPath<TorreSiengeData>} control={control} label="Confiança do dado" options={confiancaOptions} ... />
          <SelectField name={`modules.${module.slug}.controleParalelo` as FieldPath<TorreSiengeData>} control={control} label="Controle paralelo?" options={paraleloOptions} ... />
        </div>
        <div className="mt-3">
          <TextareaField name={`modules.${module.slug}.observacoes` as FieldPath<TorreSiengeData>} control={control} label="Observações" rows={2} />
        </div>
      </article>
    ))}
    ```

    Adicionar um `<h2 className="text-base font-semibold text-gray-900 mb-2">Avaliação dos módulos Sienge</h2>` e um `<p className="text-sm text-gray-500 mb-4">Para cada módulo, avalie a situação atual no Sienge.</p>` no topo, antes do map.

    Para acessar `errors.modules?.[module.slug]?.contratado?.message` com TypeScript: o cast `errors.modules?.[module.slug as keyof typeof errors.modules]?.contratado?.message` ou usar `(errors.modules as any)?.[module.slug]?.contratado?.message` se a tipagem do FieldErrors aninhado for problemática (RHF tem suporte parcial a paths aninhados em FieldErrors). Preferir o cast `keyof typeof errors.modules` quando possível; se inviável, usar `as Record<string, Record<string, { message?: string }>>` para a sub-árvore.

    NÃO usar `<table>` HTML (quebra em mobile). NÃO usar handleSubmit. NÃO incluir store nas deps. NÃO usar useFormContext.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx` exists and matches `grep -E "export function TorreSiengeSection"`
    - Source: contém `useForm<TorreSiengeData>(` com `resolver: zodResolver(torreSiengeSchema)`
    - Source: importa `SIENGE_MODULES` e `SiengeModuleSlug` de `@/schemas/torre-sienge`
    - Source: contém `SIENGE_MODULES.map(` (itera os 12 módulos)
    - Source: usa notação de ponto para names — `grep -E "modules\\.\\$\\{module\\.slug\\}\\.(contratado|usoReal|confiancaDado|controleParalelo|observacoes)" TorreSiengeSection.tsx | wc -l` retorna ≥ 5
    - Source: contém ≥ 4 ocorrências de `<SelectField` dentro do map (4 colunas select por módulo)
    - Source: contém ≥ 1 ocorrência de `<TextareaField` dentro do map (coluna observações)
    - Source: contém as 4 constantes de options — `grep -E "contratadoOptions|usoRealOptions|confiancaOptions|paraleloOptions" TorreSiengeSection.tsx | wc -l` retorna ≥ 8 (declaração + uso)
    - Source: paraleloOptions tem labels exatos — `grep -E "Sim, Excel|Sim, BI|Sim, outro sistema|Sim, informal" TorreSiengeSection.tsx | wc -l` retorna ≥ 4
    - Source: contém `max-w-5xl` (largura ampliada para tabela)
    - Source: NÃO contém `<table` ou `<tr` ou `<td` (cards responsivos, não tabela HTML)
    - Source: o sync useEffect NÃO inclui `store` nas deps
    - Source: grep `useFormContext|FormProvider|handleSubmit` retorna 0 ocorrências
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
    - Behavior: `cd roteiro-unificado && npm run lint` não introduz novos erros
  </acceptance_criteria>
  <done>TorreSiengeSection renderiza 12 cards (um por módulo) com 4 SelectField + 1 TextareaField cada, usando FieldPath aninhado modules.{slug}.{col}; total 60 campos cobrindo fidelidade ao HTML; tsc/lint limpos. Componente não está ligado no FormLayout ainda (Plan 07).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → 60 selects/textarea → Zustand | sectionData[TorreSienge] cresce significativamente. Subscriber existente do formStore persiste em sessionStorage por tenant — isolamento mantido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | DoS | sectionData cresce com 60 campos | accept | Tamanho típico: ~5KB JSON com todos preenchidos. sessionStorage quota é ~5MB. Sem risco prático. Auto-sync via watch executa em cada keystroke; throttle/debounce vem na Phase 8. |
| T-06-04-02 | Tampering | Loop infinito watch+useEffect | mitigate | Padrão T-05-04-04: store fora das deps. |
| T-06-04-03 | Information Disclosure | Cross-tenant sectionData | mitigate | useFormStore(tenantId) namespaceia a chave sessionStorage `form-data-${tenantId}` (existente desde Phase 3). Nunca chamar useFormStore sem tenantId. |
| T-06-04-04 | Tampering | Type unsafety em errors.modules?.[slug] | mitigate | Cast controlado com `keyof typeof errors.modules` ou `Record<string, ...>`. Aceito porque RHF tem suporte parcial a FieldErrors aninhado e o cast é local ao acesso. |
| T-06-04-SC | Tampering | Pacotes npm | accept | Nenhum novo pacote. |

</threat_model>

<verification>
- `cd roteiro-unificado && npx tsc --noEmit` retorna 0 erros
- `cd roteiro-unificado && npm run lint` limpo nos novos arquivos
- `grep -c "moduleSchema" roteiro-unificado/src/schemas/torre-sienge.ts` retorna ≥ 13 (1 declaração + 12 referências)
- `grep -E "modules\\.\\$\\{module\\.slug\\}" roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx` retorna ≥ 5 matches
- `grep -E "<table|<tr|<td" roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx` retorna 0 (sem HTML table)
</verification>

<success_criteria>
- Schema aninhado com `moduleSchema` reutilizado 12 vezes (não 60 campos planos)
- SIENGE_MODULES array com 12 itens slug/label conforme HTML
- TorreSiengeSection renderiza 12 cards responsivos (article com border + p-4); cada um com 4 SelectField + 1 TextareaField
- FieldPath aninhado funciona via template literal (`modules.${slug}.{col}`)
- Layout responsivo: grid-cols-1 em mobile, grid-cols-2 em md+
- max-w-5xl para acomodar a tabela
- Sem `<table>` HTML (cards responsivos)
- tsc + lint limpos
</success_criteria>

<output>
Criar `.planning/phases/06-campos-do-formul-rio-torre-360/06-04-SUMMARY.md` ao concluir.
</output>
