---
phase: 06-campos-do-formul-rio-torre-360
plan: 06
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - roteiro-unificado/src/schemas/torre-classificacao.ts
  - roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx
autonomous: true
requirements:
  - FORM-05
tags:
  - zod
  - react-hook-form
  - form-section
  - torre-classificacao

must_haves:
  truths:
    - "Schema Zod torreClassificacaoSchema cobre 2 selects (classificacaoFinal T360-A..E, abordagemRecomendada) + 5 textareas (justificativa, fase1, fase2, foraEscopo, riscos, proximosPassos) + 1 checkbox group de 9 opções (evidenciasSolicitar)"
    - "TorreClassificacaoSection renderiza SelectField com 5 opções T360-A..E na classificacaoFinal"
    - "SelectField abordagemRecomendada renderiza 5 opções do HTML"
    - "CheckboxGroupField evidenciasSolicitar renderiza 9 evidências do HTML"
    - "Edits sincronizam para sectionData[TabKey.TorreClassificacao] sem loop"
  artifacts:
    - path: "roteiro-unificado/src/schemas/torre-classificacao.ts"
      provides: "Schema Zod da aba Torre Classificação + tipo TorreClassificacaoData"
      contains: "export const torreClassificacaoSchema"
    - path: "roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx"
      provides: "Section component da aba Torre Classificação"
      contains: "export function TorreClassificacaoSection"
  key_links:
    - from: "src/features/form/sections/TorreClassificacaoSection.tsx"
      to: "src/schemas/torre-classificacao.ts"
      via: "import { torreClassificacaoSchema, type TorreClassificacaoData }"
      pattern: "from '@/schemas/torre-classificacao'"
    - from: "src/features/form/sections/TorreClassificacaoSection.tsx"
      to: "updateSection store"
      via: "updateSection(TabKey.TorreClassificacao, ...)"
      pattern: "updateSection\\(TabKey\\.TorreClassificacao"
---

<objective>
Implementar a aba Torre Classificação (última aba Torre 360): classificação final T360-A..E, abordagem recomendada, plano macro de fases e riscos, e checklist de evidências essenciais a solicitar.

Purpose: cobrir FORM-05 para a 5ª e última aba Torre 360. Encerra o trabalho de campos da Torre 360 no schema/component layer. Sem condicionais. Estrutura segue template Section consolidado.

Output: schema Zod plano + Section component com 2 selects + 5 textareas + 1 checkbox group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-CONTEXT.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md
@.planning/phases/06-campos-do-formul-rio-torre-360/06-01-PLAN.md
@roteiro-unificado/src/stores/formStore.ts
@roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html

<interfaces>
From @/components/ui (Wave 1):
- SelectField<T>, TextareaField<T>, CheckboxGroupField<T>

From src/stores/formStore.ts:
- TabKey.TorreClassificacao = 'torre-classificacao'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar schema Zod da aba Torre Classificação</name>
  <files>roteiro-unificado/src/schemas/torre-classificacao.ts</files>
  <read_first>
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-RESEARCH.md (§Inventário — Aba Torre Classificação: 2 selects + 5 textareas + 9 checkboxes + 1 textarea final)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§torre-classificacao mesmo padrão de torre-decisao)
    - roteiro-unificado/src/schemas/createOrg.ts (padrão Zod base)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (id="t_fechamento")
  </read_first>
  <action>
    Criar `src/schemas/torre-classificacao.ts`. Importar `z`.

    Exportar `torreClassificacaoSchema = z.object({...})` com EXATAMENTE estes 9 campos (todos `.optional()`):

    Selects (2):
    1. `classificacaoFinal: z.enum(['t360-a', 't360-b', 't360-c', 't360-d', 't360-e']).optional()`
    2. `abordagemRecomendada: z.enum(['integracao-sienge', 'hibrida-sienge-bi', 'ativacao-dados', 'reconciliacao-bi', 'projeto-preparatorio']).optional()`

    Textareas livres (5):
    3. `justificativa: z.string().optional()`
    4. `fase1: z.string().optional()`
    5. `fase2: z.string().optional()`
    6. `foraEscopo: z.string().optional()`
    7. `riscos: z.string().optional()`

    Checkbox group (1, com 9 valores):
    8. `evidenciasSolicitar: z.array(z.enum(['modulos-contratados', 'confirmacao-api-bulk', 'relatorios-diretoria', 'bi-atual', 'planilhas-criticas', 'cnpjs-obras', 'fluxo-caixa', 'orcamento-realizado', 'responsaveis-area'])).optional()`

    Textarea final (1):
    9. `proximosPassos: z.string().optional()`

    Exportar `export type TorreClassificacaoData = z.infer<typeof torreClassificacaoSchema>` e `export const TORRE_CLASSIFICACAO_REQUIRED_COUNT = 0`.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/schemas/torre-classificacao.ts` exists and matches `grep -E "export const torreClassificacaoSchema"`
    - Source: contém as 9 chaves — `grep -E "\\b(classificacaoFinal|abordagemRecomendada|justificativa|fase1|fase2|foraEscopo|riscos|evidenciasSolicitar|proximosPassos)\\b" torre-classificacao.ts | sort -u | wc -l` retorna 9
    - Source: classificacaoFinal enum = `['t360-a', 't360-b', 't360-c', 't360-d', 't360-e']`
    - Source: abordagemRecomendada enum tem 5 valores incluindo `'integracao-sienge'` e `'reconciliacao-bi'` e `'projeto-preparatorio'`
    - Source: evidenciasSolicitar é `z.array(z.enum(` com 9 valores incluindo `'modulos-contratados'`, `'confirmacao-api-bulk'`, `'cnpjs-obras'`
    - Source: contém `export type TorreClassificacaoData = z.infer<typeof torreClassificacaoSchema>` e `export const TORRE_CLASSIFICACAO_REQUIRED_COUNT = 0`
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
  </acceptance_criteria>
  <done>Schema Zod da Torre Classificação com 9 chaves cobrindo todos os campos do HTML id="t_fechamento"; tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar TorreClassificacaoSection</name>
  <files>roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/schemas/torre-classificacao.ts (Task 1)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-PATTERNS.md (§TorreClassificacaoSection mesmo padrão de IdentificacaoSection sem condicionais)
    - .planning/phases/06-campos-do-formul-rio-torre-360/06-UI-SPEC.md (§Layout — max-w-2xl)
    - roteiro-unificado/src/features/form/FormLayout.tsx (T-05-04-04)
    - roteiro_unificado_completo_torre360_habilitacoes_nda_piloto_sinduscon_v2.html (id="t_fechamento" — labels exatos)
  </read_first>
  <action>
    Criar `src/features/form/sections/TorreClassificacaoSection.tsx`.

    Imports: `useForm`, `zodResolver`, `useEffect`, `useFormStore`, `TabKey`, `torreClassificacaoSchema`, tipo `TorreClassificacaoData` de `@/schemas/torre-classificacao`; `SelectField`, `TextareaField`, `CheckboxGroupField` de `@/components/ui`.

    Props: `interface TorreClassificacaoSectionProps { tenantId: string }`. Export `TorreClassificacaoSection`.

    Dentro:
    1. `const store = useFormStore(tenantId)`
    2. `const { control, watch, formState: { errors } } = useForm<TorreClassificacaoData>({ resolver: zodResolver(torreClassificacaoSchema), defaultValues: (store.sectionData[TabKey.TorreClassificacao] ?? {}) as Partial<TorreClassificacaoData>, mode: 'onBlur' })`
    3. Sync padrão `const values = watch(); useEffect(() => { store.updateSection(TabKey.TorreClassificacao, values as Record<string, unknown>) }, [values])` com `// eslint-disable-next-line react-hooks/exhaustive-deps`. Sem `store` nas deps.

    Constantes de options no topo (slug + label do HTML):
    - `classificacaoFinalOptions`: 5 — "T360-A" (t360-a), "T360-B" (t360-b), "T360-C" (t360-c), "T360-D" (t360-d), "T360-E" (t360-e). Pode incluir descrição na label se o HTML tiver: ex "T360-A — Pronto para integração", etc. Verificar o HTML para o texto exato.
    - `abordagemOptions`: 5 — "Integração Sienge/API/Bulk" (integracao-sienge), "Híbrida: Sienge + BI/planilhas" (hibrida-sienge-bi), "Ativação de dados primeiro" (ativacao-dados), "Reconciliação com BI atual" (reconciliacao-bi), "Projeto preparatório" (projeto-preparatorio)
    - `evidenciasOptions`: 9 — "Lista de módulos Sienge contratados" (modulos-contratados), "Confirmação API REST/Bulk" (confirmacao-api-bulk), "Relatórios usados pela diretoria" (relatorios-diretoria), "BI atual: prints, links ou descrição" (bi-atual), "Planilhas críticas" (planilhas-criticas), "Lista de CNPJs e obras ativas" (cnpjs-obras), "Exemplo de fluxo de caixa" (fluxo-caixa), "Exemplo de orçamento x realizado" (orcamento-realizado), "Responsáveis por área" (responsaveis-area)

    JSX root: `<form className="max-w-2xl" noValidate>`. Organização em 3 grupos:

    Grupo "Classificação da empresa":
    - `<SelectField name="classificacaoFinal" control={control} label="Classificação final" options={classificacaoFinalOptions} error={errors.classificacaoFinal?.message} />`
    - `<SelectField name="abordagemRecomendada" control={control} label="Abordagem recomendada" options={abordagemOptions} ... />`
    - `<TextareaField name="justificativa" control={control} label="Justificativa curta" />`

    Separador `<hr className="border-gray-100 my-6">`.

    Grupo "Plano macro e riscos":
    - `<TextareaField name="fase1" control={control} label="Fase 1 sugerida" />`
    - `<TextareaField name="fase2" control={control} label="Fase 2 sugerida" />`
    - `<TextareaField name="foraEscopo" control={control} label="Fora do escopo inicial" />`
    - `<TextareaField name="riscos" control={control} label="Riscos para contrato, preço e cronograma" />`

    Separador `<hr className="border-gray-100 my-6">`.

    Grupo "Evidências essenciais a solicitar":
    - `<CheckboxGroupField name="evidenciasSolicitar" control={control} label="Selecione as evidências a solicitar" options={evidenciasOptions} error={errors.evidenciasSolicitar?.message as string | undefined} />`
    - `<TextareaField name="proximosPassos" control={control} label="Próximos passos" />`

    NÃO usar handleSubmit. NÃO usar ConditionalField. NÃO incluir store nas deps.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx` exists and matches `grep -E "export function TorreClassificacaoSection"`
    - Source: contém `useForm<TorreClassificacaoData>(` com `resolver: zodResolver(torreClassificacaoSchema)`
    - Source: contém ≥ 2 ocorrências de `<SelectField` (classificacaoFinal, abordagemRecomendada)
    - Source: contém ≥ 5 ocorrências de `<TextareaField` (justificativa, fase1, fase2, foraEscopo, riscos) + 1 final (proximosPassos) = ≥ 6
    - Source: contém exatamente 1 ocorrência de `<CheckboxGroupField` para evidenciasSolicitar
    - Source: contém os labels do HTML — `grep -E "Classificação final|Abordagem recomendada|Justificativa curta|Fase 1 sugerida|Fase 2 sugerida|Fora do escopo inicial|Riscos para contrato, preço e cronograma|Próximos passos" TorreClassificacaoSection.tsx | wc -l` retorna ≥ 8
    - Source: contém todos os 9 labels de evidências — `grep -E "Lista de módulos Sienge contratados|Confirmação API REST/Bulk|Relatórios usados pela diretoria|BI atual|Planilhas críticas|Lista de CNPJs e obras ativas|Exemplo de fluxo de caixa|Exemplo de orçamento x realizado|Responsáveis por área" TorreClassificacaoSection.tsx | wc -l` retorna ≥ 9
    - Source: contém todas as 5 labels de classificacao — `grep -E "T360-A|T360-B|T360-C|T360-D|T360-E" TorreClassificacaoSection.tsx | wc -l` retorna ≥ 5
    - Source: contém labels de abordagem — `grep -E "Integração Sienge|Híbrida: Sienge|Ativação de dados primeiro|Reconciliação com BI atual|Projeto preparatório" TorreClassificacaoSection.tsx | wc -l` retorna ≥ 5
    - Source: o sync useEffect NÃO inclui `store` nas deps
    - Source: grep `ConditionalField|useFormContext|FormProvider|handleSubmit` retorna 0 ocorrências
    - Source: grep `updateSection\\(TabKey\\.TorreClassificacao` retorna ≥ 1
    - Behavior: `cd roteiro-unificado && npx tsc --noEmit` produz 0 erros
    - Behavior: `cd roteiro-unificado && npm run lint` não introduz novos erros
  </acceptance_criteria>
  <done>TorreClassificacaoSection renderiza 2 SelectField + 6 TextareaField + 1 CheckboxGroupField cobrindo todos os campos do HTML id="t_fechamento"; sync RHF→Zustand; tsc + lint limpos. Componente não está ligado no FormLayout ainda (Plan 07).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → Selects/CheckboxGroup → Zustand | Idem outras abas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | Loop infinito watch+useEffect | mitigate | Padrão T-05-04-04. |
| T-06-06-02 | Information Disclosure | Campo riscos pode conter dados de contrato | accept | sectionData tenant-namespaced em sessionStorage. Avaliação de criptografia em repouso vem na Phase 8 (persistência Supabase). |
| T-06-06-03 | Tampering | Slugs vs labels | mitigate | Enums usam slugs (`t360-a`); labels com hyphen no UI. |
| T-06-06-SC | Tampering | Pacotes npm | accept | Nenhum novo. |

</threat_model>

<verification>
- `cd roteiro-unificado && npx tsc --noEmit` retorna 0 erros
- `cd roteiro-unificado && npm run lint` limpo
- `grep -c "<SelectField" roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx` retorna ≥ 2
- `grep -c "<TextareaField" roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx` retorna ≥ 6
- `grep -c "ConditionalField" roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx` retorna 0
</verification>

<success_criteria>
- Schema cobre 9 chaves da aba Torre Classificação conforme HTML
- 2 SelectField + 6 TextareaField + 1 CheckboxGroupField renderizados
- Labels do HTML preservados; values usam slugs
- Sync watch+useEffect sem `store` nas deps
- Sem ConditionalField (não há condicionais)
- tsc + lint limpos
</success_criteria>

<output>
Criar `.planning/phases/06-campos-do-formul-rio-torre-360/06-06-SUMMARY.md` ao concluir.
</output>
