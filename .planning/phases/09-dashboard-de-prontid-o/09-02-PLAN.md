---
phase: 09-dashboard-de-prontidao
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - roteiro-unificado/src/components/admin/CompanyCard.tsx
  - roteiro-unificado/src/components/admin/CompanyCard.test.tsx
  - roteiro-unificado/src/components/admin/AssessmentSection.tsx
autonomous: true
requirements: [DASH-02]
nyquist_compliant: true

must_haves:
  truths:
    - "CompanyCard exibe nome, CNPJ, data, nível técnico e Badge G1-G5 quando há avaliação submetida"
    - "CompanyCard exibe badge cinza 'Sem avaliação' e '—' nos campos quando latestAssessment é null"
    - "AssessmentSection lista as versões de avaliação de uma org (histórico) com estados loading/empty"
  artifacts:
    - path: "roteiro-unificado/src/components/admin/CompanyCard.tsx"
      provides: "Card de empresa para o grid do AdminDashboard"
      exports: ["CompanyCard"]
    - path: "roteiro-unificado/src/components/admin/AssessmentSection.tsx"
      provides: "Seção de avaliações para o OrgDetail (hook próprio useOrgAssessments)"
      exports: ["AssessmentSection"]
  key_links:
    - from: "roteiro-unificado/src/components/admin/CompanyCard.tsx"
      to: "src/features/admin/useOrgsWithReadiness.ts (OrgWithReadiness)"
      via: "import type OrgWithReadiness"
      pattern: "OrgWithReadiness"
    - from: "roteiro-unificado/src/components/admin/CompanyCard.tsx"
      to: "/admin/orgs/:orgId"
      via: "Link com buttonVariants (sem asChild)"
      pattern: "buttonVariants"
---

<objective>
Criar os dois componentes apresentacionais do lado admin: `CompanyCard` (card de empresa do grid, com Badge G1-G5 ou estado "Sem avaliação") e `AssessmentSection` (seção de histórico de avaliações que será embutida no OrgDetail).

Purpose: CompanyCard entrega DASH-02 (card com nome, CNPJ, data, nível gerencial e técnico). AssessmentSection prepara a integração do OrgDetail (histórico de versões por org). Ambos são componentes folha, sem wiring de rota — a composição final ocorre no Plan 04.
Output: `CompanyCard.tsx` (+ teste), `AssessmentSection.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-dashboard-de-prontid-o/09-CONTEXT.md
@.planning/phases/09-dashboard-de-prontid-o/09-PATTERNS.md
@.planning/phases/09-dashboard-de-prontid-o/09-UI-SPEC.md

<interfaces>
<!-- Contratos verificados — usar diretamente. -->

OrgWithReadiness (de Plan 09-01, src/features/admin/useOrgsWithReadiness.ts):
  { id, name, cnpj: string|null, active, latestAssessment: { id, readiness_level_mgmt: string|null, readiness_level_tech: string|null, submitted_at: string|null, version } | null }

Componentes UI (src/components/ui/index.ts barrel):
  Badge (prop `grade: Grade` onde Grade = 'G1'|'G2'|'G3'|'G4'|'G5'), Card, CardHeader, CardContent, CardFooter, Skeleton
  buttonVariants({ variant, size }) — Button NÃO suporta asChild; usar <Link className={buttonVariants(...)}>

Cores e cópia (09-UI-SPEC.md):
  Badge "Sem avaliação": bg-gray-200 text-gray-600 (pill inline)
  CNPJ: text-xs text-gray-500 font-mono
  CardFooter Link: variant="ghost" size="sm", texto "Ver detalhes →" → /admin/orgs/:orgId
  Campos vazios: "—"

Data pt-BR (padrão HistoryPage.tsx):
  new Date(submitted_at).toLocaleDateString('pt-BR') + ' às ' + toLocaleTimeString('pt-BR', { hour:'2-digit', minute:'2-digit' })

Analog AssessmentSection: src/features/form/HistoryPage.tsx (hook useAssessmentHistory interno, queryKey ['assessments', orgId], Skeleton 3 cards, empty state, card por versão com Badge).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar CompanyCard em src/components/admin/CompanyCard.tsx</name>
  <files>roteiro-unificado/src/components/admin/CompanyCard.tsx, roteiro-unificado/src/components/admin/CompanyCard.test.tsx</files>
  <read_first>
    - roteiro-unificado/src/components/admin/OrgTable.tsx (padrão de pill inline cinza, Skeleton, navegação)
    - roteiro-unificado/src/features/form/HistoryPage.tsx (Badge grade, article aria-label, formatação de data pt-BR, buttonVariants no Link)
    - roteiro-unificado/src/components/ui/badge.tsx (Grade type e gradeConfig G1-G5)
    - roteiro-unificado/src/components/ui/card.tsx (Card/CardHeader/CardContent/CardFooter)
    - roteiro-unificado/src/features/admin/useOrgsWithReadiness.ts (tipo OrgWithReadiness — criado no Plan 09-01)
    - roteiro-unificado/src/features/form/useSubmitAssessment.test.ts (mock react-router-dom Link, vi.mock)
  </read_first>
  <behavior>
    - CompanyCard com org.latestAssessment=null renderiza pill "Sem avaliação" (bg-gray-200 text-gray-600) e NÃO renderiza Badge G1-G5
    - CompanyCard com org.latestAssessment=null exibe "—" nos campos de data e nível técnico
    - CompanyCard com org.latestAssessment presente renderiza <Badge grade={readiness_level_mgmt}> (ex: grade="G2")
    - CompanyCard com latestAssessment presente exibe data formatada pt-BR de submitted_at
    - CompanyCard renderiza Link "Ver detalhes →" com href para /admin/orgs/{org.id}
    - CompanyCard exibe nome da empresa e CNPJ (font-mono)
  </behavior>
  <action>
    Criar `src/components/admin/CompanyCard.tsx` exportando `CompanyCard` com prop `{ org: OrgWithReadiness }` (importar tipo de `@/features/admin/useOrgsWithReadiness`).

    Estrutura conforme 09-UI-SPEC §CompanyCard: `<Card>` com `border-gray-200 shadow-sm`; CardHeader (`pb-3`) com linha contendo nome da empresa (`text-base font-semibold text-gray-900`) + à direita o Badge G1-G5 OU pill "Sem avaliação"; abaixo o CNPJ (`text-xs text-gray-500 font-mono`). CardContent (`pt-0`): "Última avaliação: {data}" (`text-sm text-gray-500`) e "Nível técnico: {string}" (`text-sm text-gray-600`). CardFooter (`pt-3 border-t border-gray-100`): `<Link to={\`/admin/orgs/${org.id}\`} className={buttonVariants({ variant: 'ghost', size: 'sm' })}>Ver detalhes →</Link>`.

    Lógica condicional: se `org.latestAssessment` é null → pill "Sem avaliação" (`<span className="inline-flex items-center rounded-full bg-gray-200 px-2 py-0.5 text-xs font-medium text-gray-600">Sem avaliação</span>`), data e nível técnico = "—". Se presente e `readiness_level_mgmt` truthy → `<Badge grade={org.latestAssessment.readiness_level_mgmt as Grade} />`; data = `submitted_at` formatado pt-BR (ou "—" se null); nível técnico = `readiness_level_tech ?? '—'`.

    Wrapper `<article aria-label={...}>` ou usar Card com aria-label semântico (padrão HistoryPage). Importar `buttonVariants` de `@/components/ui/button` (NÃO usar `<Button asChild>` — não suportado). Cores via tokens Tailwind — nunca hardcodar hex.

    Criar `CompanyCard.test.tsx`: `vi.mock('react-router-dom', () => ({ Link: ({ children, to }) => <a href={to}>{children}</a> }))`; renderizar com `@testing-library/react` e cobrir os 6 casos do bloco behavior (assessment=null vs presente, badge, "—", data, link href, nome/CNPJ).
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run src/components/admin/CompanyCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/admin/CompanyCard.tsx` contém `export function CompanyCard`
    - `src/components/admin/CompanyCard.tsx` contém `buttonVariants(` e NÃO contém `asChild`
    - `src/components/admin/CompanyCard.tsx` contém o texto `Sem avaliação`
    - `src/components/admin/CompanyCard.tsx` contém `font-mono` (CNPJ) e o caractere `—`
    - `src/components/admin/CompanyCard.tsx` contém `to={` com `/admin/orgs/`
    - `npm test -- --run src/components/admin/CompanyCard.test.tsx` sai com código 0 cobrindo os 6 casos behavior
  </acceptance_criteria>
  <done>CompanyCard renderiza ambos os estados (com/sem avaliação), Badge G1-G5 ou pill cinza, "—" em campos vazios, Link para detalhe via buttonVariants; teste verde.</done>
</task>

<task type="auto">
  <name>Task 2: Criar AssessmentSection em src/components/admin/AssessmentSection.tsx</name>
  <files>roteiro-unificado/src/components/admin/AssessmentSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/HistoryPage.tsx (useAssessmentHistory interno: queryKey ['assessments', orgId], select de versões, Skeleton 3 cards, empty state, card por versão com Badge e pill de status)
    - roteiro-unificado/src/components/ui/badge.tsx (Badge grade)
    - roteiro-unificado/src/components/ui/skeleton.tsx (Skeleton)
    - roteiro-unificado/src/hooks/useToast.ts (useToast().error — obrigatório, nunca Sonner direto)
  </read_first>
  <action>
    Criar `src/components/admin/AssessmentSection.tsx` exportando `AssessmentSection` com prop `{ orgId: string }`. Renderiza uma `<section className="mt-8 border-t border-gray-200 pt-8">` com `<h2 className="text-lg font-semibold text-gray-900">Avaliações</h2>` seguida da lista de versões.

    Definir hook interno `useOrgAssessments(orgId)` idêntico ao `useAssessmentHistory` do HistoryPage: `useQuery({ queryKey: ['assessments', orgId], queryFn: async () => { const {data,error}=await supabase.from('assessments').select('id, version, status, submitted_at, readiness_level_mgmt, readiness_level_tech').eq('org_id', orgId).order('version', { ascending: false }); if(error) throw error; return data ?? [] }, staleTime: 60_000, enabled: !!orgId })`. O queryKey `['assessments', orgId]` é compartilhado de propósito com HistoryPage (cache reusado).

    Estados: loading → 3 Skeleton cards (padrão HistoryPage linhas 79-93). Erro → `useToast().error('Erro ao carregar dados. Tente recarregar a página.')` em useEffect sobre isError + console.error. Empty (lista vazia) → bloco centrado com heading "Nenhuma avaliação registrada" e body "Esta organização ainda não submeteu nenhuma avaliação." (cópia exata do 09-UI-SPEC). Data → lista de `<article>` por versão: "Versão {version}", pill de status ("Enviado" `bg-accent text-white` / "Rascunho" `bg-gray-100 text-gray-700`), Badge G1-G5 quando `readiness_level_mgmt` truthy, data formatada pt-BR.

    Reservar o link de exportação como botão desabilitado com tooltip "Em breve" (Phase 10/11) — opcional, conforme 09-UI-SPEC §interações; se incluir, `disabled` e sem navegação. Cores via tokens.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -i "AssessmentSection" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/admin/AssessmentSection.tsx` contém `export function AssessmentSection`
    - `src/components/admin/AssessmentSection.tsx` contém `queryKey: ['assessments', orgId]`
    - `src/components/admin/AssessmentSection.tsx` contém o texto `Avaliações` (h2) e `Nenhuma avaliação registrada` (empty state)
    - `src/components/admin/AssessmentSection.tsx` contém `useToast()` (não importa `toast` do sonner diretamente)
    - Comando de verify retorna `0` (zero erros tsc relativos a AssessmentSection)
  </acceptance_criteria>
  <done>AssessmentSection renderiza histórico de versões da org com loading/empty/data states, queryKey compartilhado com HistoryPage, sem erros de tipo. Não está ainda montado no OrgDetail (Plan 04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Supabase | AssessmentSection consulta assessments por org_id; RLS isola |
| dados JSONB/string→DOM | valores de avaliação renderizados como texto no card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-XTEN | Information Disclosure | AssessmentSection query por orgId | accept (mitigado por RLS) | `assessments_select` restringe acesso; AdminRoute já garante role=admin para a rota que monta esta seção. Sem alteração de política. |
| T-09-INJ | Tampering | readiness_level_*/version renderizados | mitigate | Valores usados apenas como texto em JSX (`{value}`); nunca `dangerouslySetInnerHTML`. Badge recebe grade como string controlada. |
</threat_model>

<verification>
- `cd roteiro-unificado && npm test -- --run src/components/admin/CompanyCard.test.tsx` verde
- `cd roteiro-unificado && npx tsc --noEmit` sem erros nos arquivos novos
- Nenhum import direto de `sonner` (apenas `useToast()`)
</verification>

<success_criteria>
- CompanyCard cobre os dois estados (com/sem avaliação) conforme DASH-02 e 09-UI-SPEC
- AssessmentSection lista versões da org com loading/empty/error states, pronta para montar no OrgDetail
- Teste de CompanyCard verde; tsc limpo
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-de-prontid-o/09-02-SUMMARY.md` when done
</output>
