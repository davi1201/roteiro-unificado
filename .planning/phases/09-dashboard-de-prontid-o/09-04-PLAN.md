---
phase: 09-dashboard-de-prontidao
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - roteiro-unificado/src/pages/admin/AdminDashboard.tsx
  - roteiro-unificado/src/pages/admin/AdminDashboard.test.tsx
  - roteiro-unificado/src/pages/admin/OrgDetail.tsx
  - roteiro-unificado/src/router.tsx
autonomous: false
requirements: [DASH-01, DASH-05]
nyquist_compliant: true

must_haves:
  truths:
    - "D-01: Admin vê grid de CompanyCards (OrgTable removida, as duas views não coexistem) com badges G1-G5 e estado 'Sem avaliação'"
    - "D-02: Botão 'Nova Organização' permanece no header do AdminDashboard; CompanyCard tem apenas link 'Ver detalhes' → /admin/orgs/:orgId"
    - "Admin filtra/busca por nome, CNPJ e nível de prontidão; contagem de resultados é exibida"
    - "Admin clica 'Ver detalhes' → OrgDetail mostra a seção de Avaliações (histórico)"
    - "Construtora acessa /form/:orgId/dashboard (rota registrada sob ProtectedRoute)"
  artifacts:
    - path: "roteiro-unificado/src/pages/admin/AdminDashboard.tsx"
      provides: "Grid de CompanyCards com filtros client-side; sem paginação"
      contains: "useOrgsWithReadiness"
    - path: "roteiro-unificado/src/router.tsx"
      provides: "Rota /form/:orgId/dashboard sob ProtectedRoute"
      contains: "/form/:orgId/dashboard"
  key_links:
    - from: "roteiro-unificado/src/pages/admin/AdminDashboard.tsx"
      to: "useOrgsWithReadiness + CompanyCard"
      via: "grid de CompanyCards filtrado por useMemo"
      pattern: "CompanyCard"
    - from: "roteiro-unificado/src/pages/admin/OrgDetail.tsx"
      to: "AssessmentSection"
      via: "<AssessmentSection orgId={orgId} /> após os Cards existentes"
      pattern: "AssessmentSection"
    - from: "roteiro-unificado/src/router.tsx"
      to: "CompanyDashboard"
      via: "rota folha sob ProtectedRoute"
      pattern: "CompanyDashboard"
---

<objective>
Wiring final da fase: reescrever `AdminDashboard` para usar `useOrgsWithReadiness` + grid de `CompanyCard` com filtros client-side (removendo OrgTable e paginação), montar `AssessmentSection` no `OrgDetail`, e registrar a rota `/form/:orgId/dashboard` no router. Encerra com checkpoint humano de verificação visual das duas views.

Purpose: Conecta os componentes das Waves 1-2 às rotas reais. Entrega DASH-01 (grid admin), DASH-05 (filtros/busca) e fecha a integração de DASH-02/03/04 nas telas. Sem este plano, os componentes existem mas não estão acessíveis.
Output: AdminDashboard reescrito (+ teste de filtros), OrgDetail com seção de avaliações, router com nova rota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-dashboard-de-prontid-o/09-CONTEXT.md
@.planning/phases/09-dashboard-de-prontid-o/09-RESEARCH.md
@.planning/phases/09-dashboard-de-prontid-o/09-PATTERNS.md
@.planning/phases/09-dashboard-de-prontid-o/09-UI-SPEC.md

<interfaces>
<!-- Contratos das Waves anteriores — usar diretamente. -->

De Plan 09-01: useOrgsWithReadiness() → useQuery<OrgWithReadiness[]> (queryKey ['orgs','with-readiness']).
  OrgWithReadiness: { id, name, cnpj: string|null, active, latestAssessment: {...} | null }
De Plan 09-02: CompanyCard ({ org: OrgWithReadiness }); AssessmentSection ({ orgId: string }).
De Plan 09-03: CompanyDashboard (página da rota /form/:orgId/dashboard).

Estado atual de AdminDashboard.tsx (REMOVER ao migrar — Armadilha 4 do RESEARCH):
  PAGE_SIZE=10, page state, totalPages, pagedOrgs useMemo, botões Anterior/Próxima.
  Manter intactos: header com h1 + Button "Nova Organização" (accent), CreateOrgModal e seu gatilho handleNewOrg, tratamento de erro via useToast em useEffect.

Estado atual de OrgDetail.tsx: Cards de org + Membros (MemberTable), AddMemberModal, ArchiveOrgDialog. Layout acima dos modais NÃO deve mudar.

router.tsx: bloco `{ element: <ProtectedRoute />, children: [ {path:'/form/:orgId'}, {path:'/form/:orgId/history'} ] }` — adicionar /form/:orgId/dashboard como terceiro filho.

Filtros (09-UI-SPEC + RESEARCH Padrão 2):
  searchTerm (busca nome/CNPJ case-insensitive), gradeFilter ('Todos'|'G1'..'G5'|'Sem avaliação').
  'Sem avaliação' filtra !org.latestAssessment; G1-G5 compara org.latestAssessment?.readiness_level_mgmt === gradeFilter.
Cópia: título "Painel de Prontidão", subtítulo "Visão geral de todas as organizações do piloto", placeholder "Buscar por nome ou CNPJ", label "Nível de prontidão", "Limpar filtros", "{N} empresa(s) encontrada(s)", empty pós-filtro "Nenhuma empresa encontrada para os filtros selecionados.", empty inicial "Nenhuma empresa cadastrada".
Loading admin: 6 Skeleton cards.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescrever AdminDashboard com grid de CompanyCards + filtros</name>
  <files>roteiro-unificado/src/pages/admin/AdminDashboard.tsx, roteiro-unificado/src/pages/admin/AdminDashboard.test.tsx</files>
  <read_first>
    - roteiro-unificado/src/pages/admin/AdminDashboard.tsx (estado atual: header, CreateOrgModal, paginação a remover, useToast)
    - roteiro-unificado/src/features/admin/useOrgsWithReadiness.ts (hook — Plan 09-01)
    - roteiro-unificado/src/components/admin/CompanyCard.tsx (CompanyCard — Plan 09-02)
    - roteiro-unificado/src/components/ui/skeleton.tsx (Skeleton para loading)
    - roteiro-unificado/src/features/form/useSubmitAssessment.test.ts (padrão de teste: mock supabase, QueryClientProvider, mock router)
  </read_first>
  <behavior>
    - filtro useMemo retorna todas as orgs quando searchTerm='' e gradeFilter='Todos'
    - filtro por nome filtra case-insensitive (busca 'xyz' encontra 'Construtora XYZ')
    - filtro por CNPJ filtra case-insensitive sobre org.cnpj
    - gradeFilter='G2' retorna apenas orgs com latestAssessment.readiness_level_mgmt === 'G2'
    - gradeFilter='Sem avaliação' retorna apenas orgs com latestAssessment === null
    - "Limpar filtros" reseta searchTerm para '' e gradeFilter para 'Todos'
  </behavior>
  <action>
    Reescrever `src/pages/admin/AdminDashboard.tsx` substituindo `useOrgs`+`OrgTable` por `useOrgsWithReadiness` + grid de `CompanyCard`.

    REMOVER completamente (Armadilha 4): `const PAGE_SIZE = 10`, `page` state, `totalPages`, `pagedOrgs` useMemo, e os botões de paginação Anterior/Próxima no JSX. Não deixar variáveis órfãs (causaria warning de no-unused-vars).

    MANTER: header com h1 (texto agora "Painel de Prontidão") + subtítulo "Visão geral de todas as organizações do piloto" + Button "Nova Organização" (`bg-accent`), `CreateOrgModal` com `handleNewOrg`, e o tratamento de erro via `useToast().error(...)` em useEffect sobre `isError`.

    ADICIONAR filtros (Padrão 2 do RESEARCH): `const [searchTerm, setSearchTerm] = useState('')`; `const [gradeFilter, setGradeFilter] = useState('Todos')`. `const filteredOrgs = useMemo(() => orgs.filter(org => { matchesSearch = searchTerm==='' || org.name.toLowerCase().includes(searchTerm.toLowerCase()) || (org.cnpj ?? '').toLowerCase().includes(searchTerm.toLowerCase()); matchesGrade = gradeFilter==='Todos' || (gradeFilter==='Sem avaliação' ? !org.latestAssessment : org.latestAssessment?.readiness_level_mgmt === gradeFilter); return matchesSearch && matchesGrade }), [orgs, searchTerm, gradeFilter])`.

    JSX de filtros (linha abaixo do header): input de busca (`placeholder="Buscar por nome ou CNPJ"`, `<label htmlFor="search">` para acessibilidade) + select de nível (`<label htmlFor="grade-filter">`, opções "Todos","G1","G2","G3","G4","G5","Sem avaliação", label "Nível de prontidão") + Button "Limpar filtros" (`variant="ghost" size="sm"`, visível quando há filtro ativo, reseta ambos) + span de contagem "{filteredOrgs.length} empresa(s) encontrada(s)".

    Grid: `<div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">`. Loading (`isLoading`) → 6 Skeleton cards. Vazio (orgs.length===0) → empty state "Nenhuma empresa cadastrada" + body. Vazio pós-filtro (filteredOrgs.length===0 mas orgs.length>0) → "Nenhuma empresa encontrada para os filtros selecionados." Dados → `filteredOrgs.map(org => <CompanyCard key={org.id} org={org} />)`.

    Para testabilidade do filtro, extrair a lógica de filtragem para função pura local exportada (ex: `export function filterOrgs(orgs, searchTerm, gradeFilter)`) e testá-la diretamente em `AdminDashboard.test.tsx` (sem precisar montar a página inteira). Mockar supabase/router conforme padrão. Cobrir os 6 casos behavior.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run src/pages/admin/AdminDashboard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/admin/AdminDashboard.tsx` contém `useOrgsWithReadiness` e `CompanyCard`
    - `src/pages/admin/AdminDashboard.tsx` NÃO contém `OrgTable`, `PAGE_SIZE`, `pagedOrgs`
    - `src/pages/admin/AdminDashboard.tsx` contém `filteredOrgs` e `useMemo`
    - `src/pages/admin/AdminDashboard.tsx` contém os textos `Buscar por nome ou CNPJ`, `Limpar filtros`, `empresa(s) encontrada(s)`
    - `src/pages/admin/AdminDashboard.tsx` mantém `CreateOrgModal` e `Nova Organização`
    - `npm test -- --run src/pages/admin/AdminDashboard.test.tsx` sai com código 0 cobrindo os 6 casos behavior
    - `cd roteiro-unificado && npx tsc --noEmit` sem erros (sem variáveis órfãs de paginação)
  </acceptance_criteria>
  <done>AdminDashboard renderiza grid de CompanyCards filtrável por nome/CNPJ/nível, sem paginação, mantendo header e CreateOrgModal; teste de filtros verde; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Montar AssessmentSection no OrgDetail e registrar rota do dashboard no router</name>
  <files>roteiro-unificado/src/pages/admin/OrgDetail.tsx, roteiro-unificado/src/router.tsx</files>
  <read_first>
    - roteiro-unificado/src/pages/admin/OrgDetail.tsx (layout atual: Cards + Membros + modais; onde inserir a seção)
    - roteiro-unificado/src/components/admin/AssessmentSection.tsx (AssessmentSection — Plan 09-02)
    - roteiro-unificado/src/router.tsx (bloco ProtectedRoute com /form/:orgId e /form/:orgId/history)
    - roteiro-unificado/src/features/form/CompanyDashboard.tsx (CompanyDashboard — Plan 09-03)
  </read_first>
  <action>
    Em `src/pages/admin/OrgDetail.tsx`: importar `import { AssessmentSection } from '@/components/admin/AssessmentSection'`. Inserir `{orgId && <AssessmentSection orgId={orgId} />}` APÓS o último `</Card>` dos blocos existentes (Membros) e ANTES dos modais (AddMemberModal/ArchiveOrgDialog). Não alterar nada acima — o layout de org + membros permanece intacto (discrição do CONTEXT: manter layout atual).

    Em `src/router.tsx`: importar `CompanyDashboard` de `@/features/form/CompanyDashboard`. Adicionar como terceiro filho do bloco `{ element: <ProtectedRoute />, children: [...] }`, após `/form/:orgId/history`: `{ path: '/form/:orgId/dashboard', element: <CompanyDashboard /> }`.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -iE "OrgDetail|router|CompanyDashboard|AssessmentSection" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/admin/OrgDetail.tsx` contém `import { AssessmentSection }` e `<AssessmentSection orgId={`
    - `src/router.tsx` contém `CompanyDashboard` (import) e `path: '/form/:orgId/dashboard'`
    - A rota /form/:orgId/dashboard está dentro do bloco `<ProtectedRoute />` (não AdminRoute)
    - Comando de verify retorna `0` (zero erros tsc relativos a estes arquivos)
    - `cd roteiro-unificado && npm run build` completa sem erros
  </acceptance_criteria>
  <done>OrgDetail exibe a seção de Avaliações abaixo dos cards existentes; rota /form/:orgId/dashboard registrada sob ProtectedRoute; build limpo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — verificação visual dos dois painéis</name>
  <files>roteiro-unificado/src/pages/admin/AdminDashboard.tsx, roteiro-unificado/src/pages/admin/OrgDetail.tsx, roteiro-unificado/src/features/form/CompanyDashboard.tsx</files>
  <action>Pausar para verificação humana após o wiring. Nenhuma alteração de código nesta task — apenas confirmação visual das duas views no browser conforme os passos abaixo.</action>
  <what-built>
    Dois painéis de prontidão completos e ligados às rotas:
    - Admin: `/admin/dashboard` com grid de CompanyCards (Badge G1-G5 / "Sem avaliação"), filtros por nome/CNPJ/nível e contagem; "Ver detalhes" → `/admin/orgs/:orgId` com seção "Avaliações" (histórico).
    - Construtora: `/form/:orgId/dashboard` com classificação atual, SectionProgress (10 abas: Não iniciado/Em progresso/Completo) e ações "Continuar Avaliação"/"Ver Histórico"; cross-tenant guard.
    Suíte de testes automatizada verde (sectionStatus, useOrgsWithReadiness, CompanyCard, filtros do AdminDashboard, guard do CompanyDashboard).
  </what-built>
  <how-to-verify>
    1. `cd roteiro-unificado && npm run dev` e abrir o app.
    2. Login como admin → `/admin/dashboard`: confirmar grid de cards (não tabela), Badge G1-G5 colorido, orgs sem avaliação com pill cinza "Sem avaliação" e campos "—".
    3. Digitar parte de um nome no busca → grid filtra; selecionar "G2" no nível → apenas orgs G2; selecionar "Sem avaliação" → apenas orgs sem avaliação; conferir contagem "{N} empresa(s) encontrada(s)"; clicar "Limpar filtros" → tudo volta.
    4. Clicar "Ver detalhes →" em um card → `/admin/orgs/:orgId`: layout de org/membros intacto acima + seção "Avaliações" listando versões (ou empty "Nenhuma avaliação registrada").
    5. Login como construtora → acessar `/form/:orgId/dashboard`: ver card de classificação atual, SectionProgress com Identificação/NDA podendo aparecer "Completo" e demais abas "Em progresso"/"Não iniciado"; botões "Continuar Avaliação" (→ formulário) e "Ver Histórico" funcionam.
    6. Como construtora, editar a URL para o orgId de OUTRA org em `/form/:orgIdOutra/dashboard` → deve redirecionar para a própria org.
    7. Responsividade: em ~768px o grid mostra 2 colunas; em mobile, 1 coluna.
  </how-to-verify>
  <verify>
    <human-check>As 7 verificações visuais/funcionais passam no browser conforme how-to-verify.</human-check>
  </verify>
  <resume-signal>Digite "approved" se as 7 verificações passam, ou descreva os problemas encontrados.</resume-signal>
  <done>Usuário confirmou as 7 verificações das duas views (admin grid+filtros+detalhe; construtora dashboard+guard+responsividade).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (orgId)→app | construtora pode trocar :orgId na rota do dashboard |
| rota /admin/*→app | acesso a painel admin restrito a role=admin |
| browser→Supabase | queries de orgs/assessments cruzam ao servidor; RLS isola |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | /form/:orgId/dashboard | mitigate | CompanyDashboard (Plan 09-03) aplica cross-tenant guard; rota fica sob `<ProtectedRoute>` (sessão exigida). Verificado no checkpoint passo 6. |
| T-09-EOP | Elevation of Privilege | /admin/dashboard, /admin/orgs/:orgId | mitigate | Rotas permanecem sob `<AdminRoute>` (role=admin) — este plano NÃO move as rotas admin para fora do AdminRoute. RLS `is_admin()` reforça no servidor. |
| T-09-INJ | Tampering | CompanyCard/AssessmentSection renderizando dados de org | mitigate | Valores (nome, cnpj, readiness) renderizados como texto; nunca `dangerouslySetInnerHTML`. Filtros operam sobre strings, sem eval. |
</threat_model>

<verification>
- `cd roteiro-unificado && npm test -- --run` (suíte completa) verde
- `cd roteiro-unificado && npx tsc --noEmit` sem erros
- `cd roteiro-unificado && npm run build` completa sem erros
- Checkpoint humano: 7 verificações aprovadas
</verification>

<success_criteria>
- AdminDashboard exibe grid de CompanyCards filtrável (DASH-01, DASH-05); paginação removida; CreateOrgModal preservado
- OrgDetail combina dados da org + seção de Avaliações (histórico)
- Rota /form/:orgId/dashboard acessível sob ProtectedRoute (DASH-03)
- Suíte de testes verde, build limpo, checkpoint humano aprovado
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-de-prontid-o/09-04-SUMMARY.md` when done
</output>
</content>
