---
phase: 09-dashboard-de-prontidao
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - roteiro-unificado/src/features/form/SectionProgress.tsx
  - roteiro-unificado/src/features/form/CompanyDashboard.tsx
  - roteiro-unificado/src/features/form/CompanyDashboard.test.tsx
autonomous: true
requirements: [DASH-03, DASH-04]
nyquist_compliant: true

must_haves:
  truths:
    - "SectionProgress mostra 10 cards (1 por aba) com estado Vazio/Em progresso/Completo correto"
    - "Construtora acessando /form/:orgId/dashboard vê sua classificação atual e o SectionProgress"
    - "Construtora tentando acessar dashboard de outra org é redirecionada para a própria"
  artifacts:
    - path: "roteiro-unificado/src/features/form/SectionProgress.tsx"
      provides: "Grid de 10 cards de progresso por aba (consome computeTabStatus)"
      exports: ["SectionProgress"]
    - path: "roteiro-unificado/src/features/form/CompanyDashboard.tsx"
      provides: "Página /form/:orgId/dashboard com cross-tenant guard, classificação e SectionProgress"
      exports: ["CompanyDashboard"]
  key_links:
    - from: "roteiro-unificado/src/features/form/SectionProgress.tsx"
      to: "src/lib/sectionStatus.ts (computeTabStatus, STATUS_TO_COMPLETENESS)"
      via: "import + REQUIRED_COUNT dos 10 schemas"
      pattern: "computeTabStatus"
    - from: "roteiro-unificado/src/features/form/CompanyDashboard.tsx"
      to: "useAuth orgId / Navigate"
      via: "cross-tenant guard orgId !== authOrgId"
      pattern: "Navigate"
---

<objective>
Criar o painel da construtora: `SectionProgress` (grid de 10 cards de progresso por aba, usando `computeTabStatus` do Plan 01) e a página `CompanyDashboard` (`/form/:orgId/dashboard`) com cross-tenant guard, card de classificação atual e ações de navegação.

Purpose: Entrega DASH-04 (indicador de progresso por seção) e DASH-03 (construtora vê seu painel individual). Ambos são componentes folha; o wiring da rota ocorre no Plan 04.
Output: `SectionProgress.tsx`, `CompanyDashboard.tsx` (+ teste do guard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-dashboard-de-prontid-o/09-CONTEXT.md
@.planning/phases/09-dashboard-de-prontid-o/09-RESEARCH.md
@.planning/phases/09-dashboard-de-prontid-o/09-PATTERNS.md
@.planning/phases/09-dashboard-de-prontid-o/09-UI-SPEC.md

<interfaces>
<!-- Contratos verificados — usar diretamente. -->

De Plan 09-01 (src/lib/sectionStatus.ts):
  computeTabStatus(tabKey: string, formData, requiredCount): TabStatus ('empty'|'in-progress'|'complete')
  STATUS_TO_COMPLETENESS: Record<TabStatus, number> (empty=0, in-progress=0.5, complete=1)

TAB_CONFIG (src/features/form/tabConfig.ts): array de { key: TabKey, label: string } — 10 abas em ordem canônica.

REQUIRED_COUNT (src/schemas/*.ts) — todos já exportados:
  IDENTIFICACAO_REQUIRED_COUNT=2, NDA_REQUIRED_COUNT=1, os outros 8 = 0
  (TORRE_DECISAO_, TORRE_SIENGE_, TORRE_ACESSO_, TORRE_CLASSIFICACAO_, HAB_VENDA_, HAB_REPOSITORIOS_, HAB_RESPONSAVEIS_, HAB_CLASSIFICACAO_)

ProgressBadge (src/features/form/ProgressBadge.tsx): prop `completeness: number` — 0=círculo vazio, 0<c<1=clock laranja, 1=check verde. aria-hidden já implementado.

useAuth (src/features/auth): retorna { orgId: string|null, isLoading: boolean, ... }.

Cross-tenant guard (padrão HistoryPage.tsx — replicar exatamente):
  if (authLoading || !orgId || !authOrgId) → <Spinner /> de página inteira (bg-primary, min-h-screen)
  if (orgId !== authOrgId) → <Navigate to={`/form/${authOrgId}/dashboard`} replace />
  senão → <CompanyDashboardContent orgId={orgId} />

useQuery draft (src/features/form/FormLayout.tsx — queryKey reusado):
  queryKey: ['assessment', 'draft', orgId]; select form_data, readiness_level_mgmt, readiness_level_tech, submitted_at, version; .eq status 'draft'; .order version desc; .limit(1).maybeSingle(); staleTime 30_000.

Cast obrigatório de form_data (tipo Json em database.ts):
  const formData = draftQuery.data?.form_data as Record<string, Record<string, unknown>> | null  // NUNCA `as any`

Cópia (09-UI-SPEC): título "Painel de Prontidão", subtítulo "Acompanhe sua classificação e o progresso do formulário", CTA "Continuar Avaliação" → /form/:orgId, "Ver Histórico" → /form/:orgId/history, "← Voltar ao Formulário", empty draft "Nenhuma avaliação enviada ainda".
Status pills SectionProgress: Vazio "Não iniciado" (bg-gray-100 text-gray-600), Em progresso "Em progresso" (bg-orange-100 text-orange-700), Completo "Completo" (bg-green-100 text-green-700).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SectionProgress em src/features/form/SectionProgress.tsx</name>
  <files>roteiro-unificado/src/features/form/SectionProgress.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/ReadinessClassification.tsx (padrão de mapa de config por estado tipo habConfig; useMemo sobre dados do form)
    - roteiro-unificado/src/features/form/ProgressBadge.tsx (prop completeness e os 3 SVGs)
    - roteiro-unificado/src/features/form/tabConfig.ts (TAB_CONFIG: key + label)
    - roteiro-unificado/src/lib/sectionStatus.ts (computeTabStatus, STATUS_TO_COMPLETENESS — criados no Plan 09-01)
    - roteiro-unificado/src/stores/formStore.ts (TabKey enum)
  </read_first>
  <action>
    Criar `src/features/form/SectionProgress.tsx` exportando `SectionProgress` com prop `{ formData: Record<string, Record<string, unknown>> | null }`.

    Importar `TAB_CONFIG` de `./tabConfig`, `ProgressBadge` de `./ProgressBadge`, `{ computeTabStatus, STATUS_TO_COMPLETENESS }` de `@/lib/sectionStatus`, `TabKey` de `@/stores/formStore`, e os 10 `*_REQUIRED_COUNT` de `@/schemas/{identificacao,torre-decisao,torre-sienge,torre-acesso,torre-classificacao,hab-venda,hab-repositorios,hab-responsaveis,hab-classificacao,nda}`.

    Montar `const REQUIRED_COUNTS: Record<TabKey, number>` mapeando cada `TabKey` ao seu `*_REQUIRED_COUNT`. Montar `const STATUS_LABELS` com as 3 entradas: empty={label:'Não iniciado', className:'bg-gray-100 text-gray-600'}, 'in-progress'={label:'Em progresso', className:'bg-orange-100 text-orange-700'}, complete={label:'Completo', className:'bg-green-100 text-green-700'}.

    Render: `<div className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3">` com `TAB_CONFIG.map(({ key, label }) => ...)`. Para cada aba: `status = computeTabStatus(key, formData, REQUIRED_COUNTS[key])`; card `<div className="rounded-lg border border-gray-200 bg-white p-4">` contendo linha com `<ProgressBadge completeness={STATUS_TO_COMPLETENESS[status]} />` + label (`text-sm font-medium text-gray-900`) e pill de status (`mt-2 inline-block rounded-full px-2 py-0.5 text-xs ${STATUS_LABELS[status].className}`) com `STATUS_LABELS[status].label`.

    Nota (RESEARCH §REQUIRED_COUNT): apenas Identificação e NDA podem atingir 'complete'; as outras 8 abas oscilam entre vazio e em progresso — isso é esperado, não corrigir. Cores via tokens.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -i "SectionProgress" | grep -v '^#' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/form/SectionProgress.tsx` contém `export function SectionProgress`
    - `src/features/form/SectionProgress.tsx` contém `computeTabStatus(` e `STATUS_TO_COMPLETENESS`
    - `src/features/form/SectionProgress.tsx` contém os 10 imports `_REQUIRED_COUNT`
    - `src/features/form/SectionProgress.tsx` contém os textos `Não iniciado`, `Em progresso`, `Completo`
    - `src/features/form/SectionProgress.tsx` contém `TAB_CONFIG.map(`
    - Comando de verify retorna `0` (zero erros tsc relativos a SectionProgress)
  </acceptance_criteria>
  <done>SectionProgress renderiza 10 cards de progresso por aba consumindo computeTabStatus + ProgressBadge, com pill de estado correto; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar CompanyDashboard (página + cross-tenant guard) em src/features/form/CompanyDashboard.tsx</name>
  <files>roteiro-unificado/src/features/form/CompanyDashboard.tsx, roteiro-unificado/src/features/form/CompanyDashboard.test.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/HistoryPage.tsx (cross-tenant guard exato, imports, useQuery, Skeleton, botão voltar com navigate)
    - roteiro-unificado/src/features/form/SectionProgress.tsx (criado na Task 1 — prop formData)
    - roteiro-unificado/src/components/ui/badge.tsx (Badge grade para classificação atual)
    - roteiro-unificado/src/features/auth/AuthProvider.tsx (useAuth retorna orgId, isLoading)
    - roteiro-unificado/src/features/form/useSubmitAssessment.test.ts (mock react-router-dom Navigate/useParams, vi.mock supabase)
  </read_first>
  <behavior>
    - CompanyDashboard com authLoading=true renderiza Spinner de página (não renderiza conteúdo)
    - CompanyDashboard com orgId (da URL) !== authOrgId renderiza <Navigate> para /form/{authOrgId}/dashboard
    - CompanyDashboard com orgId === authOrgId renderiza o conteúdo (CompanyDashboardContent)
  </behavior>
  <action>
    Criar `src/features/form/CompanyDashboard.tsx` exportando `CompanyDashboard`. Estrutura em dois componentes: o wrapper `CompanyDashboard` (guard) e `CompanyDashboardContent` (conteúdo).

    `CompanyDashboard`: `const { orgId } = useParams<{ orgId: string }>()`; `const { orgId: authOrgId, isLoading: authLoading } = useAuth()`. Cross-tenant guard idêntico ao HistoryPage: se `authLoading || !orgId || !authOrgId` → `<div className="bg-primary flex min-h-screen items-center justify-center"><Spinner size="lg" className="border-white border-t-transparent" /></div>`; se `orgId !== authOrgId` → `<Navigate to={\`/form/${authOrgId}/dashboard\`} replace />`; senão → `<CompanyDashboardContent orgId={orgId} />`.

    `CompanyDashboardContent({ orgId })`: useQuery draft com `queryKey: ['assessment', 'draft', orgId]` (mesmo do FormLayout — cache reusado), select `form_data, readiness_level_mgmt, readiness_level_tech, submitted_at, version`, `.eq('org_id', orgId).eq('status', 'draft').order('version', { ascending: false }).limit(1).maybeSingle()`, staleTime 30_000, enabled !!orgId. Cast: `const formData = draftQuery.data?.form_data as Record<string, Record<string, unknown>> | null`.

    Layout conforme 09-UI-SPEC §CompanyDashboard: container `mx-auto max-w-4xl px-4 py-8`; botão "← Voltar ao Formulário" (`variant="ghost" size="sm"`, `onClick={() => navigate(\`/form/${orgId}\`)}`, `mb-4`); h1 "Painel de Prontidão" (`text-xl font-semibold text-gray-900`); subtítulo "Acompanhe sua classificação e o progresso do formulário" (`text-sm text-gray-500 mt-1`); Card de classificação atual com Badge G1-G5 grande quando `readiness_level_mgmt` presente (senão pill "Sem avaliação") + "Última avaliação enviada: {data pt-BR de submitted_at}" ou "Nenhuma avaliação enviada" + nível técnico; `<SectionProgress formData={formData} />` (`mt-8`); footer de ações (`mt-8 flex gap-3`) com Button "Continuar Avaliação" (`variant="primary"`, navigate `/form/${orgId}`) e Button "Ver Histórico" (`variant="secondary"`, navigate `/form/${orgId}/history`).

    Loading do draft → Skeleton cards (padrão HistoryPage). Erro → `useToast().error('Erro ao carregar dados. Tente recarregar a página.')`. Empty draft (data null) → SectionProgress recebe formData=null (mostra tudo "Não iniciado") e card de classificação mostra "Nenhuma avaliação enviada ainda".

    Criar `CompanyDashboard.test.tsx`: mockar react-router-dom (`useParams`, `Navigate` como `<div data-testid="navigate" data-to={to} />`), mockar `@/features/auth/useAuth` (ou AuthProvider), mockar supabase. Cobrir os 3 casos do bloco behavior (authLoading → Spinner; orgId divergente → Navigate com data-to correto; orgId igual → conteúdo). Para o teste, focar no guard — o conteúdo pode ser verificado pela ausência do testid navigate.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run src/features/form/CompanyDashboard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/form/CompanyDashboard.tsx` contém `export function CompanyDashboard`
    - `src/features/form/CompanyDashboard.tsx` contém `orgId !== authOrgId` e `<Navigate`
    - `src/features/form/CompanyDashboard.tsx` contém `queryKey: ['assessment', 'draft', orgId]`
    - `src/features/form/CompanyDashboard.tsx` contém `<SectionProgress formData={`
    - `src/features/form/CompanyDashboard.tsx` contém `as Record<string, Record<string, unknown>> | null` (cast tipado, não `as any`)
    - `src/features/form/CompanyDashboard.tsx` contém os textos `Continuar Avaliação` e `Ver Histórico`
    - `npm test -- --run src/features/form/CompanyDashboard.test.tsx` sai com código 0 cobrindo os 3 casos behavior
  </acceptance_criteria>
  <done>CompanyDashboard aplica o cross-tenant guard (redireciona orgId divergente), busca o draft, exibe classificação atual + SectionProgress + ações de navegação; teste do guard verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (orgId param)→app | construtora pode manipular :orgId na URL para tentar ver outra org |
| browser→Supabase | query do draft cruza para o servidor; RLS isola |
| form_data JSONB→DOM | conteúdo do formulário consumido por SectionProgress como dados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | CompanyDashboard rota /form/:orgId/dashboard | mitigate | Cross-tenant guard `orgId !== authOrgId → <Navigate to /form/${authOrgId}/dashboard replace />` (idêntico ao HistoryPage). Teste automatizado cobre o redirect. |
| T-09-02 | Information Disclosure | useQuery draft por org_id | accept (mitigado por RLS) | `assessments_select` restringe company a is_org_member(org_id); query de outra org retorna 0 rows. Guard é defesa em profundidade na UI. |
| T-09-INJ | Tampering | form_data renderizado por SectionProgress | mitigate | computeTabStatus trata JSONB como dados (contagem/typeof); pills exibem strings fixas; nunca `dangerouslySetInnerHTML`. |
</threat_model>

<verification>
- `cd roteiro-unificado && npm test -- --run src/features/form/CompanyDashboard.test.tsx` verde
- `cd roteiro-unificado && npx tsc --noEmit` sem erros nos arquivos novos
- Nenhum uso de `as any` em CompanyDashboard; cast tipado de form_data presente
</verification>

<success_criteria>
- SectionProgress mostra 10 cards de progresso com estados corretos (DASH-04); apenas Identificação/NDA podem atingir "Completo"
- CompanyDashboard entrega o painel da construtora com guard de isolamento (DASH-03), classificação atual e navegação
- Teste do guard verde; tsc limpo
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-de-prontid-o/09-03-SUMMARY.md` when done
</output>
