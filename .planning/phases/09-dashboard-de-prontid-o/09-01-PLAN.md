---
phase: 09-dashboard-de-prontidao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/lib/sectionStatus.ts
  - roteiro-unificado/src/lib/sectionStatus.test.ts
  - roteiro-unificado/src/features/admin/useOrgsWithReadiness.ts
  - roteiro-unificado/src/features/admin/useOrgsWithReadiness.test.ts
autonomous: true
requirements: [DASH-01, DASH-04]
nyquist_compliant: true

must_haves:
  truths:
    - "D-07: computeTabStatus retorna 'empty'/'in-progress'/'complete' corretamente por aba usando REQUIRED_COUNT dos schemas"
    - "D-03: useOrgsWithReadiness retorna todas as orgs ativas, com latestAssessment derivada apenas da avaliação status='submitted' de maior version, e null quando não há avaliação submetida"
  artifacts:
    - path: "roteiro-unificado/src/lib/sectionStatus.ts"
      provides: "Função pura computeTabStatus + STATUS_TO_COMPLETENESS + tipo TabStatus"
      exports: ["computeTabStatus", "STATUS_TO_COMPLETENESS", "TabStatus"]
    - path: "roteiro-unificado/src/features/admin/useOrgsWithReadiness.ts"
      provides: "Hook TanStack Query com JOIN orgs+assessments e tipo OrgWithReadiness"
      exports: ["useOrgsWithReadiness", "OrgWithReadiness"]
  key_links:
    - from: "roteiro-unificado/src/lib/sectionStatus.ts"
      to: "src/stores/formStore.ts (TabKey)"
      via: "import TabKey; chaves com hífen"
      pattern: "TabKey"
    - from: "roteiro-unificado/src/features/admin/useOrgsWithReadiness.ts"
      to: "supabase orgs!left(assessments)"
      via: "PostgREST select com status na relação; filtro submitted no cliente"
      pattern: "assessments!left"
---

<objective>
Criar as duas unidades de lógica pura/testável que sustentam os dois painéis: a função `computeTabStatus` (estado de cada aba do formulário a partir do JSONB `form_data`) e o hook `useOrgsWithReadiness` (lista de orgs com a última avaliação submetida via JOIN PostgREST).

Purpose: Estes são os contratos consumidos pelos componentes apresentacionais das Waves seguintes (SectionProgress consome `computeTabStatus`; CompanyCard e AdminDashboard consomem `OrgWithReadiness`). Isolá-los em arquivos próprios garante testabilidade sem montar componentes (DASH-01, DASH-04).
Output: `src/lib/sectionStatus.ts` (+ teste) e `src/features/admin/useOrgsWithReadiness.ts` (+ teste).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-de-prontid-o/09-CONTEXT.md
@.planning/phases/09-dashboard-de-prontid-o/09-RESEARCH.md
@.planning/phases/09-dashboard-de-prontid-o/09-PATTERNS.md

<interfaces>
<!-- Contratos verificados no codebase — usar diretamente, sem explorar. -->

TabKey enum (src/stores/formStore.ts) — valores com hífen:
  Identificacao='identificacao', TorreDecisao='torre-decisao', TorreSienge='torre-sienge',
  TorreAcesso='torre-acesso', TorreClassificacao='torre-classificacao', HabVenda='hab-venda',
  HabRepositorios='hab-repositorios', HabResponsaveis='hab-responsaveis',
  HabClassificacao='hab-classificacao', Nda='nda'

REQUIRED_COUNT verificados nos schemas: Identificacao=2, Nda=1, todos os outros 8 = 0.

Analog do hook (src/features/admin/useOrgs.ts) — padrão de normalização:
  useQuery<T[]>({ queryKey, queryFn: async () => { const {data,error}=await supabase.from('orgs').select(...); if(error) throw error; return ((data ?? []) as unknown as RawRow[]).map(...) } })

Analog de teste de função pura: src/lib/readiness.test.ts (sem mocks).
Analog de teste de hook: src/features/form/useSubmitAssessment.test.ts (QueryClientProvider wrapper + vi.mock supabase).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar função pura computeTabStatus em src/lib/sectionStatus.ts</name>
  <files>roteiro-unificado/src/lib/sectionStatus.ts, roteiro-unificado/src/lib/sectionStatus.test.ts</files>
  <read_first>
    - roteiro-unificado/src/lib/readiness.ts (padrão de função pura exportável que recebe sectionData como parâmetro)
    - roteiro-unificado/src/lib/readiness.test.ts (padrão de teste de função pura sem mock — vitest describe/it/expect)
    - roteiro-unificado/src/stores/formStore.ts (valores do enum TabKey com hífen)
  </read_first>
  <behavior>
    - computeTabStatus('identificacao', null, 2) === 'empty'
    - computeTabStatus('identificacao', {}, 2) === 'empty'
    - computeTabStatus('identificacao', { identificacao: {} }, 2) === 'empty' (objeto da aba vazio)
    - computeTabStatus('torre-decisao', { 'torre-decisao': { campo: 'valor' } }, 0) === 'in-progress' (REQUIRED_COUNT=0 nunca é 'complete')
    - computeTabStatus('identificacao', { identificacao: { empresa: 'X', cnpj: '00.000.000/0001-00' } }, 2) === 'complete' (≥2 campos preenchidos)
    - computeTabStatus('nda', { nda: { nome: 'X' } }, 1) === 'complete' (1 campo, REQUIRED_COUNT=1)
    - computeTabStatus('nda', { nda: { extra: '' } }, 1) === 'in-progress' (1 chave mas valor vazio → filledCount=0 < 1)
    - Usa chave com hífen 'torre-decisao' (não camelCase) para acessar form_data
    - STATUS_TO_COMPLETENESS mapeia empty→0, in-progress→0.5, complete→1
  </behavior>
  <action>
    Criar `src/lib/sectionStatus.ts` exportando: tipo `TabStatus = 'empty' | 'in-progress' | 'complete'`; função pura `computeTabStatus(tabKey: string, formData: Record<string, unknown> | null | undefined, requiredCount: number): TabStatus`; constante `STATUS_TO_COMPLETENESS: Record<TabStatus, number>` com valores empty=0, 'in-progress'=0.5, complete=1.

    Lógica de computeTabStatus, NA ORDEM: (1) ler `tabData = formData?.[tabKey]`; (2) se `!tabData || typeof tabData !== 'object' || Object.keys(tabData).length === 0` retornar 'empty'; (3) se `requiredCount === 0` retornar 'in-progress' (verificação explícita ANTES de comparar filledCount — `0 >= 0` seria sempre true e marcaria abas vazias-de-obrigatórios como completas); (4) contar `filledCount` = valores de tabData que não são null, undefined, '', nem array vazio; (5) retornar `filledCount >= requiredCount ? 'complete' : 'in-progress'`.

    Sem React, sem useFormStore, sem useQuery — função pura espelhando o contrato de `calculateReadiness` em readiness.ts. Criar `sectionStatus.test.ts` cobrindo os casos do bloco behavior, seguindo o padrão sem-mock de readiness.test.ts.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run src/lib/sectionStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/sectionStatus.ts` contém `export function computeTabStatus(`
    - `src/lib/sectionStatus.ts` contém `export const STATUS_TO_COMPLETENESS`
    - `src/lib/sectionStatus.ts` contém `export type TabStatus`
    - `src/lib/sectionStatus.ts` contém verificação explícita `requiredCount === 0` retornando 'in-progress' antes de avaliar filledCount
    - `npm test -- --run src/lib/sectionStatus.test.ts` sai com código 0 e cobre os 9 casos do bloco behavior
  </acceptance_criteria>
  <done>computeTabStatus implementada como função pura com os 3 estados corretos; REQUIRED_COUNT=0 nunca produz 'complete'; teste verde com todos os casos behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar hook useOrgsWithReadiness em src/features/admin/useOrgsWithReadiness.ts</name>
  <files>roteiro-unificado/src/features/admin/useOrgsWithReadiness.ts, roteiro-unificado/src/features/admin/useOrgsWithReadiness.test.ts</files>
  <read_first>
    - roteiro-unificado/src/features/admin/useOrgs.ts (padrão de hook PostgREST: queryKey, queryFn, normalização com `(data ?? []) as unknown as RawRow[]`, .map)
    - roteiro-unificado/src/features/form/useSubmitAssessment.test.ts (padrão de mock supabase + makeWrapper com QueryClientProvider)
    - roteiro-unificado/src/types/database.ts (colunas de assessments: readiness_level_mgmt, readiness_level_tech, submitted_at, version, status)
  </read_first>
  <behavior>
    - useOrgsWithReadiness retorna org com latestAssessment=null quando a org não tem assessment com status='submitted'
    - useOrgsWithReadiness retorna o assessment de maior version quando há múltiplos submitted (ordena desc por version e pega o primeiro)
    - useOrgsWithReadiness NÃO inclui assessment com status='draft' em latestAssessment (filtra apenas submitted no cliente)
    - latestAssessment preserva readiness_level_mgmt, readiness_level_tech, submitted_at, version, id (sem o campo status)
  </behavior>
  <action>
    Criar `src/features/admin/useOrgsWithReadiness.ts` exportando o tipo `OrgWithReadiness = { id: string; name: string; cnpj: string | null; active: boolean; latestAssessment: { id: string; readiness_level_mgmt: string | null; readiness_level_tech: string | null; submitted_at: string | null; version: number } | null }` e a função `useOrgsWithReadiness()` que retorna `useQuery<OrgWithReadiness[]>`.

    Query: `supabase.from('orgs').select('id, name, cnpj, active, assessments!left(id, readiness_level_mgmt, readiness_level_tech, submitted_at, version, status)').eq('active', true).order('name', { ascending: true })`. queryKey `['orgs', 'with-readiness']`, `staleTime: 60_000`.

    CRÍTICO (Armadilha 1 do RESEARCH): NÃO usar `.eq('assessments.status', 'submitted')` na query — isso converte o LEFT JOIN em INNER JOIN e exclui orgs sem avaliação (viola D-04). Incluir `status` no select da relação e filtrar no cliente: dentro do `.map()`, pegar `row.assessments` (array), `.filter(a => a.status === 'submitted')`, `.sort((a,b) => b.version - a.version)`, e usar `submitted[0]` (ou null) para montar `latestAssessment` — omitindo o campo `status` do objeto final.

    Seguir o padrão de normalização de useOrgs.ts (`(data ?? []) as unknown as RawRow[]`). Criar `useOrgsWithReadiness.test.ts` com `vi.mock('@/lib/supabase', ...)` e `makeWrapper()` (QueryClientProvider com `queries: { retry: false }`), cobrindo os 4 casos do bloco behavior.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run src/features/admin/useOrgsWithReadiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/admin/useOrgsWithReadiness.ts` contém `export function useOrgsWithReadiness(`
    - `src/features/admin/useOrgsWithReadiness.ts` contém `export type OrgWithReadiness` (ou `export interface OrgWithReadiness`)
    - `src/features/admin/useOrgsWithReadiness.ts` contém `assessments!left(` no select
    - `src/features/admin/useOrgsWithReadiness.ts` NÃO contém `.eq('assessments.status'` (filtro de submitted é feito no cliente)
    - `src/features/admin/useOrgsWithReadiness.ts` contém `staleTime: 60_000`
    - `npm test -- --run src/features/admin/useOrgsWithReadiness.test.ts` sai com código 0 cobrindo os 4 casos behavior
  </acceptance_criteria>
  <done>Hook retorna orgs ativas com latestAssessment derivada apenas de submitted (maior version), null quando ausente; orgs sem avaliação preservadas; teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Supabase (PostgREST) | Query de orgs+assessments cruza para o servidor; RLS aplica isolamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-IDOR | Information Disclosure | useOrgsWithReadiness query | accept (mitigado por RLS) | RLS `orgs_select`/`assessments_select` já restringe: admin vê tudo, company vê só is_org_member. Hook não altera políticas. Sem novo vetor. |
| T-09-INJ | Tampering | form_data JSONB consumido por computeTabStatus | mitigate | computeTabStatus trata valores apenas como dados (typeof/Object.keys/contagem) — nunca executa nem renderiza JSONB. Saída é enum de string fixo. |
</threat_model>

<verification>
- `cd roteiro-unificado && npm test -- --run src/lib/sectionStatus.test.ts src/features/admin/useOrgsWithReadiness.test.ts` verde
- `cd roteiro-unificado && npx tsc --noEmit` sem erros nos dois arquivos novos
</verification>

<success_criteria>
- computeTabStatus retorna os 3 estados corretamente, com REQUIRED_COUNT=0 nunca igual a 'complete'
- useOrgsWithReadiness retorna todas as orgs ativas, latestAssessment apenas de submitted (maior version), null quando ausente, sem quebrar para orgs sem avaliação
- Ambos os testes verdes; tipos OrgWithReadiness, TabStatus, STATUS_TO_COMPLETENESS exportados para consumo das Waves 2-3
</success_criteria>

<output>
Create `.planning/phases/09-dashboard-de-prontid-o/09-01-SUMMARY.md` when done
</output>
</content>
</invoke>
