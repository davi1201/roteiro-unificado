---
phase: 05-shell-do-formul-rio-navega-o-por-abas
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - roteiro-unificado/src/features/form/useFormSection.ts
  - roteiro-unificado/src/features/form/TabNavigation.tsx
  - roteiro-unificado/src/features/form/ProgressBar.tsx
autonomous: true
requirements: [FORM-04, UX-03]
tags: [hook, sidebar, stepper, progress-bar, responsive]

must_haves:
  truths:
    - "useFormSection(tenantId, tab) retorna { data, updateField, errors, completeness } com tipos corretos"
    - "completeness é 0 se tab não está em visitedTabs; 0.01 se está visitada"
    - "updateField(field, value) chama updateSection(tab, { ...data, [field]: value }) no store"
    - "TabNavigation renderiza 10 itens de TAB_CONFIG com ProgressBadge por aba"
    - "Item ativo da sidebar usa classes bg-primary-800 text-white font-semibold; inativo usa text-primary-100 hover:bg-primary-800"
    - "Click em uma aba chama setActiveTab(tab), markTabVisited(tab) e atualiza window.location.hash"
    - "ProgressBar renderiza faixa h-1 sticky no topo com largura proporcional à média de completeness das 10 abas"
    - "Layout mobile (< md): TabNavigation vira flex-row com overflow-x-auto e pills; cada pill tem aria-label com o label completo da aba"
  artifacts:
    - path: "roteiro-unificado/src/features/form/useFormSection.ts"
      provides: "Hook que abstrai acesso à data/updateField/errors/completeness de uma aba"
      exports: ["useFormSection"]
    - path: "roteiro-unificado/src/features/form/TabNavigation.tsx"
      provides: "Stepper vertical (desktop) ou pills horizontais scrolláveis (mobile) com as 10 abas"
      exports: ["TabNavigation"]
    - path: "roteiro-unificado/src/features/form/ProgressBar.tsx"
      provides: "Faixa h-1 sticky no topo com largura calculada via completeness agregado"
      exports: ["ProgressBar"]
  key_links:
    - from: "roteiro-unificado/src/features/form/useFormSection.ts"
      to: "useFormStore(tenantId)"
      via: "import e chamada do hook do tenant"
      pattern: "useFormStore"
    - from: "roteiro-unificado/src/features/form/TabNavigation.tsx"
      to: "ProgressBadge"
      via: "import default/named para renderizar ícone por aba"
      pattern: "import.*ProgressBadge.*from"
    - from: "roteiro-unificado/src/features/form/TabNavigation.tsx"
      to: "TAB_CONFIG"
      via: "iteração TAB_CONFIG.map((tab) => ...)"
      pattern: "TAB_CONFIG\\.map"
    - from: "roteiro-unificado/src/features/form/TabNavigation.tsx"
      to: "window.location.hash"
      via: "click handler escreve hash da aba clicada"
      pattern: "window\\.location\\.hash"
---

<objective>
Construir a camada de hook + componentes presentacionais que consomem o estado expandido
da formStore (Plan 01) e renderizam o stepper de navegação e a barra de progresso:
- `useFormSection.ts`: hook que retorna `{ data, updateField, errors, completeness }` por aba (D-10)
- `TabNavigation.tsx`: stepper vertical (desktop) + pills horizontais (mobile) com 10 abas e ProgressBadge por item
- `ProgressBar.tsx`: faixa `h-1` sticky no topo, largura proporcional à média de completeness das 10 abas

Purpose: Plan 04 montará o `FormLayout` que compõe TabNavigation + ProgressBar + área de
conteúdo + botão Sair. Sem este plano, Plan 04 não tem peças visuais para compor.

Output: 3 arquivos novos em `src/features/form/`; nenhuma alteração em router/store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-RESEARCH.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-UI-SPEC.md

@roteiro-unificado/src/stores/formStore.ts
@roteiro-unificado/src/features/form/tabConfig.ts
@roteiro-unificado/src/features/form/ProgressBadge.tsx
@roteiro-unificado/src/components/layouts/AdminSidebar.tsx
@roteiro-unificado/src/lib/utils.ts

<interfaces>
<!-- Disponível após Plans 01 e 02 -->

From src/stores/formStore.ts (after Plan 01):
```typescript
export enum TabKey {
  Identificacao      = 'identificacao',
  TorreDecisao       = 'torre-decisao',
  TorreSienge        = 'torre-sienge',
  TorreAcesso        = 'torre-acesso',
  TorreClassificacao = 'torre-classificacao',
  HabVenda           = 'hab-venda',
  HabRepositorios    = 'hab-repositorios',
  HabResponsaveis    = 'hab-responsaveis',
  HabClassificacao   = 'hab-classificacao',
  Nda                = 'nda',
}
// Store state inclui:
//   activeTab: TabKey
//   visitedTabs: Set<TabKey>
//   sectionData: Partial<Record<TabKey, Record<string, unknown>>>
// Actions:
//   setActiveTab(tab), markTabVisited(tab), updateSection(tab, data), resetForm()
export function useFormStore(tenantId: string): FormStore
```

From src/features/form/tabConfig.ts:
```typescript
export interface TabConfig { key: TabKey; label: string }
export const TAB_CONFIG: TabConfig[]   // 10 itens, ordem fixa
```

From src/features/form/ProgressBadge.tsx:
```typescript
export function ProgressBadge({ completeness, className }: { completeness: number; className?: string }): JSX.Element
```

Helpers:
- `cn` de '@/lib/utils' (twMerge + clsx)
- NavLink, useLocation, useNavigate de 'react-router-dom' v7

Padrão de classes ativo/inativo da sidebar (D-06, UI-SPEC §States Per Component):
- Inativo / não visitado: text-primary-100 hover:bg-primary-800 hover:text-white
- Ativo:                  bg-primary-800 text-white font-semibold
- Item layout:            flex items-center gap-3 rounded-md px-3 h-10 text-sm
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar useFormSection.ts (hook de acesso por aba)</name>
  <files>
    roteiro-unificado/src/features/form/useFormSection.ts
  </files>
  <read_first>
    roteiro-unificado/src/stores/formStore.ts (após Plan 01 — TabKey, sectionData, visitedTabs, updateSection, useFormStore),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md (D-09 e D-10 — proxy de completeness por aba visitada),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-RESEARCH.md (linhas 396-417 — exemplo de assinatura do hook)
  </read_first>
  <action>
    Criar `src/features/form/useFormSection.ts` como hook de abstração que retorna
    o slice da aba pedida, junto com helpers de update e completeness.

    **Imports:**
    - `import { useFormStore, TabKey } from '@/stores/formStore'`

    **Assinatura:**
    ```
    export function useFormSection(tenantId: string, tab: TabKey): {
      data: Record<string, unknown>
      updateField: (field: string, value: unknown) => void
      errors: Record<string, string>
      completeness: number
    }
    ```

    **Implementação:**
    1. `const store = useFormStore(tenantId)` — assina toda a store. (Aceita re-renders por enquanto. Phase 6+ pode otimizar com `useStore(createFormStore(tenantId), selector)`.)
    2. `const data = store.sectionData[tab] ?? {}` — slice da aba; objeto vazio se ainda não foi tocada.
    3. `const updateField = (field: string, value: unknown) => store.updateSection(tab, { ...data, [field]: value })` — wrapper que mescla campo no objeto da aba.
    4. `const errors: Record<string, string> = {}` — Phase 5 não tem Zod schema; sempre vazio.
    5. `const completeness = store.visitedTabs.has(tab) ? 0.01 : 0` — proxy por "aba visitada" per D-09 (Phase 6+ sobrepõe com cálculo Zod real).
    6. Retornar `{ data, updateField, errors, completeness }`.

    **Restrições:**
    - NÃO assinar selector específico do Zustand neste plano — `useFormStore(tenantId)` retorna o snapshot completo (padrão atual). Otimização vem depois.
    - NÃO usar `useMemo` para `updateField` neste plano (Phase 6+ pode adicionar quando profiling mostrar necessidade).
    - Tipos estritos — `Record<string, unknown>` para data, `Record<string, string>` para errors.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; grep -c "useFormStore\|visitedTabs.has\|updateSection" src/features/form/useFormSection.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/useFormSection.ts` exists and exports named function `useFormSection`
    - `useFormSection.ts` imports `useFormStore` and `TabKey` from `@/stores/formStore`
    - `useFormSection.ts` signature accepts `tenantId: string` and `tab: TabKey` parameters (in that order)
    - `useFormSection.ts` return shape includes the literal identifiers `data`, `updateField`, `errors`, `completeness`
    - `useFormSection.ts` contains the literal `visitedTabs.has(tab)` (proxy completeness logic per D-09)
    - `useFormSection.ts` contains the literal `0.01` (visited-but-empty sentinel value)
    - `useFormSection.ts` calls `store.updateSection(tab, ...)` (or equivalent destructure) — `grep -c "updateSection" src/features/form/useFormSection.ts` returns at least 1
    - `npm run type-check` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>useFormSection.ts criado retornando data/updateField/errors/completeness conforme D-10; type-check passa.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar TabNavigation.tsx (stepper desktop + pills mobile)</name>
  <files>
    roteiro-unificado/src/features/form/TabNavigation.tsx
  </files>
  <read_first>
    roteiro-unificado/src/features/form/tabConfig.ts (TAB_CONFIG e TabConfig — fonte única dos itens),
    roteiro-unificado/src/features/form/ProgressBadge.tsx (assinatura: { completeness, className? }),
    roteiro-unificado/src/stores/formStore.ts (após Plan 01 — useFormStore, TabKey, activeTab, visitedTabs, setActiveTab, markTabVisited),
    roteiro-unificado/src/components/layouts/AdminSidebar.tsx (padrão de classes ativo/inativo no stepper — h-10, gap-3, bg-primary-800, text-primary-100, font-semibold),
    roteiro-unificado/src/lib/utils.ts (cn helper),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md (D-06 stepper vertical, D-09 completeness sentinel, specifics — pills mobile sem label completo),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-UI-SPEC.md (linhas 130-191 — copy, layout desktop vs mobile; linhas 226-241 — estados visuais de item e Badge)
  </read_first>
  <action>
    Criar `src/features/form/TabNavigation.tsx` — componente que consome a store
    e renderiza as 10 abas como stepper vertical (desktop, `md:` breakpoint) ou
    pills horizontais scrolláveis (mobile, `<md`). NÃO é responsável pela sidebar
    completa — é apenas a parte navegacional (FormLayout no Plan 04 envolve com
    a sidebar bg-primary e o botão Sair).

    **Imports:**
    - `import { useFormStore, TabKey } from '@/stores/formStore'`
    - `import { TAB_CONFIG } from './tabConfig'`
    - `import { ProgressBadge } from './ProgressBadge'`
    - `import { cn } from '@/lib/utils'`

    **Props:**
    ```
    interface TabNavigationProps {
      tenantId: string
    }
    export function TabNavigation({ tenantId }: TabNavigationProps): JSX.Element
    ```

    **Implementação:**

    1. `const store = useFormStore(tenantId)` — lê `activeTab`, `visitedTabs`.

    2. `handleSelect(tab: TabKey)` interna:
       - Guard antes de escrever no hash para evitar loop (RESEARCH armadilha 2):
         `if (window.location.hash !== '#' + tab) { window.location.hash = tab }`
       - `store.setActiveTab(tab)`
       - `store.markTabVisited(tab)`

    3. `completenessFor(tab: TabKey): number` interna — retorna `store.visitedTabs.has(tab) ? 0.01 : 0` (mesma lógica de useFormSection; não precisa importar o hook).

    4. **Container raiz responsivo:** `<nav aria-label="Navegação do formulário" className="flex flex-col gap-1 md:flex-col flex-row overflow-x-auto md:overflow-x-visible">`.
       Ajustar para que em mobile (`<md`) seja `flex-row overflow-x-auto` e em desktop (`md:`) seja `flex-col`. Tailwind aceita override por breakpoint — usar `"flex gap-1 overflow-x-auto md:flex-col md:overflow-x-visible flex-row"` (sem `flex-col` no base; em mobile padrão é flex-row + overflow-x-auto, em md+ vira coluna sem scroll horizontal).

    5. **Mapear TAB_CONFIG** (10 itens) com `TAB_CONFIG.map((tab) => { ... })`:
       - Calcular `const isActive = store.activeTab === tab.key`.
       - Calcular `const completeness = completenessFor(tab.key)`.
       - Renderizar um `<button type="button" key={tab.key} onClick={() => handleSelect(tab.key)} aria-current={isActive ? 'page' : undefined} aria-label={tab.label} className={cn(...)}>`:
         - Base: `flex items-center gap-3 rounded-md px-3 h-10 text-sm shrink-0 whitespace-nowrap text-left`
         - Inativo: `text-primary-100 hover:bg-primary-800 hover:text-white`
         - Ativo: `bg-primary-800 text-white font-semibold`
         - Use `cn('base classes', isActive ? 'active classes' : 'inactive classes')`.
       - Conteúdo do botão:
         - `<ProgressBadge completeness={completeness} />`
         - `<span className="hidden md:inline">{tab.label}</span>` (label escondido em mobile per specifics — `aria-label` no `<button>` mantém acessibilidade)

    **Restrições críticas:**
    - Use `<button type="button">` — NÃO `<NavLink>` (RESEARCH antipattern: `<Link to="#aba">` causa scroll-to-anchor, não navegação de tabs).
    - Cores APENAS via tokens (`text-primary-100`, `bg-primary-800`, `text-white`). Sem hex.
    - Guard do hash é obrigatório (`if (window.location.hash !== '#' + tab)`) — sem ele, o useEffect em FormLayout (Plan 04) entra em loop.
    - `shrink-0 whitespace-nowrap` em cada item para evitar wrap nas pills mobile.
    - NÃO renderizar o botão Sair — fica no FormLayout (Plan 04, D-07).
    - NÃO renderizar a ProgressBar — fica no FormLayout (Plan 04).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; grep -c "TAB_CONFIG\.map\|setActiveTab\|markTabVisited\|window\.location\.hash" src/features/form/TabNavigation.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/TabNavigation.tsx` exists and exports named function `TabNavigation`
    - `TabNavigation.tsx` imports `TAB_CONFIG` from `./tabConfig` and `ProgressBadge` from `./ProgressBadge`
    - `TabNavigation.tsx` imports `useFormStore` and `TabKey` from `@/stores/formStore`
    - `TabNavigation.tsx` accepts a `tenantId: string` prop
    - `TabNavigation.tsx` contains `TAB_CONFIG.map(` (iterates 10 entries)
    - `TabNavigation.tsx` calls `setActiveTab` and `markTabVisited` (both action names appear as identifiers): `grep -c "setActiveTab\|markTabVisited" src/features/form/TabNavigation.tsx` returns at least 2
    - `TabNavigation.tsx` contains the literal `window.location.hash` (hash sync write)
    - `TabNavigation.tsx` contains a guard against hash loop: `grep -c "window.location.hash !==" src/features/form/TabNavigation.tsx` returns at least 1
    - `TabNavigation.tsx` contains `bg-primary-800` and `text-primary-100` literals (active/inactive states per UI-SPEC)
    - `TabNavigation.tsx` uses `<button` tags (not `<NavLink` for tab items): `grep -c "<button" src/features/form/TabNavigation.tsx` returns at least 1; `grep -c "<NavLink" src/features/form/TabNavigation.tsx` returns 0
    - `TabNavigation.tsx` renders `<ProgressBadge` per item: `grep -c "<ProgressBadge" src/features/form/TabNavigation.tsx` returns at least 1
    - `TabNavigation.tsx` contains `aria-label` attribute for accessibility on each tab button
    - `TabNavigation.tsx` contains the responsive class pair: `hidden md:inline` (label hidden on mobile, shown on desktop)
    - `TabNavigation.tsx` does NOT contain hex color literals: `grep -E "#[0-9a-fA-F]{3,8}\b" src/features/form/TabNavigation.tsx` returns no matches
    - `npm run type-check` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>TabNavigation.tsx renderiza 10 itens via TAB_CONFIG, dispara setActiveTab + markTabVisited + window.location.hash com guard anti-loop, sem hex hardcoded; type-check passa.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar ProgressBar.tsx (faixa sticky h-1 no topo)</name>
  <files>
    roteiro-unificado/src/features/form/ProgressBar.tsx
  </files>
  <read_first>
    roteiro-unificado/src/stores/formStore.ts (após Plan 01 — TabKey, useFormStore, visitedTabs),
    roteiro-unificado/src/features/form/tabConfig.ts (TAB_CONFIG.length === 10 — base do denominador),
    roteiro-unificado/src/lib/utils.ts (cn helper),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md (D-05 — barra h-1 sticky topo, bg-primary preenchida, sem percentual numérico; D-09 — completeness sentinel),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-UI-SPEC.md (linhas 215-225 — estados ProgressBar 0% / parcial / 100%; classes transition-all duration-300)
  </read_first>
  <action>
    Criar `src/features/form/ProgressBar.tsx` — faixa fina sticky no topo.

    **Imports:**
    - `import { useFormStore, TabKey } from '@/stores/formStore'`
    - `import { TAB_CONFIG } from './tabConfig'`

    **Props:**
    ```
    interface ProgressBarProps {
      tenantId: string
    }
    export function ProgressBar({ tenantId }: ProgressBarProps): JSX.Element
    ```

    **Implementação:**

    1. `const store = useFormStore(tenantId)` — lê `visitedTabs`.

    2. Calcular fração: para cada item em TAB_CONFIG, `completeness = visitedTabs.has(tab.key) ? 0.01 : 0`.
       Somar e dividir por `TAB_CONFIG.length` (que é 10) → `averageCompleteness` em [0,1].
       Multiplicar por 100 para obter `pct: number` em [0,100]. Em Phase 5, o valor máximo é
       `0.01 * 10 = 0.1` → 10% — esperado (proxy de progresso visualizado). Phase 6+ substitui completeness por cálculo Zod real.

       Pode usar `TAB_CONFIG.reduce((acc, t) => acc + (store.visitedTabs.has(t.key) ? 0.01 : 0), 0) / TAB_CONFIG.length` e depois `* 100`.

    3. JSX:
       ```
       <div
         className="sticky top-0 z-40 h-1 w-full bg-gray-200"
         role="progressbar"
         aria-valuenow={Math.round(pct)}
         aria-valuemin={0}
         aria-valuemax={100}
         aria-label="Progresso geral do formulário"
       >
         <div
           className="bg-primary h-full transition-all duration-300"
           style={{ width: `${pct}%` }}
         />
       </div>
       ```

    **Restrições:**
    - NÃO renderizar texto numérico de percentual (D-05 — só barra visual).
    - NÃO usar hex hardcoded.
    - Manter `sticky top-0 z-40` — z-index acima do conteúdo da aba mas abaixo de qualquer modal/Dialog (que tipicamente usa z-50).
    - `transition-all duration-300` (UI-SPEC) para suavizar mudança de largura ao trocar de aba.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; grep -c "sticky top-0\|bg-primary\|TAB_CONFIG\|role=\"progressbar\"" src/features/form/ProgressBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/ProgressBar.tsx` exists and exports named function `ProgressBar`
    - `ProgressBar.tsx` accepts `tenantId: string` prop
    - `ProgressBar.tsx` imports `useFormStore`, `TabKey` from `@/stores/formStore` and `TAB_CONFIG` from `./tabConfig`
    - `ProgressBar.tsx` contains the literal `sticky top-0` (sticky positioning at top)
    - `ProgressBar.tsx` contains the literal `h-1` (1px height per D-05)
    - `ProgressBar.tsx` contains the literal `bg-primary` (filled portion color) and `bg-gray-200` (track color)
    - `ProgressBar.tsx` contains `role="progressbar"` and `aria-valuenow` (accessibility)
    - `ProgressBar.tsx` contains `visitedTabs.has` (uses visitedTabs to compute completeness)
    - `ProgressBar.tsx` contains `TAB_CONFIG` reference (iterates or uses its length)
    - `ProgressBar.tsx` contains `style={{ width:` (dynamic width based on percentage)
    - `ProgressBar.tsx` contains `transition-all duration-300` (smooth width change per UI-SPEC)
    - `ProgressBar.tsx` does NOT contain hex color literals: `grep -E "#[0-9a-fA-F]{3,8}\b" src/features/form/ProgressBar.tsx` returns no matches
    - `npm run type-check` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>ProgressBar.tsx renderiza faixa h-1 sticky com largura calculada via visitedTabs/TAB_CONFIG, com role=progressbar + aria-valuenow; sem hex hardcoded; type-check passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store.visitedTabs → render | Set vem da store; já validado por TabKey enum |
| window.location.hash escrita | TabNavigation escreve no hash via window.location.hash = tab; vem do enum TabKey (valores controlados) |
| tenantId prop → useFormStore | tenantId vem do FormLayout (Plan 04) que extrai de useParams; pode ser manipulado pelo usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Loop infinito hash ↔ store quando TabNavigation escreve hash e FormLayout sincroniza | mitigate | Guard `if (window.location.hash !== '#' + tab)` no handleSelect — só escreve quando muda; acceptance criterion verifica presença literal do guard |
| T-05-03-02 | Information Disclosure | aria-label mobile expõe label da aba | accept | Labels são públicos (nomes das abas do formulário); intencional para acessibilidade quando label visual está oculto (`hidden md:inline`) |
| T-05-03-03 | Elevation of Privilege | tenantId arbitrário cria store fantasma com persist key inválida | accept | RLS no Supabase é a barreira autoritativa; defense-in-depth `routeOrgId === authOrgId` está no FormLayout (Plan 04) que envolve estes componentes |
| T-05-03-SC | Tampering | npm installs | mitigate | Nenhum pacote novo instalado neste plano — não há legitimidade a verificar |
</threat_model>

<verification>
- `useFormSection` retorna `{ data, updateField, errors, completeness }` corretos por aba
- `TabNavigation` renderiza 10 itens com ProgressBadge e classes ativo/inativo corretas
- `TabNavigation` sincroniza hash + store sem loop (guard presente)
- `ProgressBar` renderiza faixa sticky com largura calculada via visitedTabs
- `npm run type-check` exits 0
- Sem hex hardcoded em nenhum dos 3 arquivos
</verification>

<success_criteria>
- 3 arquivos novos em `src/features/form/`: useFormSection.ts, TabNavigation.tsx, ProgressBar.tsx
- Todos exportam suas funções/hooks nomeadamente
- TabNavigation usa `<button type="button">` (não NavLink) e tem guard contra loop de hash
- ProgressBar tem role=progressbar e aria-valuenow
- Tokens semânticos em todos os arquivos; sem hex
- `npm run type-check` passa
</success_criteria>

<output>
Create `.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-03-SUMMARY.md` when done
</output>
