---
phase: 05-shell-do-formul-rio-navega-o-por-abas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/features/form/ProgressBadge.tsx
autonomous: true
requirements: [UX-03]
tags: [ui, svg, progress-indicator, presentational]

must_haves:
  truths:
    - "ProgressBadge é um componente puro presentacional que recebe completeness: number e renderiza um ícone SVG inline diferente por estado"
    - "completeness === 0 renderiza círculo vazio com classe text-primary-300"
    - "completeness > 0 && completeness < 1 renderiza ícone de relógio (clock) com classe text-accent"
    - "completeness === 1 renderiza checkmark com classe text-green-500"
    - "Todos os SVGs têm aria-hidden=\"true\" (decorativos — a semântica vem do contexto do stepper)"
  artifacts:
    - path: "roteiro-unificado/src/features/form/ProgressBadge.tsx"
      provides: "Ícone visual de status por aba — 3 estados (não iniciado / em progresso / completo)"
      exports: ["ProgressBadge"]
  key_links:
    - from: "roteiro-unificado/src/features/form/ProgressBadge.tsx"
      to: "Tailwind v4 tokens"
      via: "Classes text-primary-300, text-accent, text-green-500 (sem hex hardcoded)"
      pattern: "text-(primary-300|accent|green-500)"
---

<objective>
Criar `ProgressBadge.tsx` — componente puro presentacional que recebe
`completeness: number` e renderiza um ícone SVG inline em 3 estados, seguindo D-11
do CONTEXT e §States Per Component do UI-SPEC. Componente sem acoplamento à
store ou ao TabKey: receba apenas um número, retorne JSX.

Purpose: Plan 03 importará este componente para o `TabNavigation` (sidebar + pills mobile).
Plan 04 não usa diretamente. Isolar este componente em Wave 1 permite
desenvolvimento paralelo com Plan 01 (sem dependência de TabKey).

Output: Arquivo de componente único exportando `ProgressBadge` por named export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-UI-SPEC.md

@roteiro-unificado/src/components/layouts/AdminSidebar.tsx
@roteiro-unificado/src/lib/utils.ts

<interfaces>
<!-- Padrão de ícone Heroicons inline já estabelecido no projeto (AdminSidebar.tsx) -->

Padrão inline SVG (de src/components/layouts/AdminSidebar.tsx):
- <svg xmlns="..." fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor"
       className="h-5 w-5" aria-hidden="true">
    <path strokeLinecap="round" strokeLinejoin="round" d="..." />
  </svg>

Paths a usar (Heroicons outline, 24x24):
- Check circle (completo):  M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0z
- Clock (em progresso):      M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0z
- Circle empty (não iniciado): <circle cx="12" cy="12" r="9" /> (sem <path>; usar <circle>)

Tokens Tailwind v4 disponíveis (de src/index.css):
- text-primary-300: cinza azulado apagado (--color-primary-300)
- text-accent: laranja (#F28C28)
- text-green-500: token utilitário Tailwind nativo (não em @theme — mas RESEARCH.md / UI-SPEC mencionam text-green-500)

NÃO usar:
- text-g5 (existe no @theme mas é cor de avaliação, não de UI status de progresso)
- hex hardcoded (forbidden)
- nenhuma biblioteca de ícones (heroicons-react, lucide) — usar SVG inline
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar ProgressBadge.tsx com 3 estados SVG inline</name>
  <files>
    roteiro-unificado/src/features/form/ProgressBadge.tsx
  </files>
  <read_first>
    roteiro-unificado/src/components/layouts/AdminSidebar.tsx (padrão SVG inline Heroicons — viewBox, strokeWidth, aria-hidden, path d),
    roteiro-unificado/src/lib/utils.ts (helper cn para combinar classes — opcional, só se aceitar prop className),
    roteiro-unificado/src/index.css (tokens @theme — confirmar text-primary-300 e text-accent existem; text-green-500 é utilitário Tailwind nativo),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md (D-11 — mapeamento completeness → ícone/cor),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-UI-SPEC.md (linhas 232-241 — tabela de estados ProgressBadge; tamanho h-4 w-4)
  </read_first>
  <action>
    Criar `src/features/form/ProgressBadge.tsx` como componente funcional puro
    seguindo o padrão de SVG inline de AdminSidebar.tsx (linhas 27-41).

    **Assinatura:**
    - `interface ProgressBadgeProps { completeness: number; className?: string }`
    - `export function ProgressBadge({ completeness, className }: ProgressBadgeProps): JSX.Element`

    **Branching por estado (em ordem; primeiro match vence):**

    1. `completeness === 1` → retornar SVG checkmark circle, cor `text-green-500`:
       - Atributos: `xmlns="http://www.w3.org/2000/svg"`, `fill="none"`, `viewBox="0 0 24 24"`,
         `strokeWidth={1.5}`, `stroke="currentColor"`, `aria-hidden="true"`.
       - className: `cn('h-4 w-4 text-green-500', className)` (se aceitar className) ou apenas `"h-4 w-4 text-green-500"`.
       - Conteúdo: `<path strokeLinecap="round" strokeLinejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0z" />`.

    2. `completeness > 0 && completeness < 1` → retornar SVG clock, cor `text-accent`:
       - Mesmos atributos do bloco 1 mas className `"h-4 w-4 text-accent"`.
       - Conteúdo: `<path strokeLinecap="round" strokeLinejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0z" />`.

    3. `else` (incluindo `completeness === 0` e valores fora do esperado) → retornar SVG círculo vazio, cor `text-primary-300`:
       - Mesmos atributos mas className `"h-4 w-4 text-primary-300"`.
       - Conteúdo: `<circle cx="12" cy="12" r="9" />` (NÃO `<path>` — usar tag `<circle>` com fill implícito de `none` herdado do `<svg fill="none">`).

    **Restrições:**
    - Importar apenas `cn` se for usar `className` opcional; caso contrário, sem imports além do React JSX implícito.
    - NÃO importar `TabKey` — este componente é agnóstico de aba.
    - NÃO conectar à store — recebe `completeness` como prop pura.
    - NÃO usar `text-g5` (é cor de avaliação G1-G5, não UI de status).
    - NÃO usar hex hardcoded — `grep -i "#" src/features/form/ProgressBadge.tsx` deve retornar apenas linhas de comentário/SVG path; nenhum literal de cor hex (`#123B66`, `#F28C28`, `#22c55e`).
    - Adicionar `displayName` opcional (`ProgressBadge.displayName = 'ProgressBadge'`) para devtools — não obrigatório.
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; grep -c "text-green-500\|text-accent\|text-primary-300" src/features/form/ProgressBadge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/ProgressBadge.tsx` exists and exports named function `ProgressBadge`
    - `ProgressBadge.tsx` accepts a `completeness: number` prop (TypeScript interface or inline type)
    - `ProgressBadge.tsx` contains the literal `text-green-500` (completed state color)
    - `ProgressBadge.tsx` contains the literal `text-accent` (in-progress state color)
    - `ProgressBadge.tsx` contains the literal `text-primary-300` (empty state color)
    - `ProgressBadge.tsx` contains `aria-hidden="true"` on the SVG element(s)
    - `ProgressBadge.tsx` contains `<circle cx="12" cy="12" r="9"` (empty state geometry)
    - `ProgressBadge.tsx` contains a conditional comparing `completeness === 1` (or equivalent literal `=== 1`) and another comparing `< 1` or `> 0`
    - `ProgressBadge.tsx` does NOT contain hex color literals: `grep -E "#[0-9a-fA-F]{3,8}\b" src/features/form/ProgressBadge.tsx` returns no matches
    - `ProgressBadge.tsx` does NOT import from `@/stores/formStore` or any store path (no coupling to TabKey or store): `grep -c "from '@/stores" src/features/form/ProgressBadge.tsx` returns 0
    - `npm run type-check` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>ProgressBadge.tsx criado com 3 ramos de renderização, tokens semânticos sem hex hardcoded, type-check passa, sem acoplamento ao store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prop completeness → render | `completeness` é número numérico; sem entrada de string ou HTML do usuário |
| SVG inline → DOM | SVG hardcoded no JSX; React escapa atributos automaticamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Prop `completeness` recebe NaN ou número fora de [0,1] | accept | Componente trata graciosamente: NaN/negativos/>1 caem no ramo `else` → ícone "não iniciado" cinza. Sem crash ou render aberrante. |
| T-05-02-02 | Information Disclosure | SVG decorativo expõe informação visual de status | accept | Status visual é intencional (UX-03); `aria-hidden="true"` evita leitura redundante por screen readers — semântica vem do TabNavigation pai (Plan 03) |
| T-05-02-SC | Tampering | npm installs | mitigate | Nenhum pacote novo instalado neste plano — não há legitimidade a verificar |
</threat_model>

<verification>
- `ProgressBadge` renderiza 3 ícones distintos conforme completeness 0 / parcial / 1
- Sem hex hardcoded — apenas tokens Tailwind
- Sem acoplamento à store ou TabKey — componente reutilizável e puro
- `npm run type-check` exits 0
</verification>

<success_criteria>
- `src/features/form/ProgressBadge.tsx` existe e exporta `ProgressBadge`
- 3 estados de renderização (0 / parcial / 1) com cores corretas
- `aria-hidden="true"` em todos os SVGs
- Sem hex hardcoded; sem import de `@/stores`
- `npm run type-check` passa
</success_criteria>

<output>
Create `.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-02-SUMMARY.md` when done
</output>
