---
phase: 05-shell-do-formul-rio-navega-o-por-abas
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - roteiro-unificado/src/features/form/FormLayout.tsx
  - roteiro-unificado/src/router.tsx
autonomous: false
requirements: [FORM-01, FORM-04, UX-03]
tags: [layout, react-router, hash-sync, wiring]

must_haves:
  truths:
    - "FormLayout renderiza em /form/:orgId substituindo o placeholder '<div>Form Page — Phase 5</div>'"
    - "Layout desktop tem sidebar bg-primary à esquerda (min-w-[220px] max-w-[300px]) e área de conteúdo flex-1"
    - "Sidebar contém marca 'Roteiro Unificado', TabNavigation (stepper), divisor e botão Sair com mt-auto no rodapé"
    - "Botão Sair chama signOut() e navega para /login com replace:true"
    - "Layout mobile (< md) vira coluna: ProgressBar topo, TabNavigation horizontal scrollável, conteúdo full width"
    - "Hash do URL sincroniza com activeTab: ao montar lê location.hash; ao mudar location.hash via popstate atualiza store.setActiveTab"
    - "Acesso a /form/:orgId quando routeOrgId !== authOrgId redireciona para /form/${authOrgId} (defense-in-depth contra cross-tenant)"
    - "Conteúdo da aba ativa renderiza placeholder textual identificando a aba (Phase 6+ substitui por campos reais)"
  artifacts:
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "Layout principal do formulário: sidebar com TabNavigation + botão Sair, área de conteúdo, hash sync"
      exports: ["FormLayout"]
    - path: "roteiro-unificado/src/router.tsx"
      provides: "Rota /form/:orgId renderiza <FormLayout /> em vez do placeholder de Phase 5"
      contains: "element: <FormLayout"
  key_links:
    - from: "roteiro-unificado/src/router.tsx"
      to: "FormLayout"
      via: "ProtectedRoute children element"
      pattern: "element:\\s*<FormLayout"
    - from: "roteiro-unificado/src/features/form/FormLayout.tsx"
      to: "useFormStore + setActiveTab"
      via: "Hash sync useEffect dispara setActiveTab quando location.hash muda"
      pattern: "setActiveTab"
    - from: "roteiro-unificado/src/features/form/FormLayout.tsx"
      to: "useAuth().signOut + useNavigate"
      via: "Botão Sair chama signOut() + navigate('/login', { replace: true })"
      pattern: "signOut\\(\\)"
    - from: "roteiro-unificado/src/features/form/FormLayout.tsx"
      to: "useParams().orgId"
      via: "orgId da rota usado como tenantId do useFormStore"
      pattern: "useParams"
---

<objective>
Compor o `FormLayout` final integrando todas as peças do Plan 03 (TabNavigation,
ProgressBar) + ProgressBadge (Plan 02) + formStore (Plan 01), e religar o
`router.tsx` para que `/form/:orgId` renderize `<FormLayout />` em vez do placeholder
atual "<div>Form Page — Phase 5</div>".

Purpose: Este é o plano de wiring final da Phase 5. Após este plano, qualquer
construtora logada que acesse `/form/:orgId` verá o shell de 10 abas navegáveis
com URL hash sync, sidebar `bg-primary`, ProgressBar sticky, botão Sair, e
verificação de cross-tenant (`routeOrgId === authOrgId`). Phase 6 sobrepõe o
conteúdo da aba ativa com campos reais.

Output: `FormLayout.tsx` em `src/features/form/`; `router.tsx` atualizado. Plano
inclui checkpoint humano final (não-autônomo) para verificação visual e funcional
do shell completo no browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-RESEARCH.md
@.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-UI-SPEC.md

@roteiro-unificado/src/router.tsx
@roteiro-unificado/src/components/routing/ProtectedRoute.tsx
@roteiro-unificado/src/components/layouts/AdminLayout.tsx
@roteiro-unificado/src/components/layouts/AdminHeader.tsx
@roteiro-unificado/src/features/auth/useAuth.ts
@roteiro-unificado/src/features/auth/AuthProvider.tsx
@roteiro-unificado/src/stores/formStore.ts
@roteiro-unificado/src/features/form/tabConfig.ts
@roteiro-unificado/src/features/form/TabNavigation.tsx
@roteiro-unificado/src/features/form/ProgressBar.tsx
@roteiro-unificado/src/features/form/ProgressBadge.tsx
@roteiro-unificado/src/components/ui/index.ts
@roteiro-unificado/src/components/ui/button.tsx
@roteiro-unificado/src/components/ui/spinner.tsx

<interfaces>
<!-- Disponível após Plans 01-03 -->

From src/stores/formStore.ts (Plan 01):
```typescript
export enum TabKey { Identificacao, TorreDecisao, ..., Nda }  // 10 valores
export function useFormStore(tenantId: string): FormStore  // tem activeTab, visitedTabs, setActiveTab, markTabVisited
```

From src/features/form/tabConfig.ts (Plan 01):
```typescript
export const TAB_CONFIG: { key: TabKey; label: string }[]  // 10 itens
```

From src/features/form/TabNavigation.tsx (Plan 03):
```typescript
export function TabNavigation({ tenantId }: { tenantId: string }): JSX.Element
```

From src/features/form/ProgressBar.tsx (Plan 03):
```typescript
export function ProgressBar({ tenantId }: { tenantId: string }): JSX.Element
```

From src/features/auth/useAuth.ts:
```typescript
export function useAuth(): {
  user: User | null
  session: Session | null
  role: Enums<'member_role'> | null
  orgId: string | null               // auth orgId (vem de org_members)
  isLoading: boolean
  signOut: () => Promise<void>
}
```

From react-router-dom v7.15.1:
- useParams<{ orgId: string }>()  → { orgId: string | undefined }
- useLocation() → { hash: string, pathname, ... }
- useNavigate() → (to, options?) => void

Router atual (linhas 28-36 de src/router.tsx) — substituir o placeholder:
{
  element: <ProtectedRoute />,
  children: [
    { path: '/form/:orgId', element: <div>Form Page — Phase 5</div> },
  ],
},

Tab key set (todos os valores do enum) para validar hash:
['identificacao', 'torre-decisao', 'torre-sienge', 'torre-acesso',
 'torre-classificacao', 'hab-venda', 'hab-repositorios',
 'hab-responsaveis', 'hab-classificacao', 'nda']
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar FormLayout.tsx (sidebar + conteúdo + hash sync + cross-tenant guard + botão Sair)</name>
  <files>
    roteiro-unificado/src/features/form/FormLayout.tsx
  </files>
  <read_first>
    roteiro-unificado/src/components/layouts/AdminLayout.tsx (padrão flex h-screen / sidebar fixa / main flex-1 — referência de estrutura),
    roteiro-unificado/src/components/layouts/AdminHeader.tsx (padrão handleSignOut: await signOut + navigate('/login', { replace: true }) + toast.error em catch),
    roteiro-unificado/src/components/routing/ProtectedRoute.tsx (Spinner full-screen quando isLoading; padrão de loading),
    roteiro-unificado/src/features/auth/AuthProvider.tsx (interface AuthContextType com user/orgId/signOut/isLoading),
    roteiro-unificado/src/features/auth/useAuth.ts (hook export),
    roteiro-unificado/src/stores/formStore.ts (após Plan 01 — TabKey, useFormStore, setActiveTab, markTabVisited),
    roteiro-unificado/src/features/form/TabNavigation.tsx (após Plan 03 — recebe tenantId, renderiza pills/stepper),
    roteiro-unificado/src/features/form/ProgressBar.tsx (após Plan 03 — recebe tenantId, faixa sticky),
    roteiro-unificado/src/features/form/tabConfig.ts (TAB_CONFIG — para encontrar label da aba ativa),
    roteiro-unificado/src/components/ui/button.tsx (variant ghost; isLoading via prop),
    roteiro-unificado/src/components/ui/spinner.tsx (Spinner size lg para loading da store),
    roteiro-unificado/src/hooks/useToast.ts (toast.error wrapper),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-CONTEXT.md (D-05 a D-08 — sidebar width, botão Sair mt-auto, barra de progresso topo; armadilha 3 — usar useParams().orgId como tenantId),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-RESEARCH.md (linhas 217-238 — padrão URL hash navigation; linhas 289-299 — guarda anti-loop; linhas 300-313 — cross-tenant: routeOrgId vs authOrgId),
    .planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-UI-SPEC.md (linhas 152-188 — diagrama desktop e mobile; linhas 196-209 — Interaction Contract; linhas 217-241 — copy "Sair" + estados)
  </read_first>
  <action>
    Criar `src/features/form/FormLayout.tsx` que monta o shell completo do formulário.

    Imports obrigatórios:
    - `useEffect` de 'react'
    - `useParams`, `useLocation`, `useNavigate`, `Navigate` de 'react-router-dom'
    - `useAuth` de '@/features/auth/useAuth'
    - `useFormStore`, `TabKey` de '@/stores/formStore'
    - `TAB_CONFIG` de './tabConfig'
    - `TabNavigation` de './TabNavigation'
    - `ProgressBar` de './ProgressBar'
    - `Button`, `Spinner` de '@/components/ui'
    - `useToast` de '@/hooks/useToast'

    Assinatura: `export function FormLayout(): JSX.Element` — sem props.

    Lógica passo a passo:

    1. Hooks no topo do componente, em ordem: `useParams<{ orgId: string }>()` para extrair `routeOrgId`,
       `useAuth()` para obter `{ orgId: authOrgId, signOut, isLoading }`, `useLocation()` para `location.hash`,
       `useNavigate()` para o redirect pós-signOut, `useToast()` para mensagens de erro.

    2. Defense-in-depth cross-tenant guard (per RESEARCH security + threat T-05-04-01) ANTES de chamar
       `useFormStore`: se `isLoading` é true OU `routeOrgId` é undefined OU `authOrgId` é null → retornar
       o Spinner full-screen (mesma estrutura de ProtectedRoute: `<div className="bg-primary flex min-h-screen items-center justify-center"><Spinner size="lg" className="border-white border-t-transparent" /></div>`).
       Se ambos resolvidos e `routeOrgId !== authOrgId` → retornar `<Navigate to={\`/form/${authOrgId}\`} replace />`.
       Apenas depois desses guards o `useFormStore(routeOrgId)` é chamado — `tenantId` é `routeOrgId`.

    3. Hash sync `useEffect` com dep array `[location.hash]` (NUNCA inclua `store` nas deps —
       provoca loops): ler `location.hash.replace('#', '')`; comparar contra o conjunto de keys de
       TAB_CONFIG (`TAB_CONFIG.map(t => t.key) as string[]`); se incluído, usar como TabKey;
       caso contrário fallback `TabKey.Identificacao`. ANTES de chamar `setActiveTab` verificar
       `if (store.activeTab !== matched)` para evitar dispatch desnecessário (RESEARCH armadilha 2).
       Em seguida chamar `store.markTabVisited(matched)`.

    4. Handler `handleSignOut` (async, espelhar AdminHeader linhas 11-18): `try { await signOut();
       navigate('/login', { replace: true }) } catch { toast.error('Erro ao encerrar sessão') }`.

    5. Identificar `activeTabConfig` para o título do conteúdo:
       `const activeTabConfig = TAB_CONFIG.find(t => t.key === store.activeTab) ?? TAB_CONFIG[0]`.

    JSX do layout (estrutura desktop + mobile via Tailwind responsivo) — descrição da árvore esperada
    (escrever em JSX real no arquivo):

    - Raiz: `<div>` com classes `flex min-h-screen flex-col bg-gray-50`.
    - Primeiro filho: `<ProgressBar tenantId={tenantId} />` (faixa sticky no topo).
    - Segundo filho: `<div>` com classes `flex flex-1 flex-col md:flex-row` (coluna no mobile, linha no desktop).
      - Filho 1 (sidebar): `<aside>` com classes `bg-primary flex w-full flex-col text-white md:min-w-[220px] md:max-w-[300px]`.
        - Bloco de marca: `<div>` com classes `border-primary-800 border-b px-4 py-4 hidden md:block`,
          contendo `<span className="text-base font-semibold">Roteiro Unificado</span>`.
        - Bloco da navegação: `<div>` com classes `flex-1 p-3`, contendo `<TabNavigation tenantId={tenantId} />`.
        - Bloco do Sair (rodapé): `<div>` com classes `mt-auto border-t border-primary-800 p-3 hidden md:block`,
          contendo `<Button variant="ghost" size="sm" className="w-full justify-start text-white hover:bg-primary-800" onClick={handleSignOut}>Sair</Button>`.
      - Filho 2 (main): `<main>` com classes `flex-1 p-4 md:p-6`, contendo:
        - `<h1 className="text-xl font-semibold text-gray-900">{activeTabConfig.label}</h1>`
        - `<p className="mt-2 text-sm text-gray-500">Esta seção ainda não possui campos. Em breve os campos estarão disponíveis aqui.</p>`

    Observação sobre mobile: a sidebar ocupa `w-full` em mobile (ProgressBar topo, TabNavigation
    horizontal scrollável dentro dela). As classes `hidden md:block` no bloco da marca e no bloco
    do botão Sair escondem esses elementos no mobile (botão Sair mobile pode ser adicionado em Phase 12).

    Restrições críticas:
    - SEM hex hardcoded — apenas tokens (`bg-primary`, `text-white`, `border-primary-800`, `bg-gray-50`, `text-gray-900`, `text-gray-500`).
    - NÃO incluir `store` nas deps do useEffect de hash sync.
    - O botão Sair tem label EXATA `Sair` (D-07 / UI-SPEC §Copywriting — uma palavra).
    - Empty state textual EXATO: `Esta seção ainda não possui campos. Em breve os campos estarão disponíveis aqui.` (UI-SPEC §Copywriting Contract).
    - O cross-tenant guard é OBRIGATÓRIO antes de chamar `useFormStore(tenantId)` para impedir criação de store com key `form-progress-null` ou de outra org (RESEARCH armadilha 3 + threat T-05-04-01).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; grep -c "useParams\|signOut\|setActiveTab\|markTabVisited\|<TabNavigation\|<ProgressBar\|Navigate to" src/features/form/FormLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/form/FormLayout.tsx` exists and exports named function `FormLayout`
    - `FormLayout.tsx` imports `useParams`, `useLocation`, `useNavigate`, `Navigate` from `react-router-dom`
    - `FormLayout.tsx` imports `useAuth` from `@/features/auth/useAuth`
    - `FormLayout.tsx` imports `useFormStore` and `TabKey` from `@/stores/formStore`
    - `FormLayout.tsx` imports `TAB_CONFIG` from `./tabConfig`
    - `FormLayout.tsx` imports `TabNavigation` from `./TabNavigation` and `ProgressBar` from `./ProgressBar`
    - `FormLayout.tsx` renders `<TabNavigation tenantId={tenantId}` and `<ProgressBar tenantId={tenantId}`
    - `FormLayout.tsx` contains a `<Navigate to=` redirect for cross-tenant mismatch
    - `FormLayout.tsx` contains an inequality comparison between `routeOrgId` and `authOrgId`: `grep -c "routeOrgId !== authOrgId\|authOrgId !== routeOrgId" src/features/form/FormLayout.tsx` returns at least 1
    - `FormLayout.tsx` contains a `useEffect` watching `location.hash` and calling `setActiveTab` and `markTabVisited`
    - `FormLayout.tsx` contains the literal `mt-auto` (Sair button container at bottom of sidebar per D-07)
    - `FormLayout.tsx` contains the literal `min-w-[220px]` and `max-w-[300px]` (sidebar width per D-08)
    - `FormLayout.tsx` contains the literal `Sair` as the logout button label
    - `FormLayout.tsx` calls `await signOut()` and `navigate('/login', { replace: true })` in the logout handler
    - `FormLayout.tsx` contains the literal empty-state copy: `Esta seção ainda não possui campos. Em breve os campos estarão disponíveis aqui.`
    - `FormLayout.tsx` does NOT contain hex color literals: `grep -E "#[0-9a-fA-F]{3,8}\b" src/features/form/FormLayout.tsx` returns no matches
    - `npm run type-check` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>FormLayout.tsx renderiza shell completo com sidebar bg-primary, TabNavigation, ProgressBar, botão Sair, hash sync useEffect e cross-tenant guard; type-check passa; sem hex.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Religar router.tsx — substituir placeholder de /form/:orgId por FormLayout</name>
  <files>
    roteiro-unificado/src/router.tsx
  </files>
  <read_first>
    roteiro-unificado/src/router.tsx (estado atual — linha 33 tem o placeholder a substituir),
    roteiro-unificado/src/features/form/FormLayout.tsx (após Task 1 — confirma export),
    roteiro-unificado/src/components/routing/ProtectedRoute.tsx (não modificar — apenas confirmar que ProtectedRoute envelopa /form/:orgId)
  </read_first>
  <action>
    Modificar `src/router.tsx` para substituir o placeholder atual e adicionar o import.

    Mudança 1 — Adicionar import no topo do arquivo (após os imports existentes de páginas/layouts):
    `import { FormLayout } from '@/features/form/FormLayout'`

    Mudança 2 — Substituir o bloco ProtectedRoute (atualmente linhas 28-36 de src/router.tsx).
    O bloco a remover contém `path: '/form/:orgId'` com `element: <div>Form Page — Phase 5</div>`.
    O bloco novo deve manter `element: <ProtectedRoute />`, `children` array contendo
    o objeto `{ path: '/form/:orgId', element: <FormLayout /> }` — apenas o `element` da rota muda;
    o guard `<ProtectedRoute />` permanece intocado.

    Restrições:
    - Manter os outros blocos do `createBrowserRouter` (Navigate `/`, `/login`, `/forgot-password`, `/reset-password`, AdminRoute) EXATAMENTE como estão.
    - Após esta mudança, `grep -c "Form Page — Phase 5" src/router.tsx` deve retornar 0.
    - NÃO alterar `ProtectedRoute` (o guard de sessão); FormLayout é renderizado dentro dele.
    - NÃO alterar o bloco AdminRoute (Phase 4).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run lint -- src/router.tsx &amp;&amp; npm run type-check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `roteiro-unificado/src/router.tsx` contains the import line for `FormLayout`: `grep -c "import.*FormLayout.*from.*@/features/form/FormLayout" src/router.tsx` returns 1
    - `roteiro-unificado/src/router.tsx` contains `element: <FormLayout` (the route element references FormLayout)
    - `roteiro-unificado/src/router.tsx` does NOT contain the placeholder literal `Form Page — Phase 5`: `grep -c "Form Page — Phase 5" src/router.tsx` returns 0
    - `roteiro-unificado/src/router.tsx` still contains `path: '/form/:orgId'` (route preserved)
    - `roteiro-unificado/src/router.tsx` still contains `element: <ProtectedRoute />` (guard preserved)
    - `roteiro-unificado/src/router.tsx` still references the existing admin routes (`/admin/dashboard`, `/admin/orgs/:orgId`) — these were not touched: `grep -c "/admin/dashboard\|/admin/orgs/:orgId" src/router.tsx` returns at least 2
    - `npm run lint` from `roteiro-unificado/` exits 0 for `src/router.tsx`
    - `npm run type-check` from `roteiro-unificado/` exits 0
    - `npm run build` from `roteiro-unificado/` exits 0 (TypeScript + Vite production build succeeds)
  </acceptance_criteria>
  <done>router.tsx religado: /form/:orgId renderiza FormLayout; placeholder removido; lint + type-check + build passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual + funcional do shell do formulário no browser</name>
  <files>
    (checkpoint — sem arquivos modificados; verifica artefatos produzidos pelas Tasks 1 e 2)
  </files>
  <read_first>
    roteiro-unificado/src/features/form/FormLayout.tsx (artefato a verificar),
    roteiro-unificado/src/router.tsx (wiring a verificar),
    .planning/ROADMAP.md (linhas 170-178 — UATs da Phase 5 que este checkpoint cobre)
  </read_first>
  <action>
    Checkpoint humano. Não escrever código. Não modificar arquivos. Iniciar o dev server,
    executar o roteiro de verificação manual abaixo e aguardar a aprovação do usuário antes
    de criar o SUMMARY desta fase.

    Passos do roteiro (executar em ordem):

    1. Iniciar dev server: `cd roteiro-unificado && npm run dev`.
    2. Fazer login como uma construtora (não admin) → ser redirecionado para `/form/:orgId`. Esperado:
       sidebar azul à esquerda, 10 abas listadas com ícones, barra fina no topo, conteúdo "Identificação".
    3. Clicar na aba "Torre Decisão" → conteúdo muda; URL muda para `/form/:orgId#torre-decisao`;
       ícone laranja (clock) aparece ao lado de Torre Decisão.
    4. Pressionar Back no browser → volta ao hash anterior; aba ativa atualiza junto.
    5. Recarregar a página (F5) com hash `#hab-venda` → app reabre direto na aba Hab. Venda.
    6. Reduzir janela do browser para < 768px → sidebar vira pills horizontais scrolláveis no topo;
       conteúdo abaixo; labels das abas escondidos (só ícones); `aria-label` mantido.
    7. Restaurar viewport desktop. Clicar no botão "Sair" no rodapé da sidebar → redireciona para `/login`.
    8. Login novamente. Acessar manualmente `/form/<orgId-de-outra-org>` na URL → redireciona
       automaticamente para `/form/<seu-orgId>` (cross-tenant guard funciona).
    9. Abrir DevTools → Application → Local Storage: verificar chave `form-progress-${tenantId}`
       existe e contém `activeTab` e `visitedTabs` mas NÃO contém `sectionData`.
    10. DevTools → Session Storage: verificar chave `form-data-${tenantId}` (criada após qualquer
        mudança em sectionData; em Phase 5 fica `{}` ou ausente até Phase 6 tocar em campos).
    11. Console: verificar limpo — sem warnings de loop infinito, sem
        `TypeError: visitedTabs.has is not a function` após reload.

    Após coletar evidências, registrar o resultado no canal de chat para a aprovação final.
  </action>
  <verify>
    <human-check>Usuário confirma manualmente os 11 passos acima e sinaliza "approved" ou descreve falhas.</human-check>
  </verify>
  <acceptance_criteria>
    - Dev server inicia sem erro (`npm run dev` em `roteiro-unificado/`)
    - `/form/:orgId` renderiza sidebar azul + 10 abas + barra fina superior + conteúdo da aba ativa
    - Clicar em uma aba muda conteúdo, URL hash e ícone de status (em progresso → laranja)
    - Browser back/forward navega entre abas via popstate sem reload
    - Deep-link com hash (`/form/:orgId#hab-venda`) abre na aba correta
    - Viewport < 768px exibe pills horizontais scrolláveis no topo
    - Botão "Sair" redireciona para `/login` (sessão encerrada)
    - Acesso a `/form/<outra-org-id>` redireciona para `/form/<self-org-id>` (cross-tenant guard)
    - localStorage `form-progress-${tenantId}` contém activeTab + visitedTabs mas NÃO sectionData
    - Console limpo — sem loop infinito nem TypeError após reload
    - Usuário envia sinal de aprovação ("approved") OU descreve falha específica para revisão
  </acceptance_criteria>
  <done>Usuário aprova o checkpoint indicando que os 11 passos foram verificados e a Phase 5 está pronta para SUMMARY e fechamento.</done>
  <resume-signal>Digite "approved" se todos os passos passarem. Se algum falhar, descreva qual passo e o erro observado para que a equipe ajuste antes de fechar a Phase 5.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL /form/:orgId → FormLayout | orgId vem da URL — controlado pelo usuário; cross-tenant guard valida contra authOrgId |
| location.hash → setActiveTab | Hash vem do browser/usuário; valor não-TabKey cai em fallback Identificacao |
| signOut → /login redirect | useNavigate replace:true não mantém /form/:orgId no histórico |
| useFormStore(tenantId) | tenantId é routeOrgId; só após o guard de cross-tenant — store nunca criada com tenant alheio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | Acesso direto a /form/<outra-org-id> via URL | mitigate | FormLayout valida `routeOrgId === authOrgId` antes de renderizar; mismatch → `<Navigate to={\`/form/${authOrgId}\`} replace />`. Acceptance criterion verifica presença da comparação no código. RLS no Supabase (Phase 2) é a barreira autoritativa. |
| T-05-04-02 | Tampering | location.hash arbitrário (ex: `#admin-panel`) força aba inexistente | mitigate | useEffect compara hash com TAB_CONFIG.map(t => t.key); valor inválido cai no fallback `TabKey.Identificacao`. Sem crash, sem renderização aberrante. |
| T-05-04-03 | Information Disclosure | sectionData de outra org carregada do localStorage devido a troca de tenantId no URL | mitigate | persist key namespaceada `form-progress-${tenantId}` isola por tenant; cross-tenant guard impede o useFormStore ser chamado com tenantId alheio antes do redirect; sessionStorage também segue chave `form-data-${tenantId}`. |
| T-05-04-04 | Denial of Service | Loop infinito hash ↔ store quando hash sync useEffect e TabNavigation handler interagem | mitigate | TabNavigation guarda `if (window.location.hash !== '#' + tab)` antes de escrever; FormLayout useEffect compara `store.activeTab !== matched` antes de chamar setActiveTab. Dupla proteção contra loop. |
| T-05-04-05 | Repudiation | signOut falha silenciosamente | mitigate | handler `handleSignOut` envolve `signOut()` em try/catch; em catch chama `toast.error('Erro ao encerrar sessão')` para feedback ao usuário (padrão de AdminHeader). |
| T-05-04-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo instalado nesta fase (RESEARCH §Stack Padrão). Não há legitimidade a verificar. |
</threat_model>

<verification>
- `/form/:orgId` renderiza FormLayout com sidebar bg-primary + 10 abas + ProgressBar + botão Sair
- URL hash sincroniza com aba ativa nos dois sentidos (clique ↔ popstate ↔ deep-link)
- Cross-tenant guard funciona: `/form/<outra-org>` → redirect para `/form/<auth-org>`
- Botão Sair chama signOut + navigate('/login', { replace: true })
- Layout mobile (< md) vira pills horizontais
- `npm run build` exits 0
- localStorage tem só navegação (activeTab/visitedTabs); sessionStorage tem só sectionData
- Sem hex hardcoded em FormLayout.tsx
- Checkpoint humano aprovado
</verification>

<success_criteria>
- `src/features/form/FormLayout.tsx` existe e exporta `FormLayout`
- `src/router.tsx` substituiu `<div>Form Page — Phase 5</div>` por `<FormLayout />`
- `npm run build` exits 0
- Checkpoint Task 3 aprovado pelo usuário (visual + funcional)
- Todos os UATs da Phase 5 do ROADMAP passam manualmente
</success_criteria>

<output>
Create `.planning/phases/05-shell-do-formul-rio-navega-o-por-abas/05-04-SUMMARY.md` when done
</output>
