---
phase: 09.5-layout-design-system-overhaul
plan: 04
type: execute
wave: 2
depends_on:
  - 09.5-01
files_modified:
  - roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx
autonomous: true
requirements:
  - UX-06

must_haves:
  truths:
    - 'IdentificacaoSection renderiza 4 cards semânticos: Dados da Empresa, Reunião/Participantes, Responsáveis, Escopo, Prioridades'
    - 'Cada card tem header com ícone colorido (blue/amber/green/purple/blue) + título + subtitle'
    - 'Cards estão organizados em FormCardRow (alguns full-width, alguns cols-2)'
    - 'Todos os 12 campos do schema identificacaoSchema continuam funcionais (register/setValue/watch/control inalterados)'
    - 'Validação Zod, máscara CNPJ e sync RHF→store continuam idênticos à versão pré-refactor'
  artifacts:
    - path: 'roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx'
      provides: 'Section refatorado em cards semânticos (proof-of-concept do padrão)'
      contains: 'FormCard'
  key_links:
    - from: 'roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx'
      to: 'roteiro-unificado/src/features/form/FormCard.tsx'
      via: 'import + wrap de cada grupo semântico'
      pattern: 'import.*FormCard'
    - from: 'roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx'
      to: 'roteiro-unificado/src/features/form/FormCardRow.tsx'
      via: 'import + organização em grid responsivo'
      pattern: 'FormCardRow'
---

<objective>
Refatorar `IdentificacaoSection.tsx` para usar `FormCard` + `FormCardRow` como prova de conceito do padrão de cards semânticos. Nenhuma mudança em schema, validação, formato de dados ou comportamento RHF — apenas reorganização visual.

Purpose: Antes de aplicar o padrão nas 9 sections restantes (Plan 05), validar que o pattern funciona com um schema real, com máscara CNPJ, com Controller, com erros inline e com sync para a store. Identificação é o melhor candidato porque tem 5 grupos semânticos claros (alinhados ao sketch).

Output: 1 arquivo refatorado. Diff visualmente significativo, mas sem regressão funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md
@.claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md

<interfaces>
<!-- Componentes do plan 01 -->
From roteiro-unificado/src/features/form/FormCard.tsx:
```typescript
export type FormCardIconColor = 'blue' | 'amber' | 'green' | 'purple'
export interface FormCardProps {
  icon: ReactNode
  iconColor: FormCardIconColor
  title: string
  subtitle?: string
  children: ReactNode
  className?: string
}
```

From roteiro-unificado/src/features/form/FormCardRow.tsx:

```typescript
export interface FormCardRowProps {
  cols?: 1 | 2;
  children: ReactNode;
  className?: string;
}
```

<!-- Schema do form -->

From roteiro-unificado/src/schemas/identificacao.ts (presumido pelo uso):

```typescript
identificacaoSchema com 12 campos:
- empresa (required)
- cnpj (required, regex CNPJ)
- dataReuniao
- sponsorPiloto
- participantes (textarea)
- responsavelSienge
- responsavelHabilitacoes
- quemConduzComercial
- numCnpjsEscopo
- numObrasAtivas
- prioridadeTorre360 (textarea)
- prioridadeHabilitacoes (textarea)
```

<!-- Componentes UI existentes -->

From roteiro-unificado/src/components/ui:

- Input (com props error, errorMessage, type, placeholder, etc.)
- TextareaField (Controller wrapper — name, control, label, placeholder, error)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refatorar IdentificacaoSection para usar FormCard + FormCardRow</name>
  <files>roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx (versão atual completa — entender que tem 5 grupos separados por `<hr>` hoje)
    - roteiro-unificado/src/features/form/FormCard.tsx (criado em plan 01)
    - roteiro-unificado/src/features/form/FormCardRow.tsx (criado em plan 01)
    - .claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md (especialmente o exemplo de IdentificacaoSection no HTML pattern e a regra de layout grid)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (seção `### Form sections — card layout semântico` — regra cols-2)
  </read_first>
  <action>
    Refatorar o JSX do `<form>` mantendo TODO o código de hooks (useForm, useEffect com watch subscription, formatCnpj) intacto.

    **1. Imports novos:**
    ```typescript
    import { FormCard } from '../FormCard'
    import { FormCardRow } from '../FormCardRow'
    ```

    **2. Substituir o JSX dentro do `<form>` por 4 FormCardRow seguindo a estrutura do sketch:**

    Estrutura final (mantém max-w-2xl removido — cards usam largura natural até `max-w-4xl` para permitir cols-2):

    ```jsx
    <form className="flex flex-col gap-4" noValidate>

      {/* ROW 1 — full width: Dados da Empresa (campos críticos) */}
      <FormCardRow cols={1}>
        <FormCard
          icon={<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor" className="h-4 w-4"><path strokeLinecap="round" strokeLinejoin="round" d="M3 21h18M5 21V7l8-4v18M19 21V11l-6-4" /></svg>}
          iconColor="blue"
          title="Dados da Empresa"
          subtitle="Identificação básica no piloto"
        >
          <div className="flex flex-col gap-4">
            {/* Campo empresa */}
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Empresa / grupo<span className="text-g1 ml-0.5">*</span></label>
              <Input type="text" placeholder="Digite aqui..." error={!!errors.empresa} errorMessage={errors.empresa?.message} {...register('empresa')} />
            </div>
            {/* Campo cnpj com max-w para não esticar */}
            <div className="flex flex-col gap-1 max-w-xs">
              <label className="text-sm font-semibold text-gray-700">CNPJ principal<span className="text-g1 ml-0.5">*</span></label>
              <Input type="text" inputMode="numeric" placeholder="00.000.000/0000-00" maxLength={18} value={watch('cnpj') ?? ''} error={!!errors.cnpj} errorMessage={errors.cnpj?.message} onChange={(e) => setValue('cnpj', formatCnpj(e.target.value), { shouldValidate: true })} />
            </div>
          </div>
        </FormCard>
      </FormCardRow>

      {/* ROW 2 — cols-2: Reunião | Responsáveis (densidade similar, complementares, sem textareas longas) */}
      <FormCardRow cols={2}>
        <FormCard
          icon={<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor" className="h-4 w-4"><path strokeLinecap="round" strokeLinejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 0 1 2.25-2.25h13.5A2.25 2.25 0 0 1 21 7.5v11.25m-18 0A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75m-18 0v-7.5A2.25 2.25 0 0 1 5.25 9h13.5A2.25 2.25 0 0 1 21 11.25v7.5" /></svg>}
          iconColor="green"
          title="Reunião e Participantes"
          subtitle="Quando e quem participou"
        >
          <div className="flex flex-col gap-4">
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Data da reunião</label>
              <Input type="date" error={!!errors.dataReuniao} errorMessage={errors.dataReuniao?.message} {...register('dataReuniao')} />
            </div>
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Sponsor do piloto</label>
              <Input type="text" placeholder="Nome e cargo" error={!!errors.sponsorPiloto} errorMessage={errors.sponsorPiloto?.message} {...register('sponsorPiloto')} />
            </div>
            <TextareaField name="participantes" control={control} label="Participantes e papéis" placeholder="Diretoria, financeiro, engenharia..." error={errors.participantes?.message} />
          </div>
        </FormCard>

        <FormCard
          icon={<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor" className="h-4 w-4"><path strokeLinecap="round" strokeLinejoin="round" d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z" /></svg>}
          iconColor="amber"
          title="Responsáveis"
          subtitle="Quem responde por cada frente"
        >
          <div className="flex flex-col gap-4">
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Responsável técnico/Sienge</label>
              <Input type="text" placeholder="Nome e contato" error={!!errors.responsavelSienge} errorMessage={errors.responsavelSienge?.message} {...register('responsavelSienge')} />
            </div>
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Resp. habilitações/documentos</label>
              <Input type="text" placeholder="Nome e contato" error={!!errors.responsavelHabilitacoes} errorMessage={errors.responsavelHabilitacoes?.message} {...register('responsavelHabilitacoes')} />
            </div>
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Conduz oportunidades comerciais?</label>
              <Input type="text" placeholder="Diretoria, comercial..." error={!!errors.quemConduzComercial} errorMessage={errors.quemConduzComercial?.message} {...register('quemConduzComercial')} />
            </div>
          </div>
        </FormCard>
      </FormCardRow>

      {/* ROW 3 — full width: Escopo (2 campos curtos lado a lado) */}
      <FormCardRow cols={1}>
        <FormCard
          icon={<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor" className="h-4 w-4"><path strokeLinecap="round" strokeLinejoin="round" d="M3.75 3v11.25A2.25 2.25 0 0 0 6 16.5h2.25M3.75 3h-1.5m1.5 0h16.5m0 0h1.5m-1.5 0v11.25A2.25 2.25 0 0 1 18 16.5h-2.25m-7.5 0h7.5m-7.5 0-1 3m8.5-3 1 3m0 0 .5 1.5m-.5-1.5h-9.5m0 0-.5 1.5M9 11.25v1.5M12 9v3.75m3-6v6" /></svg>}
          iconColor="green"
          title="Escopo do Piloto"
          subtitle="Abrangência"
        >
          <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Nº de CNPJs/SPEs no escopo</label>
              <Input type="text" placeholder="Quantidade e observações" error={!!errors.numCnpjsEscopo} errorMessage={errors.numCnpjsEscopo?.message} {...register('numCnpjsEscopo')} />
            </div>
            <div className="flex flex-col gap-1">
              <label className="text-sm font-semibold text-gray-700">Nº de obras/empreendimentos</label>
              <Input type="text" placeholder="Quantidade, status e observações" error={!!errors.numObrasAtivas} errorMessage={errors.numObrasAtivas?.message} {...register('numObrasAtivas')} />
            </div>
          </div>
        </FormCard>
      </FormCardRow>

      {/* ROW 4 — full width: Prioridades (textareas lado a lado dentro do card) */}
      <FormCardRow cols={1}>
        <FormCard
          icon={<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth={1.5} stroke="currentColor" className="h-4 w-4"><path strokeLinecap="round" strokeLinejoin="round" d="m3.75 13.5 10.5-11.25L12 10.5h8.25L9.75 21.75 12 13.5H3.75Z" /></svg>}
          iconColor="purple"
          title="Prioridades"
          subtitle="Principais focos do piloto"
        >
          <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
            <TextareaField name="prioridadeTorre360" control={control} label="Principal prioridade para Torre 360" placeholder="Descreva a principal prioridade..." error={errors.prioridadeTorre360?.message} />
            <TextareaField name="prioridadeHabilitacoes" control={control} label="Principal prioridade para Habilitações" placeholder="Descreva a principal prioridade..." error={errors.prioridadeHabilitacoes?.message} />
          </div>
        </FormCard>
      </FormCardRow>

    </form>
    ```

    **3. PRESERVAR sem mudar:**
    - useForm config completo (resolver, defaultValues, mode).
    - useEffect com `watch((values) => store.updateSection(TabKey.Identificacao, ...))`.
    - Helper `formatCnpj`.
    - Imports de `useForm`, `zodResolver`, `useFormStore`, `TabKey`, `identificacaoSchema`, `Input`, `TextareaField`.

    **4. REMOVER:**
    - Todos os `<hr className="my-6 border-gray-100" />` (cards substituem separadores).
    - Comentários de "Grupo 1: ... Grupo 5: ..." (estrutura semântica agora explícita nos títulos dos cards).
    - Classe `max-w-2xl` no `<form>` (substituída por `max-w-4xl` para acomodar cols-2 em telas largas).

    **5. Decisão sobre os ícones SVG (Claude's discretion per CONTEXT.md):** os 5 ícones inline acima são da biblioteca Heroicons (outline 24×24 com strokeWidth=1.5, redimensionados para `h-4 w-4`). Manter strokes monocromos — o background colorido do card-icon vem do `FormCard iconColor`. NÃO usar emojis.

    **6. NÃO USAR:**
    - Dependências novas (heroicons npm, lucide etc.). SVG inline.
    - Mudança em schema, validação ou contrato com o store.

  </action>
  <acceptance_criteria>
    - `grep -c "import.*FormCard" roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `>= 2` (FormCard + FormCardRow)
    - `grep -c "<FormCard " roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `5` (5 cards: Dados, Reunião, Responsáveis, Escopo, Prioridades)
    - `grep -c "<FormCardRow" roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `4`
    - `grep -c 'iconColor="blue"\|iconColor="amber"\|iconColor="green"\|iconColor="purple"' roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `>= 5`
    - `grep -c 'cols={2}' roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `>= 1` (Reunião | Responsáveis lado a lado)
    - `grep -c "<hr " roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `0` (separadores removidos)
    - `grep -c "register('empresa')\|register('cnpj')\|register('dataReuniao')\|register('sponsorPiloto')\|register('responsavelSienge')\|register('responsavelHabilitacoes')\|register('quemConduzComercial')\|register('numCnpjsEscopo')\|register('numObrasAtivas')" roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `>= 9` (todos os 9 registers preservados)
    - `grep -c 'name="participantes"\|name="prioridadeTorre360"\|name="prioridadeHabilitacoes"' roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `>= 3` (3 textareas preservadas)
    - `grep -c "formatCnpj\|setValue" roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `>= 2` (máscara CNPJ preservada)
    - `grep -c "store.updateSection.*TabKey.Identificacao\|watch((values)" roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx` retorna `>= 1` (sync RHF→store preservado)
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
    - `cd roteiro-unificado && npm run lint` sem novos erros
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>IdentificacaoSection renderiza 5 cards em 4 rows (1 full, 1 cols-2, 2 full); todos os 12 campos do schema preservados; validação/máscara/sync funcionando.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                              |
| ---------------------------- | ------------------------------------------------------------------------ |
| user input → RHF → formStore | Validação Zod inalterada — entrada continua passando pelo mesmo resolver |

## STRIDE Threat Register

| Threat ID | Category  | Component                | Disposition | Mitigation Plan                                                                                                                                                 |
| --------- | --------- | ------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09.5-11 | Tampering | IdentificacaoSection JSX | accept      | Refactor é puramente visual; lógica de validação Zod (identificacaoSchema), máscara CNPJ e sync para store permanecem idênticas. Sem nova superfície de ataque. |

</threat_model>

<verification>
- `npm run build` verde.
- `npm run lint` sem novos erros.
- Manualmente (verificado no plan 05 checkpoint): preencher CNPJ — máscara funciona; deixar empresa em branco e dar blur — erro inline aparece; navegar para outra aba e voltar — dados preservados no store.
</verification>

<success_criteria>

- IdentificacaoSection refatorado em 5 cards semânticos com ícones coloridos.
- Layout responsivo: cards menores lado a lado em >= 768px, empilham em mobile.
- Zero regressão funcional: todos os 12 campos, validação Zod, máscara CNPJ e sync para store mantidos.
- Plan 05 pode usar este section como referência para refatorar os 9 restantes.
  </success_criteria>

<output>
Create `.planning/phases/09.5-layout-design-system-overhaul/09.5-04-SUMMARY.md` when done.
</output>
