---
phase: 09.5-layout-design-system-overhaul
plan: 05
type: execute
wave: 3
depends_on:
  - 09.5-02
  - 09.5-03
  - 09.5-04
files_modified:
  - roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx
  - roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx
  - roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx
  - roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx
  - roteiro-unificado/src/features/form/sections/HabVendaSection.tsx
  - roteiro-unificado/src/features/form/sections/HabRepositoriosSection.tsx
  - roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx
  - roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx
  - roteiro-unificado/src/features/form/sections/NdaSection.tsx
autonomous: false
requirements:
  - UX-01
  - UX-03
  - UX-06

must_haves:
  truths:
    - 'Todas as 9 sections restantes (Torre Decisão, Sienge, Acesso, Classificação, Hab Venda, Repositórios, Responsáveis, Classificação, NDA) usam FormCard + FormCardRow'
    - 'Cada section tem 2-5 cards organizados por grupos semânticos (sem hr separadores)'
    - 'Zero regressão funcional: schemas Zod, validação, controllers RHF e sync para formStore inalterados'
    - 'Sidebar admin colapsa/expande corretamente e persiste estado entre reloads'
    - 'Footer universal aparece em todas as 10 abas do formulário com navegação prev/next funcional'
    - "AutosaveIndicator mostra 'Salvo há X min' após preencher campo e aguardar 1.5s"
  artifacts:
    - path: 'roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx'
      provides: 'Torre Decisão em cards'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx'
      provides: 'Torre Sienge em cards (12 módulos preservados)'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx'
      provides: 'Torre Acesso em cards'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx'
      provides: 'Torre Classificação em cards'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/HabVendaSection.tsx'
      provides: 'Hab Venda em cards (10 cenários preservados)'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/HabRepositoriosSection.tsx'
      provides: 'Hab Repositórios em cards (14 domínios preservados)'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx'
      provides: 'Hab Responsáveis em cards (10 atividades preservadas)'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx'
      provides: 'Hab Classificação em cards'
      contains: 'FormCard'
    - path: 'roteiro-unificado/src/features/form/sections/NdaSection.tsx'
      provides: 'NDA em cards (texto NDA + aceite + dados signatário)'
      contains: 'FormCard'
  key_links:
    - from: '9 section files'
      to: 'FormCard / FormCardRow'
      via: 'import + wrap de grupos semânticos'
      pattern: 'import.*FormCard'
---

<objective>
Aplicar o padrão `FormCard` + `FormCardRow` (validado em IdentificacaoSection — Plan 04) às 9 sections restantes do formulário, e fechar a fase com checkpoint humano cobrindo todos os fluxos visuais: sidebar admin colapsável, topbar branca, footer universal, autosave indicator, cards em todas as 10 abas.

Purpose: Sem este plan a refatoração visual fica incompleta — 9 sections continuariam com `<hr>` separators no estilo legacy. UX-06 (formulário redesenhado) exige consistência em todas as abas.

Output: 9 sections refatoradas (~similar effort cada — substituir grupos delimitados por `<hr>` por FormCards) + verificação humana visual end-to-end blocking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md
@.claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md

<interfaces>
<!-- Padrão de referência -->
From roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx (após Plan 04):
- Imports: FormCard, FormCardRow do '../FormCard' e '../FormCardRow'
- Estrutura: <form className="flex flex-col gap-4">
- Cards organizados em FormCardRow cols={1|2}
- iconColor cycles: blue (principal/identificação), green (escopo/dados), amber (pessoas/responsáveis), purple (estratégia/prioridades)

<!-- Componentes do plan 01 -->

From roteiro-unificado/src/features/form/FormCard.tsx:

```typescript
export interface FormCardProps {
  icon: ReactNode;
  iconColor: 'blue' | 'amber' | 'green' | 'purple';
  title: string;
  subtitle?: string;
  children: ReactNode;
  className?: string;
}
```

From roteiro-unificado/src/features/form/FormCardRow.tsx:

```typescript
export interface FormCardRowProps {
  cols?: 1 | 2;
  children: ReactNode;
  className?: string;
}
```

<!-- Sections existentes (PRESERVAR todo o código de useForm/watch/store sync) -->

Todos seguem o mesmo padrão:

- useForm com zodResolver + defaultValues do store
- useEffect com watch subscription → store.updateSection(TabKey.X, values)
- JSX com `<form className="max-w-2xl">` ou similar contendo grupos separados por `<hr>` ou divs com `mb-6`
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refatorar 4 sections da Torre (Decisão, Sienge, Acesso, Classificação)</name>
  <files>roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx, roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx, roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx, roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx (PADRÃO DE REFERÊNCIA após Plan 04 — replicar estrutura)
    - roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx (versão atual — identificar grupos semânticos)
    - roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx (versão atual — entender a estrutura de 12 módulos × 5 colunas; cada módulo é um candidato a card OU mantém-se em layout único de tabela/grid dentro de UM card "Módulos do Sienge")
    - roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx (versão atual)
    - roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx (versão atual)
    - .claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md (regra cols-2 e CSS patterns)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (seção `### Form sections — card layout semântico` — Claude's discretion sobre ordem dos cards e ícones SVG)
  </read_first>
  <action>
    Para cada uma das 4 sections, aplicar a transformação seguindo o padrão estabelecido por IdentificacaoSection (Plan 04):

    **Padrão genérico de refactor:**
    1. Adicionar imports: `import { FormCard } from '../FormCard'` e `import { FormCardRow } from '../FormCardRow'`.
    2. Identificar grupos semânticos no JSX atual (geralmente delimitados por `<hr>`, comentários `{/* Grupo N */}`, ou `<div className="mb-6">`).
    3. Substituir o `<form className="max-w-2xl">` por `<form className="flex flex-col gap-4">`.
    4. Wrap cada grupo em `<FormCard icon={<svg ...>} iconColor="..." title="..." subtitle="...">`.
    5. Organizar cards em `<FormCardRow cols={1|2}>` aplicando a regra de cols-2: cards com densidade similar, complementares, sem textareas longas → lado a lado; cards com textareas/conteúdo longo → full-width.
    6. Remover todos os `<hr>` separators e comentários "Grupo N".
    7. Preservar 100% do código de hooks (useForm, useEffect com watch subscription, helpers como formatCnpj se houver).

    **Decisões específicas por section (Claude's discretion):**

    **TorreDecisaoSection** — sugestão de cards:
    - Card 1 (blue, full-width): "Estrutura de Decisão" — frequência de reunião, modelo de info, BI (com ConditionalField "Qual BI?")
    - Card 2 (green, cols-2): "Nível Gerencial" + "Indicadores Atuais"
    - Card 3 (purple, full-width): "Decisões a melhorar" (CheckboxGroupField) + textareas de plano

    **TorreSiengeSection** — sugestão de cards:
    - Card 1 (blue, full-width): "Contexto Sienge" — módulos contratados (overview)
    - Card 2 (amber, full-width): "Módulos do Sienge" — wrap em UM card grande contendo a grid existente de 12 módulos × 5 colunas (NÃO transformar cada módulo em card separado — fragmentaria demais)
    - Decisão: o card "Módulos do Sienge" pode ter `subtitle="12 módulos avaliados em 5 dimensões"` para contextualizar a grid

    **TorreAcessoSection** — sugestão de cards:
    - Card 1 (blue, cols-2): "Acesso a Dados" (4 selects iniciais) + "Ferramentas Atuais" (CheckboxGroupField com 6 opções)
    - Card 2 (amber, full-width): "Processos de Acesso" — 4 selects restantes + textareas

    **TorreClassificacaoSection** — sugestão de cards:
    - Card 1 (blue, full-width): "Classificação Torre 360" — T360-A..E (5 selects empilhados)
    - Card 2 (green, full-width): "Abordagem" — select abordagem + 5 textareas de plano em grid
    - Card 3 (purple, full-width): "Evidências" — CheckboxGroupField de 9 evidências

    **Ícones SVG (Claude's discretion):** usar Heroicons outline 24×24 strokeWidth=1.5 redimensionados para `h-4 w-4` (mesmo padrão de Plan 04). Sugestões:
    - Torre Decisão: ícone de gráfico/dashboard
    - Torre Sienge: ícone de banco de dados/servidor
    - Torre Acesso: ícone de chave/lock
    - Torre Classificação: ícone de checklist/trophy

    **PRESERVAR em TODAS as 4 sections:**
    - `useForm` config (resolver, defaultValues, mode).
    - `useEffect` com `subscription = watch((values) => store.updateSection(TabKey.X, values))`.
    - Imports de `Input`, `SelectField`, `TextareaField`, `RadioGroupField`, `CheckboxGroupField`, `ConditionalField` (apenas adicionar imports novos de FormCard/FormCardRow).
    - Schemas Zod, constantes de options (reuniaoOptions, infoOptions, biOptions, etc.).
    - Lógica de `unregister` em ConditionalField (TorreDecisaoSection).

    **REMOVER em todas:**
    - `<hr className="...">` separadores.
    - Comentários `{/* Grupo N: ... */}`.
    - `max-w-2xl` no `<form>` (substituir por `max-w-4xl flex flex-col gap-4`).

  </action>
  <acceptance_criteria>
    - Para CADA uma das 4 sections, `grep -c "import.*FormCard" <file>` retorna `>= 2` (FormCard + FormCardRow)
    - Para CADA uma das 4 sections, `grep -c "<FormCard " <file>` retorna `>= 2` (mínimo 2 cards)
    - Para CADA uma das 4 sections, `grep -c "<hr " <file>` retorna `0` (separadores removidos)
    - Para CADA uma das 4 sections, `grep -c "watch((values)\|store.updateSection" <file>` retorna `>= 1` (sync RHF→store preservado)
    - TorreSiengeSection: `grep -c "moduleSchema\|módulos\|modulosSienge" roteiro-unificado/src/features/form/sections/TorreSiengeSection.tsx` retorna `>= 1` (lógica dos 12 módulos preservada)
    - TorreDecisaoSection: `grep -c "ConditionalField\|biOptions" roteiro-unificado/src/features/form/sections/TorreDecisaoSection.tsx` retorna `>= 1` (campo condicional BI preservado)
    - TorreAcessoSection: `grep -c "CheckboxGroupField" roteiro-unificado/src/features/form/sections/TorreAcessoSection.tsx` retorna `>= 1` (checkbox group preservado)
    - TorreClassificacaoSection: `grep -c "CheckboxGroupField" roteiro-unificado/src/features/form/sections/TorreClassificacaoSection.tsx` retorna `>= 1` (9 evidências preservadas)
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
    - `cd roteiro-unificado && npm run lint` sem novos erros
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>4 sections da Torre 360 refatoradas em cards semânticos; toda a lógica preservada; build/lint verdes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refatorar 5 sections de Habilitações + NDA (Hab Venda, Repositórios, Responsáveis, Classificação, NDA)</name>
  <files>roteiro-unificado/src/features/form/sections/HabVendaSection.tsx, roteiro-unificado/src/features/form/sections/HabRepositoriosSection.tsx, roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx, roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx, roteiro-unificado/src/features/form/sections/NdaSection.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/sections/IdentificacaoSection.tsx (PADRÃO DE REFERÊNCIA após Plan 04)
    - roteiro-unificado/src/features/form/sections/HabVendaSection.tsx (versão atual — 6 flat + 10 cenários × 5 colunas)
    - roteiro-unificado/src/features/form/sections/HabRepositoriosSection.tsx (versão atual — checkbox + 4 selects + textarea + 14 domínios)
    - roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx (versão atual — checkbox + 4 selects + textarea + 10 atividades)
    - roteiro-unificado/src/features/form/sections/HabClassificacaoSection.tsx (versão atual — 4 selects + 5 textareas)
    - roteiro-unificado/src/features/form/sections/NdaSection.tsx (versão atual — texto + 4 InputFields + Controller boolean + textarea)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (Claude's discretion sobre ordem dos cards)
  </read_first>
  <action>
    Mesma estratégia do Task 1, aplicada a 5 sections de Habilitações + NDA.

    **Sugestão de cards por section (Claude's discretion):**

    **HabVendaSection:**
    - Card 1 (blue, full-width): "Visão Geral de Venda" — 6 campos flat
    - Card 2 (amber, full-width): "Cenários de Venda" — wrap em UM card a grid de 10 cenários × 5 colunas (mesma decisão do TorreSienge — não fragmentar)

    **HabRepositoriosSection:**
    - Card 1 (blue, cols-2): "Tipos de Repositório" (CheckboxGroupField) + "Configuração" (4 selects)
    - Card 2 (green, full-width): "Observações" (textarea)
    - Card 3 (amber, full-width): "Domínios do Repositório" — grid de 14 domínios × 5 colunas

    **HabResponsaveisSection:**
    - Card 1 (blue, cols-2): "Tipos de Responsabilidade" (CheckboxGroupField) + "Detalhamento" (4 selects)
    - Card 2 (green, full-width): "Observações" (textarea)
    - Card 3 (amber, full-width): "Atividades" — grid de 10 atividades × 5 colunas

    **HabClassificacaoSection:**
    - Card 1 (blue, full-width): "Classificação Habilitações" — 4 selects (hab-a..hab-e)
    - Card 2 (purple, full-width): "Plano por Dimensão" — 5 textareas em grid

    **NdaSection:**
    - Card 1 (blue, full-width): "Termo de Confidencialidade" — texto NDA scrollable + checkbox de aceite (Controller boolean)
    - Card 2 (amber, full-width): "Dados do Signatário" — 4 InputFields + textarea observações

    **Ícones SVG sugeridos:**
    - HabVenda: ícone de carrinho/handshake
    - HabRepositorios: ícone de pasta/database
    - HabResponsaveis: ícone de grupo de pessoas
    - HabClassificacao: ícone de medal/star
    - NDA: ícone de documento/cadeado

    **PRESERVAR em TODAS as 5 sections:**
    - useForm config, useEffect com watch subscription para store.
    - Imports de InputField, SelectField, TextareaField, CheckboxGroupField, RadioGroupField (apenas adicionar FormCard/FormCardRow).
    - Schemas Zod (especialmente nda com `z.literal(true)` no aceite).
    - Constantes NDA_TEXT, cenarioOptions, dominiosOptions, etc.
    - Controller boolean para checkbox de aceite no NdaSection.

    **CRÍTICO para NdaSection:** o texto NDA precisa permanecer scrollable dentro do card. Sugestão de styling do card body para esta seção:
    - O card "Termo de Confidencialidade" pode ter um div interno com `max-h-96 overflow-y-auto border border-gray-200 rounded p-3` envolvendo o texto NDA, seguido pelo checkbox de aceite logo abaixo (fora do scroll area).

    **REMOVER em todas:** `<hr>` e comentários "Grupo N", `max-w-2xl` no form (substituir por `max-w-4xl flex flex-col gap-4`).

  </action>
  <acceptance_criteria>
    - Para CADA uma das 5 sections, `grep -c "import.*FormCard" <file>` retorna `>= 2`
    - Para CADA uma das 5 sections, `grep -c "<FormCard " <file>` retorna `>= 2`
    - Para CADA uma das 5 sections, `grep -c "<hr " <file>` retorna `0`
    - Para CADA uma das 5 sections, `grep -c "watch((values)\|store.updateSection" <file>` retorna `>= 1`
    - HabVendaSection: `grep -c "cenarios\|10\|cenarioSchema" roteiro-unificado/src/features/form/sections/HabVendaSection.tsx` retorna `>= 1` (10 cenários preservados)
    - HabRepositoriosSection: `grep -c "CheckboxGroupField" roteiro-unificado/src/features/form/sections/HabRepositoriosSection.tsx` retorna `>= 1` (CheckboxGroupField preservado) e `grep -c "dominios\|14\|dominioSchema" roteiro-unificado/src/features/form/sections/HabRepositoriosSection.tsx` retorna `>= 1` (14 domínios preservados)
    - HabResponsaveisSection: `grep -c "CheckboxGroupField" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` retorna `>= 1` e `grep -c "atividades\|10\|atividadeSchema" roteiro-unificado/src/features/form/sections/HabResponsaveisSection.tsx` retorna `>= 1`
    - NdaSection: `grep -c "NDA_TEXT\|aceitaTermos\|aceito" roteiro-unificado/src/features/form/sections/NdaSection.tsx` retorna `>= 2` (texto NDA + aceite preservados); `grep -c "Controller\|z.literal(true)" roteiro-unificado/src/features/form/sections/NdaSection.tsx` retorna `>= 1` (Controller boolean preservado)
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
    - `cd roteiro-unificado && npm run lint` sem novos erros
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>5 sections de Habilitações + NDA refatoradas em cards semânticos; aceite NDA preservado; texto NDA scrollable dentro do card; build/lint verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — verificação visual end-to-end da Fase 9.5</name>
  <what-built>
    Plan 01 entregou tokens CSS (sidebar-width, header-height), fonte Inter, hook useSidebarCollapsed, componentes FormCard, FormCardRow, AutosaveIndicator.

    Plan 02 entregou: AdminSidebar colapsável (240↔60px) com toggle e user pill; AdminHeader branco com breadcrumb; AdminLayout com margin dinâmico.

    Plan 03 entregou: formStore.lastSavedAt + setLastSaved; useAutosave chama setLastSaved após sucesso; FormLayout com topbar de status (nome da aba + AutosaveIndicator) e footer universal prev/next/submit.

    Plan 04 entregou: IdentificacaoSection refatorado em 5 FormCards.

    Tasks 1 e 2 deste plan entregaram: 9 sections restantes refatoradas em FormCards (Torre Decisão, Sienge, Acesso, Classificação; Hab Venda, Repositórios, Responsáveis, Classificação; NDA).

  </what-built>
  <how-to-verify>
    Rodar `cd roteiro-unificado && npm run dev` e abrir http://localhost:5173.

    **A) Admin shell (login como admin):**
    1. Fazer login como usuário admin (credenciais do seed). Verificar que após login a tela `/admin/dashboard` carrega.
    2. Confirmar TOPBAR é BRANCA (não azul). Confirmar que a breadcrumb diz "Admin › Organizações".
    3. Confirmar que NÃO há mais botão "Encerrar sessão" no topbar (sign-out migrou para sidebar).
    4. Clicar no toggle (botão pill branco no canto direito da sidebar, ~12px da borda direita). Confirmar que:
       - Sidebar encolhe de 240px para 60px com transição visível (~250ms).
       - Main area acompanha (não há gap branco entre sidebar e conteúdo).
       - Labels dos nav items desaparecem (apenas ícones visíveis).
       - User pill no footer da sidebar mostra apenas o avatar (sem email, sem botão Sair).
    5. Clicar no toggle novamente. Confirmar que sidebar expande de volta para 240px, labels reaparecem, user pill mostra email truncado + botão Sair.
    6. Recarregar a página (F5). Confirmar que o estado da sidebar (expanded ou collapsed) persiste — está salvo em localStorage com key `sidebar_collapsed`.
    7. Navegar para `/admin/orgs/<algum-orgId>` clicando em uma org da listagem. Confirmar breadcrumb muda para "Admin › Organizações › Org <8chars>".
    8. No user pill da sidebar (expandida), clicar em "Sair". Confirmar que faz logout e redireciona para /login.

    **B) Formulário (login como construtora):**
    9. Fazer login como usuário de construtora (role company). Confirmar redirecionamento para `/form/:orgId`.
    10. Confirmar que aparece STRIP DE STATUS (h-10) no topo do conteúdo principal com:
        - Esquerda: nome da aba ativa (ex: "Identificação") em font-semibold.
        - Direita: AutosaveIndicator — inicialmente mostrando "Não salvo" (pois `lastSavedAt === null`).
    11. Confirmar que IdentificacaoSection mostra 5 CARDS em vez de campos soltos:
        - Card "Dados da Empresa" (ícone azul) full-width.
        - Cards "Reunião e Participantes" (verde) + "Responsáveis" (âmbar) LADO A LADO em >= 768px de largura.
        - Card "Escopo do Piloto" (verde) full-width.
        - Card "Prioridades" (roxo) full-width.
    12. Preencher CNPJ — confirmar que a máscara funciona (`00.000.000/0000-00`).
    13. Aguardar ~1.5s sem digitar. Confirmar que:
        - Toast "Salvo às HH:MM" aparece (autosave).
        - AutosaveIndicator no topbar muda para "Salvo há menos de 1 min" com ponto verde.
    14. Confirmar que existe FOOTER STICKY no rodapé com:
        - "← Anterior" (desabilitado/cinza pois Identificação é a primeira aba).
        - "Salvar rascunho" (link-style, cinza).
        - "Próxima aba →" (primary, laranja).
    15. Clicar "Próxima aba →". Confirmar que navega para "Torre Decisão", URL hash muda para `#torre-decisao`, e a strip de status atualiza o nome da aba.
    16. Confirmar que "Torre Decisão" também mostra CARDS (não mais hr separators). Confirmar que o ConditionalField "Qual BI?" aparece/desaparece corretamente ao mudar o select "Existe BI hoje?".
    17. Clicar "← Anterior". Confirmar volta para Identificação preservando os dados preenchidos.
    18. Navegar até a aba "NDA" (clicar Próxima múltiplas vezes ou usar TabNavigation na sidebar do formulário). Verificar em cada aba que:
        - Há pelo menos 2 cards.
        - Os campos do schema continuam funcionais.
        - "Torre Sienge" preserva a grid de 12 módulos × 5 colunas (dentro de um card grande).
        - "Hab Venda" preserva 10 cenários, "Hab Repositórios" 14 domínios, "Hab Responsáveis" 10 atividades.
    19. Na aba NDA, confirmar que:
        - O texto NDA está em um card e é scrollable.
        - O checkbox "Li e aceito os termos" está visível.
        - O footer mostra "Enviar Avaliação" NO LUGAR de "Próxima aba" (botão primary).
    20. Clicar "Enviar Avaliação" sem marcar o checkbox. Confirmar que aparece erro inline de validação (z.literal(true) bloqueia).

    **C) Responsividade:**
    21. Abrir DevTools, simular viewport 768px (tablet). Confirmar que:
        - Sidebar admin continua funcional.
        - Cards `cols={2}` se reorganizam para empilhar (1 coluna).
        - Footer do formulário continua visível e funcional.

    **D) Build de produção:**
    22. Rodar `cd roteiro-unificado && npm run build` — confirmar que completa sem erros.
    23. Rodar `cd roteiro-unificado && npm run lint` — confirmar zero novos erros (warnings ok).
    24. Rodar `cd roteiro-unificado && npx vitest run` — confirmar todos os testes passam.

    Registrar em VERIFICATION.md cada item (24 total) com PASS/FAIL e descrição da evidência (screenshot se possível ou nota textual).

  </how-to-verify>
  <resume-signal>
    Resposta esperada: "approved" (todos os 24 itens passaram) ou descrição numerada dos itens que falharam (ex: "item 13: AutosaveIndicator não atualiza após save — toast aparece mas indicator continua 'Não salvo'").

    Se houver falhas, será criado plan 06 (gap closure) para cobrir.

  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                 |
| ------------------------ | ----------------------------------------------------------- |
| user input → 9 RHF forms | Validação Zod inalterada — schemas não modificados          |
| Section JSX → store      | Sync via watch subscription preservado em todas as sections |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                                                         |
| --------- | ---------------------- | ---------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09.5-12 | Tampering              | 9 sections refatoradas | accept      | Refactor é puramente visual; lógica de validação, sync para store e schemas Zod permanecem idênticos. Plan 04 já validou o padrão sem regressão.                                        |
| T-09.5-13 | Information Disclosure | NdaSection texto NDA   | accept      | Texto NDA está em uma constante hardcoded (NDA_TEXT). Card wrap não muda quem pode ver — apenas reorganização visual.                                                                   |
| T-09.5-14 | Elevation of Privilege | NdaSection aceite      | mitigate    | Schema Zod com `z.literal(true)` continua validando o aceite no submit; refactor não altera essa garantia. Checkpoint humano (Task 3, item 20) confirma que submit bloqueia sem aceite. |

</threat_model>

<verification>
- Tasks 1 e 2: `npm run build` + `npm run lint` verdes; greps confirmam padrão FormCard aplicado em 9 sections; nenhum hr remanescente; sync RHF→store preservado.
- Task 3: 24 itens de UAT verificados manualmente pelo usuário; falhas geram plan de gap closure.
</verification>

<success_criteria>

- 9 sections refatoradas em cards semânticos seguindo o padrão de IdentificacaoSection.
- Zero regressão funcional: schemas, validação, sync, máscaras e Controllers preservados.
- Sidebar admin colapsa/expande com persistência; topbar branca com breadcrumb; footer universal em todas as 10 abas do formulário; AutosaveIndicator atualizando corretamente.
- Build, lint e testes verdes.
- Checkpoint humano aprovado (resposta "approved").
  </success_criteria>

<output>
Create `.planning/phases/09.5-layout-design-system-overhaul/09.5-05-SUMMARY.md` when done. Include UAT outcomes from Task 3 in the summary.
</output>
