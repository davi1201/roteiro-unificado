---
phase: 09.5-layout-design-system-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/index.css
  - roteiro-unificado/index.html
  - roteiro-unificado/src/hooks/useSidebarCollapsed.ts
  - roteiro-unificado/src/features/form/FormCard.tsx
  - roteiro-unificado/src/features/form/FormCardRow.tsx
  - roteiro-unificado/src/features/form/AutosaveIndicator.tsx
autonomous: true
requirements:
  - UX-01
  - UX-03
  - UX-06

must_haves:
  truths:
    - "Tokens `--sidebar-width`, `--sidebar-width-collapsed`, `--header-height` existem no @theme do index.css"
    - "Fonte Inter é carregada via Google Fonts no index.html"
    - "Hook `useSidebarCollapsed()` retorna `[collapsed, toggleCollapsed]` e persiste em localStorage com key `sidebar_collapsed`"
    - "Componente `FormCard` aceita props `icon`, `iconColor`, `title`, `subtitle`, `children` e renderiza header colorido + body"
    - "Componente `FormCardRow` aceita prop `cols={1|2}` e renderiza grid responsivo"
    - "Componente `AutosaveIndicator` aceita prop `lastSaved: Date | null` e exibe ponto verde + 'Salvo há X min'"
  artifacts:
    - path: "roteiro-unificado/src/index.css"
      provides: "Tokens de layout (sidebar/header) adicionados ao @theme"
      contains: "--sidebar-width"
    - path: "roteiro-unificado/index.html"
      provides: "Import Google Fonts Inter"
      contains: "fonts.googleapis.com"
    - path: "roteiro-unificado/src/hooks/useSidebarCollapsed.ts"
      provides: "Hook compartilhado de estado collapsed da sidebar admin"
      exports: ["useSidebarCollapsed"]
    - path: "roteiro-unificado/src/features/form/FormCard.tsx"
      provides: "Componente presentacional de card com header colorido"
      exports: ["FormCard"]
    - path: "roteiro-unificado/src/features/form/FormCardRow.tsx"
      provides: "Wrapper de grid de cards (1 ou 2 colunas)"
      exports: ["FormCardRow"]
    - path: "roteiro-unificado/src/features/form/AutosaveIndicator.tsx"
      provides: "Indicador visual de autosave"
      exports: ["AutosaveIndicator"]
  key_links:
    - from: "roteiro-unificado/src/hooks/useSidebarCollapsed.ts"
      to: "window.localStorage"
      via: "getItem/setItem com key 'sidebar_collapsed'"
      pattern: "sidebar_collapsed"
---

<objective>
Construir a base reutilizável da Fase 9.5: tokens CSS de layout, fonte Inter, hook de estado da sidebar admin, e componentes presentacionais (`FormCard`, `FormCardRow`, `AutosaveIndicator`) que serão consumidos pelas Waves 2 e 3.

Purpose: Plans 02/03/04/05 dependem desses primitivos. Sem eles, cada plan teria que repetir o mesmo CSS/HOC, gerando inconsistência visual e cross-plan file conflicts.

Output: 6 arquivos criados/modificados (4 novos, 2 existentes ampliados). Nenhum consumidor — Wave 2 conecta tudo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md
@.claude/skills/sketch-findings-roteiro-unificado/references/layout-navegacao.md
@.claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md
@.claude/skills/sketch-findings-roteiro-unificado/sources/themes/default.css

<interfaces>
<!-- Tailwind v4 @theme block (existing pattern from src/index.css) -->
<!-- Tokens são declarados dentro de @theme {} e ficam disponíveis como classes Tailwind (ex: bg-primary, w-sidebar). -->
<!-- IMPORTANTE: --color-primary atual usa oklch() com #123B66 como referência — NÃO substituir pelo #1e3a5f do sketch (decisão em CONTEXT.md). -->

Existing src/index.css @theme tokens that MUST be preserved:
- --color-primary: oklch(0.348 0.088 252.7) /* #123B66 */
- --color-accent: oklch(0.735 0.161 58.8) /* #F28C28 */
- --color-primary-{50..900}, --color-accent-{50..900}
- --color-g1..g5
- --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif
- --radius-sm, --radius-md, --radius-lg, --radius-xl

Existing useToast hook signature (src/hooks/useToast.ts):
- useToast() → { success, error, warning, loading, promise }
- toast.success(message: string, options?: { duration?: number })

Existing formStore selector signature (used by AutosaveIndicator):
- useFormStore(tenantId: string) → FormStore — NOTA: a store atual NÃO expõe lastSavedAt. Para AutosaveIndicator, receber lastSaved via prop neste plano (wiring fica para plan 03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar tokens de layout no @theme + importar fonte Inter</name>
  <files>roteiro-unificado/src/index.css, roteiro-unificado/index.html</files>
  <read_first>
    - roteiro-unificado/src/index.css (preservar todos os tokens existentes — paleta primary/accent, g1-g5, fontes, raios)
    - roteiro-unificado/index.html (entender estrutura atual antes de adicionar `<link>` Google Fonts)
    - .claude/skills/sketch-findings-roteiro-unificado/sources/themes/default.css (referência de tokens)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (seção `### CSS / Tailwind tokens`)
  </read_first>
  <action>
    Em `roteiro-unificado/src/index.css`, dentro do bloco `@theme {}` existente, adicionar (sem remover nenhum token existente):

    ```
    --sidebar-width: 240px;
    --sidebar-width-collapsed: 60px;
    --header-height: 56px;
    ```

    Esses tokens ficam disponíveis como classes Tailwind v4 (`w-sidebar`, `w-sidebar-collapsed`, `h-header`).

    NÃO alterar:
    - `--color-primary` (manter oklch atual = #123B66 — decisão CONTEXT.md, não usar #1e3a5f do sketch)
    - `--color-accent` (manter #F28C28)
    - Qualquer outro token de cor, fonte, raio ou g1-g5

    Em `roteiro-unificado/index.html`, dentro de `<head>`, adicionar (antes do `<title>` para evitar FOUT):

    ```html
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    ```

    Não modificar o `<title>`, `<meta>` ou o `<script type="module" src="/src/main.tsx">` existentes.
  </action>
  <acceptance_criteria>
    - `grep -c "sidebar-width:" roteiro-unificado/src/index.css` retorna `>= 1`
    - `grep -c "sidebar-width-collapsed:" roteiro-unificado/src/index.css` retorna `>= 1`
    - `grep -c "header-height:" roteiro-unificado/src/index.css` retorna `>= 1`
    - `grep -c "color-primary:" roteiro-unificado/src/index.css` retorna `>= 1` (token original preservado)
    - `grep -c "color-accent:" roteiro-unificado/src/index.css` retorna `>= 1` (token original preservado)
    - `grep -c "fonts.googleapis.com" roteiro-unificado/index.html` retorna `>= 1`
    - `grep -c "family=Inter" roteiro-unificado/index.html` retorna `>= 1`
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript ou de build
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Tokens de layout disponíveis para Tailwind; Inter carrega no browser; build verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar hook useSidebarCollapsed + componente AutosaveIndicator</name>
  <files>roteiro-unificado/src/hooks/useSidebarCollapsed.ts, roteiro-unificado/src/hooks/useSidebarCollapsed.test.ts, roteiro-unificado/src/features/form/AutosaveIndicator.tsx, roteiro-unificado/src/features/form/AutosaveIndicator.test.tsx</files>
  <read_first>
    - roteiro-unificado/src/hooks/useAutosave.ts (referência de pattern para hook + uso de toast/store)
    - roteiro-unificado/src/hooks/useToast.ts (signature do toast wrapper)
    - roteiro-unificado/src/stores/formStore.ts (entender que `lastSavedAt` NÃO está na store hoje — AutosaveIndicator deve receber via prop, não selecionar do store)
    - roteiro-unificado/vitest.config.ts (entender setup de testes)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (decisões sobre AutosaveIndicator)
  </read_first>
  <behavior>
    Hook `useSidebarCollapsed`:
    - Test 1: retorna `[false, fn]` quando localStorage está vazio (default expandido).
    - Test 2: retorna `[true, fn]` quando localStorage tem `sidebar_collapsed = "true"`.
    - Test 3: chamar a função toggle inverte o estado e escreve em localStorage (`"true"` → `"false"`).
    - Test 4: chamar toggle duas vezes volta ao estado original.

    Componente `AutosaveIndicator`:
    - Test 1: com `lastSaved={null}` e `isSaving={false}`, renderiza texto "Não salvo".
    - Test 2: com `isSaving={true}`, renderiza texto "Salvando...".
    - Test 3: com `lastSaved={new Date()}` e `isSaving={false}`, renderiza texto começando com "Salvo há" e contém ponto verde (elemento com classe `bg-g5` ou `bg-[var(--color-g4)]`).
    - Test 4: com `error={true}`, renderiza texto "Falha ao salvar" (cor vermelha — classe `text-g1` ou similar).
  </behavior>
  <action>
    **Hook `roteiro-unificado/src/hooks/useSidebarCollapsed.ts`:**

    Exportar named export `useSidebarCollapsed(): [boolean, () => void]`.

    Implementação:
    - `const [collapsed, setCollapsed] = useState<boolean>(() => { ... })` — initializer lê `localStorage.getItem('sidebar_collapsed') === 'true'`, com try/catch (proteção SSR e quota).
    - `useEffect(() => { try { localStorage.setItem('sidebar_collapsed', String(collapsed)) } catch {} }, [collapsed])` — persiste a cada mudança.
    - `const toggle = useCallback(() => setCollapsed((c) => !c), [])`.
    - Retornar `[collapsed, toggle]` (tupla, igual `useState`).

    A chave de localStorage DEVE ser exatamente `sidebar_collapsed` (snake_case, mesmo padrão do CONTEXT.md).

    **Testes `roteiro-unificado/src/hooks/useSidebarCollapsed.test.ts`:**

    Usar `@testing-library/react` `renderHook` e `act`. Limpar `localStorage.clear()` em `beforeEach`. Testar os 4 casos descritos em `<behavior>`.

    **Componente `roteiro-unificado/src/features/form/AutosaveIndicator.tsx`:**

    Props (interface explícita exportada):
    ```ts
    export interface AutosaveIndicatorProps {
      lastSaved: Date | null
      isSaving?: boolean
      error?: boolean
      className?: string
    }
    ```

    Renderização:
    - Container: `<div className="flex items-center gap-1.5 text-[11px] text-gray-500 {className}">`.
    - Se `error` → exibir `<span className="text-g1">Falha ao salvar</span>`.
    - Else if `isSaving` → exibir `<span>Salvando...</span>`.
    - Else if `lastSaved === null` → exibir `<span>Não salvo</span>`.
    - Else → exibir `<span className="h-1.5 w-1.5 rounded-full bg-g5" aria-hidden="true" />` seguido de `<span>Salvo há {formatRelativeMinutes(lastSaved)}</span>`.

    Helper interno `formatRelativeMinutes(date: Date): string`:
    - diff = (Date.now() - date.getTime()) / 60000.
    - Se `diff < 1` → "menos de 1 min".
    - Se `diff < 60` → `${Math.floor(diff)} min`.
    - Senão → `${Math.floor(diff/60)} h`.

    Componente é PURO PRESENTACIONAL — não usa `useFormStore`, não usa `useAutosave`. O wiring com `lastSavedAt` real fica no plano 03 (FormLayout passa via prop).

    **Testes `roteiro-unificado/src/features/form/AutosaveIndicator.test.tsx`:**

    Usar `@testing-library/react` `render` + `screen.getByText`. Cobrir os 4 cenários do `<behavior>`. Para o teste de "Salvo há", usar `lastSaved = new Date(Date.now() - 5 * 60_000)` e esperar texto contendo `5 min`.
  </action>
  <acceptance_criteria>
    - `grep -c "export function useSidebarCollapsed" roteiro-unificado/src/hooks/useSidebarCollapsed.ts` retorna `1`
    - `grep -c "sidebar_collapsed" roteiro-unificado/src/hooks/useSidebarCollapsed.ts` retorna `>= 2` (read + write)
    - `grep -c "export function AutosaveIndicator\|export const AutosaveIndicator" roteiro-unificado/src/features/form/AutosaveIndicator.tsx` retorna `1`
    - `grep -c "export interface AutosaveIndicatorProps" roteiro-unificado/src/features/form/AutosaveIndicator.tsx` retorna `1`
    - `grep -c "Salvo há\|Salvando\|Falha ao salvar" roteiro-unificado/src/features/form/AutosaveIndicator.tsx` retorna `>= 3` (todos os estados)
    - `cd roteiro-unificado && npx vitest run src/hooks/useSidebarCollapsed.test.ts` → todos os 4 testes passam
    - `cd roteiro-unificado && npx vitest run src/features/form/AutosaveIndicator.test.tsx` → todos os 4 testes passam
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npx vitest run src/hooks/useSidebarCollapsed.test.ts src/features/form/AutosaveIndicator.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>Hook persiste corretamente; AutosaveIndicator renderiza 4 estados; testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar componentes FormCard + FormCardRow (presentacionais)</name>
  <files>roteiro-unificado/src/features/form/FormCard.tsx, roteiro-unificado/src/features/form/FormCard.test.tsx, roteiro-unificado/src/features/form/FormCardRow.tsx, roteiro-unificado/src/features/form/FormCardRow.test.tsx</files>
  <read_first>
    - .claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md (CSS patterns completos: `.card`, `.card-header`, `.card-icon.{blue|amber|green|purple}`, `.card-body`, `.card-row.cols-{1|2}`)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (seção `### Form sections — card layout semântico`)
    - roteiro-unificado/src/lib/utils.ts (verificar se `cn()` está disponível — pattern já usado em AdminSidebar)
  </read_first>
  <behavior>
    `FormCard`:
    - Test 1: renderiza `title` no header e `children` no body.
    - Test 2: renderiza `subtitle` quando passado, não renderiza container de subtitle quando ausente.
    - Test 3: com `iconColor="blue"`, o ícone está dentro de elemento com classe `bg-primary` (token #123B66).
    - Test 4: com `iconColor="amber"`, o ícone está dentro de elemento com classe `bg-accent`.
    - Test 5: com `iconColor="green"`, o ícone está dentro de elemento com classe `bg-g5` ou cor verde explícita.
    - Test 6: com `iconColor="purple"`, o ícone está dentro de elemento com background roxo (classe `bg-purple-600` ou inline style `#7c3aed`).

    `FormCardRow`:
    - Test 1: com `cols={1}` renderiza container com classe `grid-cols-1`.
    - Test 2: com `cols={2}` renderiza container com classe `md:grid-cols-2` (responsivo — empilha em mobile).
    - Test 3: renderiza todos os `children` passados.
  </behavior>
  <action>
    **Componente `roteiro-unificado/src/features/form/FormCard.tsx`:**

    Props (interface explícita exportada):
    ```ts
    export type FormCardIconColor = 'blue' | 'amber' | 'green' | 'purple'

    export interface FormCardProps {
      icon: ReactNode
      iconColor: FormCardIconColor
      title: string
      subtitle?: string
      children: ReactNode
      className?: string
    }
    ```

    Estrutura JSX (Tailwind v4 — usar tokens do projeto, NUNCA hex inline para azul/laranja):
    ```
    <section className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm {className}">
      <header className="flex items-center gap-2.5 border-b border-gray-200 bg-gray-50 px-4 py-3">
        <span className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-md text-white ${iconColorClass}`}>{icon}</span>
        <div className="flex flex-col">
          <span className="text-[13px] font-bold text-gray-900">{title}</span>
          {subtitle && <span className="text-[11px] text-gray-500">{subtitle}</span>}
        </div>
      </header>
      <div className="p-4">{children}</div>
    </section>
    ```

    Mapping `iconColorClass` (object lookup, não switch):
    - `blue` → `'bg-primary'` (token #123B66 do projeto)
    - `amber` → `'bg-accent'` (token #F28C28 do projeto)
    - `green` → `'bg-g5'` (verde da escala G5)
    - `purple` → `'bg-[#7c3aed]'` (cor do sketch, sem token equivalente — usar arbitrary value)

    NÃO incluir lógica de store, controllers ou validação. Componente 100% presentacional.

    **Componente `roteiro-unificado/src/features/form/FormCardRow.tsx`:**

    Props:
    ```ts
    export interface FormCardRowProps {
      cols?: 1 | 2
      children: ReactNode
      className?: string
    }
    ```

    Estrutura JSX:
    ```
    <div className={cn('grid gap-4', cols === 2 ? 'grid-cols-1 md:grid-cols-2' : 'grid-cols-1', className)}>
      {children}
    </div>
    ```

    Default `cols = 1`. Em mobile (< 768px) sempre empilha (regra do CONTEXT.md "cards menores ficam lado a lado em viewport ≥ 768px; empilham em mobile").

    **Testes:**

    `FormCard.test.tsx`: 6 testes do `<behavior>`. Para os testes de cor, usar `container.querySelector('.bg-primary')` etc. e verificar que o elemento NÃO é null.

    `FormCardRow.test.tsx`: 3 testes do `<behavior>`. Verificar via `container.firstChild` que possui as classes esperadas.
  </action>
  <acceptance_criteria>
    - `grep -c "export function FormCard\|export const FormCard" roteiro-unificado/src/features/form/FormCard.tsx` retorna `1`
    - `grep -c "export interface FormCardProps" roteiro-unificado/src/features/form/FormCard.tsx` retorna `1`
    - `grep -c "bg-primary\|bg-accent\|bg-g5\|7c3aed" roteiro-unificado/src/features/form/FormCard.tsx` retorna `>= 4` (4 cores de ícone)
    - `grep -c "export function FormCardRow\|export const FormCardRow" roteiro-unificado/src/features/form/FormCardRow.tsx` retorna `1`
    - `grep -c "md:grid-cols-2" roteiro-unificado/src/features/form/FormCardRow.tsx` retorna `>= 1`
    - `cd roteiro-unificado && npx vitest run src/features/form/FormCard.test.tsx src/features/form/FormCardRow.test.tsx` → todos os testes passam
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npx vitest run src/features/form/FormCard.test.tsx src/features/form/FormCardRow.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>FormCard renderiza header colorido (4 cores) + body; FormCardRow alterna grid 1/2 cols responsivo; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localStorage | Leitura/escrita de `sidebar_collapsed` em localStorage (dado de UI, não sensível) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.5-01 | Tampering | useSidebarCollapsed | accept | Valor em localStorage é apenas estado de UI (boolean). Tampering apenas resulta em sidebar abrir/fechar — sem impacto de segurança. |
| T-09.5-02 | Denial of Service | useSidebarCollapsed | mitigate | Wrap localStorage.getItem/setItem em try/catch — sessões com quota excedida ou Safari private mode não quebram a UI. |
| T-09.5-03 | Information Disclosure | AutosaveIndicator | accept | Componente só renderiza timestamp relativo ("Salvo há 3 min") — sem PII, sem tokens, sem dados de form. |
</threat_model>

<verification>
- Build do projeto verde (`npm run build`) — todos os tokens TypeScript inferidos corretamente.
- Testes Vitest do hook + 3 componentes verdes (`npx vitest run`).
- Tokens `--sidebar-width`, `--sidebar-width-collapsed`, `--header-height` presentes no `@theme {}` do `index.css`.
- Fonte Inter carrega no browser ao rodar `npm run dev` (verificar Network tab).
- Hook `useSidebarCollapsed` persiste corretamente entre reloads (verificável manualmente, mas coberto por testes).
</verification>

<success_criteria>
- 4 arquivos novos criados: `useSidebarCollapsed.ts`, `FormCard.tsx`, `FormCardRow.tsx`, `AutosaveIndicator.tsx` (+ 4 arquivos de teste).
- 2 arquivos modificados: `index.css` (3 tokens novos), `index.html` (import Inter).
- Nenhum arquivo existente quebrado: `npm run build` + `npm run test` verdes.
- Wave 2 pode importar `FormCard`, `FormCardRow`, `AutosaveIndicator`, `useSidebarCollapsed` sem mais nenhum trabalho de setup.
</success_criteria>

<output>
Create `.planning/phases/09.5-layout-design-system-overhaul/09.5-01-SUMMARY.md` when done.
</output>
