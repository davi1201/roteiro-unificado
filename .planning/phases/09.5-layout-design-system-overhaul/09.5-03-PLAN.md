---
phase: 09.5-layout-design-system-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 09.5-01
files_modified:
  - roteiro-unificado/src/stores/formStore.ts
  - roteiro-unificado/src/hooks/useAutosave.ts
  - roteiro-unificado/src/features/form/FormLayout.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "FormLayout exibe topbar de status (h-10) acima do conteúdo com nome da aba ativa à esquerda e AutosaveIndicator à direita"
    - "FormLayout exibe footer sticky em TODAS as 10 abas com 'Anterior' / 'Salvar rascunho' / 'Próxima aba' (botão Enviar substitui 'Próxima aba' na aba NDA)"
    - "Botão 'Anterior' é desabilitado quando activeTab é a primeira aba (Identificação)"
    - "Botão 'Próxima aba' navega para a próxima aba do TAB_CONFIG e atualiza o hash da URL"
    - "AutosaveIndicator no topbar lê lastSavedAt do formStore e exibe 'Salvo há X min' após save bem-sucedido"
    - "Botão 'Enviar Avaliação' permanece exclusivo da aba NDA e abre o dialog de confirmação existente"
  artifacts:
    - path: "roteiro-unificado/src/stores/formStore.ts"
      provides: "Campo lastSavedAt: Date | null adicionado ao state + action setLastSaved"
      contains: "lastSavedAt"
    - path: "roteiro-unificado/src/hooks/useAutosave.ts"
      provides: "Hook atualizado para chamar setLastSaved(new Date()) após save bem-sucedido"
      contains: "setLastSaved"
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "Topbar de status + footer universal prev/next/submit"
      contains: "AutosaveIndicator"
  key_links:
    - from: "roteiro-unificado/src/features/form/FormLayout.tsx"
      to: "roteiro-unificado/src/features/form/AutosaveIndicator.tsx"
      via: "import + render com prop lastSaved"
      pattern: "<AutosaveIndicator"
    - from: "roteiro-unificado/src/features/form/FormLayout.tsx"
      to: "roteiro-unificado/src/features/form/tabConfig.ts"
      via: "TAB_CONFIG.findIndex para navegação prev/next"
      pattern: "TAB_CONFIG"
    - from: "roteiro-unificado/src/hooks/useAutosave.ts"
      to: "roteiro-unificado/src/stores/formStore.ts"
      via: "createFormStore(tenantId).getState().setLastSaved(new Date())"
      pattern: "setLastSaved"
---

<objective>
Refatorar o shell do formulário: adicionar topbar de status (nome da aba ativa + AutosaveIndicator) e footer sticky universal com botões prev/next em TODAS as abas, mantendo o botão "Enviar Avaliação" exclusivo da aba NDA.

Purpose: Hoje a navegação entre abas só acontece via sidebar (TabNavigation). Sketch finding 002 estabeleceu footer universal com prev/next como padrão de UX. AutosaveIndicator dá feedback contínuo do estado de save (UX-03 — indicador visual de progresso/status).

Output: 3 arquivos modificados. Nenhum schema/store de domínio alterado — apenas um campo `lastSavedAt` no formStore (ephemeral, não persistido). Dialog de submissão preservado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md
@.claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md

<interfaces>
<!-- Componente do plan 01 -->
From roteiro-unificado/src/features/form/AutosaveIndicator.tsx:
```typescript
export interface AutosaveIndicatorProps {
  lastSaved: Date | null
  isSaving?: boolean
  error?: boolean
  className?: string
}
export function AutosaveIndicator(props: AutosaveIndicatorProps): JSX.Element
```

<!-- Store atual (formStore.ts) — para adicionar lastSavedAt -->
From roteiro-unificado/src/stores/formStore.ts:
```typescript
export enum TabKey { Identificacao = 'identificacao', TorreDecisao = 'torre-decisao', ..., Nda = 'nda' }

interface FormState {
  currentStep: number
  completedSteps: Set<number>
  activeTab: TabKey
  visitedTabs: Set<TabKey>
  sectionData: Partial<Record<TabKey, Record<string, unknown>>>
}
// ESTE PLANO ADICIONA: lastSavedAt: Date | null + setLastSaved(date: Date | null): void

export function useFormStore(tenantId: string): FormStore
export function createFormStore(tenantId: string): StoreApi<FormStore>
```

<!-- TabConfig (ordem canônica das 10 abas) -->
From roteiro-unificado/src/features/form/tabConfig.ts:
```typescript
export interface TabConfig { key: TabKey; label: string }
export const TAB_CONFIG: TabConfig[] = [
  { key: TabKey.Identificacao, label: 'Identificação' },
  ... (8 abas intermediárias) ...
  { key: TabKey.Nda, label: 'NDA' },
]
// Primeira = TAB_CONFIG[0] = Identificacao. Última = TAB_CONFIG[9] = Nda.
```

<!-- useAutosave atual (será ampliado) -->
From roteiro-unificado/src/hooks/useAutosave.ts:
```typescript
export function useAutosave(tenantId: string): void
// Faz UPDATE/INSERT no Supabase com debounce 1500ms.
// Após sucesso: toastRef.current.success(`Salvo às HH:MM`)
// ESTE PLANO ADICIONA: chamada a createFormStore(tenantId).getState().setLastSaved(new Date()) após sucesso.
```

<!-- Persist partialize NÃO deve incluir lastSavedAt -->
<!-- Atualmente partialize só inclui: currentStep, completedSteps, activeTab, visitedTabs -->
<!-- Manter assim: lastSavedAt é ephemeral (re-deriva no próximo save). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar campo lastSavedAt no formStore + ampliar useAutosave para gravá-lo</name>
  <files>roteiro-unificado/src/stores/formStore.ts, roteiro-unificado/src/stores/formStore.test.ts, roteiro-unificado/src/hooks/useAutosave.ts, roteiro-unificado/src/hooks/useAutosave.test.ts</files>
  <read_first>
    - roteiro-unificado/src/stores/formStore.ts (entender persist + partialize — lastSavedAt NÃO deve ser persistido)
    - roteiro-unificado/src/stores/formStore.test.ts (entender padrão de teste do store)
    - roteiro-unificado/src/hooks/useAutosave.ts (entender fluxo de save existente — adicionar setLastSaved após sucesso, antes do toast)
    - roteiro-unificado/src/hooks/useAutosave.test.ts (entender mocks de supabase e padrão de teste)
  </read_first>
  <behavior>
    formStore:
    - Test 1: novo store tem `lastSavedAt === null` por default.
    - Test 2: `setLastSaved(new Date('2026-05-24'))` atualiza `lastSavedAt` para a data passada.
    - Test 3: `setLastSaved(null)` reseta para null.
    - Test 4: `lastSavedAt` NÃO é persistido em localStorage (verificar com `JSON.parse(localStorage.getItem('form-progress-test-tenant'))` — chave não deve existir no objeto persistido).

    useAutosave:
    - Test 1 (extensão do teste existente): após UPDATE bem-sucedido, `createFormStore(tenantId).getState().lastSavedAt` é uma instância de Date (não null).
    - Test 2 (extensão do teste existente): após INSERT bem-sucedido (sem draft existente), `lastSavedAt` também é Date.
    - Test 3 (caso novo): após save com erro, `lastSavedAt` NÃO é atualizado (permanece null se era null antes).
  </behavior>
  <action>
    **1. `formStore.ts` — adicionar campo + action:**

    Em `interface FormState`, adicionar:
    ```typescript
    lastSavedAt: Date | null
    ```

    Em `interface FormActions`, adicionar:
    ```typescript
    setLastSaved: (date: Date | null) => void
    ```

    Em `initialState`:
    ```typescript
    lastSavedAt: null
    ```

    Na factory do `create<FormStore>()`, dentro do callback de `persist`, adicionar a action:
    ```typescript
    setLastSaved: (date) => set({ lastSavedAt: date }),
    ```

    NO `partialize` da config de persist, **NÃO incluir `lastSavedAt`** (já é o comportamento atual — lista explícita não menciona; manter assim). Comentário acima do partialize: `// lastSavedAt é ephemeral — não persiste (re-deriva no próximo save bem-sucedido).`

    No `onRehydrateStorage`, garantir que `state.lastSavedAt = null` ao reidratar (defensivo — evita estado inconsistente se persist legacy retornar valor).

    No `resetForm()` action, adicionar `lastSavedAt: null` ao set.

    **2. `useAutosave.ts` — chamar setLastSaved após sucesso:**

    Dentro do callback de `setTimeout(async () => { ... })`, no branch de sucesso (após o `if (saveError) {...}` else):

    ANTES de chamar `toastRef.current.success(...)`, adicionar:
    ```typescript
    store.getState().setLastSaved(new Date())
    ```

    NO branch de erro (`if (saveError)`), NÃO chamar setLastSaved — manter o valor anterior intacto.

    **3. Testes `formStore.test.ts` (adicionar describe block novo):**

    ```typescript
    describe('lastSavedAt', () => {
      // Test 1: default null
      // Test 2: setLastSaved(new Date()) atualiza
      // Test 3: setLastSaved(null) reseta
      // Test 4: não persiste — após setLastSaved, ler localStorage e verificar que key não está no JSON
    })
    ```

    **4. Testes `useAutosave.test.ts` (adicionar describe block novo OU asserts nos testes existentes):**

    Após o save bem-sucedido (depois do `await flushPromises()` ou equivalente), assertar:
    ```typescript
    expect(createFormStore('test-tenant').getState().lastSavedAt).toBeInstanceOf(Date)
    ```

    Para o teste de erro, assertar que `lastSavedAt` permanece null:
    ```typescript
    expect(createFormStore('test-tenant').getState().lastSavedAt).toBeNull()
    ```

    **5. NÃO MUDAR:**
    - Signature de `useAutosave(tenantId)` (sem retorno).
    - Lógica de debounce 1500ms.
    - Estratégia SELECT-then-UPDATE-or-INSERT.
    - Toast wrapper (manter `toastRef.current.success`).
  </action>
  <acceptance_criteria>
    - `grep -c "lastSavedAt" roteiro-unificado/src/stores/formStore.ts` retorna `>= 3` (interface + initialState + reset)
    - `grep -c "setLastSaved" roteiro-unificado/src/stores/formStore.ts` retorna `>= 2` (interface FormActions + implementação)
    - `grep -c "setLastSaved" roteiro-unificado/src/hooks/useAutosave.ts` retorna `>= 1`
    - `grep -v '^//' roteiro-unificado/src/stores/formStore.ts | grep -A 5 "partialize:" | grep -c "lastSavedAt"` retorna `0` (NÃO persistido)
    - `cd roteiro-unificado && npx vitest run src/stores/formStore.test.ts` → todos os testes passam (incluindo novos)
    - `cd roteiro-unificado && npx vitest run src/hooks/useAutosave.test.ts` → todos os testes passam (incluindo novos)
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npx vitest run src/stores/formStore.test.ts src/hooks/useAutosave.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>formStore expõe lastSavedAt + setLastSaved; useAutosave grava timestamp após sucesso; testes verdes; nada persistido em localStorage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar topbar de status no FormLayout (nome da aba + AutosaveIndicator)</name>
  <files>roteiro-unificado/src/features/form/FormLayout.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.tsx (versão atual — entender estrutura: ProgressBar, aside sidebar, main com h1, footer NDA)
    - roteiro-unificado/src/features/form/AutosaveIndicator.tsx (criado no plan 01 — assinatura de props)
    - roteiro-unificado/src/stores/formStore.ts (após Task 1 — acessar `store.lastSavedAt`)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (seção `### FormLayout.tsx — topbar do formulário`)
    - .claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md (CSS pattern do autosave + topbar)
  </read_first>
  <action>
    Modificar `FormLayout.tsx` para inserir uma faixa de status no topo do `<main>` (entre o aside da sidebar e o conteúdo da seção).

    **1. Import:**
    ```typescript
    import { AutosaveIndicator } from './AutosaveIndicator'
    ```

    **2. Dentro do `<main>` (atualmente: `<main className={`flex-1 p-4 md:p-6 ${... === Nda ? 'pb-20' : ''}`}>`), INSERIR ANTES do `<h1>`:**

    ```jsx
    <div className="-mx-4 mb-4 flex h-10 items-center justify-between border-b border-gray-200 bg-white px-4 md:-mx-6 md:px-6">
      <span className="text-[13px] font-semibold text-gray-900">{activeTabConfig.label}</span>
      <AutosaveIndicator lastSaved={store.lastSavedAt} />
    </div>
    ```

    Observação sobre o `-mx-4 md:-mx-6`: contrabalança o `p-4 md:p-6` do `<main>` para que a strip ocupe toda a largura. Se essa abordagem causar overflow horizontal, alternativa: refatorar o `<main>` para ter padding zero no topo e padding lateral apenas no wrapper interno do conteúdo. Preferir a versão `-mx`.

    **3. REMOVER o `<h1 className="text-xl font-semibold text-gray-900">{activeTabConfig.label}</h1>`** — o nome da aba agora vive na strip de status. NÃO remover `<ReadinessClassification tenantId={tenantId} />` — ele permanece logo abaixo da strip.

    **4. Manter intactos:**
    - Cross-tenant guard.
    - `useQuery` do draft.
    - `useEffect` de hidratação.
    - `useAutosave(tenantId)`.
    - `useEffect` de hash sync.
    - `ProgressBar` no topo (acima da strip — manter ordem atual).
    - Dialog de submissão.

    **5. NÃO ADICIONAR:**
    - Estado local de `isSaving` ou `error` — Task 1 garante que `lastSavedAt` é a única fonte. Para fase futura, expor flags adicionais; por ora, o `AutosaveIndicator` mostra "Não salvo" enquanto `lastSavedAt === null`.
  </action>
  <acceptance_criteria>
    - `grep -c "AutosaveIndicator" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 2` (import + render)
    - `grep -c "store.lastSavedAt\|lastSavedAt" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 1`
    - `grep -c "h-10\|status\|border-b" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 1` (strip presente)
    - `grep -v '^//' roteiro-unificado/src/features/form/FormLayout.tsx | grep -c "<h1"` retorna `0` (h1 removido, label migrou para strip)
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Strip de status aparece acima do conteúdo com label da aba à esquerda e AutosaveIndicator à direita; nada quebrado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Substituir footer da aba NDA por footer universal prev/next/submit</name>
  <files>roteiro-unificado/src/features/form/FormLayout.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.tsx (após Task 2 — versão com strip de status)
    - roteiro-unificado/src/features/form/tabConfig.ts (TAB_CONFIG — ordem canônica das 10 abas para prev/next)
    - .planning/phases/09.5-layout-design-system-overhaul/09.5-CONTEXT.md (seção `### FormLayout.tsx — footer sticky`)
    - .claude/skills/sketch-findings-roteiro-unificado/references/form-controls.md (CSS pattern do sticky footer)
  </read_first>
  <action>
    Substituir o bloco condicional atual de footer (que aparece SÓ na aba NDA) por um footer universal renderizado para TODAS as abas, com botões prev/next/save + botão Enviar exclusivo da NDA.

    **1. Derivar prev/next:**

    Logo abaixo da linha `const activeTabConfig = TAB_CONFIG.find(...) ?? TAB_CONFIG[0]`, adicionar:
    ```typescript
    const activeIndex = TAB_CONFIG.findIndex((t) => t.key === store.activeTab)
    const prevTab = activeIndex > 0 ? TAB_CONFIG[activeIndex - 1] : null
    const nextTab = activeIndex >= 0 && activeIndex < TAB_CONFIG.length - 1 ? TAB_CONFIG[activeIndex + 1] : null
    const isNdaTab = store.activeTab === TabKey.Nda

    function goToTab(key: TabKey) {
      store.setActiveTab(key)
      window.history.replaceState(null, '', `#${key}`)
      store.markTabVisited(key)
    }
    ```

    NOTA sobre `window.history.replaceState`: o FormLayout existente já usa esse padrão (em decisão Phase 5 — eslint-plugin-react-hooks v7 proíbe `window.location.hash = X` direto). Manter consistência.

    **2. REMOVER o bloco condicional:**
    ```jsx
    {store.activeTab === TabKey.Nda && !draftQuery.isLoading && (
      <div className="sticky bottom-0 flex justify-end border-t border-gray-200 bg-white px-4 py-4 md:px-6">
        <Button variant="primary" size="lg" ... >Enviar Avaliação</Button>
      </div>
    )}
    ```

    **3. ADICIONAR no mesmo lugar (fora do `<main>`, dentro do flex column do shell) o footer universal:**

    ```jsx
    {!draftQuery.isLoading && (
      <div className="sticky bottom-0 z-10 flex items-center justify-between border-t border-gray-200 bg-white px-4 py-3 md:px-6">
        <Button
          variant="ghost"
          size="md"
          disabled={!prevTab}
          onClick={() => prevTab && goToTab(prevTab.key)}
          aria-label="Aba anterior"
        >
          ← Anterior
        </Button>
        <div className="flex items-center gap-3">
          <Button
            variant="ghost"
            size="sm"
            className="text-gray-500 hover:text-gray-700 underline-offset-2 hover:underline"
            onClick={() => toast.info?.('Rascunho será salvo automaticamente em 1.5s') ?? toast.success('Salvo automaticamente')}
            aria-label="Salvar rascunho manualmente"
          >
            Salvar rascunho
          </Button>
          {isNdaTab ? (
            <Button
              variant="primary"
              size="md"
              onClick={() => setIsSubmitOpen(true)}
              aria-label="Enviar avaliação"
            >
              Enviar Avaliação
            </Button>
          ) : (
            <Button
              variant="primary"
              size="md"
              disabled={!nextTab}
              onClick={() => nextTab && goToTab(nextTab.key)}
              aria-label="Próxima aba"
            >
              Próxima aba →
            </Button>
          )}
        </div>
      </div>
    )}
    ```

    Sobre o handler de "Salvar rascunho": como o autosave já cuida disso, o botão é mais visual/reassurance. Use o toast wrapper existente; se `toast.info` não existir, fallback para `toast.success('Salvo automaticamente')`. NÃO disparar mutation manual — manter contrato do useAutosave intocado.

    **4. REMOVER `pb-20` do `<main>` quando estava aplicado na aba NDA** — agora todas as abas têm footer, então o padding inferior precisa ser consistente. Mudar para:
    ```jsx
    <main className="flex-1 p-4 pb-24 md:p-6 md:pb-24">
    ```
    O `pb-24` garante que o conteúdo não fica oculto atrás do footer sticky (24 × 0.25rem = 6rem ≈ 96px > altura típica do footer ~64px).

    **5. Dialog de submissão (`isSubmitOpen`) permanece intacto.**

    **6. NÃO MUDAR:**
    - `useAutosave` (autosave continua automático).
    - Cross-tenant guard.
    - Hash sync useEffect.
    - Lógica de hidratação.
  </action>
  <acceptance_criteria>
    - `grep -c "TAB_CONFIG.findIndex" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 1`
    - `grep -c "prevTab\|nextTab" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 4` (deriv + uso)
    - `grep -c "Próxima aba\|Anterior" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 2`
    - `grep -c "Salvar rascunho" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 1`
    - `grep -c "Enviar Avaliação" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 1` (preservado, agora dentro do footer universal)
    - `grep -v '^//' roteiro-unificado/src/features/form/FormLayout.tsx | grep -c "activeTab === TabKey.Nda && !draftQuery.isLoading"` retorna `0` (bloco condicional antigo removido)
    - `grep -c "goToTab\|window.history.replaceState" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 2`
    - `grep -c "pb-24\|pb-20" roteiro-unificado/src/features/form/FormLayout.tsx` retorna `>= 1` (espaço para footer)
    - `cd roteiro-unificado && npm run build` completa sem erros TypeScript
    - `cd roteiro-unificado && npm run lint` sem novos erros
  </acceptance_criteria>
  <verify>
    <automated>cd roteiro-unificado && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>Footer aparece em todas as 10 abas com prev/next/salvar; aba NDA mostra "Enviar Avaliação" no lugar de "Próxima aba"; navegação atualiza hash e store; dialog de submissão preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → formStore | Estado em memória; lastSavedAt é apenas marcador de UI |
| FormLayout → Supabase | Submissão via mutation existente (não alterada); RLS já garante isolamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.5-07 | Tampering | formStore lastSavedAt | accept | Campo é ephemeral; manipulação em DevTools só afeta display do indicador. Sem efeito de segurança. |
| T-09.5-08 | Information Disclosure | AutosaveIndicator | accept | Apenas exibe timestamp relativo; sem PII, sem dados de form. |
| T-09.5-09 | Denial of Service | goToTab handler | mitigate | Navegação prev/next valida `prevTab`/`nextTab` antes de chamar setActiveTab — evita undefined access; window.history.replaceState não dispara erro em edge cases. |
| T-09.5-10 | Elevation of Privilege | botão "Enviar Avaliação" | accept | Lógica de submissão (submitMutation) é inalterada — RLS já valida que apenas membros da org podem submeter. Botão só dispara dialog; submit real continua passando pelo Supabase. |
</threat_model>

<verification>
- `npm run build` verde.
- `npm run lint` sem novos erros.
- Testes do formStore + useAutosave verdes (incluindo novos asserts de lastSavedAt).
- Manualmente: ao abrir /form/:orgId, footer aparece em todas as abas; navegação prev/next funciona; aba NDA mostra "Enviar Avaliação"; após preencher campo e aguardar 1.5s, AutosaveIndicator atualiza para "Salvo há menos de 1 min".
</verification>

<success_criteria>
- formStore expõe `lastSavedAt: Date | null` e `setLastSaved` (não persistido).
- useAutosave grava timestamp após cada save bem-sucedido.
- FormLayout tem topbar de status com label da aba + AutosaveIndicator.
- FormLayout tem footer sticky universal em todas as 10 abas com prev/next/salvar.
- Aba NDA continua tendo botão "Enviar Avaliação" (agora dentro do footer universal).
- Dialog de submissão preservado; nenhuma regressão funcional.
</success_criteria>

<output>
Create `.planning/phases/09.5-layout-design-system-overhaul/09.5-03-SUMMARY.md` when done.
</output>
