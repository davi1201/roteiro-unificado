---
phase: 10-exporta-o-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/package.json
  - roteiro-unificado/package-lock.json
  - roteiro-unificado/src/lib/pdf/types.ts
  - roteiro-unificado/src/lib/pdf/styles.ts
  - roteiro-unificado/src/lib/pdf/PDFFieldRow.tsx
  - roteiro-unificado/src/lib/pdf/PDFSectionHeader.tsx
  - roteiro-unificado/src/lib/pdf/PDFFooter.tsx
  - roteiro-unificado/src/lib/pdf/index.ts
  - roteiro-unificado/src/lib/pdf/index.test.ts
  - roteiro-unificado/src/lib/pdf/PDFDocument.test.tsx
  - roteiro-unificado/src/features/form/ExportPdfButton.test.tsx
autonomous: true
requirements: [EXPORT-05]
must_haves:
  truths:
    - "@react-pdf/renderer está instalado e disponível para import"
    - "O entry point do chunk PDF (src/lib/pdf/index.ts) só pode ser carregado via dynamic import — nenhum import estático no bundle principal"
    - "Existem primitivos de PDF reutilizáveis (PDFFieldRow, PDFSectionHeader, PDFFooter) consumindo a paleta azul/laranja via hex direto"
    - "Os arquivos de teste Wave 0 existem como scaffolds executáveis"
  artifacts:
    - path: "roteiro-unificado/src/lib/pdf/types.ts"
      provides: "Contrato AssessmentPDFData e GeneratePDFOptions"
      contains: "AssessmentPDFData"
    - path: "roteiro-unificado/src/lib/pdf/styles.ts"
      provides: "COLORS (paleta) + StyleSheet compartilhado"
      contains: "123B66"
    - path: "roteiro-unificado/src/lib/pdf/index.ts"
      provides: "generateAndOpenPDF (entry imperativo do chunk)"
      exports: ["generateAndOpenPDF"]
    - path: "roteiro-unificado/src/lib/pdf/PDFFieldRow.tsx"
      provides: "Linha label/valor com fallback '—' (D-03)"
    - path: "roteiro-unificado/src/lib/pdf/PDFSectionHeader.tsx"
      provides: "Barra de cabeçalho de seção azul"
    - path: "roteiro-unificado/src/lib/pdf/PDFFooter.tsx"
      provides: "Rodapé fixo com paginação"
  key_links:
    - from: "roteiro-unificado/src/lib/pdf/index.ts"
      to: "@react-pdf/renderer"
      via: "import estático de pdf() — permitido APENAS dentro do chunk pdf/"
      pattern: "from '@react-pdf/renderer'"
    - from: "roteiro-unificado/src/lib/pdf/index.ts"
      to: "window.open"
      via: "abertura de preview em nova aba (D-04)"
      pattern: "window\\.open"
---

<objective>
Estabelecer a fundação do chunk de exportação PDF: instalar `@react-pdf/renderer@4.5.1`, criar o entry point isolado para lazy-loading (EXPORT-05), os contratos de tipos, a paleta/StyleSheet compartilhada e os componentes primitivos reutilizáveis (linha de campo, cabeçalho de seção, rodapé). Também cria os scaffolds de teste do Wave 0.

Purpose: Isolar toda a dependência pesada `@react-pdf/renderer` em um chunk que nunca entra no bundle principal, e fornecer os contratos/primitivos que o Plan 02 (PDFDocument + seções) consumirá sem precisar explorar a base de código.
Output: Chunk `src/lib/pdf/` com entry imperativo, tipos, estilos, 3 primitivos PDF e 3 arquivos de teste scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-exporta-o-pdf/10-CONTEXT.md
@.planning/phases/10-exporta-o-pdf/10-RESEARCH.md
@.planning/phases/10-exporta-o-pdf/10-UI-SPEC.md

<interfaces>
<!-- Contratos extraídos da base de código — usar diretamente, sem explorar. -->

form_data (coluna JSONB em assessments) é indexado pelos valores string do enum TabKey:
'identificacao' | 'torre-decisao' | 'torre-sienge' | 'torre-acesso' | 'torre-classificacao'
| 'hab-venda' | 'hab-repositorios' | 'hab-responsaveis' | 'hab-classificacao' | 'nda'
Cada chave aponta para Record<string, unknown> (campos da aba). Pode estar ausente (aba não preenchida).

Aba Identificação (src/schemas/identificacao.ts) — campos:
  empresa, cnpj, cidadeUf, dataReuniao, participantes, sponsorPiloto, responsavelSienge,
  responsavelHabilitacoes, quemConduzComercial, numCnpjsEscopo, numObrasAtivas,
  prioridadeTorre360, prioridadeHabilitacoes

Aba Torre Sienge (src/schemas/torre-sienge.ts) — estrutura aninhada:
  form_data['torre-sienge'].modules.{slug}.{coluna}
  colunas: contratado, usoReal, confiancaDado, controleParalelo, observacoes
  SIENGE_MODULES exporta os 12 slugs + labels (cadastros, financeiro, ...)

readiness/grade vem das colunas assessments.readiness_level_mgmt (G1-G5) e readiness_level_tech.
</interfaces>

<paleta>
<!-- Cores hardcoded no contexto react-pdf (tokens Tailwind NÃO funcionam em StyleSheet). RESEARCH §Padrão 3 / UI-SPEC §Color. -->
primary #123B66 | accent #F28C28 | white #FFFFFF | textPrimary #111827 | textMuted #6B7280
textEmpty #9CA3AF | border #E5E7EB
G1 #B91C1C | G2 #F28C28 | G3 #D97706 | G4 #1D4ED8 | G5 #16A34A
</paleta>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @react-pdf/renderer e criar contratos + estilos do chunk</name>
  <files>roteiro-unificado/package.json, roteiro-unificado/src/lib/pdf/types.ts, roteiro-unificado/src/lib/pdf/styles.ts</files>
  <read_first>
    - roteiro-unificado/package.json (confirmar que @react-pdf/renderer ainda não está presente)
    - .planning/phases/10-exporta-o-pdf/10-RESEARCH.md §Auditoria de Legitimidade de Pacotes (pacote [ASSUMED], aprovado via npm view — checkpoint de legitimidade no Task 1.5)
    - .planning/phases/10-exporta-o-pdf/10-UI-SPEC.md §Color e §PDFDocument (valores hex e tipografia pt)
  </read_first>
  <action>
    Instalar a dependência exata via `cd roteiro-unificado && npm install @react-pdf/renderer@4.5.1` (versão verificada na RESEARCH, sem postinstall script). Criar `src/lib/pdf/types.ts` exportando a interface `AssessmentPDFData` (campos: orgName string, cnpj string|null, version number, grade string|null para o nível gerencial G1-G5, gradeTech string|null, generatedAt Date, formData Record<string, unknown>) e a interface `GeneratePDFOptions` (assessmentId string, version number, orgName string, cnpj string|null, grade string|null). Criar `src/lib/pdf/styles.ts` exportando a const `COLORS` (objeto com as 12 chaves de cor da seção paleta acima, valores hex como const assertion) e o `StyleSheet.create()` compartilhado com os estilos da seção PDFDocument do UI-SPEC: page (padding 40, fontFamily 'Helvetica', fontSize 10), coverPage, coverBrand, coverBrandAccent, coverOrgName, coverMeta, sectionHeader, sectionHeaderText, fieldRow, fieldLabel (width '40%'), fieldValue (width '60%'), fieldEmpty (cor textEmpty), footer (position absolute, fixed), footerText. Exportar também um helper `gradeColor(grade: string | null): string` que mapeia 'G1'..'G5' para COLORS.g1..g5 e retorna COLORS.textMuted como fallback. Importar `StyleSheet` de '@react-pdf/renderer'.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm ls @react-pdf/renderer && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "pdf/index\|PDFFieldRow\|PDFSectionHeader\|PDFFooter\|index.test\|PDFDocument.test\|ExportPdfButton.test" | grep -c "error TS" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @react-pdf/renderer` reporta versão 4.5.1 instalada
    - `src/lib/pdf/types.ts` exporta `AssessmentPDFData` e `GeneratePDFOptions`
    - `src/lib/pdf/styles.ts` exporta `COLORS` contendo a string '#123B66' (primary) e '#F28C28' (accent), o `StyleSheet` compartilhado e a função `gradeColor`
    - Nenhum erro TypeScript nos arquivos types.ts e styles.ts
  </acceptance_criteria>
  <done>Dependência instalada na versão correta; types.ts e styles.ts compilam e exportam os contratos esperados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1.5: Verificação de legitimidade do pacote @react-pdf/renderer</name>
  <what-built>Instalação de `@react-pdf/renderer@4.5.1` (classificado [ASSUMED] na RESEARCH — slopcheck indisponível em tempo de pesquisa).</what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/@react-pdf/renderer e confirmar: versão 4.5.1 publicada, ~900k downloads/semana, repositório github.com/diegomura/react-pdf.
    2. Confirmar que não há postinstall script suspeito: `cd roteiro-unificado && npm view @react-pdf/renderer@4.5.1 scripts`.
    3. Confirmar histórico do repositório (7+ anos, 15k+ stars).
  </how-to-verify>
  <resume-signal>Digite "approved" para confirmar a legitimidade ou descreva o problema encontrado.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Criar primitivos PDF — PDFFieldRow, PDFSectionHeader, PDFFooter</name>
  <files>roteiro-unificado/src/lib/pdf/PDFFieldRow.tsx, roteiro-unificado/src/lib/pdf/PDFSectionHeader.tsx, roteiro-unificado/src/lib/pdf/PDFFooter.tsx</files>
  <read_first>
    - roteiro-unificado/src/lib/pdf/styles.ts (estilos e COLORS criados no Task 1 — consumir diretamente)
    - .planning/phases/10-exporta-o-pdf/10-RESEARCH.md §Padrão 4 (rodapé fixo com render prop) e §Linha de campo com fallback "—"
    - .planning/phases/10-exporta-o-pdf/10-UI-SPEC.md §PDFDocument (header de seção, linha de campo, rodapé)
  </read_first>
  <action>
    Criar três componentes funcionais usando exclusivamente primitivos de '@react-pdf/renderer' (View, Text) e os estilos de `./styles`. `PDFFieldRow` recebe props `label: string` e `value: unknown`; computa display = se value não é undefined/null/'' então String(value) senão '—'; renderiza View(fieldRow) com Text(fieldLabel) e Text(fieldValue ou fieldEmpty quando display === '—') — implementa D-03 (campo vazio mostra "—" em cinza). `PDFSectionHeader` recebe `title: string` e renderiza View(sectionHeader) com Text(sectionHeaderText) — barra azul #123B66 com texto branco. `PDFFooter` renderiza View(footer) com a prop `fixed` e dois Text: esquerda "SuaEquipe.IA — Relatório Confidencial" (copy do UI-SPEC §Copywriting) e direita usando a render prop `render={({ pageNumber, totalPages }) => \`${pageNumber} / ${totalPages}\`}`. Nenhum hook React, nenhuma classe Tailwind, nenhum import de store/router neste módulo (RESEARCH §Anti-padrões).
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -L "className" src/lib/pdf/PDFFieldRow.tsx src/lib/pdf/PDFSectionHeader.tsx src/lib/pdf/PDFFooter.tsx | wc -l | grep -qx 3 && grep -q "fixed" src/lib/pdf/PDFFooter.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Os três arquivos não contêm a string `className` (não usam Tailwind dentro do react-pdf)
    - `PDFFieldRow.tsx` contém a lógica de fallback que renderiza `'—'` para valores vazios
    - `PDFFooter.tsx` usa a prop `fixed` e a render prop com `pageNumber` e `totalPages`
    - `PDFSectionHeader.tsx` aceita prop `title` e usa o estilo `sectionHeader`
    - Nenhum import de hooks React, Zustand ou react-router nos três arquivos
  </acceptance_criteria>
  <done>Três primitivos PDF criados, puros (sem hooks/Tailwind), consumindo styles.ts; rodapé fixo com paginação funcionando.</done>
</task>

<task type="auto">
  <name>Task 3: Criar entry imperativo (generateAndOpenPDF) + scaffolds de teste Wave 0</name>
  <files>roteiro-unificado/src/lib/pdf/index.ts, roteiro-unificado/src/lib/pdf/index.test.ts, roteiro-unificado/src/lib/pdf/PDFDocument.test.tsx, roteiro-unificado/src/features/form/ExportPdfButton.test.tsx</files>
  <read_first>
    - roteiro-unificado/src/lib/pdf/types.ts (GeneratePDFOptions criado no Task 1)
    - roteiro-unificado/src/lib/supabase.ts (cliente supabase — padrão de query .from().select().eq().single())
    - .planning/phases/10-exporta-o-pdf/10-RESEARCH.md §Padrão 2 (API imperativa pdf().toBlob() + window.open) e §Arquitetura de Validação (lacunas Wave 0)
  </read_first>
  <action>
    Criar `src/lib/pdf/index.ts` exportando `generateAndOpenPDF(opts: GeneratePDFOptions): Promise<void>`: (1) faz `supabase.from('assessments').select('id, version, form_data, readiness_level_mgmt, readiness_level_tech, submitted_at').eq('id', opts.assessmentId).single()` e lança Error('Erro ao buscar dados da avaliação') se error ou sem data; (2) gera o blob com `await pdf(<PDFDocument data={...} />).toBlob()` montando AssessmentPDFData a partir de opts + data (generatedAt = new Date(), grade = opts.grade, gradeTech = data.readiness_level_tech, formData = data.form_data); (3) `const url = URL.createObjectURL(blob); window.open(url, '_blank')`; (4) `setTimeout(() => URL.revokeObjectURL(url), 60000)`. Import de `pdf` de '@react-pdf/renderer' e de `PDFDocument` de './PDFDocument' — este import será resolvido no Plan 02. Para destravar este plano sem o PDFDocument real, criar um stub TEMPORÁRIO mínimo `src/lib/pdf/PDFDocument.tsx` exportando `PDFDocument` que retorna um `<Document><Page><Text>placeholder</Text></Page></Document>` aceitando prop `data: AssessmentPDFData` — o Plan 02 substitui este stub pela implementação completa (adicionar comentário `// STUB — substituído no Plan 10-02`). Criar os 3 scaffolds de teste: `src/lib/pdf/index.test.ts` (mock de supabase + mock de window.open; verifica que generateAndOpenPDF chama window.open com '_blank' — pode iniciar como `it.todo` se o mock de pdf() for complexo, mas o arquivo deve existir e rodar); `src/lib/pdf/PDFDocument.test.tsx` (it.todo para "campos vazios exibem '—'" e "renderiza com dados completos"); `src/features/form/ExportPdfButton.test.tsx` (it.todo para "botão só aparece em versões submitted", "estados IDLE/LOADING/ERROR", "aria-label por versão"). Cada arquivo de teste deve conter ao menos um bloco describe/it executável para que `npm test` não falhe por arquivo vazio.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -q "window.open" src/lib/pdf/index.ts && grep -q "revokeObjectURL" src/lib/pdf/index.ts && npm test -- --run src/lib/pdf/index.test.ts src/lib/pdf/PDFDocument.test.tsx src/features/form/ExportPdfButton.test.tsx 2>&1 | grep -qE "Test Files.*(passed|todo)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pdf/index.ts` exporta `generateAndOpenPDF`, contém `window.open(url, '_blank')` e `URL.revokeObjectURL`
    - `src/lib/pdf/index.ts` busca colunas `form_data, readiness_level_mgmt, readiness_level_tech` filtrando por `id`
    - Existe um stub `PDFDocument.tsx` marcado com o comentário `STUB — substituído no Plan 10-02`
    - Os 3 arquivos de teste existem e `npm test --run` sobre eles roda sem erro de coleta (passed ou todo)
  </acceptance_criteria>
  <done>Entry imperativo criado com fetch + blob + window.open + revoke; stub PDFDocument permite compilação; 3 scaffolds de teste executáveis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase | Query de `assessments` por `id` cruza para o banco (RLS aplica isolamento por org) |
| npm registry → build | Instalação de `@react-pdf/renderer` introduz código de terceiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | install @react-pdf/renderer@4.5.1 | mitigate | slopcheck indisponível → pacote [ASSUMED]; checkpoint blocking-human (Task 1.5) verifica npmjs.com/package, ausência de postinstall e histórico do repositório antes de prosseguir |
| T-10-02 | Information Disclosure | fetch de form_data por assessmentId | mitigate | RLS do Supabase (verificado Phase 2/8.1) bloqueia SELECT de assessments de outras orgs; query usa o id já filtrado pela RLS — sem bypass |
| T-10-SC | Tampering | npm install | mitigate | slopcheck + checkpoint blocking-human para o pacote [ASSUMED] (Task 1.5) — nunca auto-aprovável |
</threat_model>

<verification>
- `npm ls @react-pdf/renderer` → 4.5.1
- `npm run build` (executado no Plan 03) deve produzir chunk separado sem @react-pdf no index principal
- `npm test -- --run src/lib/pdf` roda os scaffolds sem erro de coleta
</verification>

<success_criteria>
- `@react-pdf/renderer@4.5.1` instalado e legitimidade verificada
- Chunk `src/lib/pdf/` com entry imperativo isolado (importável só via dynamic import)
- Contratos (types.ts), paleta/estilos (styles.ts) e 3 primitivos PDF criados
- 3 arquivos de teste Wave 0 executáveis
</success_criteria>

<output>
Create `.planning/phases/10-exporta-o-pdf/10-01-SUMMARY.md` when done
</output>
