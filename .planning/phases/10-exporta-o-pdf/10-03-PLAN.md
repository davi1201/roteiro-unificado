---
phase: 10-exporta-o-pdf
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - roteiro-unificado/src/features/form/useOrgInfo.ts
  - roteiro-unificado/src/features/form/ExportPdfButton.tsx
  - roteiro-unificado/src/features/form/ExportPdfButton.test.tsx
  - roteiro-unificado/src/features/form/HistoryPage.tsx
autonomous: false
requirements: [EXPORT-01, EXPORT-04, EXPORT-05]
must_haves:
  truths:
    - "Cada card de versão com status 'submitted' na HistoryPage exibe um botão 'Exportar PDF' (EXPORT-04)"
    - "Clicar 'Exportar PDF' faz lazy-load do chunk pdf, gera o PDF da versão clicada e abre preview em nova aba (D-04)"
    - "O botão mostra estado de loading ('Gerando...') durante a geração e dispara toast de erro em falha"
    - "O bundle principal (index.js) não contém @react-pdf/renderer — chunk é carregado só ao clicar (EXPORT-05)"
  artifacts:
    - path: "roteiro-unificado/src/features/form/useOrgInfo.ts"
      provides: "Hook leve para SELECT name, cnpj FROM orgs WHERE id = orgId"
      exports: ["useOrgInfo"]
    - path: "roteiro-unificado/src/features/form/ExportPdfButton.tsx"
      provides: "Botão com state machine IDLE/LOADING/ERROR + dynamic import do chunk"
      exports: ["ExportPdfButton"]
    - path: "roteiro-unificado/src/features/form/HistoryPage.tsx"
      provides: "Integração do ExportPdfButton em cards submitted"
      contains: "ExportPdfButton"
  key_links:
    - from: "roteiro-unificado/src/features/form/ExportPdfButton.tsx"
      to: "roteiro-unificado/src/lib/pdf/index.ts"
      via: "dynamic import — await import('@/lib/pdf/index')"
      pattern: "import\\('@/lib/pdf"
    - from: "roteiro-unificado/src/features/form/HistoryPage.tsx"
      to: "roteiro-unificado/src/features/form/ExportPdfButton.tsx"
      via: "render em cards submitted ao lado de 'Ver detalhes'"
      pattern: "ExportPdfButton"
---

<objective>
Conectar a geração de PDF à UI: criar o hook `useOrgInfo` (nome + CNPJ da org para a capa), o componente `ExportPdfButton` com state machine IDLE/LOADING/ERROR que faz dynamic import do chunk pdf (EXPORT-05) e gera o PDF da versão específica (EXPORT-04), e integrá-lo na HistoryPage em cada card submitted. Verificar via build que o chunk é isolado e validar visualmente o PDF gerado.

Purpose: Fechar o fluxo end-to-end de exportação na HistoryPage da construtora, confirmando lazy-loading efetivo e identidade visual correta.
Output: useOrgInfo + ExportPdfButton (testes completos) + HistoryPage integrada + checkpoint visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-exporta-o-pdf/10-CONTEXT.md
@.planning/phases/10-exporta-o-pdf/10-RESEARCH.md
@.planning/phases/10-exporta-o-pdf/10-UI-SPEC.md
@.planning/phases/10-exporta-o-pdf/10-01-SUMMARY.md
@.planning/phases/10-exporta-o-pdf/10-02-SUMMARY.md

<interfaces>
<!-- Chunk pdf (Plans 01-02) — consumir só via dynamic import. -->
src/lib/pdf/index.ts: generateAndOpenPDF(opts: GeneratePDFOptions): Promise<void>
  GeneratePDFOptions = { assessmentId, version, orgName, cnpj: string|null, grade: string|null }

<!-- HistoryPage existente (src/features/form/HistoryPage.tsx) — ponto de integração. -->
AssessmentRow = { id, version, status, submitted_at, readiness_level_mgmt, readiness_level_tech, created_at }
- bloco de botões de ação por card: <div className="flex shrink-0 items-center gap-2"> contém
  {isSubmitted && <Button variant="secondary" size="sm">Ver detalhes</Button>}
  e o botão "Iniciar Nova Revisão" (index 0, mostRecentIsSubmitted).
- HistoryPageContent recebe { orgId } e já roda dentro do cross-tenant guard.

<!-- Padrões do projeto. -->
useOrgDetail (src/features/admin/useOrgDetail.ts) faz SELECT id,name,cnpj,active,created_at FROM orgs — referência de padrão useQuery (queryKey ['orgs', orgId]).
Button (src/components/ui): variant 'secondary'|'primary'|'ghost', size 'sm', prop isLoading.
useToast (hook do projeto): .success() / .error() — NUNCA importar toast do sonner direto.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook useOrgInfo + ExportPdfButton com state machine e dynamic import</name>
  <files>roteiro-unificado/src/features/form/useOrgInfo.ts, roteiro-unificado/src/features/form/ExportPdfButton.tsx, roteiro-unificado/src/features/form/ExportPdfButton.test.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/admin/useOrgDetail.ts (padrão useQuery para orgs)
    - roteiro-unificado/src/components/ui/button.tsx (props variant/size/isLoading)
    - roteiro-unificado/src/hooks/useToast.ts (ou caminho real do hook — wrapper .error())
    - roteiro-unificado/src/features/form/ExportPdfButton.test.tsx (scaffold it.todo do Plan 01)
    - .planning/phases/10-exporta-o-pdf/10-RESEARCH.md §Padrão 1 (ExportPdfButton com dynamic import)
    - .planning/phases/10-exporta-o-pdf/10-UI-SPEC.md §Botão "Exportar PDF" — Máquina de Estados e §Acessibilidade
  </read_first>
  <behavior>
    - Test 1: Para uma versão `submitted`, o ExportPdfButton renderiza com texto "Exportar PDF" e `aria-label="Exportar PDF — Versão {N}"`.
    - Test 2: Ao clicar, entra em estado loading (texto "Gerando...", botão disabled, aria-busy="true") e chama a função de geração importada dinamicamente.
    - Test 3: Quando a geração rejeita, `useToast().error` é chamado com "Falha ao gerar PDF. Tente novamente." e o botão volta a IDLE.
  </behavior>
  <action>
    Criar `useOrgInfo(orgId: string | undefined)`: useQuery com queryKey ['org-info', orgId], queryFn fazendo `supabase.from('orgs').select('name, cnpj').eq('id', orgId).single()`, enabled !!orgId, staleTime 300_000; retorna { orgName: data?.name, cnpj: data?.cnpj ?? null, isLoading }. Criar `ExportPdfButton` com props { assessmentId: string; version: number; orgName: string; cnpj: string | null; grade: string | null }: usa useState<boolean> isLoading e useToast(); handler async handleExport faz setIsLoading(true), try { const { generateAndOpenPDF } = await import('@/lib/pdf/index'); await generateAndOpenPDF({ assessmentId, version, orgName, cnpj, grade }) } catch { toast.error('Falha ao gerar PDF. Tente novamente.') } finally { setIsLoading(false) }. Renderiza Button variant="secondary" size="sm" isLoading={isLoading} onClick={handleExport} aria-label={`Exportar PDF — Versão ${version}`} aria-busy={isLoading} com children isLoading ? 'Gerando...' : 'Exportar PDF'. NUNCA usar import estático de '@/lib/pdf/index' nem de '@react-pdf/renderer' (EXPORT-05 — só dynamic import). Completar ExportPdfButton.test.tsx implementando os 3 testes do behavior, mockando `@/lib/pdf/index` (vi.mock) e o useToast; remover it.todo.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -q "import('@/lib/pdf" src/features/form/ExportPdfButton.tsx && ! grep -E "^import .*@/lib/pdf/index" src/features/form/ExportPdfButton.tsx && ! grep -q "@react-pdf/renderer" src/features/form/ExportPdfButton.tsx && npm test -- --run src/features/form/ExportPdfButton.test.tsx 2>&1 | grep -qE "Test Files.*passed" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `useOrgInfo.ts` exporta `useOrgInfo` retornando orgName e cnpj via SELECT name, cnpj FROM orgs
    - `ExportPdfButton.tsx` usa `await import('@/lib/pdf/index')` (dynamic) e NÃO contém nenhum import estático top-level de '@/lib/pdf/index' nem de '@react-pdf/renderer'
    - O botão tem `aria-label` com o número da versão e `aria-busy` ligado ao estado loading
    - Erro de geração dispara `useToast().error('Falha ao gerar PDF. Tente novamente.')`
    - Os 3 testes de ExportPdfButton.test.tsx passam (não it.todo)
  </acceptance_criteria>
  <done>useOrgInfo + ExportPdfButton implementados com dynamic import e state machine; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar ExportPdfButton na HistoryPage</name>
  <files>roteiro-unificado/src/features/form/HistoryPage.tsx</files>
  <read_first>
    - roteiro-unificado/src/features/form/HistoryPage.tsx (estado atual — bloco de botões de ação por card)
    - roteiro-unificado/src/features/form/ExportPdfButton.tsx (Task 1)
    - roteiro-unificado/src/features/form/useOrgInfo.ts (Task 1)
    - .planning/phases/10-exporta-o-pdf/10-UI-SPEC.md §HistoryPage (layout "[Ver detalhes] [Exportar PDF]" side-by-side em gap-2)
  </read_first>
  <action>
    Em `HistoryPageContent`, chamar `const { orgName, cnpj } = useOrgInfo(orgId)` no topo (junto aos outros hooks). No bloco de botões de ação de cada card (a `<div className="flex shrink-0 items-center gap-2">`), adicionar, dentro do `{isSubmitted && (...)}`, o `<ExportPdfButton assessmentId={row.id} version={row.version} orgName={orgName ?? '—'} cnpj={cnpj} grade={row.readiness_level_mgmt} />` ao lado de "Ver detalhes" (Ver detalhes à esquerda, Exportar PDF à direita, no mesmo gap-2). Importar ExportPdfButton e useOrgInfo no topo do arquivo. Não alterar o cross-tenant guard, o skeleton, o empty state, nem a lógica de "Iniciar Nova Revisão". Manter "Ver detalhes" intacto.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -q "ExportPdfButton" src/features/form/HistoryPage.tsx && grep -q "useOrgInfo" src/features/form/HistoryPage.tsx && npm run lint 2>&1 | grep -c "error" | grep -qx 0 && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `HistoryPage.tsx` importa e renderiza `ExportPdfButton` dentro do bloco `{isSubmitted && ...}` de cada card
    - `useOrgInfo(orgId)` é chamado em HistoryPageContent e seus valores são passados como props orgName/cnpj
    - O botão "Ver detalhes" e "Iniciar Nova Revisão" permanecem inalterados
    - `npm run lint` e `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>HistoryPage exibe "Exportar PDF" ao lado de "Ver detalhes" em cada card submitted; sem erros de lint/TS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação de bundle isolado + PDF visual end-to-end</name>
  <what-built>ExportPdfButton integrado na HistoryPage; chunk pdf lazy-loaded; PDF completo (capa + 10 seções + tabela final) gerado client-side e aberto em nova aba.</what-built>
  <how-to-verify>
    1. BUNDLE ISOLADO (EXPORT-05): rodar `cd roteiro-unificado && npm run build`. Confirmar no output que `@react-pdf` aparece em um chunk separado (ex: nome contendo "pdf" ou hash próprio) e NÃO dentro do chunk `index-*.js` principal. Comando de apoio: `ls -la dist/assets/ | sort -k5 -n` (o chunk react-pdf deve ser um arquivo grande separado). Se @react-pdf estiver no index principal, REPROVAR.
    2. LAZY-LOAD EM RUNTIME: `npm run preview` (ou `npm run dev`), logar como construtora com avaliações enviadas, abrir DevTools → Network, navegar para `/form/:orgId/history`. Confirmar que NENHUM arquivo @react-pdf carrega ao abrir a página. Clicar "Exportar PDF" e confirmar que o chunk pdf carrega só nesse momento.
    3. PDF VISUAL: ao clicar, o botão mostra "Gerando..." e uma nova aba abre com o PDF. Verificar na capa: fundo azul #123B66, marca "SuaEquipe.IA" com acento laranja, nome da construtora, CNPJ, "Avaliação v{N}", data de geração, badge G1-G5 colorido.
    4. SEÇÕES: percorrer o PDF — todas as 10 abas aparecem; campos não preenchidos mostram "—" (D-03); a última página tem a tabela de classificação G1-G5 com o nível gerencial em destaque.
    5. VERSÃO HISTÓRICA (EXPORT-04): se houver 2+ versões, exportar a versão 1 e confirmar que os dados são da v1, não da mais recente.
    6. ACENTOS (A4 da RESEARCH): conferir que acentos PT-BR (ã, ç, é) renderizam corretamente. Se aparecerem caracteres quebrados, registrar para tratamento (Font.register com Inter TTF).
    7. ERRO: (opcional) simular falha de rede e confirmar toast "Falha ao gerar PDF. Tente novamente.".
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas (ex: chunk não isolado, acentos quebrados, seção faltando, cor errada).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Click handler → window.open | Abertura de nova aba a partir de evento de usuário |
| HistoryPage → chunk pdf (dynamic import) | Fronteira de carregamento de código sob demanda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Denial of Service (UX) | window.open bloqueado por popup blocker | accept | Chamada dentro de handler de clique do usuário — popup blockers em geral permitem (RESEARCH A5); fallback documentado se reprovar no checkpoint |
| T-10-06 | Tampering | dynamic import permitir bundle vazar @react-pdf no chunk principal | mitigate | Verificação de build no checkpoint (Task 3 passo 1+2) confirma chunk isolado e ausência de import estático (gate de lint no Task 1) |
| T-10-07 | Information Disclosure | exportar versão de outra org via assessmentId | mitigate | RLS do Supabase + cross-tenant guard já presente em HistoryPage (orgId !== authOrgId → redirect) — sem nova superfície |
</threat_model>

<verification>
- `npm run build` → @react-pdf em chunk separado, ausente do index principal (EXPORT-05)
- Network tab → chunk pdf só carrega ao clicar (lazy-load efetivo)
- PDF abre em nova aba com capa, 10 seções e tabela final corretas
- `npm test -- --run` → suite completa verde
</verification>

<success_criteria>
- "Exportar PDF" presente em cada card submitted da HistoryPage (EXPORT-04)
- Clique → lazy-load + geração + preview em nova aba (D-04, EXPORT-01)
- Bundle principal sem @react-pdf/renderer confirmado no build (EXPORT-05)
- Checkpoint visual aprovado
</success_criteria>

<output>
Create `.planning/phases/10-exporta-o-pdf/10-03-SUMMARY.md` when done
</output>
