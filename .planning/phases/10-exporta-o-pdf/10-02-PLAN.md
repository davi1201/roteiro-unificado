---
phase: 10-exporta-o-pdf
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - roteiro-unificado/src/lib/pdf/fieldMaps.ts
  - roteiro-unificado/src/lib/pdf/PDFCoverPage.tsx
  - roteiro-unificado/src/lib/pdf/PDFSectionTorre360.tsx
  - roteiro-unificado/src/lib/pdf/PDFSectionHabilitacoes.tsx
  - roteiro-unificado/src/lib/pdf/PDFSectionFinal.tsx
  - roteiro-unificado/src/lib/pdf/PDFDocument.tsx
  - roteiro-unificado/src/lib/pdf/PDFDocument.test.tsx
autonomous: true
requirements: [EXPORT-01, EXPORT-02]
must_haves:
  truths:
    - "A capa do PDF exibe nome da construtora, CNPJ, versão, data de geração e classificação G1-G5 gerencial em destaque (D-02)"
    - "Todas as 10 abas do formulário aparecem no PDF como seções; abas não preenchidas mostram '—' em todos os campos (D-03)"
    - "A última página contém a tabela de classificação G1-G5 por dimensão com o nível gerencial destacado (EXPORT-02)"
    - "A identidade visual azul #123B66 / laranja #F28C28 é aplicada na capa e nos cabeçalhos de seção (EXPORT-01)"
  artifacts:
    - path: "roteiro-unificado/src/lib/pdf/PDFCoverPage.tsx"
      provides: "Capa azul com marca SuaEquipe.IA + metadados + badge G1-G5"
      contains: "SuaEquipe.IA"
    - path: "roteiro-unificado/src/lib/pdf/PDFSectionTorre360.tsx"
      provides: "5 seções Torre 360 (Identificação, Decisão, Sienge, Acesso, Classificação)"
    - path: "roteiro-unificado/src/lib/pdf/PDFSectionHabilitacoes.tsx"
      provides: "4 seções Hab + NDA"
    - path: "roteiro-unificado/src/lib/pdf/PDFSectionFinal.tsx"
      provides: "Tabela de classificação G1-G5 (EXPORT-02)"
    - path: "roteiro-unificado/src/lib/pdf/PDFDocument.tsx"
      provides: "Documento raiz que compõe capa + seções + rodapé"
      contains: "Document"
    - path: "roteiro-unificado/src/lib/pdf/fieldMaps.ts"
      provides: "Mapeamento label↔chave do form_data por aba"
  key_links:
    - from: "roteiro-unificado/src/lib/pdf/PDFDocument.tsx"
      to: "roteiro-unificado/src/lib/pdf/PDFSectionTorre360.tsx"
      via: "composição das seções dentro de Document"
      pattern: "PDFSectionTorre360"
    - from: "roteiro-unificado/src/lib/pdf/PDFSectionTorre360.tsx"
      to: "form_data por aba"
      via: "optional chaining formData?.['torre-decisao']?.[campo] ?? '—'"
      pattern: "formData"
---

<objective>
Construir o `PDFDocument` completo: capa com identidade visual (D-01/D-02), as 5 seções da Torre 360, as 4 seções de Habilitações + NDA (D-03 — seções vazias mostram "—"), e a página final com a tabela de classificação G1-G5 (EXPORT-02). Substitui o stub criado no Plan 01.

Purpose: Entregar o relatório PDF visualmente fiel à paleta azul/laranja, cobrindo todas as seções preenchidas e não preenchidas, com classificações destacadas — núcleo dos requisitos EXPORT-01 e EXPORT-02.
Output: `PDFDocument.tsx` real + capa + 3 arquivos de seção + mapa de campos por aba.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-exporta-o-pdf/10-CONTEXT.md
@.planning/phases/10-exporta-o-pdf/10-RESEARCH.md
@.planning/phases/10-exporta-o-pdf/10-UI-SPEC.md
@.planning/phases/10-exporta-o-pdf/10-01-SUMMARY.md

<interfaces>
<!-- Artefatos do Plan 01 (Wave 1) — consumir diretamente. -->
src/lib/pdf/styles.ts: exporta COLORS (12 chaves hex), o StyleSheet `styles` compartilhado, gradeColor(grade)
src/lib/pdf/types.ts: AssessmentPDFData { orgName, cnpj, version, grade, gradeTech, generatedAt, formData }
src/lib/pdf/PDFFieldRow.tsx: <PDFFieldRow label value /> — fallback '—' (D-03)
src/lib/pdf/PDFSectionHeader.tsx: <PDFSectionHeader title /> — barra azul
src/lib/pdf/PDFFooter.tsx: <PDFFooter /> — rodapé fixo com paginação

<!-- Estrutura do form_data (JSONB) por aba — chaves = valores do enum TabKey. -->
'identificacao': empresa, cnpj, cidadeUf, dataReuniao, participantes, sponsorPiloto,
  responsavelSienge, responsavelHabilitacoes, quemConduzComercial, numCnpjsEscopo,
  numObrasAtivas, prioridadeTorre360, prioridadeHabilitacoes
'torre-sienge': modules.{slug}.{contratado|usoReal|confiancaDado|controleParalelo|observacoes}
  — SIENGE_MODULES (src/schemas/torre-sienge.ts) tem os 12 slugs+labels
'torre-decisao' | 'torre-acesso' | 'torre-classificacao' | 'hab-venda' | 'hab-repositorios'
  | 'hab-responsaveis' | 'hab-classificacao' | 'nda': Record<string, unknown>
TAB_CONFIG (src/features/form/tabConfig.ts) tem key+label das 10 abas em ordem canônica.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mapa de campos por aba + capa do PDF</name>
  <files>roteiro-unificado/src/lib/pdf/fieldMaps.ts, roteiro-unificado/src/lib/pdf/PDFCoverPage.tsx</files>
  <read_first>
    - roteiro-unificado/src/schemas/identificacao.ts (nomes exatos dos campos da aba Identificação)
    - roteiro-unificado/src/schemas/torre-decisao.ts (campos da aba Torre Decisão)
    - roteiro-unificado/src/schemas/torre-acesso.ts (campos da aba Torre Acesso)
    - roteiro-unificado/src/schemas/torre-classificacao.ts (campos da aba Torre Classificação)
    - roteiro-unificado/src/lib/pdf/styles.ts (COLORS, styles, gradeColor — do Plan 01)
    - roteiro-unificado/src/lib/pdf/types.ts (AssessmentPDFData)
    - .planning/phases/10-exporta-o-pdf/10-UI-SPEC.md §PDFDocument (capa) e §Copywriting (título "Relatório de Prontidão Gerencial")
  </read_first>
  <action>
    Criar `src/lib/pdf/fieldMaps.ts` exportando, para cada aba não-Sienge, um array ordenado de `{ key: string; label: string }` mapeando a chave real do schema para um label legível em pt-br (ex: identificacao → [{key:'empresa',label:'Empresa'},{key:'cnpj',label:'CNPJ'},{key:'cidadeUf',label:'Cidade/UF'},...]). Cobrir as 9 abas com campos flat (identificacao, torre-decisao, torre-acesso, torre-classificacao, hab-venda, hab-repositorios, hab-responsaveis, hab-classificacao, nda). A aba torre-sienge é tratada à parte (estrutura aninhada) no Task 2 via SIENGE_MODULES. Exportar um objeto `FIELD_MAPS: Record<string, {key,label}[]>` indexado pelas chaves de TabKey. Criar `PDFCoverPage.tsx` (Page size A4, style coverPage, SEM rodapé): marca "SuaEquipe" + ".IA" estilizada — texto branco 24pt Bold com o ".IA" em laranja accent (D-01, sem asset); abaixo: nome da construtora (orgName, 18pt branco), CNPJ (12pt cinza-claro), "Avaliação v{version}" (14pt branco), "Gerado em {generatedAt formatado pt-BR via toLocaleDateString('pt-BR')}" (12pt); badge G1-G5 gerencial em destaque (círculo ~40px com backgroundColor = gradeColor(grade) e label do grade em branco); título "Relatório de Prontidão Gerencial". Usar optional chaining e fallback '—' onde aplicável. Apenas primitivos react-pdf (Document NÃO — só Page/View/Text), sem className.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -q "FIELD_MAPS" src/lib/pdf/fieldMaps.ts && grep -q "SuaEquipe" src/lib/pdf/PDFCoverPage.tsx && grep -q "gradeColor" src/lib/pdf/PDFCoverPage.tsx && ! grep -q "className" src/lib/pdf/PDFCoverPage.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `fieldMaps.ts` exporta `FIELD_MAPS` cobrindo as 9 abas flat com chaves idênticas às dos schemas Zod (ex: 'cidadeUf', 'numObrasAtivas')
    - `PDFCoverPage.tsx` renderiza a marca "SuaEquipe.IA" com acento laranja, orgName, cnpj, version, data formatada pt-BR e badge de grade colorido via `gradeColor`
    - Capa não contém `className`
    - Capa usa fallback '—' para campos ausentes (cnpj null)
  </acceptance_criteria>
  <done>Mapa de campos das 9 abas flat criado com chaves fiéis aos schemas; capa azul com marca, metadados D-02 e badge G1-G5.</done>
</task>

<task type="auto">
  <name>Task 2: Seções Torre 360, Habilitações/NDA e Classificação Final</name>
  <files>roteiro-unificado/src/lib/pdf/PDFSectionTorre360.tsx, roteiro-unificado/src/lib/pdf/PDFSectionHabilitacoes.tsx, roteiro-unificado/src/lib/pdf/PDFSectionFinal.tsx</files>
  <read_first>
    - roteiro-unificado/src/lib/pdf/fieldMaps.ts (FIELD_MAPS criado no Task 1)
    - roteiro-unificado/src/schemas/torre-sienge.ts (SIENGE_MODULES + colunas do módulo)
    - roteiro-unificado/src/lib/pdf/PDFFieldRow.tsx, PDFSectionHeader.tsx (primitivos do Plan 01)
    - roteiro-unificado/src/lib/pdf/styles.ts (COLORS, gradeColor)
    - .planning/phases/10-exporta-o-pdf/10-UI-SPEC.md §PDFDocument (página final: tabela 5 colunas Grade/Descrição/Gerencial/Técnico/Status; nível gerencial 32pt centralizado) e §Copywriting ("Classificação de Prontidão", "Nível Gerencial:")
  </read_first>
  <action>
    Criar `PDFSectionTorre360.tsx` recebendo `formData: Record<string, unknown>`: renderiza 5 blocos (Identificação, Torre Decisão, Torre Sienge, Torre Acesso, Torre Classificação). Para as 4 abas flat, cada bloco = PDFSectionHeader(label da aba via TAB_CONFIG) + map sobre FIELD_MAPS[tabKey] renderizando PDFFieldRow(label, formData?.[tabKey]?.[key]). Para torre-sienge: PDFSectionHeader('Torre Sienge') + map sobre SIENGE_MODULES renderizando, por módulo, um sub-cabeçalho (Text com o label do módulo) e PDFFieldRows para as 5 colunas (Contratado, Uso Real, Confiança do Dado, Controle Paralelo, Observações) lendo formData?.['torre-sienge']?.modules?.[slug]?.[coluna]. Aplicar a prop `break` no primeiro View de torre-sienge para evitar corte de página (RESEARCH §Armadilha 4). `PDFSectionHabilitacoes.tsx` análogo para hab-venda, hab-repositorios, hab-responsaveis, hab-classificacao e nda — todas flat via FIELD_MAPS. `PDFSectionFinal.tsx` recebe `grade: string|null` e `gradeTech: string|null`: PDFSectionHeader/Text "Classificação de Prontidão" (h2), tabela com 5 colunas (Grade, Descrição, Gerencial, Técnico, Status) listando G1..G5 com a descrição (G1 Crítico, G2 Baixo, G3 Médio, G4 Bom, G5 Excelente) e marcando a linha correspondente ao grade gerencial; abaixo "Nível Gerencial:" como label em destaque + o grade 32pt centralizado com cor gradeColor(grade). Todas as seções usam D-03 (campos vazios → '—' via PDFFieldRow), só primitivos react-pdf, sem className.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -q "PDFSectionHeader" src/lib/pdf/PDFSectionTorre360.tsx && grep -q "SIENGE_MODULES" src/lib/pdf/PDFSectionTorre360.tsx && grep -q "break" src/lib/pdf/PDFSectionTorre360.tsx && grep -q "Classificação de Prontidão" src/lib/pdf/PDFSectionFinal.tsx && grep -L "className" src/lib/pdf/PDFSectionTorre360.tsx src/lib/pdf/PDFSectionHabilitacoes.tsx src/lib/pdf/PDFSectionFinal.tsx | wc -l | grep -qx 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `PDFSectionTorre360.tsx` renderiza as 5 abas Torre 360; a sub-seção Sienge itera os 12 SIENGE_MODULES com as 5 colunas
    - `PDFSectionTorre360.tsx` usa a prop `break` no início da seção Sienge
    - `PDFSectionHabilitacoes.tsx` renderiza hab-venda, hab-repositorios, hab-responsaveis, hab-classificacao e nda
    - `PDFSectionFinal.tsx` contém a tabela com 5 colunas e os labels "Classificação de Prontidão" e "Nível Gerencial:"
    - Os três arquivos não contêm `className`
    - Acesso ao form_data sempre via optional chaining com fallback '—'
  </acceptance_criteria>
  <done>5 seções Torre 360 (incl. Sienge aninhado), 4 seções Hab + NDA, e tabela final G1-G5 implementadas com D-03 aplicado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compor PDFDocument raiz (substitui stub) + completar testes</name>
  <files>roteiro-unificado/src/lib/pdf/PDFDocument.tsx, roteiro-unificado/src/lib/pdf/PDFDocument.test.tsx</files>
  <read_first>
    - roteiro-unificado/src/lib/pdf/PDFDocument.tsx (stub do Plan 01 — substituir)
    - roteiro-unificado/src/lib/pdf/PDFDocument.test.tsx (scaffold it.todo do Plan 01)
    - roteiro-unificado/src/lib/pdf/PDFCoverPage.tsx, PDFSectionTorre360.tsx, PDFSectionHabilitacoes.tsx, PDFSectionFinal.tsx, PDFFooter.tsx
    - roteiro-unificado/src/lib/pdf/types.ts (AssessmentPDFData)
    - .planning/phases/10-exporta-o-pdf/10-RESEARCH.md §Exemplos de Código (estrutura do PDFDocument raiz)
    - roteiro-unificado/vitest.config.ts (config de testes; como renderToString do react-pdf é testado)
  </read_first>
  <behavior>
    - Test 1: `PDFDocument` recebendo AssessmentPDFData completo renderiza sem lançar (via @react-pdf/renderer renderToString ou pdf().toString()) e a saída contém o orgName e o nome de marca "SuaEquipe.IA".
    - Test 2: `PDFDocument` recebendo formData vazio ({}) renderiza sem lançar e a saída contém ao menos uma ocorrência de "—" (D-03 — seções vazias).
    - Test 3: A saída contém o texto "Classificação de Prontidão" (página final presente).
  </behavior>
  <action>
    Substituir o stub `PDFDocument.tsx` pela implementação real: componente recebendo `data: AssessmentPDFData`, retornando `<Document title={\`Relatório de Prontidão Gerencial — ${data.orgName} v${data.version}\`} author="SuaEquipe.IA" subject="Avaliação de Prontidão">` contendo: PDFCoverPage(data) (Page própria, sem rodapé); uma Page A4 com PDFFooter + PDFSectionTorre360(formData=data.formData); uma Page A4 com PDFFooter + PDFSectionHabilitacoes(formData=data.formData); uma Page A4 com PDFFooter + PDFSectionFinal(grade=data.grade, gradeTech=data.gradeTech). Remover o comentário STUB. Completar `PDFDocument.test.tsx` implementando os 3 testes do bloco behavior — usar a API de renderização do @react-pdf/renderer apropriada para ambiente de teste node (ex: `await ReactPDF.renderToString(<PDFDocument data={...} />)` ou `await pdf(<PDFDocument .../>).toString()`); montar um AssessmentPDFData completo e um vazio como fixtures inline. Garantir que os testes passam (não it.todo).
  </action>
  <verify>
    <automated>cd roteiro-unificado && npm test -- --run src/lib/pdf/PDFDocument.test.tsx 2>&1 | grep -qE "Test Files.*passed" && ! grep -q "STUB" src/lib/pdf/PDFDocument.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `PDFDocument.tsx` compõe PDFCoverPage + PDFSectionTorre360 + PDFSectionHabilitacoes + PDFSectionFinal, cada Page de conteúdo com PDFFooter
    - O comentário `STUB` foi removido de PDFDocument.tsx
    - Os 3 testes de `PDFDocument.test.tsx` passam (não it.todo): orgName/marca presentes, "—" presente para formData vazio, "Classificação de Prontidão" presente
    - `npx tsc --noEmit` retorna 0 erros TS no chunk pdf
  </acceptance_criteria>
  <done>PDFDocument real compondo todas as páginas; testes de renderização (completo + vazio + página final) verdes; sem erros TS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form_data (JSONB livre) → render PDF | Estrutura pode variar entre versões antigas/novas; acesso sem validação pode quebrar render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service (render crash) | PDFSection*.tsx lendo form_data aninhado | mitigate | Acesso sempre via optional chaining + fallback '—' (RESEARCH §Armadilha 5); teste com formData vazio garante não-crash |
| T-10-04 | Information Disclosure | dados da org renderizados no PDF | accept | PDF gerado client-side, aberto localmente; dados já pertencem à org autenticada (RLS na origem) — sem nova superfície de exposição |
</threat_model>

<verification>
- `npm test -- --run src/lib/pdf` → seção PDFDocument verde
- `npx tsc --noEmit` → 0 erros no chunk pdf
- Verificação visual real do PDF ocorre no Plan 03 (checkpoint humano)
</verification>

<success_criteria>
- Capa com identidade azul/laranja + metadados D-02 + badge G1-G5 (EXPORT-01)
- Todas as 10 abas como seções; vazias com "—" (D-03, EXPORT-02)
- Tabela final de classificação G1-G5 (EXPORT-02)
- PDFDocument real substitui o stub; testes de renderização verdes
</success_criteria>

<output>
Create `.planning/phases/10-exporta-o-pdf/10-02-SUMMARY.md` when done
</output>
