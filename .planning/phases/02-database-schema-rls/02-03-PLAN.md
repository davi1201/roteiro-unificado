---
phase: 02-database-schema-rls
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified: []
autonomous: false
requirements:
  - ORG-01
  - ORG-02
  - ORG-04

user_setup:
  - service: supabase
    why: "Aplicar migrations ao banco remoto via CLI ou Dashboard SQL Editor"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens → Generate new token"
    dashboard_config:
      - task: "Obter Project Reference ID"
        location: "Supabase Dashboard → Project Settings → General → Reference ID"
      - task: "[Fallback] Executar SQL manualmente"
        location: "Supabase Dashboard → SQL Editor — executar cada migration em ordem, depois seed.sql"

must_haves:
  truths:
    - "Tabelas orgs, org_members e assessments existem no banco remoto com colunas, constraints e RLS habilitado"
    - "Usuário empresa1@teste.com autenticado não consegue ver dados de outras orgs"
    - "Usuário admin@suaequipe.ia autenticado consegue SELECT em todas as orgs e assessments"
    - "Seed data existe: 2 orgs, 2 usuários, 2 memberships, 1 assessment draft"
  artifacts:
    - path: "supabase/migrations/20260522000001_create_enums.sql"
      provides: "Enums aplicados ao banco"
    - path: "supabase/seed.sql"
      provides: "Dados de teste aplicados ao banco"
  key_links:
    - from: "supabase CLI / Dashboard SQL Editor"
      to: "Supabase PostgreSQL remoto"
      via: "supabase db push ou SQL Editor"
      pattern: "supabase db push"
---

<objective>
Aplicar todos os 8 arquivos de migration e o seed.sql ao banco Supabase remoto, verificar que o schema foi criado corretamente e confirmar o isolamento RLS via testes manuais com os usuários de seed.

Purpose: TypeScript compila contra database.ts, mas o banco remoto só reflete o schema após o push. Sem este plano, a fase fica com falso-positivo de verificação — o app compila mas o banco não tem as tabelas.
Output: Banco Supabase remoto com schema completo, RLS ativo e seed data inserido. Pronto para Phase 3 (Auth).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-database-schema-rls/02-CONTEXT.md
@.planning/phases/02-database-schema-rls/02-RESEARCH.md
@.planning/phases/02-database-schema-rls/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar pré-condições locais e aplicar migrations ao Supabase remoto</name>
  <read_first>
    .planning/phases/02-database-schema-rls/02-RESEARCH.md (seção "Complete Migration Deployment Flow" e "Fallback: Dashboard SQL Editor")
    supabase/migrations/ (todos os 8 arquivos — verificar que existem antes de tentar push)
    supabase/seed.sql (verificar que existe)
  </read_first>
  <files></files>
  <action>
    ETAPA 1 — Verificar pré-condições locais (pode ser automatizado):
    - Confirmar que existem 8 arquivos em supabase/migrations/: `ls supabase/migrations/*.sql | wc -l`
    - Confirmar que supabase/seed.sql existe: `ls supabase/seed.sql`
    - Se qualquer arquivo estiver faltando, PARAR e investigar o Plano 02-02.

    ETAPA 2 — Tentar via Supabase CLI (caminho preferido):
    a. Verificar se CLI está instalado: `supabase --version`
    b. Se instalado, autenticar: `supabase login` (abre browser; requer SUPABASE_ACCESS_TOKEN ou login interativo)
    c. Linkar ao projeto remoto: `supabase link --project-ref <PROJECT_REF>`
       O PROJECT_REF está disponível em: Supabase Dashboard → Project Settings → General → Reference ID
    d. Dry-run para verificar: `supabase db push --dry-run`
    e. Aplicar migrations: `supabase db push`
    f. Aplicar seed: executar seed.sql separadamente via Dashboard SQL Editor (supabase db push --include-seed requer Docker local)

    ETAPA 3 — Se CLI não funcionar (fallback Dashboard SQL Editor):
    a. Abrir Supabase Dashboard → SQL Editor
    b. Executar cada migration em ordem crescente (000001 → 000002 → ... → 000008)
    c. Executar supabase/seed.sql por último
    d. Verificar que não houve erros em nenhuma execução

    NOTA: Não usar `supabase db reset` no projeto remoto — isso destrói todos os dados. O reset é apenas para ambiente local com Docker.
  </action>
  <verify>
    <automated>ls supabase/migrations/*.sql | wc -l | grep -q "^8$" && ls supabase/seed.sql && echo "8 migration files + seed.sql confirmed — ready for remote push"</automated>
  </verify>
  <done>Pré-condições locais verificadas (8 migrations + seed.sql existem). Migrations e seed aplicados ao banco remoto via CLI ou Dashboard SQL Editor sem erros de execução.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar schema remoto e isolamento RLS com usuários de teste</name>
  <read_first>
    .planning/ROADMAP.md (UAT da Phase 2 — critérios de aceitação a verificar)
  </read_first>
  <what-built>
    Schema Supabase completo com:
    - 3 tabelas (orgs, org_members, assessments) com RLS ativo
    - Funções is_admin() e is_org_member() com SECURITY DEFINER
    - Policies de isolamento multi-tenant em todas as tabelas
    - 2 usuários de teste com dados seed
  </what-built>
  <how-to-verify>
    VERIFICAÇÃO 1 — Confirmar que tabelas existem com estrutura correta (Supabase Dashboard → Table Editor):
    - Tabela `orgs` existe com colunas: id, name, cnpj, active, created_at
    - Tabela `org_members` existe com colunas: id, org_id, user_id, role, created_at
    - Tabela `assessments` existe com colunas: id, org_id, status, version, form_data, readiness_level_mgmt, readiness_level_tech, created_at, submitted_at
    - RLS está marcado como ativo em cada tabela (ícone de cadeado ou coluna "RLS enabled")

    VERIFICAÇÃO 2 — Confirmar funções helper (Supabase Dashboard → Database → Functions):
    - Função `is_admin` aparece na lista com schema `public`
    - Função `is_org_member` aparece na lista com schema `public`

    VERIFICAÇÃO 3 — Confirmar seed data (Supabase Dashboard → Table Editor):
    - Tabela `orgs` mostra 2 linhas: "SuaEquipe.IA (Admin)" e "Construtora Teste 1"
    - Tabela `org_members` mostra 2 linhas com roles 'admin' e 'company'
    - Tabela `assessments` mostra 1 linha com status 'draft'
    - Supabase Dashboard → Authentication → Users lista: admin@suaequipe.ia e empresa1@teste.com

    VERIFICAÇÃO 4 — Testar isolamento RLS via SQL Editor (Supabase Dashboard → SQL Editor):
    Execute o seguinte SQL para verificar a perspectiva do usuário empresa1 (simula RLS via set_config):
    ```
    -- Pegar o user_id de empresa1:
    SELECT id FROM auth.users WHERE email = 'empresa1@teste.com';
    -- Usar o UUID retornado no próximo teste:
    SET LOCAL role = 'authenticated';
    SET LOCAL "request.jwt.claim.sub" = '<UUID_empresa1>';
    SELECT * FROM public.assessments;
    -- Esperado: 1 linha (o assessment draft de empresa1)
    SELECT * FROM public.orgs;
    -- Esperado: 1 linha (apenas Construtora Teste 1)
    ```

    VERIFICAÇÃO 5 — Testar acesso admin (deve ver tudo):
    ```
    SET LOCAL role = 'authenticated';
    SET LOCAL "request.jwt.claim.sub" = '<UUID_admin>';
    SELECT count(*) FROM public.orgs;
    -- Esperado: 2 (ambas as orgs)
    SELECT count(*) FROM public.assessments;
    -- Esperado: 1 (assessment de empresa1)
    ```

    VERIFICAÇÃO 6 — Testar login dos usuários de teste:
    Supabase Dashboard → Authentication → Sign In (ou via cliente JS):
    - admin@suaequipe.ia / Admin@123 → deve retornar sessão válida (não null)
    - empresa1@teste.com / Empresa@123 → deve retornar sessão válida (não null)

    ALTERNATIVA SIMPLIFICADA: Se SET LOCAL não estiver disponível no Dashboard,
    confirmar ao menos as Verificações 1, 2 e 3 (estrutura + seed data visível).
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se todos os itens passaram, ou descreva qual problema encontrou (ex: "login falhou", "RLS não está isolando", "tabela X não existe", "aplicado via Dashboard SQL Editor", "só pude verificar estrutura mas não isolamento").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI / Dashboard → Supabase remote | Migrations executam com privilégio de superuser no banco remoto |
| seed.sql passwords | Hash via pgcrypto no momento de execução — senhas plaintext nunca persistidas no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-D01 | Tampering | supabase db reset no remote | mitigate | Comentário explícito em seed.sql e neste plano proibindo db reset no remote; usar apenas db push |
| T-02-D02 | Information Disclosure | SUPABASE_ACCESS_TOKEN em env | mitigate | Token de acesso não deve ser commitado em .env; usar apenas em env var local temporária ou via supabase login browser flow |
| T-02-D03 | Tampering | Migration aplicada fora de ordem | mitigate | Prefixos de timestamp garantem ordem correta; dry-run confirma sequência antes do push real |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Este plano não instala pacotes npm; Supabase CLI instalado via brew (tap oficial) no Plano 01 |
</threat_model>

<verification>
Verificação final da Phase 2 (após Plano 03 aprovado):
1. `cd roteiro-unificado && npx tsc --noEmit` retorna exit 0
2. Supabase Dashboard mostra 3 tabelas com RLS ativo, 2 funções, 2 usuários, 2 orgs, 1 assessment
3. RLS ativo: empresa1 só vê sua org; admin vê todas
4. Todos os UAT items do ROADMAP Phase 2 marcados como passando:
   - [ ] Usuário empresa1 autenticado consegue SELECT apenas em assessments da sua org
   - [ ] Tentativa de SELECT em assessment de outra org retorna 0 linhas (RLS ativo)
   - [ ] Usuário admin consegue SELECT em todas as orgs e assessments
   - [ ] INSERT em assessment com org_id inválido falha com erro de RLS
   - [ ] Migration roda sem erros via supabase db push
   - [ ] Seed cria os usuários de teste com roles corretos
</verification>

<success_criteria>
- Schema aplicado ao banco remoto sem erros (8 migrations + seed)
- Tabelas orgs, org_members e assessments visíveis no Dashboard com colunas e RLS corretos
- Login funciona para admin@suaequipe.ia e empresa1@teste.com
- RLS isola dados de empresa1 de outras orgs (ORG-01, ORG-04)
- Phase 3 (Auth) pode começar com banco funcional
</success_criteria>

<output>
Criar `.planning/phases/02-database-schema-rls/02-03-SUMMARY.md` ao concluir
</output>
