---
phase: 03-authentication-roteamento-por-role
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - roteiro-unificado/src/features/auth/useUser.ts
  - roteiro-unificado/src/stores/formStore.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "useUser() expõe profile, role, orgId, isAdmin como abstração sobre AuthContext"
    - "isAdmin é true apenas quando role === 'admin'"
    - "formStore persist key é form-progress-${tenantId} — sem hardcode de string genérica"
    - "formStore aceita tenantId via parâmetro para criar store com key correta"
  artifacts:
    - path: "roteiro-unificado/src/features/auth/useUser.ts"
      provides: "Hook useUser() com profile, role, orgId, isAdmin"
      exports: ["useUser", "UserProfile"]
    - path: "roteiro-unificado/src/stores/formStore.ts"
      provides: "Zustand store com persist key namespaceada por tenantId"
      contains: "form-progress-${tenantId}"
  key_links:
    - from: "roteiro-unificado/src/features/auth/useUser.ts"
      to: "roteiro-unificado/src/features/auth/useAuth.ts"
      via: "import { useAuth } from './useAuth'"
      pattern: "useAuth\\(\\)"
    - from: "roteiro-unificado/src/stores/formStore.ts"
      to: "AuthContext"
      via: "tenantId passado como argumento"
      pattern: "form-progress-\\$\\{tenantId\\}"
---

<objective>
Criar o hook useUser() como abstração de alto nível sobre o AuthContext; atualizar o formStore para namespacing correto da persist key por tenant (corrige o risco registrado no STATE.md).

Purpose: useUser() será consumido por todos os componentes que precisam de dados do usuário logado sem acessar AuthContext diretamente. O namespace do formStore elimina o risco de cross-tenant leakage documentado em STATE.md e CONTEXT.md (decisão de discretion).

Output: `src/features/auth/useUser.ts` com hook tipado; `src/stores/formStore.ts` atualizado com persist key `form-progress-${tenantId}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-authentication-roteamento-por-role/03-CONTEXT.md
@.planning/phases/03-authentication-roteamento-por-role/03-01-SUMMARY.md

<interfaces>
<!-- Contratos criados no plano 03-01 que este plano consome. -->

De roteiro-unificado/src/features/auth/useAuth.ts (criado em 03-01):
```typescript
export function useAuth(): AuthContextType
// AuthContextType: { user, session, role, orgId, isLoading, signOut }
// role: 'admin' | 'company' | null
// orgId: string | null
```

De roteiro-unificado/src/stores/formStore.ts (a modificar):
```typescript
// persist key atual: name: 'form-progress'
// Precisa ser: name: `form-progress-${tenantId}`
// useFormStore — store com currentStep, completedSteps (Set<number>)
// FormState, FormActions, FormStore types existem
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar hook useUser()</name>
  <files>roteiro-unificado/src/features/auth/useUser.ts</files>
  <read_first>
    - roteiro-unificado/src/features/auth/useAuth.ts — interface AuthContextType que useUser consome
    - roteiro-unificado/src/features/auth/AuthProvider.tsx — para entender a forma do User e os campos expostos
    - .planning/phases/03-authentication-roteamento-por-role/03-CONTEXT.md — seção Canonical Refs sobre useUser
  </read_first>
  <behavior>
    - useUser() chama useAuth() internamente e re-expõe campos como abstração nomeada
    - Exporta interface UserProfile: `{ id: string; email: string | undefined }`
    - Retorna `{ profile: UserProfile | null, role: 'admin' | 'company' | null, orgId: string | null, isAdmin: boolean, isLoading: boolean, signOut: () => Promise<void> }`
    - isAdmin === (role === 'admin')
    - profile é null quando user é null; quando user não é null, profile = { id: user.id, email: user.email }
    - signOut é repassado diretamente do useAuth()
    - isLoading é repassado diretamente do useAuth()
  </behavior>
  <action>
    Criar `roteiro-unificado/src/features/auth/useUser.ts`.

    Importar useAuth de ./useAuth. Definir e exportar interface UserProfile com campos id: string e email: string | undefined. Exportar função useUser() que:
    1. Chama const { user, role, orgId, isLoading, signOut } = useAuth()
    2. Deriva profile: user ? { id: user.id, email: user.email } : null
    3. Deriva isAdmin: role === 'admin'
    4. Retorna o objeto com todos os campos acima

    Não duplicar lógica de AuthContext — apenas re-expor com semântica explícita.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "features/auth/useUser" || echo "sem erros em useUser"</automated>
  </verify>
  <acceptance_criteria>
    - useUser.ts contém `export interface UserProfile`
    - useUser.ts contém `isAdmin: role === 'admin'`
    - useUser.ts importa de './useAuth' (não reimplementa AuthContext)
    - `npx tsc --noEmit` não reporta erros em useUser.ts
  </acceptance_criteria>
  <done>useUser.ts criado sem erros TypeScript; expõe profile, role, orgId, isAdmin, isLoading, signOut.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Atualizar formStore com persist key namespaceada por tenantId</name>
  <files>roteiro-unificado/src/stores/formStore.ts</files>
  <read_first>
    - roteiro-unificado/src/stores/formStore.ts — ler o arquivo completo antes de modificar; entender a estrutura atual de persist
    - .planning/phases/03-authentication-roteamento-por-role/03-CONTEXT.md — decisão de discretion sobre namespace tenantId
    - .planning/STATE.md — pendente registrado: "Phase 3: Namespace Zustand persist key by tenantId"
  </read_first>
  <behavior>
    - A store NÃO usa mais uma persist key fixa 'form-progress'
    - Criar factory function `createFormStore(tenantId: string)` que retorna uma store com name: `form-progress-${tenantId}`
    - Exportar useFormStore como função curried: `useFormStore(tenantId: string)` OU manter a store existente e adicionar factory
    - Estratégia preferida: exportar `createFormStore(tenantId: string): StoreApi<FormStore>` + um hook `useFormStore(tenantId: string)` que internamente cria/memoiza a store por tenantId usando um Map
    - O tipo FormStore, FormState, FormActions e o initialState permanecem iguais — apenas a persist key muda
    - O comentário de aviso `// ⚠️ Fase 3` deve ser removido e substituído por comentário descrevendo a solução implementada
  </behavior>
  <action>
    Modificar `roteiro-unificado/src/stores/formStore.ts`.

    Manter as interfaces FormState, FormActions, FormStore e initialState sem alteração. Substituir o `create(persist(...))` inline por uma factory:

    ```
    const storesByTenant = new Map<string, ReturnType<typeof create<FormStore>>>()

    export function createFormStore(tenantId: string) {
      if (!storesByTenant.has(tenantId)) {
        storesByTenant.set(tenantId, create<FormStore>()(persist(..., { name: `form-progress-${tenantId}` })))
      }
      return storesByTenant.get(tenantId)!
    }
    ```

    Exportar também `useFormStore` como alias conveniente para uso com hook do zustand — usar a abordagem `(tenantId: string) => useStore(createFormStore(tenantId))`.

    Remover o comentário antigo de warning `// ⚠️  Fase 3: substituir 'form-progress' por ...` e adicionar JSDoc descrevendo o namespace.

    IMPORTANTE: Não incluir código literal com fenced code blocks nesta action — o executor implementa; o código acima é apenas orientação de padrão, não código a copiar literalmente.
  </action>
  <verify>
    <automated>cd roteiro-unificado && grep -v '^[[:space:]]*//' src/stores/formStore.ts | grep -c "form-progress-\${tenantId}"</automated>
  </verify>
  <acceptance_criteria>
    - formStore.ts NÃO contém `name: 'form-progress'` como string literal simples (sem template)
    - formStore.ts contém o padrão `form-progress-${tenantId}` no persist name
    - formStore.ts exporta `createFormStore` function que aceita tenantId: string
    - `npx tsc --noEmit` não reporta erros em formStore.ts
    - O comentário `// ⚠️  Fase 3: substituir` foi removido
  </acceptance_criteria>
  <done>formStore.ts atualizado com persist key namespaceada; comentário de warning removido; TypeScript compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| client→localStorage | Dados do formulário por tenant são persistidos aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | formStore localStorage key | mitigate | Namespace `form-progress-${tenantId}` garante isolamento — usuário A não lê dados de usuário B no mesmo browser |
| T-03-05 | Tampering | isAdmin derivado de role | accept | role vem do AuthContext que busca do Supabase com auth.uid() — não pode ser alterado pelo cliente |
</threat_model>

<verification>
- `grep -c "form-progress-\${tenantId}" roteiro-unificado/src/stores/formStore.ts` retorna >= 1
- `grep -c "createFormStore" roteiro-unificado/src/stores/formStore.ts` retorna >= 1
- `grep -c "isAdmin" roteiro-unificado/src/features/auth/useUser.ts` retorna >= 1
- `cd roteiro-unificado && npx tsc --noEmit` passa sem erros
</verification>

<success_criteria>
- useUser.ts criado com profile, role, orgId, isAdmin, isLoading, signOut
- formStore.ts usa persist key `form-progress-${tenantId}` via factory createFormStore
- TypeScript compila sem erros nos arquivos modificados/criados
</success_criteria>

<output>
Criar `.planning/phases/03-authentication-roteamento-por-role/03-02-SUMMARY.md` ao concluir
</output>
