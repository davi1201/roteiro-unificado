---
phase: 03-authentication-roteamento-por-role
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - roteiro-unificado/src/pages/Login.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Página /login renderiza card centralizado sobre fundo azul bg-primary"
    - "Card contém título 'Roteiro Unificado', subtítulo 'Piloto Sinduscon', campos email e senha, link 'Esqueci minha senha' e botão 'Entrar'"
    - "Validação inline com Zod/RHF exibe erros abaixo de cada campo (D-06)"
    - "Erro de autenticação exibe toast.error('Email ou senha inválidos') — nunca outro texto (D-04, D-05)"
    - "Botão 'Entrar' fica disabled com Spinner inline durante isSubmitting (D-07)"
    - "Auth flicker exibe Spinner full-screen bg-primary enquanto isLoading do AuthContext (D-08)"
  artifacts:
    - path: "roteiro-unificado/src/pages/Login.tsx"
      provides: "Página de login completa com form RHF/Zod + UI spec"
      exports: ["Login"]
  key_links:
    - from: "roteiro-unificado/src/pages/Login.tsx"
      to: "roteiro-unificado/src/features/auth/useAuth.ts"
      via: "import { useAuth } from '@/features/auth/useAuth'"
      pattern: "useAuth\\(\\)"
    - from: "roteiro-unificado/src/pages/Login.tsx"
      to: "roteiro-unificado/src/hooks/useToast.ts"
      via: "import { useToast } from '@/hooks/useToast'"
      pattern: "toast\\.error"
---

<objective>
Criar a página de Login completa, fiel ao contrato visual do UI-SPEC e às decisões D-01 a D-10 do CONTEXT.md.

Purpose: Primeiro ponto de contato do usuário com o app. Deve apresentar formulário funcional com validação inline, tratamento de erro genérico anti-enumeração, loading state no botão e proteção contra auth flicker.

Output: `src/pages/Login.tsx` — componente completo, totalmente funcional, sem dependências de rotas ainda (plano 03-05 conecta o router).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-authentication-roteamento-por-role/03-CONTEXT.md
@.planning/phases/03-authentication-roteamento-por-role/03-UI-SPEC.md
@.planning/phases/03-authentication-roteamento-por-role/03-01-SUMMARY.md

<interfaces>
<!-- Componentes UI e hooks disponíveis para uso na página de Login. -->

De roteiro-unificado/src/components/ui/ (via @/components/ui):
```typescript
// Button — isLoading prop disponível; usar className="bg-accent hover:bg-accent-600 text-white w-full"
export const Button: React.ForwardRefExoticComponent<ButtonProps>
// isLoading: boolean — renderiza spinner interno + disabled automático

// Input — error e errorMessage props disponíveis
export const Input: React.ForwardRefExoticComponent<InputProps>
// error: boolean — borda vermelha + focus ring g1
// errorMessage: string — exibe <p className="text-g1 text-xs"> abaixo

// Card, CardHeader, CardContent — containers de layout
export function Card, CardHeader, CardContent

// Spinner — usar para auth flicker
export function Spinner({ size?: 'sm' | 'md' | 'lg', className?: string })
// size="lg" className="border-white border-t-transparent" para auth flicker
```

De roteiro-unificado/src/hooks/useToast.ts:
```typescript
export function useToast(): { error: (msg: string) => void, success: ..., ... }
```

De roteiro-unificado/src/features/auth/useAuth.ts (criado em 03-01):
```typescript
export function useAuth(): { user, session, role, orgId, isLoading, signOut }
// isLoading: true durante inicialização (D-08)
```

De roteiro-unificado/src/lib/supabase.ts:
```typescript
export const supabase = createClient<Database>(url, key)
// supabase.auth.signInWithPassword({ email, password }): Promise<AuthResponse>
// AuthResponse.error: AuthError | null
// AuthResponse.data.session: Session | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar página Login.tsx com form RHF/Zod e UI contract</name>
  <files>roteiro-unificado/src/pages/Login.tsx</files>
  <read_first>
    - .planning/phases/03-authentication-roteamento-por-role/03-UI-SPEC.md — Layout Contracts seção /login, Copywriting Contract, Color e Interaction States
    - .planning/phases/03-authentication-roteamento-por-role/03-CONTEXT.md — D-01 a D-10 completos
    - roteiro-unificado/src/components/ui/button.tsx — confirmar isLoading prop e como o spinner é renderizado
    - roteiro-unificado/src/components/ui/input.tsx — confirmar error/errorMessage props
    - roteiro-unificado/src/components/ui/card.tsx — confirmar classes do CardHeader e CardContent
    - roteiro-unificado/src/components/ui/spinner.tsx — confirmar interface do Spinner
  </read_first>
  <behavior>
    Auth flicker (D-08):
    - Se isLoading do AuthContext for true: renderizar `<div className="min-h-screen bg-primary flex items-center justify-center"><Spinner size="lg" className="border-white border-t-transparent" /></div>`
    - Se isLoading for false: renderizar o form de login

    Layout externo (D-01):
    - Container: `<div className="min-h-screen bg-primary flex items-center justify-center px-4">`
    - Card: `<Card className="w-full max-w-[400px] shadow-md">`

    CardHeader (D-02):
    - `<CardHeader className="flex flex-col items-center gap-1 text-center p-6">`
    - `<h1 className="text-2xl font-bold text-gray-900">Roteiro Unificado</h1>`
    - `<p className="text-sm text-gray-500">Piloto Sinduscon</p>`

    CardContent — formulário:
    - `<CardContent className="p-6 pt-0 flex flex-col gap-4">`
    - Schema Zod: objeto com email (string, email(), mensagem "Insira um email válido", required "O email é obrigatório") e password (string, min(1, "A senha é obrigatória"))
    - useForm com zodResolver, mode: 'onBlur'
    - Campo email: label "Email", Input com type="email", placeholder="seu@email.com", error/errorMessage de formState.errors.email
    - Campo senha: label "Senha", Input com type="password", placeholder="••••••••", error/errorMessage de formState.errors.password
    - Link "Esqueci minha senha" (D-03): `<Link to="/forgot-password" className="text-sm text-primary hover:underline self-end">Esqueci minha senha</Link>` — usar Link do react-router-dom; posicionado dentro do CardContent após o campo senha

    Botão submit (D-07):
    - `<Button type="submit" isLoading={isSubmitting} className="bg-accent hover:bg-accent-600 text-white w-full">`
    - Texto quando idle: "Entrar"; quando isSubmitting: "Entrando..." (passar como children)

    Handler onSubmit (D-04, D-05):
    - Chamar supabase.auth.signInWithPassword({ email, password })
    - Se error não é null: chamar toast.error('Email ou senha inválidos') — SEMPRE este texto, independente do error.message
    - Se success: roteamento pós-login é implementado no plano 03-06; por ora, não redirecionar

    IMPORTANTE: Link do react-router-dom só funciona dentro de RouterProvider (plano 03-05). Usar Link com import direto — não causará erro de compilação; só falhará em runtime se não houver router, o que será corrigido no plano 03-05.
  </behavior>
  <action>
    Criar `roteiro-unificado/src/pages/Login.tsx`.

    Imports necessários: React, useForm de react-hook-form, zodResolver de @hookform/resolvers/zod, z de zod, Link de react-router-dom, supabase de @/lib/supabase, useAuth de @/features/auth/useAuth, useToast de @/hooks/useToast, Button/Input/Card/CardHeader/CardContent/Spinner de @/components/ui.

    Definir loginSchema Zod com campos email e password. Inferir tipo LoginFormData. Criar componente Login que:
    1. Chama useAuth() e extrai isLoading
    2. Chama useToast() e extrai error
    3. Chama useForm com zodResolver(loginSchema) e mode: 'onBlur'
    4. Implementa onSubmit assíncrono
    5. Retorna auth flicker se isLoading, senão retorna o layout do card

    Copiar textos EXATAMENTE do copywriting contract do UI-SPEC: "Roteiro Unificado", "Piloto Sinduscon", "Entrar", "Entrando...", "Email ou senha inválidos", "Esqueci minha senha".
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "pages/Login" || echo "sem erros em Login"</automated>
  </verify>
  <acceptance_criteria>
    - Login.tsx contém `min-h-screen bg-primary flex items-center justify-center` (container externo)
    - Login.tsx contém `max-w-[400px]` (card width per D-01)
    - Login.tsx contém o texto exato `Roteiro Unificado` e `Piloto Sinduscon` (D-02)
    - Login.tsx contém `Esqueci minha senha` com `to="/forgot-password"` (D-03)
    - Login.tsx contém `Email ou senha inválidos` apenas como argumento de toast.error (D-04)
    - Login.tsx contém `isLoading={isSubmitting}` no Button (D-07)
    - Login.tsx contém a lógica de auth flicker com `Spinner size="lg"` quando `isLoading` do AuthContext é true (D-08)
    - Login.tsx NÃO contém nenhum hex hardcodado como `#123B66` ou `#F28C28`
    - `npx tsc --noEmit` não reporta erros em Login.tsx
  </acceptance_criteria>
  <done>Login.tsx criado com layout correto, validação RHF/Zod, auth flicker, erro genérico via toast e botão com loading state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| usuário→formulário | Email e senha inseridos pelo usuário |
| formulário→Supabase Auth | Credenciais enviadas via SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | Erro de login | mitigate | D-04: sempre "Email ou senha inválidos" — nunca revela se email existe (anti-enumeração) |
| T-03-07 | Tampering | Duplo-submit | mitigate | D-07: botão disabled + isLoading durante submissão — previne múltiplas chamadas |
| T-03-08 | Spoofing | Credenciais inválidas | accept | Supabase Auth valida credenciais server-side; rate limiting é responsabilidade do Supabase |
</threat_model>

<verification>
- `grep -c "Email ou senha inválidos" roteiro-unificado/src/pages/Login.tsx` retorna 1 (apenas no toast.error)
- `grep -c "max-w-\[400px\]" roteiro-unificado/src/pages/Login.tsx` retorna >= 1
- `grep -c "bg-primary" roteiro-unificado/src/pages/Login.tsx` retorna >= 1
- `grep -c "Esqueci minha senha" roteiro-unificado/src/pages/Login.tsx` retorna >= 1
- `grep -c "isLoading" roteiro-unificado/src/pages/Login.tsx` retorna >= 2 (auth flicker + button)
- `cd roteiro-unificado && npx tsc --noEmit` passa sem erros
</verification>

<success_criteria>
- Login.tsx criado com todos os contratos visuais e de interação do UI-SPEC honrados
- D-01 a D-10 implementados conforme especificado
- Sem erros TypeScript
</success_criteria>

<output>
Criar `.planning/phases/03-authentication-roteamento-por-role/03-03-SUMMARY.md` ao concluir
</output>
