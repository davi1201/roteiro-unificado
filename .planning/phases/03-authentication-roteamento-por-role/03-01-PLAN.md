---
phase: 03-authentication-roteamento-por-role
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/features/auth/AuthProvider.tsx
  - roteiro-unificado/src/features/auth/useAuth.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "AuthContext existe e expõe user, session, role, orgId, isLoading, signOut"
    - "onAuthStateChange está inscrito e limpa sessão ao receber SIGNED_OUT"
    - "isLoading é true até a primeira resposta do onAuthStateChange"
    - "useAuth() lança erro se chamado fora de AuthProvider"
    - "Dependências react-router-dom, react-hook-form, zod, @hookform/resolvers estão instaladas"
  artifacts:
    - path: "roteiro-unificado/src/features/auth/AuthProvider.tsx"
      provides: "AuthContext e AuthProvider component"
      exports: ["AuthContext", "AuthProvider"]
    - path: "roteiro-unificado/src/features/auth/useAuth.ts"
      provides: "Hook useAuth que consome AuthContext"
      exports: ["useAuth"]
  key_links:
    - from: "AuthProvider.tsx"
      to: "roteiro-unificado/src/lib/supabase.ts"
      via: "import { supabase } from '@/lib/supabase'"
      pattern: "supabase\\.auth\\.onAuthStateChange"
    - from: "AuthProvider.tsx"
      to: "roteiro-unificado/src/types/database.ts"
      via: "Tables<'org_members'>"
      pattern: "org_members"
---

<objective>
Instalar dependências de roteamento e formulário; criar o AuthProvider e o hook useAuth() que formam a fundação de toda a autenticação da fase.

Purpose: Sem o AuthProvider, nenhuma outra parte desta fase pode ser implementada. Este plano entrega o contexto de autenticação completo com persistência de sessão via Supabase e a query de role/orgId na tabela org_members.

Output: Pacotes instalados; `src/features/auth/AuthProvider.tsx` com AuthContext tipado; `src/features/auth/useAuth.ts` com hook useAuth().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-authentication-roteamento-por-role/03-CONTEXT.md
@.planning/phases/03-authentication-roteamento-por-role/03-UI-SPEC.md

<interfaces>
<!-- Contratos existentes que o executor precisa — extraídos do código base. -->

De roteiro-unificado/src/lib/supabase.ts:
```typescript
import { createClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'
export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey)
```

De roteiro-unificado/src/types/database.ts:
```typescript
export type Tables<T extends keyof Database['public']['Tables']> =
  Database['public']['Tables'][T] extends { Row: infer R } ? R : never
export type Enums<T extends keyof Database['public']['Enums']> = Database['public']['Enums'][T]
// member_role: 'admin' | 'company'
// org_members.Row: { id, org_id, user_id, role: member_role, created_at }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Instalar dependências de roteamento e formulário</name>
  <files>roteiro-unificado/package.json</files>
  <read_first>
    - roteiro-unificado/package.json — verificar versões já instaladas antes de instalar
  </read_first>
  <action>
    No diretório `roteiro-unificado/`, executar:
    `npm install react-router-dom react-hook-form zod @hookform/resolvers`

    Não especificar versões — usar as mais recentes estáveis. Após instalar, confirmar que as quatro entradas aparecem em `dependencies` no package.json. @supabase/supabase-js já está instalado — não reinstalar.
  </action>
  <verify>
    <automated>cd roteiro-unificado && node -e "require('./node_modules/react-router-dom/package.json'); require('./node_modules/react-hook-form/package.json'); require('./node_modules/zod/package.json'); require('./node_modules/@hookform/resolvers/package.json'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém entradas para react-router-dom, react-hook-form, zod e @hookform/resolvers em dependencies
    - node_modules/react-router-dom, react-hook-form, zod e @hookform/resolvers existem
  </acceptance_criteria>
  <done>Quatro pacotes instalados e presentes em node_modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Criar AuthProvider e hook useAuth()</name>
  <files>
    roteiro-unificado/src/features/auth/AuthProvider.tsx
    roteiro-unificado/src/features/auth/useAuth.ts
  </files>
  <read_first>
    - roteiro-unificado/src/lib/supabase.ts — cliente Supabase tipado a importar
    - roteiro-unificado/src/types/database.ts — tipos Tables e Enums; org_members.role
    - .planning/phases/03-authentication-roteamento-por-role/03-CONTEXT.md — D-08, D-09, decisões de AuthContext
  </read_first>
  <behavior>
    - AuthContext tipado expõe: `user: User | null`, `session: Session | null`, `role: 'admin' | 'company' | null`, `orgId: string | null`, `isLoading: boolean`, `signOut: () => Promise<void>`
    - isLoading começa true e vira false após primeira chamada do onAuthStateChange (D-08)
    - onAuthStateChange SIGNED_OUT: limpa role/orgId, define user/session como null (D-09)
    - onAuthStateChange SIGNED_IN / INITIAL_SESSION: busca org_members onde user_id = session.user.id via supabase.from('org_members').select('org_id, role').eq('user_id', ...).single(); popula role e orgId do resultado
    - signOut: chama supabase.auth.signOut() (redirect para /login é responsabilidade do Router — plano 06)
    - useAuth(): se contexto for null, lança Error('useAuth deve ser usado dentro de AuthProvider')
    - AuthProvider aceita children: ReactNode e retorna AuthContext.Provider com valor correto
    - Criar diretório src/features/auth/ se não existir
  </behavior>
  <action>
    Criar `roteiro-unificado/src/features/auth/AuthProvider.tsx`:

    Definir interface AuthContextType com os campos acima. Criar AuthContext com createContext tipado (valor inicial null). Criar AuthProvider como componente React com useState para user, session, role, orgId, isLoading. No useEffect, chamar supabase.auth.getSession() para obter sessão inicial, depois subscrever supabase.auth.onAuthStateChange(async (event, session) => { ... }). Quando session não é null, buscar org_members com select('org_id, role').eq('user_id', session.user.id).single() — preencher role e orgId ou null em caso de erro/ausência. Definir isLoading = false após primeiro evento. Retornar cleanup da subscription no useEffect. Implementar signOut como async function que chama supabase.auth.signOut(). Retornar AuthContext.Provider value={{ user, session, role, orgId, isLoading, signOut }}.

    Criar `roteiro-unificado/src/features/auth/useAuth.ts`:

    Importar useContext e AuthContext de ./AuthProvider. Exportar useAuth() que lê o contexto; se null, lança Error('useAuth deve ser usado dentro de AuthProvider'); retorna o contexto. Não duplicar a interface — importar AuthContextType de ./AuthProvider.

    Exportar AuthContextType de AuthProvider.tsx para uso em outros módulos.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "features/auth" || echo "sem erros em features/auth"</automated>
  </verify>
  <acceptance_criteria>
    - AuthProvider.tsx contém `export const AuthContext = createContext`
    - AuthProvider.tsx contém `supabase.auth.onAuthStateChange`
    - AuthProvider.tsx contém `org_members` (query de role/orgId)
    - AuthProvider.tsx contém `isLoading` com valor inicial `true`
    - useAuth.ts contém `throw new Error` para uso fora do Provider
    - `npx tsc --noEmit` não reporta erros nos arquivos criados
  </acceptance_criteria>
  <done>AuthProvider e useAuth criados sem erros TypeScript; AuthContext tipado com user, session, role, orgId, isLoading, signOut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| client→Supabase Auth | Credenciais de login cruzam esta fronteira |
| Supabase→client | Token JWT retornado; role/orgId vindos de org_members |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | AuthProvider — query org_members | mitigate | Query com .single() retorna null em caso de erro; role/orgId ficam null — sem exposição de dados de outras orgs |
| T-03-02 | Tampering | onAuthStateChange | accept | Evento vem do SDK Supabase autenticado; manipulação requereria comprometimento do SDK |
| T-03-03 | Spoofing | session.user.id | mitigate | ID vem do JWT validado pelo Supabase; nunca confiar em id vindo do cliente diretamente |
| T-03-SC | Tampering | npm install react-router-dom react-hook-form zod @hookform/resolvers | mitigate | Verificar pacotes em npmjs.com antes de instalar; registros bem estabelecidos com histórico confiável |
</threat_model>

<verification>
- `npx tsc --noEmit` no diretório roteiro-unificado passa sem erros
- `grep -c "onAuthStateChange" roteiro-unificado/src/features/auth/AuthProvider.tsx` retorna >= 1
- `grep -c "org_members" roteiro-unificado/src/features/auth/AuthProvider.tsx` retorna >= 1
- `grep -c "isLoading" roteiro-unificado/src/features/auth/AuthProvider.tsx` retorna >= 1
- `grep -c "throw new Error" roteiro-unificado/src/features/auth/useAuth.ts` retorna >= 1
</verification>

<success_criteria>
- Pacotes react-router-dom, react-hook-form, zod e @hookform/resolvers instalados
- AuthProvider.tsx exporta AuthContext e AuthProvider com AuthContextType tipado
- useAuth.ts exporta useAuth() com guard contra uso fora do Provider
- TypeScript compila sem erros nos arquivos criados
</success_criteria>

<output>
Criar `.planning/phases/03-authentication-roteamento-por-role/03-01-SUMMARY.md` ao concluir
</output>
