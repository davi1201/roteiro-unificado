---
phase: 04-gest-o-de-organiza-es-painel-admin
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - roteiro-unificado/src/components/admin/ArchiveOrgDialog.tsx
  - roteiro-unificado/src/features/admin/useArchiveOrg.ts
  - roteiro-unificado/src/pages/admin/AdminDashboard.tsx
  - roteiro-unificado/src/pages/admin/OrgDetail.tsx
autonomous: true
requirements: [ORG-03]
tags: [admin, orgs, archive, mutation, dialog]

must_haves:
  truths:
    - "Admin clica em 'Arquivar' (na linha da tabela ou no header do detalhe) e ArchiveOrgDialog abre"
    - "Dialog exibe título 'Arquivar organização', corpo com nome da org em negrito, e dois botões: 'Sim, arquivar' (danger) + 'Manter organização' (secondary)"
    - "Confirmar 'Sim, arquivar' chama UPDATE supabase.from('orgs').update({ active: false }).eq('id', orgId)"
    - "Sucesso: invalida queryKey ['orgs'] e ['orgs', orgId] + toast.success('Organização arquivada') + dialog fecha"
    - "Org arquivada permanece na tabela mas com badge cinza 'Arquivada' e botão Arquivar substituído por '—' (desabilitado)"
    - "Erro: toast.error específico, dialog permanece aberto"
    - "Click overlay / Escape / 'Manter organização' fecha sem efeito"
  artifacts:
    - path: "roteiro-unificado/src/features/admin/useArchiveOrg.ts"
      provides: "Hook useMutation reutilizável para UPDATE active=false"
      exports: ["useArchiveOrg"]
    - path: "roteiro-unificado/src/components/admin/ArchiveOrgDialog.tsx"
      provides: "Dialog de confirmação controlado (estado open + org info via props)"
      exports: ["ArchiveOrgDialog"]
    - path: "roteiro-unificado/src/pages/admin/AdminDashboard.tsx"
      provides: "Handler handleArchive wired ao ArchiveOrgDialog (substituindo placeholder do Plan 03)"
    - path: "roteiro-unificado/src/pages/admin/OrgDetail.tsx"
      provides: "Botão 'Arquivar' no header do detalhe + wiring ao ArchiveOrgDialog"
  key_links:
    - from: "roteiro-unificado/src/components/admin/ArchiveOrgDialog.tsx"
      to: "useArchiveOrg().mutate"
      via: "Submit handler do botão 'Sim, arquivar'"
      pattern: "useArchiveOrg\\("
    - from: "roteiro-unificado/src/features/admin/useArchiveOrg.ts"
      to: "supabase.from('orgs').update"
      via: "useMutation.mutationFn"
      pattern: "from\\(['\"]orgs['\"]\\)\\.update\\(\\{\\s*active:\\s*false"
    - from: "roteiro-unificado/src/pages/admin/AdminDashboard.tsx"
      to: "ArchiveOrgDialog"
      via: "Render condicional baseado em state { orgId, orgName } ou null"
      pattern: "<ArchiveOrgDialog"
---

<objective>
Implementar o fluxo de arquivamento de organização (D-XX implícito do ROADMAP — "Implementar desativação
de organização: campo active em orgs; botão 'Arquivar' com dialog de confirmação"). Inclui hook
`useArchiveOrg` reutilizável, componente `ArchiveOrgDialog` de confirmação, e wiring dos dois pontos
de entrada: botão na linha da tabela (AdminDashboard) e botão no header da página de detalhe (OrgDetail).

Purpose: Org arquivada continua existindo (dados preservados) mas é visualmente marcada como inativa
e perde a capacidade de receber novos membros (Plan 04 já desabilitou "Convidar Membro" quando
!org.active). Não há DELETE — apenas UPDATE active=false.

Output: Admin consegue arquivar uma org, ela aparece com badge cinza na listagem, e Convidar Membro
fica desabilitado para essa org.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-gest-o-de-organiza-es-painel-admin/04-CONTEXT.md
@.planning/phases/04-gest-o-de-organiza-es-painel-admin/04-PATTERNS.md
@.planning/phases/04-gest-o-de-organiza-es-painel-admin/04-UI-SPEC.md
@.planning/phases/04-gest-o-de-organiza-es-painel-admin/04-01-PLAN.md
@.planning/phases/04-gest-o-de-organiza-es-painel-admin/04-03-PLAN.md
@.planning/phases/04-gest-o-de-organiza-es-painel-admin/04-04-PLAN.md

@roteiro-unificado/src/lib/supabase.ts
@roteiro-unificado/src/types/database.ts
@roteiro-unificado/src/components/ui/index.ts
@roteiro-unificado/src/hooks/useToast.ts

<interfaces>
From src/components/ui/dialog.tsx (Plan 01):
```typescript
<Dialog open={boolean} onClose={() => void}>
  <DialogHeader><DialogTitle /><DialogDescription /></DialogHeader>
  <DialogContent />
  <DialogFooter />
</Dialog>
```

From @tanstack/react-query:
```typescript
useMutation({ mutationFn, onSuccess, onError })
useQueryClient().invalidateQueries({ queryKey })
```

From src/types/database.ts:
```typescript
type OrgUpdate = Database['public']['Tables']['orgs']['Update']
// shape: { id?, name?, cnpj?, active?, created_at? }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar hook useArchiveOrg (useMutation reutilizável)</name>
  <files>roteiro-unificado/src/features/admin/useArchiveOrg.ts</files>
  <read_first>
    roteiro-unificado/src/features/admin/useOrgs.ts (criado em Plan 02 — padrão de hook),
    roteiro-unificado/src/lib/supabase.ts,
    roteiro-unificado/src/types/database.ts,
    .planning/phases/04-gest-o-de-organiza-es-painel-admin/04-UI-SPEC.md (linhas 348-353 — toast copy "Organização arquivada")
  </read_first>
  <action>
    Criar `src/features/admin/useArchiveOrg.ts`.

    Imports:
    - `import { useMutation, useQueryClient } from '@tanstack/react-query'`
    - `import { supabase } from '@/lib/supabase'`
    - `import { useToast } from '@/hooks/useToast'`

    Hook:
    ```
    export function useArchiveOrg() {
      const queryClient = useQueryClient()
      const toast = useToast()

      return useMutation({
        mutationFn: async (orgId: string) => {
          const { error } = await supabase
            .from('orgs')
            .update({ active: false })
            .eq('id', orgId)
          if (error) throw error
        },
        onSuccess: (_data, orgId) => {
          queryClient.invalidateQueries({ queryKey: ['orgs'] })
          queryClient.invalidateQueries({ queryKey: ['orgs', orgId] })
          toast.success('Organização arquivada')
        },
        onError: () => {
          toast.error('Erro ao arquivar organização. Tente novamente.')
        },
      })
    }
    ```

    O segundo argumento `orgId` do callback `onSuccess(data, variables)` corresponde ao argumento passado para `mutate()`.

    Invalida `['orgs']` (atualiza listagem com badge Arquivada) E `['orgs', orgId]` (atualiza detalhe se aberto).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; grep -c "export function useArchiveOrg\|update({ active: false })\|invalidateQueries({ queryKey: \\['orgs'\\]" src/features/admin/useArchiveOrg.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/features/admin/useArchiveOrg.ts` exists
    - Exports named function `useArchiveOrg`
    - Returns a `useMutation` result (calls `useMutation({...})` and returns it)
    - `mutationFn` calls `supabase.from('orgs').update({ active: false }).eq('id', orgId)`
    - `onSuccess` invalidates both `['orgs']` and `['orgs', orgId]` queries
    - Contains literal `Organização arquivada` (success toast)
    - Contains literal `Erro ao arquivar organização. Tente novamente.` (error toast)
    - `npm run type-check` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>Hook useArchiveOrg disponível e invalida as 2 query keys relevantes no sucesso.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar ArchiveOrgDialog (confirmação com Dialog do Plan 01)</name>
  <files>roteiro-unificado/src/components/admin/ArchiveOrgDialog.tsx</files>
  <read_first>
    roteiro-unificado/src/components/ui/dialog.tsx (criado em Plan 01 — Dialog/DialogHeader/etc.),
    roteiro-unificado/src/components/admin/CreateOrgModal.tsx (criado em Plan 03 — padrão de modal com mutation),
    roteiro-unificado/src/features/admin/useArchiveOrg.ts (Task 1 deste plano),
    .planning/phases/04-gest-o-de-organiza-es-painel-admin/04-UI-SPEC.md (linhas 316-326 — fluxo de confirmação; linhas 383-390 — copy exato do dialog: título, corpo, botões)
  </read_first>
  <action>
    Criar `src/components/admin/ArchiveOrgDialog.tsx`.

    Imports:
    - `import { Button, Dialog, DialogHeader, DialogTitle, DialogContent, DialogFooter } from '@/components/ui'`
    - `import { useArchiveOrg } from '@/features/admin/useArchiveOrg'`

    Props:
    ```
    interface ArchiveOrgDialogProps {
      open: boolean
      orgId: string | null
      orgName: string | null
      onClose: () => void
    }
    ```

    Componente:
    ```
    export function ArchiveOrgDialog({ open, orgId, orgName, onClose }: ArchiveOrgDialogProps) {
      const archiveOrgMutation = useArchiveOrg()

      const handleConfirm = () => {
        if (!orgId) return
        archiveOrgMutation.mutate(orgId, {
          onSuccess: () => onClose(),
        })
      }

      // Só renderiza Dialog quando há org alvo
      if (!orgId || !orgName) return null

      return (
        <Dialog open={open} onClose={onClose}>
          <DialogHeader>
            <DialogTitle>Arquivar organização</DialogTitle>
          </DialogHeader>

          <DialogContent>
            <p className="text-sm text-gray-700">
              Tem certeza que deseja arquivar <strong className="font-semibold">{orgName}</strong>?
              A organização e seus dados serão mantidos, mas ela não aparecerá mais como ativa
              na listagem.
            </p>
          </DialogContent>

          <DialogFooter>
            <Button
              type="button"
              variant="secondary"
              onClick={onClose}
              disabled={archiveOrgMutation.isPending}
            >
              Manter organização
            </Button>
            <Button
              type="button"
              variant="danger"
              onClick={handleConfirm}
              isLoading={archiveOrgMutation.isPending}
            >
              {archiveOrgMutation.isPending ? 'Arquivando...' : 'Sim, arquivar'}
            </Button>
          </DialogFooter>
        </Dialog>
      )
    }
    ```

    Copy literal exigido (UI-SPEC §Copywriting Contract):
    - Título: "Arquivar organização"
    - Corpo: "Tem certeza que deseja arquivar **{nome da org}**? A organização e seus dados serão mantidos, mas ela não aparecerá mais como ativa na listagem."
    - Botão danger default/loading: "Sim, arquivar" / "Arquivando..."
    - Botão dispensar: "Manter organização"

    `onClose` é passado para o callback `onSuccess` do `mutate()` para fechar o dialog APÓS a mutation completar (não imediatamente — UX confirmando ação).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; grep -c "useArchiveOrg\|<Dialog open={open}\|Sim, arquivar\|Manter organização" src/components/admin/ArchiveOrgDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `roteiro-unificado/src/components/admin/ArchiveOrgDialog.tsx` exists
    - Exports `ArchiveOrgDialog` with props `{ open, orgId, orgName, onClose }`
    - Uses `useArchiveOrg()` from `@/features/admin/useArchiveOrg`
    - Contains literals: `Arquivar organização`, `Sim, arquivar`, `Arquivando...`, `Manter organização`
    - Renders the org name with `<strong` markup (bolded inside the body text)
    - Uses `<Button` with `variant="danger"` for the confirm action
    - Calls `archiveOrgMutation.mutate(orgId, { onSuccess: () => onClose() })`
    - Returns `null` early when `orgId` or `orgName` is null/undefined
    - `npm run type-check` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>Dialog de confirmação com copy correto, botão danger com isLoading, fecha após mutation success.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire ArchiveOrgDialog ao AdminDashboard + OrgDetail</name>
  <files>
    roteiro-unificado/src/pages/admin/AdminDashboard.tsx,
    roteiro-unificado/src/pages/admin/OrgDetail.tsx
  </files>
  <read_first>
    roteiro-unificado/src/pages/admin/AdminDashboard.tsx (versão do Plan 03 — handleArchive ainda é placeholder),
    roteiro-unificado/src/pages/admin/OrgDetail.tsx (versão do Plan 04 — comentário "Botão Arquivar será wired no Plan 05"),
    roteiro-unificado/src/components/admin/ArchiveOrgDialog.tsx (Task 2 deste plano),
    .planning/phases/04-gest-o-de-organiza-es-painel-admin/04-UI-SPEC.md (linhas 273-279 — header de detalhe com botão Arquivar danger)
  </read_first>
  <action>
    **Modificar `src/pages/admin/AdminDashboard.tsx`:**

    1. Adicionar import:
       `import { ArchiveOrgDialog } from '@/components/admin/ArchiveOrgDialog'`

    2. Adicionar state ao topo do componente (junto com `isCreateModalOpen`):
       ```
       const [archiveTarget, setArchiveTarget] = useState<{ id: string; name: string } | null>(null)
       ```

    3. Substituir o `handleArchive` placeholder (que chamava `toast.info('Funcionalidade disponível em breve')`) por:
       ```
       const handleArchive = (orgId: string, orgName: string) => {
         setArchiveTarget({ id: orgId, name: orgName })
       }
       ```

    4. Renderizar o dialog perto do `<CreateOrgModal>` (dentro do return, fora da `<div className="space-y-6">`):
       ```
       <ArchiveOrgDialog
         open={archiveTarget !== null}
         orgId={archiveTarget?.id ?? null}
         orgName={archiveTarget?.name ?? null}
         onClose={() => setArchiveTarget(null)}
       />
       ```

    **Modificar `src/pages/admin/OrgDetail.tsx`:**

    1. Adicionar imports:
       `import { ArchiveOrgDialog } from '@/components/admin/ArchiveOrgDialog'`
       (`useState` já é importado para `isAddMemberOpen` — reutilizar)

    2. Adicionar state:
       `const [isArchiveOpen, setIsArchiveOpen] = useState(false)`

    3. Adicionar botão "Arquivar" no header da página (dentro da `<div className="flex items-center justify-between">`, substituindo o comentário "Botão Arquivar será wired no Plan 05"):
       ```
       <Button
         variant="danger"
         onClick={() => setIsArchiveOpen(true)}
         disabled={!org.active}
       >
         {org.active ? 'Arquivar' : 'Arquivada'}
       </Button>
       ```

    4. Renderizar o dialog perto do `<AddMemberModal>` (dentro do return, depois dele):
       ```
       <ArchiveOrgDialog
         open={isArchiveOpen}
         orgId={org.id}
         orgName={org.name}
         onClose={() => setIsArchiveOpen(false)}
       />
       ```

    Comportamento garantido:
    - Click em "Arquivar" na tabela do dashboard abre o dialog com a org específica.
    - Click em "Arquivar" no header do detalhe abre o dialog com a org atual.
    - Confirmação no dialog dispara mutation, fecha o dialog no sucesso, e org muda para badge "Arquivada" na listagem (via invalidação).
    - Botão "Arquivar" no detalhe fica disabled e exibe "Arquivada" quando `!org.active` (orgs já arquivadas não podem ser arquivadas de novo).
    - Botão "Arquivar" na linha da tabela já está disabled quando !org.active desde Plan 02 (per OrgTable).
  </action>
  <verify>
    <automated>cd roteiro-unificado &amp;&amp; npm run type-check &amp;&amp; npm run lint -- src/pages/admin/ src/components/admin/ArchiveOrgDialog.tsx src/features/admin/useArchiveOrg.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `roteiro-unificado/src/pages/admin/AdminDashboard.tsx` imports `ArchiveOrgDialog`
    - Contains `useState<{ id: string; name: string } | null>(null)` (or equivalent typed state for archive target)
    - Calls `setArchiveTarget({ id: orgId, name: orgName })` inside handleArchive
    - Renders `<ArchiveOrgDialog` with `open`, `orgId`, `orgName`, `onClose` props
    - Does NOT still contain the placeholder `Funcionalidade disponível em breve` for archive handler (new-org placeholder already removed in Plan 03)
    - `roteiro-unificado/src/pages/admin/OrgDetail.tsx` imports `ArchiveOrgDialog`
    - `OrgDetail.tsx` contains `useState(false)` (or equivalent) for `isArchiveOpen` state
    - `OrgDetail.tsx` contains a `<Button variant="danger"` with `onClick={() => setIsArchiveOpen(true)}` and `disabled={!org.active}`
    - `OrgDetail.tsx` renders `<ArchiveOrgDialog` with `open={isArchiveOpen}`
    - `OrgDetail.tsx` does NOT still contain comment placeholder mentioning "Plan 05" / "será wired"
    - `npm run build` from `roteiro-unificado/` exits 0
    - `npm run lint` from `roteiro-unificado/` exits 0
  </acceptance_criteria>
  <done>Ambas as páginas (dashboard e detalhe) disparam o ArchiveOrgDialog; org arquivada aparece com badge cinza após confirmação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase UPDATE orgs | Admin envia UPDATE active=false em orgs.id; RLS is_admin() autoriza |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Tampering | Atacante chama UPDATE com other_fields além de active | accept | RLS is_admin() autoriza admin a fazer qualquer UPDATE em orgs. Mutation hardcoded apenas envia `{ active: false }` — não há campos extras controlados por input do usuário. |
| T-04-27 | Denial of Service | Admin arquiva todas as orgs sem confirmação | mitigate | ArchiveOrgDialog requer confirmação explícita ("Sim, arquivar"). Arquivamento é reversível via UPDATE active=true (manual no DB para o piloto — fora do escopo de UI da Phase 4). |
| T-04-28 | Repudiation | Admin nega ter arquivado uma org | accept | Supabase logs registram a UPDATE com auth.uid() do admin. Audit log explícito não está no escopo. |
| T-04-29 | Information Disclosure | Listagem revela orgs arquivadas para outros admins | accept | Todos os admins podem ver todas as orgs (ativas e arquivadas) — comportamento intencional para gestão interna. |
| T-04-30 | Tampering | Cliente bypassa o dialog e chama mutation diretamente via DevTools | accept | Admin tem permissão RLS para fazer a UPDATE. UI dialog é UX de segurança, não controle de acesso. Comportamento aceito. |
| T-04-31 | Tampering | Construtora (role=company) tenta arquivar org via DevTools | mitigate | RLS de orgs.UPDATE requer is_admin() (Phase 2). Tentativa de role=company retorna 0 linhas afetadas + erro silencioso. AdminRoute upstream também bloqueia acesso à página. |
</threat_model>

<verification>
- Admin clica "Arquivar" na linha da org X → dialog abre com nome em negrito
- Confirmar "Sim, arquivar" executa UPDATE no Supabase → toast success → linha muda para badge cinza "Arquivada"
- Click "Manter organização" / Escape / overlay fecha sem efeito
- Botão "Convidar Membro" na página de detalhe fica disabled após arquivar (Plan 04 já implementou via `!org.active`)
- `npm run build` completa sem erros
</verification>

<success_criteria>
- `useArchiveOrg` exporta useMutation que UPDATE active=false e invalida ['orgs'] + ['orgs', orgId]
- `ArchiveOrgDialog` renderiza nome da org em negrito e tem botões "Sim, arquivar" (danger) + "Manter organização" (secondary)
- AdminDashboard.handleArchive abre o dialog com estado `archiveTarget`
- OrgDetail tem botão "Arquivar" no header que abre o mesmo dialog
- Após confirmar, org permanece visível mas com badge "Arquivada" cinza
</success_criteria>

<output>
Create `.planning/phases/04-gest-o-de-organiza-es-painel-admin/04-05-SUMMARY.md` when done
</output>
