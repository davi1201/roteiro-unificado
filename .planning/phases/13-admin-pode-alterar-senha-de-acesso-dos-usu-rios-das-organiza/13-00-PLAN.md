---
phase: 13
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - roteiro-unificado/src/components/admin/__tests__/MemberTable.test.tsx
  - roteiro-unificado/src/components/admin/__tests__/ResetPasswordModal.test.tsx
  - roteiro-unificado/src/features/form/__tests__/ChangePasswordModal.test.tsx
autonomous: true
requirements:
  - D-01
  - D-04
  - D-06
must_haves:
  truths:
    - "Três arquivos de teste existem nos caminhos corretos com estrutura válida"
    - "npx vitest run executa sem erro de parsing nos novos arquivos"
    - "Cada stub declara pelo menos um teste RED que falha por ausência do componente"
  artifacts:
    - path: "roteiro-unificado/src/components/admin/__tests__/MemberTable.test.tsx"
      provides: "Stub de teste RTL para MemberTable (N-03)"
    - path: "roteiro-unificado/src/components/admin/__tests__/ResetPasswordModal.test.tsx"
      provides: "Stub de teste RTL para ResetPasswordModal (N-03, N-04)"
    - path: "roteiro-unificado/src/features/form/__tests__/ChangePasswordModal.test.tsx"
      provides: "Stub de teste RTL para ChangePasswordModal (N-04)"
  key_links:
    - from: "roteiro-unificado/src/components/admin/__tests__/MemberTable.test.tsx"
      to: "roteiro-unificado/src/components/admin/MemberTable.tsx"
      via: "import relativo"
      pattern: "from.*MemberTable"
---

<objective>
Criar stubs de teste (Wave 0) para os três componentes que serão implementados na fase 13. Os stubs declaram os comportamentos esperados como testes RED — eles falham agora porque os componentes/props não existem, e passarão após a implementação.

Propósito: Garantir que cada plan subsequente tenha um comando de verify automatizado funcional. Sem esses stubs, os planos de implementação não têm forma de verificar automaticamente os comportamentos corretos.

Output: Três arquivos de teste com diretórios `__tests__` criados; stubs compilam e geram falhas esperadas.
</objective>

<execution_context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado/src/components/admin/CompanyCard.test.tsx
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado/src/schemas/identificacao.test.ts
</execution_context>

<context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/ROADMAP.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-CONTEXT.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-VALIDATION.md

<interfaces>
<!-- Tipos existentes que os stubs precisam para compilar -->

De roteiro-unificado/src/types/database.ts (relevante):
- OrgMemberWithEmail: { user_id: string, email: string, role: 'admin' | 'company', ... }

MemberTable props atuais (src/components/admin/MemberTable.tsx):
- members: OrgMemberWithEmail[] | undefined
- isLoading: boolean

Após modificação (Wave 3, mas stubs devem já declarar a nova prop):
- onResetPassword?: (userId: string) => void   ← nova prop que será adicionada

ChangePasswordModal props (será criado em Wave 2):
- open: boolean
- onClose: () => void

ResetPasswordModal props (será criado em Wave 2):
- userId: string
- memberEmail?: string
- open: boolean
- onClose: () => void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar stubs de teste para MemberTable e ResetPasswordModal</name>
  <files>
    roteiro-unificado/src/components/admin/__tests__/MemberTable.test.tsx
    roteiro-unificado/src/components/admin/__tests__/ResetPasswordModal.test.tsx
  </files>
  <read_first>
    - roteiro-unificado/src/components/admin/CompanyCard.test.tsx — padrão de import, describe/it, vi.mock e fixtures tipadas a replicar
    - roteiro-unificado/src/components/admin/MemberTable.tsx — interface atual de props e shape do componente existente
    - roteiro-unificado/src/types/database.ts — tipo OrgMemberWithEmail para fixture tipada
  </read_first>
  <action>
Criar o diretório `roteiro-unificado/src/components/admin/__tests__/` e os dois arquivos de stub.

MemberTable.test.tsx — dois testes que cobrem N-03 (VALIDATION.md):
- Importar MemberTable de `../MemberTable`
- Importar tipo OrgMemberWithEmail de `@/types/database`
- Criar duas fixtures: memberCompany com role `'company'` e memberAdmin com role `'admin'`; preencher todos os campos obrigatórios do tipo (user_id, email, role, org_id, created_at)
- Mockar `react-router-dom` (se MemberTable usar Link/useNavigate) usando vi.mock seguindo o padrão de CompanyCard.test.tsx
- Teste 1: render com memberCompany + onResetPassword={vi.fn()} → screen.getByRole('button', { name: /redefinir senha/i }) deve existir
- Teste 2: render com memberAdmin + onResetPassword={vi.fn()} → screen.queryByRole('button', { name: /redefinir senha/i }) deve ser null
- Nota: estes testes vão FALHAR agora (prop onResetPassword ainda não existe na MemberTable) — isso é esperado; eles passarão após Wave 3

ResetPasswordModal.test.tsx — dois testes que cobrem comportamento visual:
- Importar ResetPasswordModal de `../ResetPasswordModal` (arquivo que será criado em Wave 2)
- Stub com vi.mock para supabase (`@/lib/supabase`), useToast (`@/hooks/useToast`), e @tanstack/react-query
- Teste 1: quando open=true, o Dialog renderiza e exibe "Redefinir senha" como título
- Teste 2: quando open=false, o Dialog não renderiza conteúdo visível
- Estes testes vão FALHAR agora (arquivo não existe) — esperado

Padrão de mock para Supabase (adaptar de CompanyCard.test.tsx se ele mockar, caso contrário usar):
vi.mock('@/lib/supabase', () => ({ supabase: { functions: { invoke: vi.fn() } } }))
vi.mock('@/hooks/useToast', () => ({ useToast: () => ({ success: vi.fn(), error: vi.fn() }) }))
vi.mock('@tanstack/react-query', async () => { const real = await vi.importActual('@tanstack/react-query'); return { ...real, useMutation: vi.fn(() => ({ mutate: vi.fn(), isPending: false })) } })
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run src/components/admin/__tests__/ --reporter=verbose 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - Diretório `roteiro-unificado/src/components/admin/__tests__/` existe
    - `MemberTable.test.tsx` contém describe('MemberTable', ...) com pelo menos 2 testes
    - `ResetPasswordModal.test.tsx` contém describe('ResetPasswordModal', ...) com pelo menos 2 testes
    - `npx vitest run src/components/admin/__tests__/` roda sem erro de sintaxe/parse — testes podem FALHAR com "Cannot find module" ou assertion errors, mas não com syntax errors
  </acceptance_criteria>
  <done>Dois arquivos de stub existem em `src/components/admin/__tests__/`; vitest os reconhece e os executa (ainda que com falhas esperadas por ausência dos módulos implementados)</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar stub de teste para ChangePasswordModal</name>
  <files>
    roteiro-unificado/src/features/form/__tests__/ChangePasswordModal.test.tsx
  </files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.test.tsx — padrão de mock para features/form (useAuth, useFormStore, react-router-dom)
    - roteiro-unificado/src/components/admin/CompanyCard.test.tsx — padrão geral de teste de componente RTL
  </read_first>
  <action>
Criar o diretório `roteiro-unificado/src/features/form/__tests__/` e o arquivo ChangePasswordModal.test.tsx.

O stub deve conter três testes que cobrem N-04 (VALIDATION.md):
- Importar ChangePasswordModal de `../ChangePasswordModal` (arquivo que será criado em Wave 2)
- Mockar dependências: `@/lib/supabase` com supabase.auth.updateUser e supabase.auth.signInWithPassword como vi.fn(); `@/hooks/useToast` retornando { success: vi.fn(), error: vi.fn() }; `@tanstack/react-query` com useMutation como vi.fn retornando { mutate: vi.fn(), isPending: false }; `@/features/auth/useAuth` retornando { user: { email: 'test@example.com' } }
- Teste 1 (N-04): render com open=true → modal exibe três campos ("Senha atual", "Nova senha", "Confirmar nova senha")
- Teste 2: render com open=true → botão "Salvar" está presente e o botão "Cancelar" está presente
- Teste 3: render com open=false → conteúdo do modal não está visível
- Estes testes vão FALHAR agora (arquivo não existe) — esperado

Usar padrão de wrapper com QueryClientProvider e BrowserRouter se o componente precisar (seguir FormLayout.test.tsx se ele usa esse wrapper, senão omitir).
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run src/features/form/__tests__/ --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Diretório `roteiro-unificado/src/features/form/__tests__/` existe
    - `ChangePasswordModal.test.tsx` contém describe('ChangePasswordModal', ...) com pelo menos 3 testes
    - `npx vitest run src/features/form/__tests__/` roda sem erro de sintaxe — falhas por módulo ausente são esperadas
  </acceptance_criteria>
  <done>Arquivo stub existe em `src/features/form/__tests__/`; vitest o reconhece e executa</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → source files | Import de módulos que ainda não existem (Wave 0 por definição) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-W0-01 | Tampering | Test stubs | accept | Stubs são artefatos de desenvolvimento; sem dados sensíveis; sem acesso a runtime real |
| T-13-W0-SC | Tampering | npm/pip install | accept | Nenhum pacote novo instalado neste plano; todas as dependências (vitest, @testing-library/react) já estão no package.json |
</threat_model>

<verification>
Após ambas as tarefas:

```bash
cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run --reporter=verbose 2>&1 | tail -20
```

Resultado esperado: suite existente passa (verde); novos stubs aparecem como FAIL com "Cannot find module" ou falhas de assertion — não como erros de parse/sintaxe.
</verification>

<success_criteria>
- Três arquivos de stub existem nos caminhos corretos
- Nenhum erro de sintaxe TypeScript nos stubs (compilam com tsc --noEmit)
- Suite existente (schemas, outros componentes) continua verde
- Novos stubs são reconhecidos pelo vitest como suites de teste válidas
</success_criteria>

<output>
Criar `.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-00-SUMMARY.md` quando concluído
</output>
