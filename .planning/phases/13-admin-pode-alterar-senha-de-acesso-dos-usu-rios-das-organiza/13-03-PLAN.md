---
phase: 13
plan: "03"
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - roteiro-unificado/src/components/admin/MemberTable.tsx
  - roteiro-unificado/src/pages/admin/OrgDetail.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
must_haves:
  truths:
    - "MemberTable exibe coluna 'Ações' com botão 'Redefinir senha' para linhas com role='company'"
    - "MemberTable NÃO exibe o botão para linhas com role='admin'"
    - "Clicar em 'Redefinir senha' em OrgDetail abre ResetPasswordModal com o userId correto"
    - "OrgDetail passa memberEmail (via lookup nos members) para o modal"
    - "isLoading={false} permanece hardcoded em MemberTable (Pitfall 3 do RESEARCH.md)"
  artifacts:
    - path: "roteiro-unificado/src/components/admin/MemberTable.tsx"
      provides: "Tabela com coluna Ações e prop onResetPassword"
      contains: "onResetPassword"
    - path: "roteiro-unificado/src/pages/admin/OrgDetail.tsx"
      provides: "Controller com estado resetPasswordMemberId e renderização condicional do modal"
      contains: "resetPasswordMemberId"
  key_links:
    - from: "roteiro-unificado/src/pages/admin/OrgDetail.tsx"
      to: "roteiro-unificado/src/components/admin/MemberTable.tsx"
      via: "prop onResetPassword callback"
      pattern: "onResetPassword.*setResetPasswordMemberId"
    - from: "roteiro-unificado/src/pages/admin/OrgDetail.tsx"
      to: "roteiro-unificado/src/components/admin/ResetPasswordModal.tsx"
      via: "renderização condicional com resetPasswordMemberId"
      pattern: "resetPasswordMemberId && .*ResetPasswordModal"
---

<objective>
Conectar o Fluxo A (admin reset de senha) ao painel de administração: adicionar coluna "Ações" na MemberTable e gerenciar estado do modal em OrgDetail.

Propósito: Após este plano, o admin consegue clicar em "Redefinir senha" em qualquer membro com role='company' e o modal abre com o contexto correto (userId + email do membro).

Output: MemberTable com prop nova; OrgDetail com estado e modal conectado. Sem novas rotas ou arquivos.
</objective>

<execution_context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado/src/components/admin/AddMemberModal.tsx
</execution_context>

<context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/ROADMAP.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-CONTEXT.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-PATTERNS.md

<interfaces>
<!-- Interfaces relevantes extraídas do codebase -->

MemberTable props ATUAIS (src/components/admin/MemberTable.tsx):
  interface MemberTableProps {
    members: OrgMemberWithEmail[] | undefined
    isLoading: boolean
  }

MemberTable props APÓS MODIFICAÇÃO:
  interface MemberTableProps {
    members: OrgMemberWithEmail[] | undefined
    isLoading: boolean
    onResetPassword?: (userId: string) => void   ← adicionar esta prop
  }

OrgMemberWithEmail (src/types/database.ts):
  { user_id: string, email: string, role: 'admin' | 'company', org_id: string, created_at: string }

OrgDetail estado atual (src/pages/admin/OrgDetail.tsx):
  const [isAddMemberOpen, setIsAddMemberOpen] = useState(false)
  const [isArchiveOpen, setIsArchiveOpen] = useState(false)
  Linha 79: <MemberTable members={members} isLoading={false} />

OrgDetail estado APÓS MODIFICAÇÃO:
  const [resetPasswordMemberId, setResetPasswordMemberId] = useState<string | null>(null)

ResetPasswordModal props (criado em Wave 2):
  interface ResetPasswordModalProps {
    userId: string
    memberEmail?: string
    open: boolean
    onClose: () => void
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Expandir MemberTable com coluna Ações e prop onResetPassword</name>
  <files>
    roteiro-unificado/src/components/admin/MemberTable.tsx
  </files>
  <behavior>
    - Render com membro role='company' e onResetPassword={vi.fn()} → screen.getByRole('button', { name: /redefinir senha/i }) deve existir (N-03)
    - Render com membro role='admin' e onResetPassword={vi.fn()} → screen.queryByRole('button', { name: /redefinir senha/i }) deve ser null (N-03)
    - Clicar no botão de membro company deve chamar onResetPassword com o user_id correto
    - Render sem prop onResetPassword → botão não aparece para nenhum membro (prop é opcional)
  </behavior>
  <read_first>
    - roteiro-unificado/src/components/admin/MemberTable.tsx — LER COMPLETO antes de qualquer edição; entender estrutura atual de thead/tbody, células existentes e skeleton rows
    - roteiro-unificado/src/components/admin/__tests__/MemberTable.test.tsx — entender exatamente o que os stubs esperam para garantir que os testes passem
    - roteiro-unificado/src/components/ui/index.ts (ou barrel) — confirmar que Button está exportado de @/components/ui
  </read_first>
  <action>
Modificar MemberTable.tsx com precisão cirúrgica — três adições, sem alterar nada existente:

1. Adicionar prop à interface MemberTableProps: `onResetPassword?: (userId: string) => void`

2. No thead, adicionar quarta coluna "Ações" após a coluna "Role" existente. Usar exatamente a mesma classe CSS da coluna "Role": `className="w-36 px-4 py-3 text-left text-sm font-semibold text-gray-600"`. Conteúdo: `Ações`.

3. No tbody, adicionar quarta célula `<td>` em cada linha com o botão condicional:
   `{member.role === 'company' && onResetPassword && (<Button variant="secondary" size="sm" onClick={() => onResetPassword(member.user_id)}>Redefinir senha</Button>)}`
   Classe da célula: `className="px-4 py-3"`

4. No branch isLoading (skeleton rows), adicionar quarta célula td de skeleton com `<Skeleton className="h-4 w-24" />` para manter alinhamento da tabela.

MANTER INALTERADO: prop isLoading: boolean (Pitfall 3 do RESEARCH.md), todas as outras props e células existentes, estilo e estrutura da tabela.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run src/components/admin/__tests__/MemberTable.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - MemberTable.tsx contém `onResetPassword?: (userId: string) => void` na interface
    - MemberTable.tsx contém `member.role === 'company' && onResetPassword &&` no tbody
    - MemberTable.tsx contém `<th` com texto `Ações`
    - `npx vitest run src/components/admin/__tests__/MemberTable.test.tsx` — todos os testes passam
    - `npx tsc --noEmit` — sem erros em MemberTable.tsx
  </acceptance_criteria>
  <done>MemberTable com coluna Ações; botão visível para role='company', ausente para role='admin'; testes N-03 passando</done>
</task>

<task type="auto">
  <name>Tarefa 2: Conectar ResetPasswordModal ao OrgDetail</name>
  <files>
    roteiro-unificado/src/pages/admin/OrgDetail.tsx
  </files>
  <read_first>
    - roteiro-unificado/src/pages/admin/OrgDetail.tsx — LER COMPLETO antes de editar; identificar: linhas de useState existentes, linha 79 (MemberTable sem onResetPassword), renderização condicional dos modais existentes (AddMemberModal, ArchiveOrgDialog), imports no topo
    - roteiro-unificado/src/components/admin/ResetPasswordModal.tsx — confirmar a assinatura de props (userId, memberEmail?, open, onClose)
  </read_first>
  <action>
Modificar OrgDetail.tsx com três adições (per D-02 — sem dialog extra de confirmação):

1. Adicionar import: `import { ResetPasswordModal } from '@/components/admin/ResetPasswordModal'` no bloco de imports do topo, após os imports existentes de modais.

2. Adicionar estado após os estados isAddMemberOpen/isArchiveOpen existentes:
   `const [resetPasswordMemberId, setResetPasswordMemberId] = useState<string | null>(null)`

3. Modificar a linha 79 (chamada de MemberTable) para incluir o callback:
   ANTES: `<MemberTable members={members} isLoading={false} />`
   DEPOIS: `<MemberTable members={members} isLoading={false} onResetPassword={(userId) => setResetPasswordMemberId(userId)} />`
   CRÍTICO: manter `isLoading={false}` exatamente como estava (Pitfall 3)

4. Adicionar renderização condicional do ResetPasswordModal após os modais existentes (após o último bloco de modal: AddMemberModal e ArchiveOrgDialog):
   `{resetPasswordMemberId && (<ResetPasswordModal userId={resetPasswordMemberId} memberEmail={members?.find(m => m.user_id === resetPasswordMemberId)?.email} open={!!resetPasswordMemberId} onClose={() => setResetPasswordMemberId(null)} />)}`

MANTER INALTERADO: todos os estados existentes, props de MemberTable exceto onResetPassword, todos os outros modais e sua lógica.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx tsc --noEmit 2>&1 | grep -E "OrgDetail|ResetPasswordModal" || echo "Sem erros TS em OrgDetail e ResetPasswordModal"</automated>
  </verify>
  <acceptance_criteria>
    - OrgDetail.tsx contém `resetPasswordMemberId` como useState<string | null>(null)
    - OrgDetail.tsx contém `onResetPassword={(userId) => setResetPasswordMemberId(userId)}`
    - OrgDetail.tsx contém `isLoading={false}` na chamada de MemberTable (sem alteração)
    - OrgDetail.tsx contém `ResetPasswordModal` com `userId={resetPasswordMemberId}` e `memberEmail={members?.find(...)`
    - OrgDetail.tsx importa `ResetPasswordModal` de `@/components/admin/ResetPasswordModal`
    - `npx tsc --noEmit` — sem erros TypeScript em OrgDetail.tsx
    - `npx vitest run --reporter=verbose` — suite completa continua verde (sem regressões)
  </acceptance_criteria>
  <done>OrgDetail gerencia estado resetPasswordMemberId; MemberTable recebe callback onResetPassword; ResetPasswordModal renderiza condicionalmente com userId e email do membro</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → MemberTable | Admin vê botão "Redefinir senha" apenas para membros company (fronteira de autorização visual) |
| OrgDetail → ResetPasswordModal | userId passado via estado local; não vem de URL params ou querystring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Elevation of Privilege | MemberTable — botão visível para role admin | mitigate | Condicional `member.role === 'company' && onResetPassword` garante que botão não aparece para linhas admin (N-03); verificado por teste RTL |
| T-13-10 | Tampering | OrgDetail — resetPasswordMemberId vem de members existentes | accept | userId vem de OrgMemberWithEmail do banco, não de input do usuário; sem risco de injeção via UI |
| T-13-11 | Information Disclosure | memberEmail exibido no DialogDescription | accept | Admin já tem acesso à listagem de emails dos membros da org; exibir email no modal não adiciona risco |
| T-13-SC | Tampering | npm/pip install | accept | Nenhum pacote instalado neste plano |
</threat_model>

<verification>
```bash
# Testes N-03 passando (botão ausente para role='admin')
cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run src/components/admin/__tests__/MemberTable.test.tsx --reporter=verbose

# TypeScript sem erros
cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx tsc --noEmit 2>&1 | tail -5

# Suite completa verde
cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run 2>&1 | tail -10
```
</verification>

<success_criteria>
- MemberTable.tsx com coluna Ações e prop onResetPassword?: (userId: string) => void
- OrgDetail.tsx com estado resetPasswordMemberId e ResetPasswordModal condicional
- isLoading={false} preservado em OrgDetail (Pitfall 3 respeitado)
- Testes N-03 passando em verde
- TypeScript compila sem erros
- Nenhuma regressão na suite existente
</success_criteria>

<output>
Criar `.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-03-SUMMARY.md` quando concluído
</output>
