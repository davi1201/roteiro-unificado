---
phase: 13
plan: "04"
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - roteiro-unificado/src/features/form/FormLayout.tsx
autonomous: true
requirements:
  - D-03
  - D-06
must_haves:
  truths:
    - "FormLayout exibe botão 'Alterar senha' no footer da sidebar antes do botão 'Sair'"
    - "Clicar em 'Alterar senha' abre ChangePasswordModal"
    - "isChangePasswordOpen inicia como false e abre/fecha corretamente"
    - "Sidebar footer usa flex-col gap-2 para empilhar os dois botões verticalmente"
  artifacts:
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "FormLayout com botão 'Alterar senha' e estado de modal"
      contains: "isChangePasswordOpen"
  key_links:
    - from: "roteiro-unificado/src/features/form/FormLayout.tsx"
      to: "roteiro-unificado/src/features/form/ChangePasswordModal.tsx"
      via: "renderização condicional com isChangePasswordOpen"
      pattern: "isChangePasswordOpen && .*ChangePasswordModal"
---

<objective>
Conectar o Fluxo B (company self-service) ao FormLayout: adicionar botão "Alterar senha" no footer da sidebar e gerenciar o estado do ChangePasswordModal.

Propósito: Após este plano, usuários com role='company' conseguem acessar o modal de alteração de senha diretamente da sidebar do formulário, sem sair do contexto de preenchimento.

Output: FormLayout.tsx modificado com botão novo e modal conectado. Este plano é paralelo ao 13-03 pois toca arquivo diferente (sem overlap de files_modified).
</objective>

<execution_context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado/src/features/form/FormLayout.tsx
</execution_context>

<context>
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/ROADMAP.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-CONTEXT.md
@/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-PATTERNS.md

<interfaces>
<!-- Padrão do sidebar footer em FormLayout (extraído de PATTERNS.md — linhas 196–205) -->

ANTES (footer atual):
  <div className="border-primary-800 mt-auto border-t p-3">
    <Button variant="ghost" size="sm" className="hover:bg-primary-800 w-full justify-start text-white" onClick={handleSignOut}>
      Sair
    </Button>
  </div>

DEPOIS (footer com botão novo):
  <div className="border-primary-800 mt-auto border-t p-3 flex flex-col gap-2">
    <Button variant="ghost" size="sm" className="hover:bg-primary-800 w-full justify-start text-white" onClick={() => setIsChangePasswordOpen(true)}>
      Alterar senha
    </Button>
    <Button variant="ghost" size="sm" className="hover:bg-primary-800 w-full justify-start text-white" onClick={handleSignOut}>
      Sair
    </Button>
  </div>

Padrão de estado de modal existente em FormLayout (linha 93):
  const [isSubmitOpen, setIsSubmitOpen] = useState(false)
  ← adicionar: const [isChangePasswordOpen, setIsChangePasswordOpen] = useState(false)

Padrão de renderização condicional existente (linhas 318–352):
  {/* Dialog de submissão existente */}
  ← adicionar após: {isChangePasswordOpen && (<ChangePasswordModal open={isChangePasswordOpen} onClose={() => setIsChangePasswordOpen(false)} />)}

ChangePasswordModal props (criado em Wave 2):
  { open: boolean, onClose: () => void }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar botão 'Alterar senha' e conectar ChangePasswordModal ao FormLayout</name>
  <files>
    roteiro-unificado/src/features/form/FormLayout.tsx
  </files>
  <read_first>
    - roteiro-unificado/src/features/form/FormLayout.tsx — LER COMPLETO antes de editar; identificar: linha exata do estado isSubmitOpen, localização exata do footer da sidebar (div com mt-auto border-t), onde o Dialog de submissão é renderizado, imports existentes no topo
    - roteiro-unificado/src/features/form/ChangePasswordModal.tsx — confirmar assinatura de props (open, onClose)
  </read_first>
  <action>
Modificar FormLayout.tsx com quatro adições precisas (per D-03):

1. Adicionar import: `import { ChangePasswordModal } from '@/features/form/ChangePasswordModal'` no bloco de imports do topo.

2. Adicionar estado logo após o estado isSubmitOpen existente (linha ~93):
   `const [isChangePasswordOpen, setIsChangePasswordOpen] = useState(false)`

3. Modificar o footer da sidebar (div com className contendo "mt-auto border-t"):
   - Adicionar `flex flex-col gap-2` à div do footer (sem remover as classes existentes)
   - Inserir Button "Alterar senha" ANTES do Button "Sair" existente, com:
     variant="ghost" size="sm" className="hover:bg-primary-800 w-full justify-start text-white" onClick={() => setIsChangePasswordOpen(true)
   - Manter o Button "Sair" EXATAMENTE como está (não alterar onClick, classes ou texto)

4. Adicionar renderização condicional do ChangePasswordModal após o Dialog de submissão existente:
   `{isChangePasswordOpen && (<ChangePasswordModal open={isChangePasswordOpen} onClose={() => setIsChangePasswordOpen(false)} />)}`

MANTER INALTERADO: toda a lógica de abas, autosave, submissão, navegação, guards de tenant, todos os estados existentes, todas as outras seções do JSX.
  </action>
  <verify>
    <automated>cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx tsc --noEmit 2>&1 | grep -E "FormLayout|ChangePasswordModal" || echo "Sem erros TS em FormLayout" && npx vitest run src/features/form/FormLayout.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - FormLayout.tsx importa ChangePasswordModal de `@/features/form/ChangePasswordModal`
    - FormLayout.tsx contém `isChangePasswordOpen` como useState(false)
    - FormLayout.tsx contém `onClick={() => setIsChangePasswordOpen(true)` em Button "Alterar senha"
    - FormLayout.tsx contém `isChangePasswordOpen && (<ChangePasswordModal`
    - O botão "Sair" ainda existe e seu onClick={handleSignOut} não foi alterado
    - O div do footer da sidebar contém `flex flex-col gap-2` (dois botões empilhados)
    - `npx tsc --noEmit` — sem erros TypeScript em FormLayout.tsx
    - `npx vitest run src/features/form/FormLayout.test.tsx` — suite existente do FormLayout continua verde (sem regressões)
  </acceptance_criteria>
  <done>FormLayout com botão "Alterar senha" no footer da sidebar; estado isChangePasswordOpen gerencia abertura/fechamento; ChangePasswordModal renderiza condicionalmente</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FormLayout sidebar → ChangePasswordModal | Botão abre modal com supabase.auth.signInWithPassword + updateUser; sem dados externos passados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Spoofing | FormLayout — botão "Alterar senha" visível para todos os roles | accept | FormLayout só é renderizado para usuários autenticados com role='company' (rota protegida via ProtectedRoute + CompanyRoute no router); admin nunca acessa FormLayout |
| T-13-13 | Tampering | FormLayout.tsx — modificação de componente crítico | mitigate | Modificação cirúrgica (4 adições, zero remoções de lógica existente); FormLayout.test.tsx verifica que suite existente continua verde |
| T-13-SC | Tampering | npm install | accept | Nenhum pacote instalado neste plano |
</threat_model>

<verification>
```bash
# TypeScript sem erros em FormLayout
cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx tsc --noEmit 2>&1 | grep FormLayout || echo "Sem erros"

# Testes de FormLayout continuam verdes (sem regressão)
cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run src/features/form/FormLayout.test.tsx --reporter=verbose

# Suite completa verde
cd "/Users/DaviAlves/Documents/Desenvolvimento/Giba/Roteiro Unificado/roteiro-unificado" && npx vitest run 2>&1 | tail -10
```
</verification>

<success_criteria>
- FormLayout.tsx contém botão "Alterar senha" no footer da sidebar antes de "Sair" (per D-03)
- ChangePasswordModal conectado via isChangePasswordOpen
- FormLayout.test.tsx continua verde (sem regressão)
- TypeScript compila sem erros
</success_criteria>

<output>
Criar `.planning/phases/13-admin-pode-alterar-senha-de-acesso-dos-usu-rios-das-organiza/13-04-SUMMARY.md` quando concluído
</output>
