---
phase: quick
plan: 260525-n1d
type: execute
wave: 1
depends_on: []
files_modified:
  - roteiro-unificado/src/features/form/HistoryContent.tsx
  - roteiro-unificado/src/features/form/HistoryPage.tsx
  - roteiro-unificado/src/features/form/TabNavigation.tsx
  - roteiro-unificado/src/features/form/FormLayout.tsx
autonomous: true
requirements: [QUICK-260525-n1d]

must_haves:
  truths:
    - "A construtora vê um item 'Histórico' no menu lateral, abaixo das 10 etapas do formulário"
    - "Ao clicar em 'Histórico', a lista de versões aparece dentro do <main>, sem trocar de rota/página"
    - "Ao clicar em qualquer etapa do formulário, o conteúdo volta a renderizar a seção do formulário dentro do mesmo <main>"
    - "A rota separada /form/:orgId/history continua funcionando (não quebra) usando o mesmo conteúdo extraído"
  artifacts:
    - path: "roteiro-unificado/src/features/form/HistoryContent.tsx"
      provides: "Conteúdo da lista de histórico (sem wrapper de página), reutilizável em FormLayout e HistoryPage"
      contains: "export function HistoryContent"
    - path: "roteiro-unificado/src/features/form/FormLayout.tsx"
      provides: "Sidebar com item Histórico + render condicional form/histórico dentro do <main>"
  key_links:
    - from: "roteiro-unificado/src/features/form/FormLayout.tsx"
      to: "roteiro-unificado/src/features/form/HistoryContent.tsx"
      via: "import e render condicional quando a view ativa é 'historico'"
      pattern: "HistoryContent"
    - from: "roteiro-unificado/src/features/form/TabNavigation.tsx"
      to: "FormLayout view state"
      via: "callback de seleção do item Histórico"
      pattern: "onSelectHistory|Histórico"
---

<objective>
Adicionar um item "Histórico" no menu lateral (sidebar) do formulário, ao lado/abaixo das 10 etapas, e renderizar a lista de versões de avaliação **dentro do `<main>`** do `FormLayout` — em vez de navegar para a rota separada `/form/:orgId/history`.

Purpose: Unificar a navegação da construtora em uma única tela. Hoje o histórico vive em uma página separada (`HistoryPage` na rota `/form/:orgId/history`), com seu próprio header e botão "Voltar". O usuário quer que histórico seja apenas mais um item da sidebar, mantendo o conteúdo na mesma área principal das etapas.

Output:
- Novo componente `HistoryContent.tsx` (lista de versões sem o wrapper de página) reutilizado por `FormLayout` e `HistoryPage`.
- Item "Histórico" na sidebar via `TabNavigation`.
- `FormLayout` com estado de view (`form` | `historico`) controlando o que renderiza no `<main>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@roteiro-unificado/src/features/form/FormLayout.tsx
@roteiro-unificado/src/features/form/HistoryPage.tsx
@roteiro-unificado/src/features/form/TabNavigation.tsx
@roteiro-unificado/src/features/form/tabConfig.ts

<interfaces>
<!-- Contratos relevantes já existentes no codebase. Use-os diretamente. -->

De src/features/form/HistoryPage.tsx (estado atual):
- `export function HistoryPage()` — aplica cross-tenant guard via useParams/useAuth, depois renderiza `HistoryPageContent`.
- `function HistoryPageContent({ orgId }: { orgId: string })` — wrapper de página: `div.mx-auto.max-w-3xl.px-4.py-8`, botão "← Voltar ao Formulário" (navigate(`/form/${orgId}`)), `<h1>Histórico de Avaliações</h1>`, subtítulo, e a lista de versões (skeleton / empty state / cards). Hooks internos usados: `useAssessmentHistory(orgId)` (local ao arquivo), `useNewRevision(orgId)`, `useOrgInfo(orgId)`. Renderiza `<ExportPdfButton>` e `<Badge>`.

De src/features/form/TabNavigation.tsx:
- `export function TabNavigation({ tenantId }: { tenantId: string })` — renderiza `<nav>` com `TAB_CONFIG.map(...)` produzindo um `<button>` por etapa. Usa `store.activeTab` para destacar a etapa ativa e `store.visitedTabs` para o badge "done". handleSelect escreve `window.history.replaceState(null,'','#'+tab)` + `store.setActiveTab` + `store.markTabVisited`.

De src/features/form/FormLayout.tsx:
- Sidebar desktop em `<aside>`: cabeçalho "Roteiro Unificado", `<TabNavigation tenantId={tenantId} />`, e botão "Sair" no rodapé.
- `<main>` renderiza a topbar fixa (label da aba + AutosaveIndicator), `<ReadinessClassification>`, e via `renderSection(store.activeTab, tenantId)` a seção do formulário, mais o footer sticky com "Anterior / Próxima aba / Enviar Avaliação".
- Mobile: `<TabNavigation>` também é renderizado em uma barra horizontal `md:hidden`.

De src/stores/formStore.ts:
- `enum TabKey` com 10 valores ('identificacao' ... 'nda').
- `store.activeTab`, `store.setActiveTab(tab)`, `store.markTabVisited(tab)`, `store.visitedTabs: Set<TabKey>`.

De src/router.tsx:
- Rota existente `{ path: '/form/:orgId/history', element: <HistoryPage /> }` — DEVE continuar funcionando.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair HistoryContent reutilizável de HistoryPage</name>
  <files>roteiro-unificado/src/features/form/HistoryContent.tsx, roteiro-unificado/src/features/form/HistoryPage.tsx</files>
  <action>
Criar `HistoryContent.tsx` extraindo a lista de versões de `HistoryPageContent` — SEM o wrapper de página (sem `mx-auto max-w-3xl px-4 py-8`, sem `<h1>`/subtítulo, sem botão "← Voltar ao Formulário") e SEM o cross-tenant guard (esse continua sendo responsabilidade do caller). Exportar `export function HistoryContent({ orgId }: { orgId: string })`.

Mover para `HistoryContent.tsx` o hook interno `useAssessmentHistory`, o tipo `AssessmentRow`, e toda a lógica de render dos estados: skeleton (3 cards), empty state, e a lista de `<article>` com badges de status, `<Badge grade>`, `Ver detalhes`, `<ExportPdfButton>` e `Iniciar Nova Revisão`. Manter os imports de `useNewRevision`, `useOrgInfo`, `ExportPdfButton`, `Badge`, `Skeleton`. O conteúdo extraído NÃO deve usar `useNavigate` para "voltar" (o botão voltar fica fora). Manter títulos/subtítulos opcionais: aceitar uma prop `showHeading?: boolean` (default true) que controla se renderiza o `<h1>Histórico de Avaliações</h1>` + subtítulo internamente — assim `HistoryPage` mantém o cabeçalho atual e o `FormLayout` pode passar `showHeading={false}` (a topbar do main já mostra o contexto). Sem o wrapper de largura: o container raiz de `HistoryContent` deve ser apenas um `div` com espaçamento vertical (`space-y-4` ou similar), deixando o pai controlar padding/largura.

Reescrever `HistoryPage.tsx` para: manter `export function HistoryPage()` com o cross-tenant guard idêntico ao atual; manter `HistoryPageContent` apenas como o wrapper de página (`div.mx-auto.max-w-3xl.px-4.py-8`, botão "← Voltar ao Formulário" via `useNavigate`/`navigate(`/form/${orgId}`)`) e dentro dele renderizar `<HistoryContent orgId={orgId} showHeading />`. Remover de `HistoryPage.tsx` o código que foi movido (hook `useAssessmentHistory`, tipo `AssessmentRow`, render de cards) para não duplicar.

Não inserir blocos de código aqui — seguir as assinaturas e o padrão visual já existentes no arquivo original.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "HistoryContent|HistoryPage" || echo "no type errors in History files"</automated>
  </verify>
  <done>`HistoryContent.tsx` existe e exporta `HistoryContent`; `HistoryPage.tsx` importa e usa `<HistoryContent>`; nenhuma duplicação de `useAssessmentHistory`; `npx tsc --noEmit` passa sem erros novos.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar item "Histórico" na sidebar (TabNavigation)</name>
  <files>roteiro-unificado/src/features/form/TabNavigation.tsx</files>
  <action>
Estender `TabNavigation` para aceitar duas novas props opcionais: `activeView?: 'form' | 'historico'` (default `'form'`) e `onSelectHistory?: () => void`. Manter compatibilidade: se `onSelectHistory` não for passado, o item Histórico não é renderizado (assim a rota standalone `HistoryPage`, que não usa `TabNavigation`, permanece intacta).

Após o `.map` das `TAB_CONFIG` etapas, renderizar um `<button type="button">` "Histórico" com o MESMO padrão visual dos itens de etapa (mesmas classes de `flex h-10 ... rounded-md`, estados ativo vs inativo via `cn`), porém:
- destacado como ativo quando `activeView === 'historico'` (usar as mesmas classes do estado ativo: `bg-white/[0.12] font-semibold text-white`);
- inativo: `text-white/55 hover:bg-white/[0.08] hover:text-white`;
- em vez do badge numérico de etapa, usar um pequeno ícone SVG (`h-5 w-5`, stroke currentColor, ex.: relógio/clock-history) no mesmo slot do badge para manter alinhamento;
- label "Histórico" dentro de `<span className="hidden md:inline">` (igual às etapas) e `aria-label="Histórico"` no button para mobile;
- `aria-current={activeView === 'historico' ? 'page' : undefined}`;
- `onClick={onSelectHistory}`.

Quando uma etapa do formulário é selecionada via `handleSelect`, o comportamento atual permanece — mas o componente não controla o `activeView`; é o `FormLayout` que reseta para `'form'` (ver Task 3). Para garantir feedback visual correto, quando `activeView === 'historico'`, os botões de etapa NÃO devem aparecer como ativos mesmo que `store.activeTab` aponte para uma delas: ajustar o cálculo `isActive` para `activeView !== 'historico' && store.activeTab === tab.key`.

Adicionar uma pequena separação visual antes do item Histórico (ex.: `mt-1 border-t border-white/10 pt-1` no wrapper do item, ou um divisor) para distingui-lo das etapas, mantendo o padrão profissional/operacional do skill de design.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit 2>&1 | grep -E "TabNavigation" || echo "no type errors in TabNavigation"</automated>
  </verify>
  <done>`TabNavigation` aceita `activeView` e `onSelectHistory`; quando `onSelectHistory` é passado, renderiza o item "Histórico" com estilo consistente; etapas não aparecem ativas quando `activeView === 'historico'`; `tsc` sem erros novos.</done>
</task>

<task type="auto">
  <name>Task 3: Render condicional form/histórico dentro do main (FormLayout)</name>
  <files>roteiro-unificado/src/features/form/FormLayout.tsx</files>
  <action>
Adicionar estado local de view no `FormLayout`: `const [view, setView] = useState<'form' | 'historico'>('form')`.

Passar para AMBAS as instâncias de `<TabNavigation tenantId={tenantId} />` (desktop na `<aside>` e mobile na barra `md:hidden`) as props: `activeView={view}` e `onSelectHistory={() => setView('historico')}`.

Garantir que selecionar uma etapa volte para a view de formulário: como `TabNavigation.handleSelect` chama `store.setActiveTab`, adicionar no `FormLayout` um efeito ou wrapper que, quando o usuário clicar numa etapa, faça `setView('form')`. Implementação preferida sem alterar a assinatura interna de `handleSelect`: usar um `useEffect` que observa `store.activeTab` e, se a view estava `'historico'`, NÃO force nada automaticamente (isso causaria reset indevido). Em vez disso, passar também um callback `onSelectStep?: () => void` em `TabNavigation` (adicionar essa prop opcional no Task 2 caso necessário) que o `FormLayout` usa para `setView('form')`. Se preferir manter o Task 2 sem `onSelectStep`, então no `FormLayout` chame `setView('form')` dentro de um handler — porém a forma mais limpa e à prova de loop é: adicionar `onSelectStep={() => setView('form')}` à prop de `TabNavigation` e invocá-lo dentro de `handleSelect`. Escolha a abordagem com `onSelectStep` para clareza; documentar a decisão em comentário.

No corpo do `<main>`: introduzir render condicional. Quando `view === 'historico'`, renderizar `<HistoryContent orgId={tenantId} showHeading={false} />` (importar de `'./HistoryContent'`) no lugar do bloco `renderSection(...)`. Manter visíveis a topbar fixa (mas o label deve refletir "Histórico" quando `view === 'historico'` — ajustar o `<span>` do label para `view === 'historico' ? 'Histórico' : activeTabConfig.label`). Quando `view === 'historico'`:
- NÃO renderizar `<ReadinessClassification>` nem o footer sticky de navegação (Anterior/Próxima/Enviar) — esses são específicos do formulário;
- não depender de `draftQuery.isLoading/isError` para o histórico (o `HistoryContent` tem seu próprio skeleton/estados).

Quando `view === 'form'`, manter exatamente o comportamento atual (ReadinessClassification + renderSection + footer + estados de draftQuery).

A rota `/form/:orgId/history` e o `HistoryPage` permanecem inalterados — esta tarefa só adiciona a visualização embutida.
  </action>
  <verify>
    <automated>cd roteiro-unificado && npx tsc --noEmit && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>`FormLayout` tem estado `view`; clicar "Histórico" na sidebar mostra a lista dentro do `<main>` (sem footer de formulário, sem ReadinessClassification); clicar numa etapa volta para o formulário; topbar mostra "Histórico" no modo histórico; `tsc --noEmit` e lint passam.</done>
</task>

</tasks>

<verification>
- `cd roteiro-unificado && npx tsc --noEmit` passa sem erros.
- `cd roteiro-unificado && npm run lint` passa (atenção às regras react-hooks/immutability já presentes no projeto — não atribuir a globals em handlers; usar window.history.replaceState como nos arquivos existentes).
- `cd roteiro-unificado && npm run build` conclui com sucesso.
- Manual (desenvolvedor): logar como construtora, ver o item "Histórico" na sidebar abaixo das 10 etapas; clicar abre a lista de versões dentro do `main`; clicar numa etapa volta ao formulário; acessar diretamente `/form/:orgId/history` ainda renderiza a página standalone com botão "Voltar".
</verification>

<success_criteria>
- Item "Histórico" visível na sidebar (desktop e mobile) abaixo das etapas, com estilo consistente com o skill de design (profissional/operacional, tokens existentes).
- Conteúdo do histórico renderiza dentro do `<main>` do `FormLayout`, sem mudar de rota.
- Alternância form ↔ histórico funciona sem loop de render e sem quebrar autosave/draftQuery do formulário.
- Rota standalone `/form/:orgId/history` (HistoryPage) continua funcionando, reutilizando `HistoryContent`.
- Sem duplicação da lógica de fetch/render do histórico (fonte única em `HistoryContent.tsx`).
</success_criteria>

<output>
Create `.planning/quick/260525-n1d-adicionar-history-no-menu-lateral-junto-/260525-n1d-SUMMARY.md` when done
</output>
